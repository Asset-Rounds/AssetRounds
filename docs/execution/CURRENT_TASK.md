# Current Task

## Program and card

- Phase / branch / card / global order: `S8 / phase/s8-quality / S8.1 / 32 of 36`.
- Card heading: `### S8.1 — Nonshipping exterior-light zero-fork proof`.
- Position / boundary / immediate next card: `1 of 4 / phase boundary no / S8.2 only after accepted S8.1 evidence and fresh same-phase G0`.
- Program autopilot / phase autopilot / exact S8 span / boundary integration: `enabled through accepted S9.1 / enabled / S8.1,S8.2,S8.3,S8.4 / yes at S8.4 only`.
- Frozen phase→branch→card map: `S0→phase/s0-foundation→S0.1; S1→phase/s1-shell-design→S1.1; S2→phase/s2-persistence-signs→S2.1,S2.2; S3→phase/s3-check-runner→S3.1,S3.2,S3.3,S3.4,S3.5,S3.6,S3.7; S4→phase/s4-reports→S4.1,S4.2,S4.3,S4.4,S4.5; S5→phase/s5-work-recheck→S5.1,S5.2,S5.3,S5.4; S6→phase/s6-data-rights→S6.1,S6.2,S6.3,S6.4,S6.5,S6.6; S7→phase/s7-commerce→S7.1,S7.2,S7.3,S7.4,S7.5; S8→phase/s8-quality→S8.1,S8.2,S8.3,S8.4; S9→phase/s9-release→S9.1`.
- End condition: accepted exact-main S9.1; S9.2 signing/TestFlight and S9.3 App Store submission remain owner-only.

## Predecessor and authority state

- Immutable S8 phase-main base and integrated/card base: `P=M=0b1e506dd71ba704cbfb48787d6cfa1731024d83`; this exact accepted S7 phase-close/exact-main head remains byte-for-byte fixed throughout S8.
- Accepted S7.5 product evidence: `E=56db1fc1a65b847502d1109df47cbbf1f9a6c357`; run `31873770338` / job `94986281264`, exact `phase/s7-commerce@E`, attempt 1, F25/UI enabled, terminal success, build plus `2/2` units plus `1/1` UI green.
- S7 phase close: `C=P`; `C^=E`, and `E..C` changed only append-only `docs/execution/HANDOFF.md`. Exact phase run `31874580940` / job `94988283092` succeeded at C with F25/UI enabled. Artifact `ios-ci-31874580940-1`, ID `9244506698`, size `5595704`, digest `sha256:2a74595efbc20271b4a5ea547e63650ba905c3c1dc79e8daeaa6d6c8e94149be`; all `95/95` payload checksums matched; `SHA256SUMS.txt` SHA-256 `97F67432DFC786DDADC11AAB6F8CBF1D22B3E82430D7EFE0034430242E342772`.
- Exact-main integration: remote `main` was non-force fast-forwarded from the immutable S7 base to P, then run `31875162640` / job `94989692369` succeeded at exact `main@P`, attempt 1, F25/UI enabled, build plus `2/2` units plus `1/1` UI green. Artifact `ios-ci-31875162640-1`, ID `9244662872`, size `5779025`, digest `sha256:f4d63fd0a8f33b0c54e7e26cfa1108899cf0444d3218cb4ea013436aba7618be`; all `95/95` payload checksums matched; `SHA256SUMS.txt` SHA-256 `9EC0F6E6369349E9323BD6DB02EA1531DD5CB52A98872BC3922680121BA9C37D`.
- Accepted environment: `macos26` image `20260728.0273.1`; Xcode 26.6 build `17F113`; iPhone 17 / iOS 26.2; Simulator UDID `9AA9ED9B-42D2-4F6E-B8B5-45AAB66D6404`; initial state `Shutdown`.
- `phase/s8-quality` was created non-force from exact P and initially equalled remote `main=P`. Initial phase-start authority `A1=97812d3228daa0a8226f155200a8422ead05b6bb`; `A1^=P`, and `P..A1` changes only this `docs/execution/CURRENT_TASK.md`.
- Read-only S8.1 G0 then proved one exact reusable-seam scope blocker: `SignPackLoader.load(data:)` decodes the required fixture but rejects it unless it equals `.illuminatedSignV1`, so the frozen loader→runner proof is impossible without correcting that earlier-card-owned seam. This authority correction head must directly parent A1 and change only this CURRENT_TASK. Fresh G0 must observe its exact SHA as A2, prove `A1..A2` and `P..A2` change only CURRENT_TASK, prove remote `phase/s8-quality=A2` and remote `main=P`, and record A2 later without attempting to self-record it here.
- Frozen production-defect evidence: S7.5 failed candidate run `31872838760` / job `94983995567` built and passed `2/2` units, then AX captured **Check not saved** after a completed recheck and same-session fresh check because `CheckRunnerCoordinator.finalizationAttempt` survived the prior completion. This is the plan-authorized S8 proof-discovered reusable-seam defect; S8.1 must add a deterministic regression and the smallest correction in the exact earlier-card-owned path below without adding any second-pack production branch.

## Pinned authority

- Build plan: `docs/product/BUILD_PLAN_V4.md` / `23DAAB390AF917CBE91C3044E4906F3FBF8D67D2FDFC6BC9BDE985D984F37BBD`.
- S8.1 plan anchors: `### Bundled vertical pack` → one immutable nonshipping exterior-light fixture through the existing pack/runner/snapshot/renderer interfaces; `## 11. Build slices and release gates` → S8.1 test-only second-pack proof; `## 12. Twelve must-pass launch smokes` → smoke 11 exact frozen noun/purpose display proof with no production pack lookup or renderer branch; global execution anchors remain `## 16` and `## 18`.
- Runbook: `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` / `41622B8AE241FDFCBA3A03A430D27069A29DBE39EB508C6E155D687BA8A6AA79` / selected card S8.1 only.
- Execution contract: `docs/execution/CODEX_EXECUTION_CONTRACT_V4.md` / `9E7F3ABD2CC6FB15F33E4E56C95474BFEAC79C7148D7D125C75FBF88DB9F8A93`.
- Workflow: `.github/workflows/ios-ci.yml` / `BCD64E2A42752D28844435241B5ABFCA911D04190375CBBDBFC10B45ACBA97D7` / `workflow_dispatch`.
- Selector at G0 may still equal accepted S7.5 compact JSON plus LF, 349 bytes, SHA-256 `C87DCB47F4B18FB3B6E4F637FAB459CB7F026DC35F606505E5CC61307673C62F`; after complete G0 the first support mutation replaces it with the exact S8.1 object below, 294 bytes, SHA-256 `08F058C46FD35D3904FD9636E86F06E136926110F5FBB3A39987B8DA92D1A54A`.

## Outcome and acceptance

- Outcome: load one nonshipping exterior-light manifest only from the unit-test fixture; drive it through the existing `SignPackLoader`, check/recheck runner, finalization, canonical snapshot validation, and `WorklightPDFRendererV1`; and prove its frozen nouns, evidence-purpose displays, issue labels, stage displays, outcome displays, acknowledgements, and disclaimer reach both current and history snapshot/PDF content without any runtime registry lookup or production second-pack branch.
- Loader correction truth: `load(data:)` may accept only a closed, internally valid schema-1 `SignPack` with the exact frozen semantic key sets/order/cardinalities, unique normalized registry keys/displays, and safe scalar forms. `loadBundled()` must additionally exact-compare the decoded/canonical value to `.illuminatedSignV1`, so production startup still accepts only the one shipped manifest; no fixture ID/copy or second-pack selection enters production.
- Fixture truth: the JSON is schema/content version 1, uses a unique neutral test-only pack ID and workflow key, retains the closed production semantic keys and cardinalities, and supplies deliberately distinct exterior-light nouns and display copy so a fallback to illuminated-sign copy is observable. Its exact bytes and decoded value are deterministic; it is available only through the test bundle and is absent from the app bundle.
- Full-flow truth: the fixture creates a substantive visible-condition check with exact wide/close evidence, then a linked evidence-bearing recheck so the current and strictly earlier history rows both carry only frozen fixture displays. Snapshot encoding/validation must bind the fixture pack/version and immutable copy; PDF generation must consume only the validated snapshot and remain identical if the in-memory fixture/registry is no longer consulted.
- Zero-fork truth: no production entity, route, service, permission, pack selector, marketplace, historical registry, loader branch, finalization branch, snapshot branch, or renderer branch is added. The exterior-light fixture is not app-bundled, user-selectable, public, priced, backed up as a shipped pack, or used by production startup.
- Mandatory reusable-seam correction: the already-proven stale `finalizationAttempt` must be cleared only after a completed finalization has returned its exact authoritative result, preserving in-flight retry/replay identity while ensuring the next fresh draft is classified solely from its own stage/Issue/identifiers. One deterministic same-coordinator recheck→fresh-check regression must fail on the S7 base and pass after the bounded correction.
- GOLDEN: exact exterior-light fixture nouns/purpose/current-and-history stage/outcome display copy freeze into canonical snapshots and flow through deterministic PDFs via identical production interfaces, while the repository has zero production adaptation for that fixture.
- ALT-1: malformed/unknown fixture authority fails closed; the fixture never appears in `Bundle.main`; the same-session stale-finalization regression produces one correct fresh check rather than recheck identifier validation; retry/replay semantics remain exact; no unrelated production behavior changes.
- Forbidden behavior: any shipped/public second pack, production manifest/resource/project edit, pack selection/UI/registry/marketplace, generalized workflow engine, renderer/finalization pack lookup, new entity/schema/route/service/permission/dependency, broad refactor, commerce/data-right change, S8.2/S8.3/S8.4 behavior, signing, TestFlight, submission, deployment, or release.

## Environment and exact selector

- Route / repository / refs: Windows authoring → exact GitHub Actions macOS verification; `palatis3/AssetRounds / public / main / phase/s8-quality`; never run or claim Windows Xcode/Simulator results.
- Required posture: `sandbox_mode=danger-full-access / approval_policy=never / command network enabled`; trusted repository configuration active; XcodeBuildMCP and owner-operated Mac unnecessary.
- Runner / Xcode / project: `macos-26 / Xcode 26.6 Build version 17F113 / /Applications/Xcode_26.6.app/Contents/Developer / FieldEvidenceApp.xcodeproj / FieldEvidenceApp / Debug / iOS 18.0`.
- Simulator selector: `iPhone 17 / iOS 26.2`; resolve exactly one ephemeral-job UDID.
- Tier / UI input: `N8 / run_ui_smoke=false`.
- Exact selector: `{"schemaVersion":1,"taskID":"S8.1","tier":"N8","runUISmoke":false,"setupArtifactTimeoutSeconds":300,"buildTimeoutSeconds":600,"testTimeoutSeconds":900,"uiTimeoutSeconds":0,"totalBudgetSeconds":2400,"unitTestSelectors":["FieldEvidenceAppTests/S8_1SecondPackZeroForkTests"],"uiTestSelectors":[]}` plus exactly one LF; 294 UTF-8 bytes, no BOM; SHA-256 `08F058C46FD35D3904FD9636E86F06E136926110F5FBB3A39987B8DA92D1A54A`.
- Exact commands: `bash Scripts/run-with-timeout.sh 600 bash Scripts/build-smoke.sh`; `bash Scripts/run-with-timeout.sh 900 bash Scripts/test-smoke.sh`; UI is disabled and no UI command/selector/evidence is accepted.
- Required evidence: nonempty boot/build/unit logs; Build and UnitTests result bundles; selection/runner/Xcode/Simulator/budget evidence; verified relative `SHA256SUMS.txt`. No UI log, result bundle, or screenshot is required or authorized.
- Allowed GitHub methods: read/fetch/ref/run/workflow/artifact inspection; exact-path staging/commits; non-force phase push; named workflow dispatch on exact phase ref with `run_ui_smoke=false`; exact-run observation/download. Force-push, merge commit, PR, ref rewrite/delete, main mutation before the S8 boundary, settings/secrets, App Store Connect, signing, TestFlight/App Store, deployment/release, and S9.2/S9.3 are forbidden.

## Exact allowed implementation paths

Conditional production correction paths activated only by the frozen evidence above:

- `FieldEvidenceApp/Domain/Packs/SignPackLoader.swift`
- `FieldEvidenceApp/Features/CheckRunner/CheckRunnerCoordinator.swift`

Test-only proof paths:

- `FieldEvidenceAppTests/S8_1SecondPackZeroForkTests.swift`
- `FieldEvidenceAppTests/Fixtures/S8_1ExteriorLightPackV1.json`

Standing selector exception:

- `Scripts/ci-selection.json`

No other implementation, model, project, production resource, fixture, asset, script, workflow, authority, or documentation path is allowed during S8.1 implementation. `SignPackLoader.swift` may contain only the bounded generic testable decode/validation seam while keeping `loadBundled()` exact-production-only; `CheckRunnerCoordinator.swift` may contain only the stale-finalization-attempt lifecycle correction. Neither path may mention a fixture ID/copy, add selection, or create a second-pack production branch. Existing pack types, runner/finalization, snapshot encoder/validator, evidence store, PDF renderer, models, fixtures, and test helpers may be read and reused without editing. Append-only `docs/execution/HANDOFF.md` remains separately authorized bookkeeping after accepted evidence.

## G0 and verification

- Fresh G0 must prove A1 directly parents exact P, the authority-correction head A2 directly parents A1, `P..A2` changes only this `docs/execution/CURRENT_TASK.md`, remote `phase/s8-quality=A2`, and remote `main=P`; it must also reprove both accepted S7 phase/exact-main runs and artifacts, preserve all pins, and validate the exact individual allowed paths above.
- Validate the exact S8.1 N8 selector object against runbook Section 6 and the workflow schema. The accepted S7.5 selector is permitted only as predecessor state; after complete G0 replace only `Scripts/ci-selection.json` with the frozen S8.1 object as the first implementation-support mutation.
- Implement only S8.1. The read-only loader diagnosis and prior S7.5 AX evidence activate exactly the two conditional seams, so do not spend deliberate failing candidates: add the generic closed-schema loader regression/seam, the stale-attempt regression/lifecycle fix, and the exterior-light full-flow proof in one bounded implementation. Candidate recovery follows the persistent evidence-driven direct-child loop without weakening GOLDEN/ALT, production bundled-pack identity, selector, tests, zero-fork truth, retry identity, watchdogs, or scope.
- After accepted exact-head S8.1 CI, read `KNOWN_BUGS.md`, append the immutable S8.1 HANDOFF, then enabled same-phase autopilot may commit/push exactly that append plus immediate-next S8.2 `CURRENT_TASK.md` and must run fresh S8.2 G0. Do not mutate `main` before the S8.4 phase boundary and do not start S8.3/S8.4/S9 work.
