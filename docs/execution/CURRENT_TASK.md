# Current Task

- Phase / active operation / global order: `S0 / hosted-CI infrastructure verification recovery K2 / closed S0.1 (1 of 36)`
- Program autopilot enabled / exact ordered phase→branch→card map / final owner-only boundary: `yes` / `S0→phase/s0-foundation→S0.1; S1→phase/s1-shell-design→S1.1; S2→phase/s2-persistence-signs→S2.1,S2.2; S3→phase/s3-check-runner→S3.1,S3.2,S3.3,S3.4,S3.5,S3.6,S3.7; S4→phase/s4-reports→S4.1,S4.2,S4.3,S4.4,S4.5; S5→phase/s5-work-recheck→S5.1,S5.2,S5.3,S5.4; S6→phase/s6-data-rights→S6.1,S6.2,S6.3,S6.4,S6.5,S6.6; S7→phase/s7-commerce→S7.1,S7.2,S7.3,S7.4,S7.5; S8→phase/s8-quality→S8.1,S8.2,S8.3,S8.4; S9→phase/s9-release→S9.1` / `stop after accepted S9.1 exact-main CI; S9.2 signing/TestFlight and S9.3 App Store submission are owner-only`
- Phase/card position and boundary: `S0.1 is complete / 1 of 1 / final boundary verification recovery active`; do not rerun or edit S0.1
- Phase autopilot enabled / exact authorized same-phase span: `yes` / `S0.1 only (complete; no same-phase successor)`
- Immediate next unstarted card: `S1.1` on `phase/s1-shell-design`; it may be hydrated only after green exact-main K2
- Product implementation evidence `E`: `I3=ac310dd37700d65165200f742aaec1d48d0a34d6`; exact-head P12 run `31566371521` succeeded; I3 remains the sole S0.1 implementation result
- Immutable completed bookkeeping evidence: `C=15348e92466a79efd02bfb71f5ffee043e615829` directly parents I3 and changes only `docs/execution/HANDOFF.md`; the complete S0.1 HANDOFF is immutable
- Historical card chain: `I=6dac0a660110b05643bcdaaf97113b75f54080a0` / failed run `31562160165`; `I2=481d272ec319d3210d0e393d20130c7c1f8f0e1a` / failed run `31562792005`; `A0=9590743877627cdd915a68ec581cdff88d9d6418`; accepted `I3` / run `31566371521`; `C`; `R`; `K1`
- Historical ordinary-card `M/A/M..A`: `M=I2=481d272ec319d3210d0e393d20130c7c1f8f0e1a`; `A=A0=9590743877627cdd915a68ec581cdff88d9d6418`; exactly one direct-child authority commit changing `AGENTS.md`, `docs/product/BUILD_PLAN_V4.md`, `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md`, and `docs/execution/CURRENT_TASK.md`. These are immutable completed-card evidence; active infrastructure G0 uses E/F/K below instead of a new ordinary M/A.
- Historical immutable S0 bootstrap phase-main base: `P=c3e536a03775cc8d25f42a8e31c2f24db4390d4d`
- Historical failed verification evidence: exact-main run `31567299022` at `C` and exact-main run `31571125674` at `R=1e0322cc072fda28718468bcd1b0dbe1f5d0accc` failed before `xcodebuild` because exact-UDID Simulator migration consumed the entire 180-second build wrapper. They remain non-green infrastructure evidence and must not be rerun.
- K1 evidence: `K1=0da62482db7e3f90742876ef05574de9e9569e32` directly parents R and changes exactly the five authority documents plus `.github/workflows/ios-ci.yml`; exact phase run `31579936670`, attempt 1, at phase/head=K1 is terminal failure. Its 90-entry checksum-verified artifact proves dedicated readiness passed within 300; setup/artifact/build/unit/UI enforcement passed at unchanged `120/180/180/240`; exact selectors, result bundles, screenshot, evidence validation, checksums, and upload passed; only aggregate `elapsed_seconds=727` exceeded unchanged `total_budget_seconds=720`.
- Failed verification base `F`: `K1=0da62482db7e3f90742876ef05574de9e9569e32`; at K2 construction local HEAD and remote `phase/s0-foundation` equal K1, remote HEAD and `main` equal R, local/remote `phase/s1-shell-design` are absent, and the worktree is clean
- Candidate infrastructure verification head `K2`: `OBSERVE AT POST-COMMIT G0; never self-record its SHA here`
- Required `K1..K2` ancestry and path set: K2 directly parents K1; exactly one commit changes only `AGENTS.md`, `docs/product/BUILD_PLAN_V4.md`, `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md`, `docs/execution/CODEX_EXECUTION_CONTRACT_V4.md`, `docs/execution/CURRENT_TASK.md`, and `.github/workflows/ios-ci.yml`
- Immutable infrastructure gate identity: `phase=S0; card=S0.1-closed-boundary; workflow=.github/workflows/ios-ci.yml; required-ref-roles=phase/s0-foundation then main; E=I3; first-failed-run/head=31567299022/C`; K1/run `31579936670` is the exact aggregate-total bridge in that same gate
- Infrastructure-recovery counters at K2 construction: `K2 of maximum K1/K2`; K1 phase same-head retry is unused but permanently retired; K1 main was never dispatched; K2 phase initial dispatch unused; K2 main initial dispatch unused; ordinary S0.1 product-fix budget remains historical. Live counters derive only from reachable K commits and all GitHub exact-ref/head candidate history; this prose cannot reset them.
- Pre-existing dirty paths and disposition: `NONE at K2 construction start`; during K2 construction only the exact six paths above may be dirty; post-commit G0 requires clean index/worktree/untracked state

## Pinned authority

- Build-plan path / SHA-256: `docs/product/BUILD_PLAN_V4.md` / `AF45378DEC5E48A26B083F2B08D88C3E3369381B3D9C8E317BEA757CDFA89ED9`
- Exact plan anchors: `## 11. Build slices and release gates`; `## 16. Owner preparation checklist`; `## 18. Codex execution authority`
- Implementation-runbook path / SHA-256: `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` / `85C80FD9B385FB04D61BAAB7D1ABAF7CAD2D2C0A281B30B6FF2045C31347B063`
- Selected runbook card / heading / lifecycle: `S0.1` / `### S0.1 — Repository and unsigned CI baseline` / `Steps 8–9 autonomous hosted-CI infrastructure recovery`
- Execution-contract path / SHA-256: `docs/execution/CODEX_EXECUTION_CONTRACT_V4.md` / `E87E2AC6EF8681EE8724DBB59BBE7C00EE7E0043D3FD07C0247C20F60D61B022`
- CI workflow path / SHA-256 / trigger: `.github/workflows/ios-ci.yml` / `BF2AD9828792F034007F9105DA1AA9352B982A941408F00E36CD9BA72348337D` / `workflow_dispatch` with exact `run_ui_smoke=true`
- Selector path / SHA-256: `Scripts/ci-selection.json` / `9E0050FEA98E2E99F37A9113B11B58450AA16B73E99F6842BEBEA09F338C929C`; bytes remain immutable

## Outcome and exact delta

- Outcome: preserve accepted S0.1 product implementation I3 and completed HANDOFF; close the standing guardrail's aggregate-total scheduling case without weakening acceptance; start exact-UDID Simulator migration earlier; require green exact-head phase and exact-main K2 evidence within unchanged P12; then use K2 only as S0 verification/phase-close and S1 base.
- Exact workflow delta: inside `Verify pinned toolchain, shared scheme, and simulator`, retain the existing file checks plus `xcodebuild -version` and both exact version/build greps first; move the existing runtime/device JSON capture and exact runtime/name→UDID selection before the unchanged `xcodebuild -list`, scheme assertion, `xcodebuild -showBuildSettings`, and deployment assertions; query exactly one state for that selected UDID; record request epoch/UDID/state in nonempty `simulator-boot-start.log`; accept only initial `Shutdown` or `Booted`; issue exact nonblocking `xcrun simctl boot "$simulator_udid"` only for `Shutdown` under the existing process-group timeout helper and the same 300-second ceiling; reject every other state/error; export exact destination, UDID, and request epoch.
- Exact readiness delta: the later dedicated readiness step computes elapsed time from the early request epoch and positive `remaining=300-elapsed`; runs the unchanged exact-UDID `simctl bootstatus -b` only under that remaining wrapper; records elapsed/remaining/request-to-ready in `simulator-boot.log`; and asserts request-to-ready is at most 300. Require both boot logs nonempty and include them in ordinary relative checksum evidence.
- Frozen acceptance: selector and P12 remain exact `setup/artifacts 120 / build 180 / unit 180 / UI 240 / total 720`; total start remains before checkout and endpoint remains after checksum; no setup/boot time is excluded or subtracted from 720. Build, unit, and UI commands, runner, Xcode, project, scheme, configuration, deployment target, Simulator model/runtime, exact test selectors, all preexisting result bundles/logs/screenshot/checksums, and 30-minute job ceiling remain unchanged.
- Project/app/test/script/fixture/asset/selector/HANDOFF/product delta: `NONE`
- ALT-1: `NONE` for product behavior. A separately eligible non-acceptance provider/transport failure at K2 may use only the one standing same-head retry; a K2 aggregate-total or other acceptance failure cannot retry. No K3 exists.

## Environment and selector

- Product mode: `single-user, device-local V4`
- Execution route / authoring host: `Windows authoring → GitHub Actions macOS verification` / `Microsoft Windows 11 Home 64-bit 10.0.26200`
- Repository / visibility / base / phase: `palatis3/AssetRounds` / `private solo` / `main` / `phase/s0-foundation`
- Branch control: only exact non-force advances named below; no force push, merge commit, PR, divergent-ref repair, or broad write
- Runner / Xcode: `macos-26` / `Xcode 26.6, Build version 17F113, DEVELOPER_DIR=/Applications/Xcode_26.6.app/Contents/Developer`
- Minimum deployment / project / target / shared scheme / configuration: `iOS 18.0` / `FieldEvidenceApp.xcodeproj` / `FieldEvidenceApp` / `FieldEvidenceApp` / `Debug`
- Simulator selector: `iPhone 17 / iOS 26.5`; resolve exactly one available UDID inside every ephemeral job and never reuse a pinned UDID across jobs
- UI mode / tier: `CI XCUITest` / `P12`
- Exact selector object: `{"schemaVersion":1,"taskID":"S0.1","tier":"P12","runUISmoke":true,"setupArtifactTimeoutSeconds":120,"buildTimeoutSeconds":180,"testTimeoutSeconds":180,"uiTimeoutSeconds":240,"totalBudgetSeconds":720,"unitTestSelectors":["FieldEvidenceAppTests/S0LaunchTests"],"uiTestSelectors":["FieldEvidenceAppUITests/S0LaunchUITests"]}`
- Exact ordinary commands remain: `bash Scripts/run-with-timeout.sh 180 bash Scripts/build-smoke.sh`; `bash Scripts/run-with-timeout.sh 180 bash Scripts/test-smoke.sh`; `bash Scripts/run-with-timeout.sh 240 bash Scripts/ui-smoke.sh`
- Required artifact: `ios-ci-<run_id>-<run_attempt>` with nonempty `simulator-boot-start.log`, `simulator-boot.log`, build/test/UI logs, three result bundles, final screenshot, runner/Xcode/build-settings/Simulator/selection/run/budget evidence, and verified relative `SHA256SUMS.txt`
- Tool posture: Full access, `sandbox_mode=danger-full-access`, `approval_policy=never`, command network enabled, project trusted, GitHub available, goals enabled, XcodeBuildMCP disabled. Broader capability is not a blocker and never expands product scope.

## Allowed paths and operations

- K2 paths: `AGENTS.md`
- K2 paths: `docs/product/BUILD_PLAN_V4.md`
- K2 paths: `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md`
- K2 paths: `docs/execution/CODEX_EXECUTION_CONTRACT_V4.md`
- K2 paths: `docs/execution/CURRENT_TASK.md`
- K2 paths: `.github/workflows/ios-ci.yml`
- No path cap override, glob, directory root, deletion, rename, HANDOFF exception, selector exception, or script exception applies to K2.
- Read-only Git/GitHub inspection and artifact download are authorized for exact named refs/runs/candidates.
- Commit K2 once with explicit staging of exactly the six paths.
- Closed ref-resume states: (a) phase=K1 and main/HEAD=R with S1 absent plus zero phase/main K2 candidates is initial and permits the one phase push; (b) phase=K2 and main/HEAD=R with a valid exact phase K2 history plus zero main K2 candidates resumes phase history with no re-push, and only accepted green phase CI permits the main advance; (c) phase/main/HEAD=K2 is valid only after accepted green exact phase K2 and resumes a valid exact main K2 history with no re-push. Every other ref/provenance state stops.
- From initial state only, non-force push `HEAD:refs/heads/phase/s0-foundation`; verify phase=K2 and main/HEAD=R.
- On the phase ref, apply the closed exact workflow/ref/head K2 candidate-history rule. Dispatch the initial run with `gh workflow run .github/workflows/ios-ci.yml --repo palatis3/AssetRounds --ref phase/s0-foundation -f run_ui_smoke=true` only when zero K2 candidates exist. Accept only green exact-K2 artifacts.
- After accepted phase K2, reprove or resume phase=K2/main/HEAD=R and non-force fast-forward exact K2 to `refs/heads/main` only if main is still R; if main already equals K2, do not re-push. Verify phase/main/HEAD=K2.
- On main, apply the same closed exact workflow/ref/head K2 candidate-history rule. Dispatch the initial run with `gh workflow run .github/workflows/ios-ci.yml --repo palatis3/AssetRounds --ref main -f run_ui_smoke=true` only when zero K2 candidates exist. Accept only green exact-K2 artifacts.
- K2 may receive one no-code fresh-runner dispatch on the same ref only after a separately eligible non-acceptance provider/transport signature with no compiler diagnostic, test result/assertion, selector, acceptance, product, aggregate-total, required-evidence validation, or checksum failure. An upload/transport failure remains eligible only when complete logs prove validation and checksum creation succeeded first. Never rerun a failed run ID. Any K2 aggregate-total or other acceptance failure stops immediately without a retry.
- Closed K2 candidate history, independently per exact workflow/ref/head: zero candidates → dispatch initial once; one active → wait; one green → accept; one separately eligible non-acceptance terminal failure with unused retry → dispatch retry once; exactly two are valid only when the first is that eligible failure and the second is active or green. A terminal non-green second run, aggregate-total/other acceptance failure, any other status/order, or more than two candidates stops. No K3 exists.
- After green exact-main K2, create only `phase/s1-shell-design` from K2, hydrate only S1.1 via runbook Section 3 in a CURRENT_TASK-only authority commit with `P=M=K2`, push non-force, and run fresh S1.1 G0.

## Forbidden

- Changing `docs/execution/HANDOFF.md`, `Scripts/ci-selection.json`, any script, project/app/test/fixture/asset, package, lockfile, entitlement, permission, capability, product behavior, schema, UI, navigation, persistence, camera, report, backup, StoreKit, analytics, signing, release, deployment, or submission byte in K2
- Changing runner/Xcode/Simulator selector, N8/P12/F25 values, test selectors/commands, product/card acceptance assertions, preexisting required evidence/checksum enforcement, workflow input, total-budget start/end/assertion, or 30-minute job ceiling; only the named scheduling overlap and additive nonempty checksummed `simulator-boot-start.log` are allowed
- Accepting run `31579936670`, rerunning any historical failed run, dispatching K1 again, calling C/R/K1 successful, reopening S0.1, replacing I3 as product implementation evidence, or amending HANDOFF
- Force push, merge commit, PR, divergent ref repair, settings, secrets, collaborators, release, signing, TestFlight/App Store upload, deployment, or submission
- Local Xcode, Simulator, `xcodebuild`, or XcodeBuildMCP

## K2 state machine and acceptance

1. Construct K2 from clean K1 with exactly the six allowed paths and exact delta above. Compute and pin final plan/runbook/contract/workflow hashes. Explicitly stage and commit once; do not push before post-commit G0.
2. Fresh post-commit/resume G0 proves local branch `phase/s0-foundation`, clean `K2^=K1`, one commit in `K1..K2`, exact six-path diff, `git diff --check`, all pins/anchors, selector equality, protected-tree equality, immutable I3/C/HANDOFF and failed-run evidence, local/remote S1 absent, K1 retry retirement, and exactly one closed ref/provenance state: K1/R with zero K2 candidates; K2/R with valid phase K2 history and zero main K2 candidates; or K2/K2 only after accepted green phase K2 and with valid main K2 history.
3. In initial K1/R only, re-read refs and non-force push phase K1→K2, then verify phase=K2/main=R. In phase-only K2/R, do not re-push. Apply the closed exact phase/head=K2 candidate history, permit no intervening movement, and require terminal success plus complete checksummed artifact within every unchanged ceiling.
4. After accepted phase run, if phase=K2/main=R, non-force fast-forward main R→K2 and verify phase/main/HEAD=K2; if phase/main already equal K2, do not re-push. Apply/resume the closed exact main/head=K2 candidate history and require terminal success plus complete checksummed artifact within every unchanged ceiling.
5. GOLDEN checkpoints: exact selected UDID initial state is only Shutdown or Booted; request-start evidence is nonempty/checksummed; request-to-terminal readiness succeeds within the same 300 seconds; unchanged build/unit/UI steps succeed within 180/180/240; exact S0 selectors execute non-skipped and pass; total evidence budget is at most 720 from the unchanged start through unchanged endpoint; all required artifacts/checksums pass; `head_sha=K2`; no protected byte changed.
6. Green exact-main K2 makes K2 the S0 verification/phase-close and S1 phase/card base while E=I3 remains S0.1 implementation. Create/hydrate only S1.1 as above, run fresh G0, then resume strict frozen order through S9.1.

## Stop conditions

- Stop technically, without requesting an authority/approval message, on unresolved source/hash/ancestry/path/dirty-state evidence; ref divergence/intervening movement; non-fast-forward/rejection; candidate history outside the closed authorized sequence; unlisted required path; weakened/changed selector, test, product, tier, acceptance, total-budget start/end/assertion, or preexisting evidence; compiler diagnostic; product test/acceptance failure; any K2 aggregate-total failure; a separately eligible K2 retry that remains non-green; or any ineligible K2 failure. No K3 exists.
- If a required GitHub operation or pinned environment is externally unavailable, report that external technical blocker; do not substitute or ask for guardrail approval.

## Definition of done

- K2 is a direct child of K1 with exactly the six-path guardrail/workflow diff; all pins and protected-byte proofs pass; exact-head phase and exact-main UI-enabled P12 runs are green with complete artifacts inside all unchanged ceilings; I3 and K2 are recorded separately; remote phase/main/HEAD equal K2.
- Create/resume only `phase/s1-shell-design`, hydrate only S1.1 with `P=M=K2`, pass fresh G0, and continue the existing goal through S9.1. Stop before owner-only S9.2/S9.3.
