# Current Task

## Program and card

- Phase / branch / card / global order: `S4 / phase/s4-reports / S4.5 / 16 of 36`.
- Card heading: `### S4.5 — Clerical correction and forward replacement`.
- Position / boundary / immediate next card: `5 of 5 / phase boundary yes / S5.1 only after accepted S4 phase-close and exact-main integration`.
- Program autopilot / phase autopilot / exact S4 span / boundary integration: `enabled through accepted S9.1 / enabled / S4.1,S4.2,S4.3,S4.4,S4.5 / yes at S4.5 only`.
- Frozen phase→branch→card map: `S0→phase/s0-foundation→S0.1; S1→phase/s1-shell-design→S1.1; S2→phase/s2-persistence-signs→S2.1,S2.2; S3→phase/s3-check-runner→S3.1,S3.2,S3.3,S3.4,S3.5,S3.6,S3.7; S4→phase/s4-reports→S4.1,S4.2,S4.3,S4.4,S4.5; S5→phase/s5-work-recheck→S5.1,S5.2,S5.3,S5.4; S6→phase/s6-data-rights→S6.1,S6.2,S6.3,S6.4,S6.5,S6.6; S7→phase/s7-commerce→S7.1,S7.2,S7.3,S7.4,S7.5; S8→phase/s8-quality→S8.1,S8.2,S8.3,S8.4; S9→phase/s9-release→S9.1`.
- End condition: accepted exact-main S9.1; S9.2 signing/TestFlight and S9.3 App Store submission remain owner-only.

## Predecessor and authority state

- Immutable S4 phase-main base: `P=8b33ee6a26ed4f7ccdccc8e092920b26c6e04122`; remote `main` must remain exactly P throughout S4.5 implementation and phase-close preparation.
- Accepted S4.4 integrated/card base and product implementation: `M=E=I2=4a0316d03298435a33bd8bf6130470d78b8d35f5`; first implementation `I=eb69c672ff7f35f7db32da242411ef21f704634e`; no distinct infrastructure verification head K exists.
- S4.4 task-start authority: `A=f77a364b890024d5dde5703b6ddd9ffbb46c7303`, a direct child of accepted S4.3 `2601787b1377b408568d5f07faab2bda65648e9d`; that parent-to-A diff changed exactly `docs/execution/HANDOFF.md` and `docs/execution/CURRENT_TASK.md`.
- Immutable failed S4.4 candidate: run `31681888576`, job `94388979667`, URLs `https://github.com/palatis3/AssetRounds/actions/runs/31681888576` and `https://github.com/palatis3/AssetRounds/actions/runs/31681888576/job/94388979667`, failed at exact I after all `7/7` unit tests passed because `S4_4ReportsUITests.testReportsFiltersHistoryDetailAndStrictPreviousVisitComparison` asserted `43.99999999999997 >= 44.0` at line 39; artifact `ios-ci-31681888576-1`, ID `9174156400`, size `90267888`, API/raw ZIP digest `sha256:12c81ca60812fd9c7b93a198833562aa304b982c00f331f096ac558975d4a73f`. I2 changed only `FieldEvidenceAppUITests/S4_4ReportsUITests.swift`. This failed run ID is never rerun or described as accepted.
- Accepted predecessor exact-head evidence: run `31683138727`, job `94392986217`, succeeded at exact `phase/s4-reports@M` with P12/UI enabled; URLs `https://github.com/palatis3/AssetRounds/actions/runs/31683138727` and `https://github.com/palatis3/AssetRounds/actions/runs/31683138727/job/94392986217`; artifact `ios-ci-31683138727-1`, ID `9174943725`, size `6747140`, API/raw ZIP digest `sha256:25288fa9eefa64c7b3e6b8bb23c2a7f133b76a0ad82796ef8eb1c7b0809751dc`; `SHA256SUMS.txt` SHA-256 `8D55E0BC6B63FF060F291B7F419230672E9FD83E6002B9F8EB307D2FACB897E0`; all `105/105` listed payloads independently matched with no malformed, duplicate, missing, extra, or mismatched entry; logs `build-smoke.log` / `test-smoke.log` / `ui-smoke.log` SHA-256 `04934B6F797BC04F398E639989DED48E3BDC6F38D18D4943B6790354F9486535` / `0D104A8E02A7DEC2E182B5312A85388E26FDF237B976A026C83C2694CFBCB380` / `214C8564D08D139DDC30402FC19A23070A63F198E4920FEFB8C6CF8B80D58B37`; `ui-final.png` was `1206×2622`, SHA-256 `5F029C09290B1039A3C7E1F033276F0AAD1B3ADCDCE3A96BD58330BA0FBF58AC`; `7/7` unit and `1/1` UI tests passed.
- Accepted predecessor UI truth: the golden created three visits—the first CNV visit omitted Close evidence and the two later complete visits formed the valid comparison—then relaunched persisted data at Accessibility XXXL, exercised Reports filters/history/detail/comparison and missing-evidence omission, reached all four Then/Now images, and retained a coherent terminal in-app Then-close/Now-heading boundary screenshot with no unavailable or error state.
- Predecessor runner/toolchain/destination/budgets: `macos26` image `20260728.0273.1`; Xcode 26.6 `17F113`; iPhone 17 / iOS 26.5 build `23F77` / UDID `FC5FEF2A-E933-4515-AAEF-C9FC16651D0B`; setup `19/300` s, Simulator readiness `461/900` s, build `224/600` s, unit `237/900` s, UI `565/900` s, artifact `1` s, setup-plus-artifact `20/300` s, total `1297/3300` s.
- Task-start authority head A is the direct-child S4.4→S4.5 transition commit observed after that commit exists at fresh G0; this pre-commit authority file deliberately never self-records that commit's SHA.
- Required `M..A`: exactly one direct-child same-phase transition commit changing only the append-only `docs/execution/HANDOFF.md` S4.4 entry plus this immediate-next `docs/execution/CURRENT_TASK.md`; remote `phase/s4-reports=A`, remote `main=P`, and no other path dirty at G0.
- If frozen-authority audit requires a mechanical hydration correction before implementation, its direct-child authority-correction head R changes only this `docs/execution/CURRENT_TASK.md`; fresh corrected G0 observes R, proves `A..R` is exactly that one path, remote phase=R, remote main=P, and selector still equals accepted S4.4. R is authority provenance only and is never product implementation evidence; this file deliberately never self-records R's future SHA.
- Owner approval on 2026-08-13 resolves the two remaining S4.5 authority holds. A direct-child authority-completion head R2 changes only this `docs/execution/CURRENT_TASK.md`; fresh corrected G0 observes R2, proves `R..R2` is exactly that one path, remote phase=R2, remote main=P, and selector still equals accepted S4.4. R2 is authority provenance only and is never product implementation evidence; this file deliberately never self-records R2's future SHA.
- Owner execution-policy approval on 2026-08-13 removes the prior numeric correction/verification ceiling and resumes autonomous S4.5 recovery. The immutable implementation sequence is `I=a27611a80eb0ad86a27a57bfe7f8ac478b66cdec`, `I2=794cfe3a36ec845c8e483887cc85ddadffa32870`, then `I3=dd00093901097e412fb90ce399e93280f0ee557c`. Exact I2 run `31696713370` / job `94436051688` failed after the selected Xcode build passed because the targeted unit bundle reported six assertions across three methods and UI was skipped. Exact I3 run `31701326475` / job `94450936736` passed the selected Xcode build and all 11 targeted unit tests, then failed the UI fixture preflight at `S4_5CorrectionUITests.swift:386` because `s3.preflight.time-zone-confirmed` remained `0` after the synthesized tap. This direct-child execution-policy amendment changes only `docs/execution/CURRENT_TASK.md`, preserves every product/race/selector/environment/acceptance requirement, and authorizes successive diagnosed direct-child S4.5 correction heads plus one fresh exact-head P12 candidate at a time until accepted green evidence exists. Each correction remains limited to the already-allowed S4.5 implementation/test paths and one evidenced causal delta; no acceptance weakening, later-card work, rerun-by-run-ID, or duplicate concurrent candidate is authorized. Failure counts are provenance, not an approval gate or authority ceiling. This file deliberately never self-records the amendment commit's future SHA.

## Pinned authority

- Build plan: `docs/product/BUILD_PLAN_V4.md` / `23DAAB390AF917CBE91C3044E4906F3FBF8D67D2FDFC6BC9BDE985D984F37BBD`.
- S4.5 plan anchors: `## 6. Core workflow and state truth` Report truth; `## 7. Free evaluation, subscriptions, and payments` corrections do not consume another root; `## 9. Smallest reusable architecture` seven-model correction, exact `ReportSnapshotV1`, five-field correction copy, replacement, validation, and PDF invariants; `## 14. Analytics, feedback, and learning` correction exclusion from `report_saved`; global execution anchors remain `## 11`, `## 16`, and `## 18`.
- Runbook: `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` / `41622B8AE241FDFCBA3A03A430D27069A29DBE39EB508C6E155D687BA8A6AA79` / selected card S4.5 only.
- Execution contract: `docs/execution/CODEX_EXECUTION_CONTRACT_V4.md` / `9E7F3ABD2CC6FB15F33E4E56C95474BFEAC79C7148D7D125C75FBF88DB9F8A93`.
- Workflow: `.github/workflows/ios-ci.yml` / `9FFEA51ADB2620B01FE250412716487F6102DAC7E93AEA0471943AF66C1BC2AC` / `workflow_dispatch`.
- Accepted generalized UI exporter: `Scripts/ui-smoke.sh` / `6304A318EE046B6B19F4FDDC43BB143F9B21E8150B9D332E449B87A0182D4CDB`.
- Selector at G0 may remain accepted S4.4 compact-JSON-plus-LF SHA-256 `B3F3F01BA00E88AC4405DD8EF2CD06AF83A2A1D71739F131431F78D3E8366D0E`; the first support mutation replaces it with the exact S4.5 object below, compact-JSON-plus-LF SHA-256 `F985AFA45EEB2089287834E22F258055E7562923EE65C298D2D8E5543C7731AC`.

## Outcome and acceptance

- Outcome: from one uniquely current, fully validated ready Report, add a note-only clerical correction that atomically creates a completed check/recheck revision under the same Packet/stable root, creates a pending replacement Report and canonical snapshot copied from the immediately prior current Report, advances only the Packet's current record, then uses the shared renderer for pending→ready or failed. Every prior row and snapshot/PDF/evidence byte remains readable and immutable.
- Record authority: the correction is schema 1, completed, `revisionKind=clerical_correction`, same Asset/Packet/Issue/parent/root/stage as the immediately prior current revision, `revisesRecordID` equals that revision's ID, and `evidenceSourceRecordID` points directly to the original evidence-owning record across every generation. It changes only the optional note; any nonnull note is trimmed and 1–1000 characters. It creates no EvidenceFile and cannot open, resolve, relabel, replace, or otherwise mutate an Issue.
- Frozen record truth: copy completion/observation time, acknowledgements, outcome/CNV, work fields, pack versions, and PDF-template versions exactly from the immediately prior revision; the correction completion truth remains the original substantive visit and correction never becomes a new Reports/history visit.
- Snapshot truth: decode and fully validate the immediately prior current ready Report and copy its canonical snapshot. Before canonical re-encoding, change only `reportID`, `sourceRecordID`, `snapshotCreatedAt`, `sourceApp`, and `note`; packet/stable root/effective evidence source, site/asset/display labels, stage/outcome/CNV, acknowledgements/time, pack/template, evidence, history, issues, and disclaimer remain byte-for-byte semantic copies. Today's mutable Site/Asset/Issue/pack text cannot leak into a correction.
- Replacement truth: the new Report is schema 1, same Packet, new correction as source, immutable canonical snapshot path/hash, pending with null PDF path/hash, and forward-only `replacesReportID` equal to the immediately prior current Report. The prior Report is never updated; its replaced status is derived by the unique forward reference. Rendering uses only the existing validated snapshot/renderer authority, and ready remains terminal.
- GOLDEN: one correction points directly to original evidence, revises the prior current record, replaces the prior current Report forward-only, preserves frozen labels/issue cutoff/history/evidence, creates no evidence/root/counter use, reaches ready, and opens both the corrected and prior exact cached PDFs.
- ALT-1: a second correction revises the first correction and replaces its Report, but still points evidence directly to the original evidence owner, duplicates no EvidenceFile/evidence snapshot entry, preserves every earlier byte, and opens all applicable ready prior/current PDFs.
- UI acceptance: expose one correction-entry action from the existing Report detail authority and one note-only correction view. Owner-approved exact public copy is: action `Correct report`; field `Correction note`; instruction `Change the note only. Evidence, outcome, time, and report history stay unchanged.`; actions/states `Save correction` and `Saving correction`; validation `Change the note before saving.` and `Correction note must be 1,000 characters or fewer.`; precommit failure `Correction couldn’t be saved. Nothing changed.`; persisted render failure `Correction saved, but its PDF couldn’t be created. Retry from the saved report.`; ready headline/body `Correction saved` and `The prior report and evidence remain unchanged.`; revision states `Current revision` and `Prior revision`; navigation actions `View corrected report` and `View prior report`; count `{count} of 1,000 characters`. Exact accessibility hints are: Correct report — `Change only the report note and keep the prior report.`; Correction note — `Enter a different note, up to 1,000 characters. Leave it blank to remove the current note.`; Save correction — `Creates a new report revision and keeps the prior report.`; View prior report — `Opens the immediately prior saved report.`; View corrected report — `Opens the current corrected report.` Preserve a single report-detail source of truth. Exercise the first correction, corrected/prior PDF opening, then the second-generation correction and direct-original evidence link; retain one terminal in-app screenshot. The form and result path scroll at Dynamic Type, expose accessible labels/button traits/logical order/non-color state/44-point controls, and move focus deterministically for validation, saving, failure, and ready completion.
- Negative family: dirty context; duplicate/colliding Asset/Packet/stable-root/current-record/Report/replacement authority; non-current, pending, failed, nonterminal, or unvalidated source; broken/cyclic revision or replacement chain; malformed/out-of-range note; unknown schema/pack/template; noncanonical or mismatched snapshot/PDF/evidence path/hash/bytes; unsafe generation/path/symlink/special file; race/precondition, intent/journal, file, save, render, or recovery failure. Every case fails closed without partial authority, old-byte mutation, evidence duplication, issue mutation, or guessed truth; an honest pending/failed report remains recoverable by the existing bounded renderer rules.
- Finalization-store race truth: `FinalizationIntentStore` accepts the caller's frozen generation-root identity and pins independently verified Application Support, generation, and operations/finalization directory authority. Every intent/snapshot discovery, presence check, read, create, write, verify, promote, phase replacement, rollback, cleanup, and delete is descriptor-relative and no-follow (`openat`/`fstatat`/`mkdirat`/`renameatx_np`/`unlinkat` plus anchored enumeration), with exact ownership, byte/hash, type, identity, and containing-directory durability checks. URL/FileManager path reads or mutations cannot remain authority. A failed persistent or ABA root/journal replacement cleans only exact mutation-owned bytes through retained descriptors and leaves no database mutation, journal, staging/final snapshot orphan, or touched retained/unowned byte. A bounded internal test barrier may pause at the store authority boundary solely to prove those races deterministically; it is not product behavior.
- Forbidden behavior: changing evidence/time/outcome/issue/pack/template/history/frozen labels or any field beyond note and required revision identity; mutating/deleting old records/reports/snapshots/PDF/evidence; correction as a visit; new EvidenceFile, Packet, stable root, evaluation use, or `report_saved` increment; schema/model/project/package/capability/permission; renderer/template/storage/media behavior changes; editing PDF or report in place; hosted link/backend/upload; automatic Share/export; AI/OCR/scoring/search; work/recheck entry, deletion, backup/restore, commerce, diagnostic export, feedback, or any later S5+ behavior.

## Environment and exact selector

- Route / repository / refs: Windows authoring → exact GitHub Actions macOS verification; `palatis3/AssetRounds / public / main / phase/s4-reports`; never run or claim Windows Xcode/Simulator results.
- Required posture: `sandbox_mode=danger-full-access / approval_policy=never / command network enabled`; trusted repository configuration active; XcodeBuildMCP and owner-operated Mac unnecessary.
- Runner / Xcode / project: `macos-26 / Xcode 26.6 Build version 17F113 / /Applications/Xcode_26.6.app/Contents/Developer / FieldEvidenceApp.xcodeproj / FieldEvidenceApp / Debug / iOS 18.0`.
- Simulator selector: `iPhone 17 / iOS 26.5`; resolve exactly one ephemeral-job UDID.
- Tier / UI input: `P12 / run_ui_smoke=true`.
- Exact selector: `{"schemaVersion":1,"taskID":"S4.5","tier":"P12","runUISmoke":true,"setupArtifactTimeoutSeconds":300,"buildTimeoutSeconds":600,"testTimeoutSeconds":900,"uiTimeoutSeconds":900,"totalBudgetSeconds":3300,"unitTestSelectors":["FieldEvidenceAppTests/S4_5CorrectionTests"],"uiTestSelectors":["FieldEvidenceAppUITests/S4_5CorrectionUITests"]}` plus exactly one LF; 335 UTF-8 bytes, no BOM; SHA-256 `F985AFA45EEB2089287834E22F258055E7562923EE65C298D2D8E5543C7731AC`.
- Exact commands: `bash Scripts/run-with-timeout.sh 600 bash Scripts/build-smoke.sh`; `bash Scripts/run-with-timeout.sh 900 bash Scripts/test-smoke.sh`; `bash Scripts/run-with-timeout.sh 900 bash Scripts/ui-smoke.sh`.
- Required evidence: nonempty boot/build/unit/UI logs; Build, UnitTests, and UISmoke result bundles; terminal in-app screenshot; selection/runner/Xcode/Simulator/budget evidence; verified relative `SHA256SUMS.txt`.
- Allowed GitHub methods: read/fetch/ref/run/workflow/artifact inspection; exact-path staging/commits; non-force phase push; named workflow dispatch on exact phase ref with `run_ui_smoke=true`; exact-run observation/download; after green implementation, append-only HANDOFF phase-close bookkeeping, phase verification, verified non-force main fast-forward, exact-main UI-enabled verification, exact S5 branch creation, and S5.1 hydration only. Force-push, merge commit, PR, ref rewrite/delete, settings/secrets, signing, TestFlight/App Store, deployment/release, and S9.2/S9.3 are forbidden.

## Exact allowed implementation paths

Production paths:

- `FieldEvidenceApp/Domain/Workflow/ReportCorrectionRule.swift`
- `FieldEvidenceApp/Features/Reports/ReportCorrectionView.swift`
- `FieldEvidenceApp/Features/Reports/ReportDetailView.swift`
- `FieldEvidenceApp/Infrastructure/Finalization/FinalizationService.swift`
- `FieldEvidenceApp/Infrastructure/Finalization/FinalizationRecoveryService.swift`
- `FieldEvidenceApp/Infrastructure/Reporting/ReportDeliveryCoordinator.swift`
- `FieldEvidenceApp/Infrastructure/Reporting/ReportRecoveryService.swift`
- `FieldEvidenceApp/Infrastructure/Finalization/FinalizationIntentStore.swift`

Test paths:

- `FieldEvidenceAppTests/S4_5CorrectionTests.swift`
- `FieldEvidenceAppUITests/S4_5CorrectionUITests.swift`

Standing support exception:

- `Scripts/ci-selection.json`

New `ReportCorrectionRule` is the sole pure closed correction/replacement validator and immutable value boundary. Shared `FinalizationService` creates the journaled record/Packet/report/snapshot mutation and `FinalizationRecoveryService` extends that same intent recovery to the correction's nonnull packet-before/replacement authority without weakening original finalization recovery. Existing `FinalizationIntentStore` is hardened only as required by the frozen store-race truth above; it gains no new product contract or storage namespace. Existing `ReportDeliveryCoordinator` supplies the unique fully validated ready source, constructs the correction service, renders the new pending Report through its existing renderer, and returns exact ready/prior delivery values. Existing `ReportRecoveryService` must recognize a fully valid complete replacement chain with multiple Reports under one Packet during startup/retry while preserving its whole-matrix, no-scan, no-adoption, anchored-file, and at-most-once rules; it must not reject valid correction cardinality or weaken unsafe-authority maintenance routing. New `ReportCorrectionView` owns only note entry/status/navigation using the owner-approved exact copy; existing `ReportDetailView` adds the sole correction action at that same gate. Synchronized source groups include the two new Swift files. Existing seven-model schema/contracts/snapshot DTO and encoder, validator, renderer/template, Reports/history/index/comparison, project, resources, packs, diagnostics, media, other storage behavior, and fixtures remain unchanged. Unit fixtures are synthesized inside the one test file and UI reuses existing imported media; no project/resource/fixture delta is needed. `docs/execution/HANDOFF.md` is append-only post-green bookkeeping outside the implementation commit/cap. No other path may change.

## Execution

1. Fresh G0 observes S4.5 A and proves `A^=M`; `M..A` is exactly the append-only S4.4 HANDOFF plus this S4.5 CURRENT_TASK transition. The mechanical hydration-correction lane observes R and proves `R^=A` plus exact one-path `A..R`; the owner-approved authority-completion lane observes R2 and proves `R2^=R` plus exact one-path `R..R2`. Remote phase equals the current accepted authority head, remote main=P; S4.4 A/I/I2, failed candidate, accepted run/artifact/checksum/UI evidence are complete; selector remains accepted S4.4; no other path is dirty.
2. Replace selector first, implement only the exact allowed paths, run structural/static Windows checks without claiming an iOS build, explicitly stage task-owned paths, and commit direct-child I.
3. Re-fetch, prove phase still at the intended parent, non-force push exact phase ref, and run the one-at-a-time P12 verification lane. After any non-green candidate, inspect complete causal evidence, make at most one diagnosed direct-child in-scope correction for that evidence, and dispatch one fresh exact-head candidate; repeat without another owner approval until green or a genuine scope/authority/ref blocker occurs. Accept only green exact-head CI with complete checksum-verified unit/UI/artifact evidence.
4. After accepted S4.5 implementation evidence, re-fetch, append the complete S4.5 HANDOFF, and create/push the HANDOFF-only phase-close C. Verify the exact phase ref at C with UI enabled; if a diagnosed recovery head K is required, preserve E and prove E..K contains only authorized nonproduct recovery paths.
5. Re-fetch and prove the phase ref and ancestry; advance `main` from immutable P to the exact accepted phase verification head only by verified non-force fast-forward, then require green UI-enabled exact-main CI at that same head. Only after both refs and evidence are exact may program autopilot create `phase/s5-work-recheck` from green main and hydrate S5.1 CURRENT_TASK; run fresh S5.1 G0 before any S5 implementation.

## Definition of done

- Exact green S4.5 evidence proves first- and second-generation note-only clerical correction; direct-original evidence ownership; immediately-prior revision and forward Report replacement chains; exactly five permitted snapshot changes; preserved frozen labels/issue cutoff/history/evidence and every prior byte; zero new EvidenceFile/Packet/root/evaluation use/`report_saved`; pending→ready shared rendering; exact prior/current PDF reopening; honest fail-closed/recovery behavior; accessible deterministic note-only UI; and no later behavior. The append-only phase-close handoff is verified on the exact phase ref, `main` is non-force fast-forwarded from P to that exact accepted head, exact-main UI CI is green, both remote refs are reproved, and only then may the exact S5.1 branch/authority transition begin.
