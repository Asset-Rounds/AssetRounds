# S10.4 active CI operating brief

Owner-authorized development policy H409, shared-build/segment amendment H411, efficiency/diagnostic amendment H412 and finite app-observation amendment H414. Start here, then verify CURRENT_TASK's active reading map, Frozen authority/package, Outcome, Environment and latest applicable recovery record. This brief is navigation; CURRENT_TASK and the pinned plan/runbook remain acceptance authority. Read historical details only when they resolve a current decision.

## Current task and evidence

Finish S10.4's automated evidence, then follow the authorized S10 sequence. Use live Git/GitHub identities and the latest applicable recovery record for the candidate. Re-fetch and verify local HEAD, origin/phase/s10-brand-refresh and immutable main/P before mutation. Keep changing heads, runs and progress in one timestamped working ledger, not this guide.

Each ledger entry records run ID, exact head/profile/segment, owner, current stage, original evidence/audit location, failure classification and next eligible action. Refresh active entries before using capacity; stale status does not prove a run stopped. Separate static checks, hosted compilation, five native unit identities, UI progress, strict state/PNG closure, full shard evidence and formal acceptance. Cleanup or upload success proves none of the earlier gates.

## Prepare one candidate

1. Read current source and complete relevant original failure evidence. Reuse agents for independent diagnosis and nonoverlapping work; root owns integration, refs and dispatch. Batch compatible diagnosed corrections and carry one independent integration review through incremental findings.
2. Retire superseded source-text locks and consumed implementation prescriptions. Keep a compact retirement-to-coverage map. Preserve each active purpose through focused executable semantic checks, required native regressions or independently reviewed equivalent coverage; absence of a failing test is insufficient. Never blindly repin assertions to observed values.
3. Preserve source/environment/selector/receipt bindings, required state semantics, strict AX/contrast, hostile-input rejection and final acceptance. For each changed selector, record exact product/localization provenance or captured native AX evidence before choosing expected text. No guessed title, count, frame or coordinate.
4. Run supported deterministic checks once on the combined draft. Explicitly assess affected unsupported assertions and dormant diagnostic branches. Repeat checks or reviews only for changed contracts, new failures or unresolved concerns. A static check does not compile Swift or run a Simulator.
5. Qualify the changed scope on its own exact head. An isolated minimum-profile change may use compilation, all five required native methods and affected existing routes without an automatic 67-state current-light prerequisite. Record affected/omitted coverage and review rationale; shared changes or uncertain impact require broader coverage. Do not invent a focused route or relabel accepting work as diagnostics.
6. Once the candidate is ready, freeze its bytes and selected payload and collect the complete same-head matrix. Stop optional optimization. A correction creates a new head requiring new qualification; preserve every old run/artifact and never transfer its acceptance.

## Shared builds, segments and reusable control

H411 permits one qualified immutable exact-head payload and original five-unit proof to serve all fourteen shards. Consumers independently verify complete checksummed .xctestrun/Build/Products closure, original producer/run/artifact identity, source and expiry, then use test-without-building on fresh exact-profile Simulators. No consumer rebuild/fallback or invented local unit pass. Policy and implementation review are not hosted qualification.

All seven minimum logical shards may compose source-defined segments; current AX retains its admitted segments. Each logical shard must close all 67 owned states and six common tasks, with explicit public prerequisites, entry/exit assertions and continuously executed critical journeys. Keep setup replay separate from owned proof. The source assembler validates every segment, journey, profile and shared-payload dependency. Measure time to changed code, actual branch execution, new states and complete elapsed time; fewer owned states alone do not prove speed.

H412 adds only Scripts/s10-4-ci.py and Scripts/test-s10-4-ci.py to the finite H411 support envelope. Use one reviewed parameterized controller and factual audit driven by committed source and original API identities. A new head/run supplies data, not a copied executable helper graph or fixed historical run count. Reuse semantic/control review only while the relevant bytes/contracts stay unchanged. Preserve previous helpers and records for runs that consumed them.

Prestage configuration, collector preparation and review while the producer runs; fill actual qualification identities after they exist. Never dispatch a dependent route before its gate. Every explicit dispatch records its unanswered question, exact head/profile/coverage, prior terminal audit, qualification dependency and expected evidence. Recheck refs/main-P, clean owned source, complete current-head history and repository-active runs, provider capacity, no active logical duplicate, original producer-five/payload/expiry proof and successful source dependencies. Write durable exclusive intent and retain raw response/direct run ID. An uncertain request permits reconciliation of that request only. Never rerun an old run ID, cancel work, guess a missing identity or substitute a route.

## One original collection and audit

Assign one collector and one full audit owner per run. Retain original API metadata, logs and archives once. Verify API size/digest, safe ZIP members/CRC, complete original checksum closure, exact source/environment/commands, five producer identities and strict state/audit/PNG binding. A state marker without matching audit and screenshot is incomplete. Preserve ambiguity even when a manual incident comparison adds information.

Reuse verified originals and hashes after that audit while rechecking mutable API status/expiry and inspecting new evidence. Audit older runs against immutable source projections. Parameterized summaries report facts and gaps; they do not create successful receipts, native results or human review. Classify setup, compiler/unit, native assertion, crash, collection and integrity failures separately before selecting a retry/correction. Preserve overlapping clocks rather than treating native time, setup overlap and finalization as interchangeable savings.

Inspect every terminal result's complete evidence before an evidence-based correction or retry. Refill distinct dependency-ready capacity in parallel with independent inspection. Never dispatch an unchanged known deterministic failure, duplicate an active shard or accept missing logs, xcresult, screenshot, accounting or checksum proof. Transient failures use the authorized fresh-runner recovery loop; source changes use reviewed causal corrections.

## Public preparation and bounded diagnostics

H414 permits only the reviewed DEBUG SignsRootView path/lifecycle observer and exact current Reduce Transparency launch opt-in. Records require the exact token and a current recheck-due issue; they contain fixed scalar fields and no route contents or user data. Preserve the original NavigationStack binding, writes, public actions and strict waits. Callback timing, coalescing, incomplete output and the separately read fields limit interpretation; a missing line does not prove the native Back handler did not run. All DEBUG profiles incur observation/guard overhead, and enabled logging can occur before success or failure is known. This is not a demonstrated repair. Requalify the actual new native head and retain every old original and human decision at its original head. H413's approved nine required profiles and five explicit deferrals remain a separate evidence-policy transaction, rebased onto that new native head before formal acceptance.

Shorten preparation only after an explicit public entry/exit/replay/dependency map proves unchanged durable state and continuous journeys. Preserve required states/assertions, localization and minimum-specific fixes. No copied Simulator/database, injected entitlement/data, fabricated receipt or lost owned journey. Reuse a getter only within the same stable observation; discard it across actions, waits or transitions. Measure the new native path honestly.

Concentrate diagnostics on demonstrated missing observations: shared-tuple admission, input-chain/export gaps, native incident windows, cached assertion operands, completed purchase phases, truncated tails and early-failure integrity. Retain original errors, strict audits, native source/PID/Simulator/incident bindings, bounded budgets and truthful truncation/non-atomic/missing values. Preserve already-used lazy operands before failure; do not reexecute a failed query to invent its result. A narrow completed-phase record needed before a later crash has success-route cost, which must be explicit. No blanket logging, suppression of warnings/failures or unsupported claim that logging caused a crash.

Use supported bounded native diagnostic export when original container evidence lacks a usable attachment; retain its raw originals/status separately from the UI manifest. Incident-window queries keep their qualified process/time/budget/staleness gates. Timed-out or truncated results stay incomplete. Early setup evidence may be hashed while accounting is explicitly unavailable; hashing never converts setup failure into acceptance.

## Exact routes and capacity

Author on Windows; compile and execute iOS only through authorized hosted routes. Use up to five GitHub macOS and three Bitrise M4 distinct useful dependency-ready jobs. GitHub remains formal acceptance for every minimum profile. Bitrise formal current-light/dark still requires its existing exact-head equivalence gate; prior development evidence is never promoted. Leave blocked capacity idle instead of duplicating work.

H410 is the closed compilation/unit-only Bitrise development route. Verify committed implementation, authority and exact allowed tuple before use; every new candidate needs its own qualification. Canonical F25 selectors stay unchanged; UI is omitted only for the admitted current-runtime diagnostic tuple. Inspect actual steps and original qualification receipt: the intentional nonaccepting development guard is distinct from setup/build/unit failure. Affected UI and final matrix remain required.

Minimum startup retains GitHub macos-26, Xcode26.6/17F113, iPhone SE3/iOS18.0/22A3351 and a fresh isolated Simulator. Skip redundant installation only after proving an already available exact runtime. Preserve cold provisioning and final readiness contracts. No different provider/image/runtime, unchecked cache, purchase, account/secret change, agent installation, increased watchdog or speculative sleep. H402 permits read-only inventory of existing exact-runtime capacity. A new environment requires separate evidence and authority.

## Housekeeping and completion

CURRENT_TASK's historical State/recovery entries, DECISIONS and completed HANDOFF entries are immutable provenance, not pending commands. Consolidate current guidance and superseded checks without deleting history or unowned work. At integration boundaries and approximately every eight hours, inspect actionable new accumulation using the existing coverage map; avoid rescanning unchanged history or changing a frozen candidate. Keep the s10-4-verification-housekeeping heartbeat quiet unless meaningful action is needed, and pause it after S10.4 acceptance.

Final proof remains fourteen complete logical shards on one exact head and selected qualified payload, 67 states and critical journeys per shard, 938 visual cells, 84 accessibility rows, six common tasks, strict native AX/contrast with only existing eligible exceptions, exact source/environment/command receipts, complete original checksums and genuine human visual review. S10.5 physical proof and S10.6 release evidence are separate subsequent cards. No signing, upload or submission authority is added.

## H415 — Exact GitHub image rollover

The owner approved exact GitHub image `20260907.0351.1` after minimum jobs were assigned it and rejected the old exact-image guard before native work. Other current-head jobs passed the old guard, so no universal outage or native reliability gain is claimed. During mixed rollout, a new exact pin rejects old-image allocations and offers no guaranteed immediate capacity gain or native fix, while retaining the exclusive old exact pin rejects new-image allocations. CURRENT_TASK H415 grants a two-document authority anchor followed by the finite six-path implementation with exact `s10.4-github-image-adoption-v2` provenance and unchanged macOS/Xcode/SDK/current/minimum runtime tuples and strict evidence. This is the sole prospective exception to the earlier exact-image restriction. Existing 2ce producer/consumers remain immutable evidence at their own head and do not transfer. The new implementation needs its own producer/five methods/payload, all nine required profiles and genuine new-head human review; rebase H413 over that actual native head. Observe the authority SHA only after commit; no placeholder may enter canonical bytes.
