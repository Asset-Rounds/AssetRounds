# Copy everything below into a new Codex task

I am the owner and I authorize AssetRounds V30 pre-Phase-10 provisional implementation to begin now. I invoke external authority `ASSETROUNDS-V30-PRE-S10-20260902-R2`. Before mutation, compare this owner message's literal authority ID and authority content digest to the exact `authorityID` and `authorityContentDigest` fields in the external authority JSON and package manifest. If either is absent or mismatched, hold read-only; this prompt does not issue or amend authority.

- authorityID: `ASSETROUNDS-V30-PRE-S10-20260902-R2`
- authorityContentDigest: `ab585279a32cb8e53b5656af6efb264a85ced24116ace3b1de9f56a14f19cec6`

Use the package in:

`C:\Users\palat\OneDrive\Desktop\AssetRounds V30 Globalization`

Read `C:\AssetRounds-v23-expansion\AGENTS.md` and every applicable instruction it names before branch creation or mutation, without accessing `C:\AssetRounds`. Then read every package file completely in the order specified by `EXPANSION_V30_HANDOFF.md`. Independently recompute and compare every raw hash and canonical digest in `V30_PACKAGE_MANIFEST.json`; run package validation and generator `--check` modes only, never regenerate or rewrite the immutable package. Do not trust copied hash text or a prior task summary. After creating the dedicated V30 worktree, re-read its inherited `AGENTS.md` and applicable instructions before the next mutation.

My intended outcome is one eventual AssetRounds app containing accepted Phase 10 branding, V23 expansion, and V30 globalization. Phase 10.6 is still being implemented elsewhere and may take a long time. Do not wait for it before doing the work that the V30 blueprint classifies as pre-S10 provisional.

Authority for today:

1. Create `phase/v30-globalization` and `C:\AssetRounds-v30-globalization` only from frozen V23 head `acbfb68355f903fe98638b6ef22e4814e7b48328` / tree `47e17fae6b73dccd5029ccf4ac7cca659196f225`.
2. Install all and only external-authority/manifest-enumerated V30 R2 package support artifacts on that branch. The four external Markdown package files are immutable inputs; the installed execution handoff is `docs/design/v30/execution/V30_EXECUTION_HANDOFF.md`.
3. Use the closed bootstrap exception only for branch/worktree creation, exhaustive package installation, one expected-absent G3 namespaced provisional-ledger genesis/receipt, and one Card 1 task/selector projection. It permits no product work.
4. Start Card 1 of 55: `V30-P00-C01 — Provisional authority and isolated-lane validation`, using only `docs/design/v30/execution/V30_CURRENT_TASK.md` and `docs/design/v30/execution/V30_CI_SELECTION.json` as active task/selector paths. P00-C03 validates G3; it never creates a second ledger.
5. Continue the graph-enumerated 37-card pre-S10 cohort in exact order, one current card at a time, while Phase 10 continues.
6. You may implement shipping product behavior, tests, resources, project integration, and provisional hosted branch CI when the exact current-card fence permits them.
7. If an exact allowed path intersects the frozen 86-path S10 reservation, it is permitted only with its exact pre-issued authority tuple in both the card's `allowedPaths` and `s10SharedPaths`, labeled `S10_SHARED_RECONCILIATION_REQUIRED`. Otherwise enter `CONFLICT_HOLD`. This never changes Phase 10.
8. All pre-S10 work is provisional. Graph progression through the pre-S10 cohort is allowed, but it provides no final, canonical, post-S10, exact-main, phase-close, release, or successor credit.
9. Preserve every commit, failure, superseded candidate, test result, artifact, path overlap, and replay/reimplementation requirement.

Hard isolation:

- Do not read, enumerate, status, diff, log, build, test, fetch from, poll, or mutate `C:\AssetRounds` or its active Phase 10 worktree/ref.
- Do not check whether Phase 10.6 is done. I will tell you when it is done.
- Do not mutate `main`, a Phase 10 ref, the frozen V23 worktree, or canonical V23 coordination.
- Do not merge the V30 planning branch or provisional branch wholesale.

Post-Phase-10 gate:

- No member of the graph-enumerated post-S10 cohort is selectable now.
- When I later report Phase 10.6 complete, treat my message only as a read-only trigger. Do not select post-S10 cards until a new external `V30PostS10ReconciliationAuthorityV1` is present and valid.
- Freeze provisional V30 `P`, verify frozen V23 `B`, and prove accepted `S` has Phase 10.6, lossless V23 representation of all 146 cards/230 edges, preserved unfinished V23 states, and green exact-main evidence. Then create `phase/v30-globalization-reconciliation` in `C:\AssetRounds-v30-globalization-reconciliation` from exact `S`.
- Replay or reimplement every frozen provisional-cohort delta in graph order with old/new commit and tree mappings. Never merge wholesale.
- Accepted Phase 10 owns actual brand/design conflicts; preserve V30 functionality by reapplying it to the accepted UI and rerunning invalidated evidence.
- Only the graph-enumerated post-reconciliation acceptance/integration cohort beginning at `V30-P06-C01` can provide final linguistic/product acceptance, main integration, release, future-market, or monitor evidence.

Product rules:

- Initial complete localizations: English, U.S. Spanish, Simplified Chinese, Traditional Chinese, Vietnamese, and Korean.
- Initial commercial market/storefront/project jurisdiction: United States only.
- Use Apple device and per-app language resolution, expose the effective language and iOS Settings handoff in AssetRounds Settings, and fall back to English when no supported match exists.
- Do not bundle-swizzle or create a hidden localization engine.
- Keep app language, formatting locale, authored-content language, report language, storefront, and jurisdiction independent.
- Preserve language-neutral canonical data, IDs, enums, hashes, journals, evidence, backups, and machine exports.
- Preserve user/admin-authored source content; a translation is a derived artifact with provenance.
- A translated interface never implies foreign legal/standards support or OCR, dictation, speech, grammar, support, or release capability.
- Translation is incomplete until professional/native in-context review after reconciliation.

Execution rules:

- Inherited V4 `docs/execution/CURRENT_TASK.md`, plan, runbook, and selector are frozen predecessor evidence only: never edit or use them as V30 active authority. No active card may grow the external support-artifact set.
- Hydrate each pre-S10 card only from its exact pre-issued `V30_PRE_S10_PATH_FENCES.json` entry; no added path, glob, directory root, shorthand, or guessed path. A missing required path is `CONFLICT_HOLD`, not permission to expand the fence.
- Use exact-path staging, direct-child corrections, append-only receipts, non-force pushes, and evidence-driven recovery.
- Never claim an iOS build/Simulator result from Windows. Use only the card-authorized hosted macOS workflow.
- Card 5 may adapt only its exact pre-issued workflow/helper copies on the isolated V30 branch to the typed V30 selector. Never change the inherited selector or actual Phase 10 lane. Pre-S10 native runs are optional diagnostics; unavailable runs carry no native credit, and all failures remain recorded for mandatory post-reconciliation qualification.
- Signing, TestFlight, App Store upload/submission, availability changes, foreign storefront activation, and release are not authorized.
- Never fabricate professional/native, legal, licensed, physical-device, owner-action, monitor, or deferred evidence.

Subagents:

- Use safe parallelism aggressively within the current card.
- Reuse agents with relevant context when safe.
- Prefer Luna max for bounded inventory/extraction/hashes/hygiene.
- Use Terra medium/high/xhigh/max for substantive implementation, tests, tooling, review, and CI analysis.
- Use Sol low/medium for the most consequential architecture, integration, and CI decisions.
- Give each agent an exact path fence or read-only scope, authority pins, invariants, commands, prohibitions, and required decision-ready evidence.
- Locale agents may parallelize read-only preparation only; exactly one card may mutate, checkpoint, or transition at a time. Do not let two agents edit the same path concurrently.
- Keep an independent audit lane for consequential work.

Status communication:

- Tell me the human card number and title, for example: “Card 1 of 55 — Provisional authority and isolated-lane validation.”
- Send concise updates when a card changes state.
- Continue through the pre-S10 range without polling Phase 10.

Start with read-only validation. If the authority, package, base, branch, worktree, or frozen pins mismatch, stop mutation and report the exact mismatch. Otherwise create/install the isolated V30 lane and begin Card 1 today. The 37-card pre-S10 cohort may proceed provisionally while Phase 10 remains forbidden; the 18-card post-S10 cohort remains locked.
