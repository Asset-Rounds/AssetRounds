# Paste-ready Codex prompt — Implement AssetRounds Expansion V23

<!-- V23_PACKAGE_MANIFEST_START -->
## V23 package manifest

Canonical logical hashing: decode UTF-8 with no BOM; normalize CRLF and lone CR to LF; remove the package-manifest start-marker line, every enclosed byte through the package-manifest end-marker line, and the immediately following LF when present; strip trailing SP and TAB from every remaining line; normalize to exactly one final LF; SHA-256 the resulting UTF-8 bytes; encode lowercase hexadecimal. Canonical package hashing uses `V23PackageDigestV2`: require exactly one marker-delimited manifest in each file and require all four blocks to be byte-identical; replace only the 64 lowercase hexadecimal characters in the manifest's Package digest value with 64 ASCII zeroes; normalize that zeroed block to LF, no trailing SP/TAB, and exactly one final LF; then SHA-256 `ASCII("V23PackageDigestV2") + 0x00 + UTF8(zeroedCanonicalManifestBlock) + 0x00` followed by, in ordinal-filename order, `UTF8(filename) + 0x00 + ASCII(lowercaseLogicalDigest) + 0x00`. The package digest therefore binds every visible manifest byte except its self-referential value. A verifier must compare the exact canonical block, not search for digest substrings. A hash proves integrity against a separately trusted expected digest; it is not a signature or independent authenticity claim.

Canonical structural hashing uses JSON arrays, JSON string escaping, no insignificant whitespace, UTF-8 without BOM, SHA-256, and lowercase hexadecimal. Every array-valued absence is JSON `[]`; Markdown `NONE` is display-only and never enters structural JSON. Register order is the published topological order with key order `id,title,classification,status,directPrerequisites,lineage`; graph uses that order with `id,directPrerequisites`; lineage is ascending numeric phase/card ID with `predecessor` (JSON null for added cards), `successor,disposition`. Impact uses topological order and `id,aggregateAcceptanceMemberships,invalidationConsumers,optionalCapabilityProviders,conformanceSubjects`. Relations first iterate dossier cards in published topological register order and emit base rows in fixed kind order aggregate, invalidation, conformance, contract-input, optional, predecessor provenance; then append dependency-disposition `PROVENANCE_REFERENCE` rows in the exact §6D ledger order. All relation rows use key order `source,kind,target,evidence,activation,fallback,invalidates,authority`. Contract facets retain contract-input relation order and key order `source,target,selector,sourceAuthorityRefs,activation,fallback`; source-authority refs are lexically sorted. Selectors use their published seven-row order with `id,members,consumers,predicate`. Dependency dispositions use published topological successor-card order then inherited predecessor-list order with `predecessorConsumer,predecessorProvider,currentConsumer,currentProvider,disposition,authority`. Member arrays retain their published order.

| File | Authority role | Logical SHA-256 |
|---|---|---|
| EXPANSION_V23_FOUNDATION_PLAN.md | Scope/register/graph/lineage | `4caf56670c6e211291a6a141e4c8d35762d1155c169c91668f044644f60811c6` |
| EXPANSION_V23_ARCHITECTURE_BLUEPRINT.md | Architecture/contracts/dossiers | `779d91091164d33fc81a77deb689d631c7cca1bc55a80e3806ce7f874f5b3d48` |
| EXPANSION_V23_HANDOFF.md | Execution/CI/acceptance | `ca3e563f072c1ce34207c41272e2755a8d8d474c40640a0612ab33a0deebeab9` |
| NEXT_CODEX_SESSION_PROMPT.md | Invocation only | `355d511ef73319561b7455d64f7f98cda0a655768282fb8b88f662ef9e13d2f7` |

- Package ID: `ASSETROUNDS-EXPANSION-V23-20260825`
- Manifest schema: `V23PackageManifestV1`
- Package digest algorithm: `V23PackageDigestV2`
- Package digest: `99a2719885ad1abfb8cf5d49c6b2099754bb0ba4b5d27fcbae06476aad507570`
- Card register digest: `edd6109aab118cc35c91495b789f70eb0b7c4d5f3d0780ad7a1918e5379e4cbd`
- Direct graph digest: `4e9feb8b0cb65deddd3a5802efb380911a3439e44f1a0dc56656eadb29aac2ae`
- Lineage digest: `8c5a573df6b9242096e53983da9c78cc4cb2246e6ce4b504f1e4d45d42173283`
- Impact projection digest: `a460620a0f0242fe0e71d8604284826204f000bb45fa09249c2db994dd0fa70b`
- Card relation digest: `9b5c7f664af7d79d219e3ca55a28352bc0da7d9ddf998033b9a82187b428fac4`
- Contract facet registry digest: `b255fe1249ef40cf835fb6717876f20eee864407c206e4ae3baf4a109ab8949f`
- Selector digest: `6ef4089521319677f3d69ed691d638dcc12521789c575c7939966e47670ce7f2`
- V21 dependency-disposition digest: `f30d779c19e94d57d9b3114c09ac07538588676606c78d2e369683ee91169b8c`
- Card count: `146`
- Direct edge count: `230`
<!-- V23_PACKAGE_MANIFEST_END -->

## 0. V23 execution amendment — scope-neutral operating rules

This amendment is authoritative over inherited V22 execution wording. V23 adds no product feature, foundation, persistent schema, public claim, route, root, or release platform. All 146 V22 cards and their product semantics remain intact; only execution governance changes.

### 0.1 Parallel operation with active Phase 10

V23 may begin while Phase 10 is still active. Both efforts use the same AssetRounds GitHub repository and remote, but each must use a distinct branch and a distinct Git worktree. They must never write through the same checkout. Before mutation, record the fetched remote identity, both heads, their merge base, dirty/untracked state, active worktrees, and a path-ownership ledger.

Phase 10 owns its current brand, shared-component, vocabulary, accessibility, snapshot, and accepted-state evidence paths until it closes. V23 may immediately implement dependency-ready work whose hydrated write fence is disjoint from those paths. A V23 card that reaches a Phase 10-owned path pauses only that lane; unrelated V23 work continues. Do not duplicate, replace, pre-empt, or independently freeze unfinished Phase 10 authority.

Use `phase/v23-expansion` in an adjacent `AssetRounds-v23-expansion` worktree. Phase 10 keeps its existing branch/worktree. When Phase 10 is accepted, integrate it to the repository's designated integration base first. Then fetch and non-force merge that accepted descendant into `phase/v23-expansion`. Resolve brand/shared-UI conflicts in favor of accepted Phase 10 authority while preserving V23 product behavior. Record the merge base, both parent heads, conflict paths, resolutions, and targeted reproof. Never force-push, reset, clean, overwrite, or discard either effort.

V23 may not claim final application, brand, visual, accessibility, archive, or release acceptance until accepted Phase 10 is incorporated. After incorporation, run affected-state proof for V23's changed states and one exact-candidate application closure. Do not repeat the original Phase 10 67-state freeze unless an accepted Phase 10 authority digest itself changed or the affected-state inventory proves those states were modified.

### 0.2 Dual-pool CI capacity: five standard GitHub plus three GetMac

The repository may use up to eight concurrent CI jobs when capacity is available:

- `STANDARD_GITHUB`: at most five concurrently executing standard GitHub-hosted jobs.
- `GETMAC`: at most three concurrently executing GetMac jobs using the repository's verified runner labels and configuration.
- Combined ceiling: at most eight concurrently executing jobs, with every job charged to exactly one pool.

Queue rather than exceed either pool. Matrix `max-parallel`, workflow concurrency, and the simplest available native runner controls are preferred. Do not build a custom scheduler, lease protocol, service, or additional workflow merely to reproduce a limit already enforced by the selected platform. Add a small repository controller only if current workflow inventory proves native controls cannot enforce the two ceilings across the applicable workflows.

At activation, inspect current workflow files, runner labels, repository/organization limits, active runs, and GetMac availability. Never infer that GetMac is configured from this plan alone. If GetMac is unavailable, continue with zero to five standard GitHub jobs; GetMac absence is reduced capacity, not a product blocker. If standard GitHub capacity is unavailable, use up to three verified GetMac jobs where compatible. Unknown or unclassified occupancy blocks only new dispatch into that pool until reconciled.

Use the pools for independent test shards, never concurrent source integration. Prefer one build feeding compatible test shards when immutable head/tree, toolchain, configuration, fixtures, and artifacts match. Keep official acceptance fail-fast and separate from the non-accepting diagnostic sweep.

### 0.3 Goal discipline and anti-overengineering guard

Implement the smallest production-quality change that satisfies the active V23 card and its declared acceptance evidence. Every changed path must map to a current card requirement, demonstrated causal correction, or necessary integration resolution. Do not add speculative features, generic frameworks, future-market behavior, duplicate abstractions, new roots, parallel stores/writers/renderers/importers/design systems, or unrelated cleanup.

Reuse the accepted repository's existing architecture and Phase 10 brand system. Extract or generalize only when the current requirement needs it, at least two current consumers demonstrably share the behavior, or an existing V23 contract explicitly requires the seam. Prefer local changes, existing platform APIs, deterministic fixtures, and targeted tests. Stop expanding a solution when the stated behavior is implemented, its edge cases are covered, and its declared acceptance evidence is green.

Do not turn planning contracts into shipping types or runtime machinery unless a card's actual implementation requires them. Planning receipts may remain evidence documents or CI artifacts. No requirement is satisfied by prose alone, but not every planning noun requires a Swift type.

### 0.4 Standing implementation authorization

The owner grants standing authorization for all in-scope, dependency-ready V23 implementation, bounded refactoring required by that implementation, tests, causal corrections, local commits, dedicated worktrees/branches, ordinary pushes, pull-request updates, and CI dispatch within the two-pool limits. Codex must continue autonomously through eligible work without requesting routine phase-by-phase approval.

This standing authorization does not permit destructive loss of user work, force-pushes, history rewrites, secret creation or disclosure, purchases or billing changes, runner-account provisioning, protected-branch/security-policy changes, public communication, App Store submission/promotion, legal declarations, or fabricated physical-device/external evidence. An unavailable external condition blocks only its affected lane; record it precisely and continue all unrelated READY work.

### 0.5 Completion and integration rule

Phase 10 and V23 converge into one AssetRounds application in the same repository. There is no second app and no long-lived fork. Phase 10 establishes the accepted brand/shared-UI baseline; V23 adds the preserved V22 product scope on top. Final V23 acceptance requires the combined exact candidate, no unresolved ownership conflicts, no duplicate systems, accepted affected-state brand/accessibility proof, and the existing V23 release gates.

**Package:** AssetRounds Expansion V23

Use the following as the first message in the implementation task, with all four V23 Markdown files attached or otherwise made available to that task.

---

Pre-effect release infrastructure failures use `INFRASTRUCTURE_BEFORE_EFFECT_LEASE` plus the closed `ProtectedReleasePreEffectInfrastructureReasonV1` only when the durable effect lease is absent, zero provider mutation command/response/effect is proved, and the runner captured the complete pre-effect snapshot. A failure before that snapshot emits `RELEASE_EXTERNAL_MUTATION_PRELEASE_UNCERTAIN_RESULT`, never a fabricated no-op. `ProtectedReleasePreEffectUncertainReconciliationReceiptV1` may create `ProtectedReleaseNoEffectProofSourceV1.RECONCILIATION_DERIVED` with a canonical snapshot and exact no-op reason only after independent absent-lease/zero-command or exact unchanged-provider proof, route a present lease to post-effect recovery, or remain owner-pending. It never rewrites hosted evidence or grants mutation retry.

If a durable effect-start lease exists but the mutation result artifact is missing, use `ProtectedReleaseLostPostEffectStartRecoveryReceiptV1` to bridge exact lease + terminal API job + exhaustive artifact absence into `UNKNOWN_RECONCILIATION_REQUIRED` and a separate read-only reconciliation reservation. Never apply the generic fresh-nonce rule to redispatch that mutation; release its old lane only after terminal API proof and the matching unknown bridge.

Every protected action begins with `ProtectedReleaseAdmissionIntentV1`; the active state writer atomically creates the generic authorization/gate pair and, for a control action, the matching operational AUTHORIZED cell in one `ProtectedReleaseAdmissionEffectRecoveryReceiptV1` transaction. Dispatch is forbidden until complete matching admission. Before any preclaim, postclaim/pretransfer, or posttransfer/predispatch zero-effect state terminalization, the controller must use `ProtectedReleaseZeroDispatchClosureClaimV1` plus exact `ProtectedReleaseZeroDispatchClosureClaimRecoveryReceiptV1.APPLIED_MATCHING_CLOSURE_CLAIM` to CAS the exact dispatchable predecessor to `ZERO_DISPATCH_CLOSURE_PENDING` while retaining the lane. Claim dispatch, transfer, mutation dispatch, and closure compete on that same predecessor; a losing closure may not terminalize state under the old window. Only after the closure claim wins may the matching state receipt close generic/operational cells and the queue receipt CAS closure-pending to TERMINAL after a fresh zero-queued/zero-running-job scan. Claim `NOT_CREATED | OBSERVED_CONFLICT`, preclaim expiry/cancellation/invalidation, postclaim/pretransfer invalidation, and posttransfer/predispatch invalidation use their respective typed state and queue receipts. No branch may strand or reuse the claim, fabricate a job/result, or release a lane before nondispatchable closure, state closure, and zero-job proof.

Every potential P05/product supersession, authority-only evidence incident, established-ACTIVE standalone stop, or post-commit containment-worthy stop/safety incident has a predeclared source-effect intent/ID and monotonic per-release source sequence. `ReleaseSafetyPauseSourceSequenceCellV1 = OPEN(committedSourceHighWater) | PAUSE_FENCED(frontierGeneration,effectLeaseDigest,leaseSourceHighWater,currentCommittedSourceHighWater)` is the sole sequence allocator and the common serialization cell for source commit and PAUSE effect-start. Each source effect atomically increments that cell, commits source truth, and commits `ReleaseSafetyPauseTriggerEvaluationV1.PENDING` without naming any future recovery/frontier/trigger digest; its immutable outbox binds the sequence-cell predecessor/successor and source/PENDING digests. `ReleaseSafetyPauseSourceEvaluationGenesisRecoveryReceiptV1` proves that historical atomic transition against the same cell version or a contiguous monotonic descendant, never by rewinding the current cell; matching source-effect recovery validates the closed source. Merge sources without gaps into cumulative generation-CAS `ReleaseSafetyPauseTriggerFrontierV1` records whose current pointer binds `mergedThroughSourceSequence`. An EVALUATING successor additionally binds `pendingCommittedSourceInterval=EMPTY | RANGE(lowerExclusive,upperInclusive)`; that interval preserves committed-but-not-yet-merged source/PENDING custody and grants no source validity, open-set membership, or PAUSE authority. Older recovery returns SUBSUMED_BY_NEWER_FRONTIER and never displaces newer owner/control state. Read-only recovery must reach `ACTIVE_CONFIRMED | CONFIRMED_NONACTIVE`; unavailable truth or any possible START/RESUME successor remains unresolved. Emit `ReleaseSafetyPauseTriggerV1` only after ACTIVE_CONFIRMED, binding the completed frontier digest without a hash cycle.

Safety-PAUSE effect-start must use one transaction to require `committedSourceHighWater == currentFrontier.mergedThroughSourceSequence`, bind that value as `leaseSourceHighWater`, CAS the exact frontier to PAUSE_EFFECT_IN_PROGRESS, and CAS the sequence cell OPEN→PAUSE_FENCED with the same lease. A source commit that wins first raises the shared high-water and must produce the exact zero-effect `ProtectedReleasePreEffectNoopDispositionV1.SAFETY_SOURCE_HIGH_WATER_ADVANCED`; it advances the frontier only after both source recoveries are durable. A source committed after the fence receives a sequence above `leaseSourceHighWater`, remains PENDING/DEFERRED, and cannot advance the pointer. `ReleaseSafetyPauseFrontierEffectRecoveryReceiptV1` is the sole state-writing recovery for safety admission: in one transaction it writes the generic `ProtectedReleaseEffectRecoveryReceiptV1.SAFETY_FRONTIER` wrapper, generic/control/ledger terminalization, frontier outcome, and sequence-cell reopening. UNKNOWN remains fenced. Conclusive PAUSED/COMPLETE with a nonempty deferred interval always opens one EVALUATING successor with empty validated open set, `mergedThroughSourceSequence=leaseSourceHighWater`, and that exact pending interval, without waiting for or fabricating source recovery. Conclusive NOT_APPLIED always opens one successor with only the original unresolved validated set, the same merged-through value, and the exact pending interval or EMPTY. Reopen the cell at `currentCommittedSourceHighWater`; the ordered merger then consumes the pending interval one sequence at a time after matching recoveries, and any remaining high-water gap blocks another PAUSE. The ordinary generic effect-recovery writer must reject safety admission; never bind the old PAUSE decision to a successor source set, treat a pending record as validated, or drop/misclassify a committed source. Every reconciled START or RESUME effect must emit `ReleaseSafetyPausePostEffectTriggerRecoveryReceiptV1`: first apply/recover each qualifying `PENDING_AFTER_COMMIT_POINT` source and frontier, then, for ACTIVE, create/recover the trigger and predecessor-closure/owner-pending effect. This check is mandatory even when the invalidator arrived while provider state was still INACTIVE or PAUSED.

For `OWNER_SECURITY_PROSPECTIVE_RESET`, owner approval and a fresh generation are insufficient: also require either the old job's provider maximum-runtime deadline plus grace to have elapsed, or uninterrupted proof that one native hard limit of at most five continuously enclosed that exact possibly-live job and still encloses every new job after reset. A scope/pool migration, enforcement gap, or mechanism excluding the old runner retains the debit as `UNRESOLVED_RETAIN_DEBIT`.

External hosted occupancy is a separate controller-owned generation-CAS plane. Include its expected-absent state in transport genesis, create exactly `ObservedExternalHostedOccupancySetV1.EMPTY(generation=0,genesisDigest)`, recover/adopt its full history and current digest, and permit the first complete scan only from gen 0. API uncertainty retains every debit. A deleted/unavailable run can clear only through `ExternalHostedOccupancyUnavailableTerminalResolutionReceiptV1`: authoritative terminal refetch; authenticated deletion after the provider maximum runtime plus safety grace and complete consistent live/retry scans; or an explicit owner/security prospective reset after draining all V23 work and activating a fresh policy/configuration generation. Deletion, age, timeout, 404, or owner assertion alone never clears a slot; unresolved evidence retains the debit and block.

Mandatory audited closure rules: after a protected-release claim and stage transfer, any conclusive failure before an external-effect lease must provide `ProtectedReleaseNoEffectProofSourceV1 = HOSTED_CAPTURED_SNAPSHOT | RECONCILIATION_DERIVED`, a closed reason, `effectLease=ABSENT`, and zero provider mutation command/response/effect. The active state writer must apply-once close or match authorization/gate/ledger state and, for PAUSE/RESUME/COMPLETE/safety-PAUSE, the exact `ReleaseOperationalControlAuthorityCellV1 AUTHORIZED→TERMINAL(noopDecisionDigest)` transition through one all-or-none `ProtectedReleasePreEffectNoopStateEffectRecoveryReceiptV1` transaction; a partial tuple is corruption, not a recoverable state. Only then may the acceptance controller use `ProtectedReleasePreEffectNoopQueueRecoveryReceiptV1` plus terminal API proof to close the pipeline and release its lane. Existing lease, provider command, unknown effect, mismatched source/snapshot, or applicable operational cell still AUTHORIZED/EFFECT_IN_PROGRESS quarantines this no-op path; it grants no retry or acceptance credit.

For P05-C03 `PHASED_RELEASE_ACTIVE`, require a matching live `ReleaseOperationalControlAuthorityCellV1` and exactly one state-writer-confirmed START genesis or `ExistingPhasedReleaseOperationalControlAdoptionReceiptV1`. A pre-existing/manual matching provider release uses expected-absent/matching/divergent adoption and gains no fictional START credit. Publish each monitoring receipt as exactly one allowlisted authority-only child and use `MonitorAuthorityChildFinalizationReceiptV1` to bind the predecessor attempt/chain, receipt, intended child, integration, and resulting base/TIP; only that byte-identical own-output child is compatible with terminalization. Every unrelated base/chain change still supersedes the attempt. A later monitor window atomically supersedes the prior `MONITORING_COMPLETE_NONRELEASE` terminal while creating exactly one monotonic `NOT_STARTED` successor under fresh activation; never reopen or separately orphan either half.

After the current frontier reaches ACTIVE_CONFIRMED and emits its canonical trigger, close every prior control epoch through `ReleaseSafetyPausePredecessorClosureReceiptV1`: for an AUTHORIZED/not-dispatched predecessor, first win the exact zero-dispatch closure claim and retain the lane, then close generic/control state, then terminalize the closure-pending queue only after the state receipt and a fresh zero-job scan; require full state-before-queue prelease-noop recovery for a started/no-lease predecessor; reconcile any possible effect first; or use `OWNER_PENDING_PREDECESSOR_SUPERSEDED`. Freeze epoch rules: IDLE(n) becomes safety OWNER_PENDING at n; TERMINAL(n) creates n+1; unrelated OWNER_PENDING(n) is retained as superseded while n+1 is created; an exact matching retry returns identical bytes. Then permit only fresh-owner `P05C02SafetyPauseAuthorizationV1(PAUSE_PHASED_RELEASE)` bound to the current frontier generation and cumulative ordered source-set digest. The pipeline/effect-start accepts the closed `ProtectedReleaseMutationAuthorizationV1 = ORDINARY(ProtectedReleaseAuthorizationV1) | SAFETY_CONTAINMENT_PAUSE(P05C02SafetyPauseAuthorizationV1)` with no hybrids. Under ORDINARY, matching stop/incident/P05 changes invalidate. Under SAFETY_CONTAINMENT_PAUSE, only the exact current trigger frontier and derived stop tokens are required expected inputs: a different/new invalidator committed before effect-start immediately raises the source high-water and forces the typed no-op, then advances the frontier only after its recoveries; one arriving after the PAUSE lease commits as PENDING/DEFERRED and is preserved by the frontier-effect recovery. “Release/evidence blocked” means ordinary release/evidence work and explicitly allows only this trigger-bound fresh-owner containment PAUSE. Preserve every other protected claim, transition-matrix, serialization, prelease-noop, unknown-effect, capacity, secret, and state-writer rule. This safety exception does not require the superseded product chain to become current, cannot RESUME or COMPLETE, and grants no product/release credit; those actions stay blocked until incident closure and fresh accepted P05 authority.

Implement AssetRounds Expansion V23 as the scope-identical execution-governance successor to V22 in my existing AssetRounds iPhone repository. V23 preserves every V22 product, foundation, correction, exclusion, and release requirement; it adds no feature or card. Parts & Stock and deterministic on-device structured voice remain included, and native iPad remains excluded. Do not create a second app, parallel architecture, writer, store, renderer, importer, route system, dashboard root, speech system, contact ledger, design system, or backend. Inherit the accepted S10.6 brand, colors, typography, vocabulary, and components; prove affected states and then one exact-candidate application closure.

The four authoritative source files are exactly:

1. `EXPANSION_V23_FOUNDATION_PLAN.md`
2. `EXPANSION_V23_ARCHITECTURE_BLUEPRINT.md`
3. `EXPANSION_V23_HANDOFF.md`
4. `NEXT_CODEX_SESSION_PROMPT.md`

Treat those four source files as one digest-closed package. Locate them in the task attachments or supplied directory, verify logical/package/structural SHA-256 values and require exactly four files. Do not edit V21 documents. At V23-A00, copy verified bytes into `docs/design/v23/`; commit only them as `I_A00`; create external `ProgramActivationReceiptV2` binding `I_A00`; then commit only that unchanged receipt as child `H_A00`. Never make a receipt name its containing commit. Validate `H_A00`, then start card work from it; never promote I/H alone. P00-C04's trusted-default checkpoint is the sole pre-release main-integration exception. If fetched main legitimately advances, prepare drift intent before changing the current unaccepted branch, then non-force merge only into `phase/v23-expansion` from activation through first P05 integration outside C04 repair, exact `fix/v23-p00-c04-<signature8>` for product correction, exact `infra-fix/v23-p00-c04-<signature8>` for authorized tooling repair, or exact `release-fix/v23-<card-suffix>-<signature8>` after first P05 integration; `<card-suffix>` is lowercase `pNN-cNN`. Post-C04 phase drift must reprove base/C04 compatibility. Immutable receipt plus expected-base CAS/recovery advances authority. Persist kind-specific integration intent before every C04/P05/evidence fast-forward. On retry, first search exact finalized integration/base history and accept a matching finalized child even when current main is a verified accepted authority-only descendant; otherwise classify `ABSENT_AT_OLD_BASE | APPLIED_MATCHING_INTENDED_CHILD | DIVERGENT_OR_AMBIGUOUS`. Finalize only after remote refetch and expected-base CAS agree: C04 checkpoint first enters transport-activation pending, then smoke decides acceptance; P05-C01 acceptance precedes phase integration; and an evidence child earns only its authority-only receipt after push.

First inspect the repository read-only: root and remote identity, freshly fetched `origin/main`, current branches/worktrees, head/tree, dirty and untracked state, active Phase 10 branch/head/path ownership, accepted S10.6 receipts/evidence when available, project and toolchain, schemes and test plans, GitHub Actions, GetMac runner labels/availability, and live code owners. Never reset, clean, overwrite, discard user work, or force-push. Create `phase/v23-expansion` from the agreed shared base in the adjacent dedicated worktree `AssetRounds-v23-expansion`; it uses the same repository/remote as Phase 10 but never the same branch or checkout. If the branch or path exists with divergent identity, preserve it and report the exact blocker. While S10.6 is unfinished, implement only dependency-ready V23 work whose hydrated write fence is disjoint from Phase 10 ownership; block only overlapping or brand-finalization lanes. After Phase 10 acceptance, integrate that accepted descendant first, non-force merge it into V23, resolve brand/shared-UI conflicts in favor of Phase 10 authority, and then run V23 affected-state plus exact-candidate closure.

At activation, read Foundation §§1–15, Blueprint §§1–8, complete Handoff, this prompt, and live repository evidence. Embedded V21 appendices are semantic provenance only. Rebinding imports semantics and discards V21 execution metadata. Use `BootstrapCardContextV1` plus its two-pass, attempt/head/generation-bound `BootstrapPathFenceV1` only for C01/C02/C04/C05; no pre-C05 mutation may escape that fence. `A00_BOOTSTRAP_CONTROLLER` is sole pre-C05 state/result owner at its current verified generation. Create all A00-frozen deterministic locators plus expected-absent, crash-recoverable generation-0 ledger, expected-main, acceptance, no-active-policy, and no-active-enrollment state planes before controller work. A00 is decision-only through the final C05 decision. For C01/C02, the authority-pinned trusted local runner process emits a closed tagged raw envelope: `BOOTSTRAP_LOCAL_OFFICIAL` binds the official start fence, while `BOOTSTRAP_LOCAL_DIAGNOSTIC_NON_ACCEPTING` binds the separate diagnostic plan/fence/lease and can never earn acceptance. The A00 state writer alone retrieves/reconciles either envelope, and the controller only classifies/decides. Envelope-carried producer identity never self-authenticates. Official local classification uses the BOOTSTRAP_LOCAL_CELL variant with no hosted fields; a reconciled LOST or conflict is infrastructure, releases any diagnostic lease, and requires a fresh attempt/plan/fence/nonce rather than reusing the old one. C04 uses hosted smoke after checkpoint/transport activation. C05 `PRODUCT_FAIL | INFRA_PENDING | SUPERSEDED_NONPRODUCT` uses generic state-effect recovery; only C05 ACCEPT uses the specialized adoption path.

After C05 targeted green, canonically compare all four contexts/fences and emit final-candidate semantic equivalence; mismatch generation-fenced supersedes affected/dependent attempts. Run fresh C05 official acceptance under A00 and require `BootstrapC05AcceptanceDecisionReceiptV1(officialDecision=ACCEPT, adoptionEligibility=PASS)`. Create the old-owner-signed adoption intent and CAS `AcceptanceControllerAuthorityCellV1` into `PENDING_C05_CUTOVER`; this freezes all controller mutations and grants P00-C04 no authority. From that same protected token exactly one branch may win: the intent-bounded state-writer transaction adopts the four frozen state planes and advances the cell to `PENDING_P00_C04_ACTIVATION`, or A00 advances it to `PENDING_C05_ABORT_EFFECT`. The abort branch must apply a recoverable state-writer effect that supersedes the old C05 ACCEPTANCE_RUNNING attempt and creates its next monotonic NOT_STARTED attempt before A00 can reactivate. The adoption branch changes state-writer owner at exactly the next generation, persists C05 card receipt, appends ACCEPTED, and revokes A00 state authority. While the controller remains pending, P00-C05 must emit/recover C05's timestamp-free canonical `CardAcceptanceInclusionProofV1` plus matching recovery receipt; only then may A00 use its one finalization-only CAS to activate P00-C04, binding both proof digests. Recovery must handle every prebranch, abort-effect, adoption-effect, proof-pending, proof-ready, finalization, and divergent state; never reopen an old attempt, let both branches commit, activate P00-C04 before C05 proof recovery, or rewind an epoch.

A00 pins signer/trust policy but does not invent workflow bytes. C04 uses a frozen multi-plane transport-genesis intent with absent/partial-matching/complete-matching/divergent recovery and an explicit enrollment-cell ACTIVE CAS; never create a second genesis. Each signed projection carries zero through five typed product, protected-release, protected-reconciliation, controller, activation, capacity-renewal, or rollover reservations; every actual in-progress stage/job consumes `effectiveHostedCapacity=min(provedHardLimit,5)`. Mirror/checkpoint it and CAS the independent high-water witness; branch/checkpoint/ruleset/witness/acceptance/policy/evidence/enrollment tokens must agree. Historical/manual/external jobs are not retroactive reservations or hosted-result variants: record them in `ObservedExternalHostedOccupancySetV1`, subtract every proved in-scope live job from available capacity, stop new dispatch, and enter `HOSTED_CAPACITY_CONFIGURATION_REQUIRED` for unscoped or contradictory occupancy. A product pipeline retains one lane across a minimal write-capable claim stage and a sequential read-only product stage. Claim-stage output is independently enveloped with `CREATED_MATCHING | NOT_CREATED | OBSERVED_CONFLICT`; preclaim failure must reconcile/release without inventing a claim. A `needs`-started product job that wakes before durable stage transfer must emit only `PRODUCT_STAGE_PRETRANSFER_NOOP`, execute zero checkout/cache/product work, and retain its lane. Use the commuting `ProductStagePretransferArbitrationV1`: whether transfer or apply-once no-op reconciliation wins first, exactly one epoch-1 fresh-nonce retry may emerge after both exist; a second no-op or mismatch terminates as infrastructure. Product start/precommand requires the matching claim plus transfer. Protected-release claim-guard failure/conflict likewise has a closed no-effect result, must terminalize its admitted generic/control cells before queue/lane release, and can never authorize the mutation stage. Candidate workflow changes use a prepared current-controller policy intent binding planned guard identity/spec—not a future result—followed by reconciled control result, policy CAS, and enrollment-cell rollover under `C04_CHECKPOINT | P05_PHASE`. Expiring quota evidence renews through the separate no-card-credit capacity guard while preserving mode/scope or blocks hosted work. Preintegration product acceptance stays on the current controller; only new integrated workflow bytes remain inert until activation. The state writer creates stage-keyed product/protected-release/control envelopes and reconciles every variant exactly once.

For every card, hydrate its exact register row, dossier, policy/contract/journey refs, predecessor semantic block and `V21SemanticSupersessionManifestV1` clause map, and one `DirectPrerequisiteEvidenceSetV1`. For each ordinary direct edge require immutable receipt + unique canonical `CardAcceptanceInclusionProofV1` + matching `CardAcceptanceInclusionProofRecoveryReceiptV1`/currentness + current-candidate compatibility/supersession disposition; `CandidateAcceptanceCompatibilityReceiptV1` must use its closed `DIRECT_EDGE` context with source/target/ordinal/graph digest. The proof is a timestamp-free function of the immutable accepted transition; a current successor writer may only re-derive the same bytes after ABSENT, return exact PRESENT bytes, or quarantine DIVERGENT_OR_MULTIPLE—never repeat acceptance. Selector aggregation uses only the separate `SELECTOR_MEMBER` context. Require expected/observed count equality and zero missing, duplicate, orphaned, stale, incompatible, or extra evidence. The ten exact P00-C03→P06 edges instead require `NonreleasePrerequisiteSatisfactionReceiptV1(SATISFIED_GO_EVIDENCE)` backed by P00-C03 `VALIDATED_SUPPORTED` and `NonreleaseResultInclusionProofV1`; this grants no acceptance or release credit. Also hydrate every selector/non-direct relation row, the digest-bound `ContractFacetRegistryV1` selector and exact provider-owned member list, current repository paths/symbols/owners, exact `PathFenceV1`, lifecycle/compatibility requirements, named Golden/Alternate/Hostile/Interruption/Recovery checks, claims/privacy impact, affected brand/HIG/accessibility/localization states, prior attempts, and owner conditions. A provider slice resolving to zero, multiple owners, consumer-owned types, or members outside its source authority fails closed. Guessed file paths or inferred relations are nonauthority.

The owner authorizes all 118 `IMPLEMENT_NOW`, seven `IMPLEMENT_TOOLING_NOW`, and eight bounded `PREPARE_NOW` cards—133 autonomous cards total—plus in-scope causal corrections. Do not pause for routine coding approval. The exact `AutonomousRequiredAcceptedSetV1` consumed by P05-C01 contains 132 pre-P05 receipts and excludes P05-C01 from its own input. Nine `VALIDATE_NEXT`, two `DEFER`, one `OWNER_ACTION`, and one `MONITOR` card require their stated re-entry authority and receive no silent implementation or release credit. An owner/external condition blocks only its affected lane; continue unrelated READY work.

No `VALIDATE_NEXT` or `DEFER` card—including P00-C03—is eligible before P00-C05 acceptance and canonical state-writer owner adoption. Later require its exact `NonautonomousTriggerManifestV1` row and owner evidence. An owner/trigger receipt first permits read-only hydration within a bounded path envelope; hydration derives the exact `PathFenceV1`; only deterministic subset proof and post-hydration `ExecutionAuthorityTransitionV2` permit mutation/tests. Run it only on never-integrated `validation/v23-<card-suffix>-<bounded-slug>`, where `<card-suffix>` is exactly lowercase `pNN-cNN` and never repeats `v23`. Preserve the dossier-specific source terminal and map it through the frozen manifest to `VALIDATED_SUPPORTED | VALIDATED_NOT_SUPPORTED | INCONCLUSIVE | DEFERRED_AGAIN`; the first three end in `VALIDATION_RESULT_NONRELEASE`, the last in `DEFERRED`, never an official accepting plan, `TARGETED_GREEN`, `ACCEPTED`, or a card-acceptance receipt. For C03 map GO evidence/supported, NO_GO/unsupported, INCONCLUSIVE/inconclusive, and DEFER/deferred-again; only supported can satisfy a P06 edge. A later attempt needs fresh one-shot authority and one state-writer CAS that supersedes the prior terminal and creates a distinct monotonic successor at `NOT_STARTED`; never reopen the old attempt. Enforce an immutable class/profile guard that rejects all accepting states, card receipts, selector credit, and release credit for every VALIDATE_NEXT/DEFER attempt. P05-C02 uses the two-stage owner-evidence flow and eventually accepts its release disposition. P05-C03 becomes eligible only when ordinary P05-C02 evidence proves `RELEASED | PHASED_RELEASE_ACTIVE`, uses exact `monitor/v23-p05-c03-<release-id8>-<window-id8>`, and is observation-only. It closes through `PostReleaseMonitoringReceiptV1`, its exact allowlisted authority-only child, `MonitorAuthorityChildFinalizationReceiptV1`, and state-writer effect recovery into `MONITORING_COMPLETE_NONRELEASE`; the own child is the sole base/TIP compatibility exception and every unrelated chain change invalidates. It never runs official acceptance or emits release/card credit. Any observation recommending `PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL` must bind the matching matrix-row identity and escalate to fresh owner authorization for a separate P05-C02 operational-control attempt; the observer cannot mutate release state.

Follow the Handoff’s one closed `CardExecutionStateV2` and lowest-register-ordinal selector. Only transitively reduced direct execution prerequisites determine READY. Aggregate acceptance, invalidation, provenance, optional-provider, conformance, and contract-input relations never grant readiness or code credit. A built card may enter `WAITING_ACCEPTANCE_SET` while later selector members continue. P04-C27, P04-C29, and P05-C01 consume their exact selectors at one head/tree; no prose-inferred set is allowed.

For the `STANDARD_GITHUB` pool, enforce at most five standard GitHub-hosted `in_progress` jobs repository-wide through the simplest proved native mechanism; the inherited `HostedCapacityEnforcementModeV1` applies to this pool only. Separately permit at most three concurrently executing `GETMAC` jobs using verified repository runner labels/configuration, with every job assigned to exactly one pool and a combined ceiling of eight. Prefer matrix `max-parallel`, workflow concurrency, and provider-native limits; do not build a custom scheduler when those controls suffice. If GetMac is unavailable, continue with up to five standard GitHub jobs and do not treat reduced capacity as a product blocker. If either pool has unknown occupancy, queue new work only for that pool until reconciled. Parallelize only disjoint test/work fences; one integrator serially integrates source changes without force.

Use test economy without weakening proof: cheap compiler/static/schema/architecture/claims/localization checks, then the smallest causal unit/contract checks, affected lifecycle/hostile/interruption/recovery checks, affected UI/accessibility/visual journeys, and one official accepting attempt after targeted green. Reuse accepting `xcresult` artifacts for `xccov`; do not rerun to manufacture coverage. Run performance uninstrumented.

Keep official acceptance exact-head and fail-fast. Freeze one order and carry full cell/transport identity through classification/decision/effect. The start fence binds the complete direct-prerequisite set, every edge identity/inclusion/compatibility record, effective authorization, invalidation generation, and `nonreleaseSpecialEdgeCount=0`; controller decision and state-writer effect terminal-refetch the same authority. A stale ACCEPT is superseded with no card receipt. Claim start verifies absent-or-same-job claim; product start/precommand requires the matching claim and pipeline transfer. For first independent non-success, compute class in memory, then make signed stop intent the first durable controller effect; persist classification afterward, dispatch nothing new, reconcile state, publish stopped projection, and recover every torn point. Before restart dispatch, scan unmatched authoritative terminal results and recreate the exact stop. Fully reconcile before choosing `ACCEPT | PRODUCT_FAIL | INFRA_PENDING | SUPERSEDED_NONPRODUCT`; mixed product/infra chooses product and retains blockers; orphan controller/stop-fenced terminals are infrastructure. Any rerun after infra uses fresh attempt/order/fence/nonces/claims. Exact ACCEPT requires PASS everywhere and no missing/nonpassing result, then decision/card receipt/state effect and nonrecursive accepted-transition inclusion proof. Preserve pre-stop product siblings and post-stop `LateAcceptanceProductObservationV1` as permanently nonaccepting inputs; only product-eligible observations enter correction. Use distinct `DiagnosticExecutionPlanV1`, `DiagnosticCellOutcomeV2`, diagnostic fence, and `DIAGNOSTIC_NON_ACCEPTING` dispatch. ISSUE never stops or cancels a later safe reachable cell; only infrastructure, safety, or supersession may stop, every planned cell is accounted for, and controller terminal decision plus state-writer effect recovery is mandatory. Diagnostics remain mandatory for qualifying contrast/accessibility/UI causes or observations. Group only same renderer/component, demonstrated root cause, signature family, and safe fence.

Persist receipts externally/content-addressed after candidate exists; never commit a receipt into its named head. Optional mirrors are unchanged authority-only children excluded from build inputs. Reconcile main only through prepared drift intent/receipt/state effects on the exact phase/C04-fix/C04-infra-fix/release-fix branch; integration retry checks finalized base/chain/incident history before remote-tip divergence. C04 checkpoint sets transport-activation pending; activation and smoke consume durable controller decisions and use separate recoverable effects over only state-writer-owned records, including the deterministic contradiction branch and the exact TARGETED_GREEN→INFRASTRUCTURE_PENDING resume.

First P05-C01 ACCEPT atomically initializes selection generation 0 plus refresh-cell epoch 0; a fresh accepted successor can CAS only exact `SUPERSEDED(reason,authorityDigest,generation)` to the next generation. Every P05 invalidation atomically supersedes state, selection, and any matching active refresh epoch together with three-record crash recovery. Every successful initial/successor P05 integration atomically advances expected base and initializes/resets `AuthorityOnlyEvidenceChainDispositionV1` to NO_CHILD at the new product tip while preserving prior evidence ancestry. P05 remains HANDOFF_PENDING through that effect, policy activation, and enrollment rollover; COMPLETE alone unlocks P05-C02 and only COMPLETE may authorize an artifact refresh. First owner-evidence hydration binds NO_CHILD only if main is exactly the product tip, otherwise an accepted/rejoined TIP, never its own receipt. Evidence integration atomically advances base plus TIP; P05-C03 alone may terminalize against its exact own monitoring child through `MonitorAuthorityChildFinalizationReceiptV1`, never against an unrelated advance.

If post-push verification finds any `SECRET_OR_CUSTOMER_DATA_EXPOSURE | PROHIBITED_PATH_DELTA | GOVERNED_INPUT_DELTA | ARCHIVE_IDENTITY_DELTA | SCAN_OR_POLICY_CONTRADICTION`, create the redacted tagged violation receipt and open the same release-blocking incident; exposure is only one specialized subtype. A disposition alone cannot rejoin the already-advanced main. Require the exact incident-resolution effect: independently proven false-positive finalization; appropriately approved compliant forward rejoin when retained ancestry is permissible; explicit owner-authorized history remediation when removal is required; or release-blocking abandonment. Only a completed rejoin may resume evidence work, and ordinary correction never means erasure or compliance. Canonical `ReleaseTransportSelectionV1` stays ORIGINAL/REFRESHED/SUPERSEDED; elapsed margin is derived. Use the monotonic `EMPTY | ACTIVE | TERMINAL` refresh cell keyed by selection generation and refresh epoch; never reopen an old epoch. Wait for every authorized result, make product contradiction dominate success, and route any independently proven late contradiction through current three-record P05/selection/refresh-cell supersession rather than discarding it as stale. Refresh success grants no card credit and governed-input change requires fresh P05 acceptance.

Any hosted P05-C02 release action uses only `ProtectedReleasePipelineV1`. SIGN_AND_UPLOAD alone consumes current local artifact/selection/margin; PROMOTE_TESTED_BUILD and SUBMIT_FOR_REVIEW consume immutable App Store build identity; `START_PHASED_RELEASE | PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL` consume exact App Store release/phased-release identity and provider state/generation and never rebuild merely because a local artifact expired. Freeze and enforce `ProtectedReleaseActionTransitionMatrixV1` for the pinned adapter/schema: upload produces one immutable build; promote means link the tested eligible build to the target version; submit requires that exact linked build plus complete metadata/compliance and the normalized ready state; phased rows are INACTIVE→ACTIVE, ACTIVE→PAUSED, PAUSED→ACTIVE, and ACTIVE-or-PAUSED→COMPLETE. Each authorization binds one canonical matrix-row digest and raw-provider-to-normalized-state evidence. If the provider cannot perform PAUSED→COMPLETE as one conditional call, require separately authorized RESUME then COMPLETE effects. Before queue/claim work, admit the signed action intent through one state-writer transaction over the generic authorization/gate and applicable operational cell. Before a provider command, perform one serializable effect-start transaction over that complete matching admission; ordinary invalidators compete on the gate, while a safety-PAUSE treats only its exact trigger source as expected. Use provider conditional generation/idempotency where available; otherwise owner-gate uncertainty. Secrets stay in the protected environment and never enter app/repository/projection/log/artifact data. Reconcile `APPLIED_CONFIRMED | NOT_APPLIED_SAFE_TO_RETRY | UNKNOWN_RECONCILIATION_REQUIRED | INFRA_PENDING | PRODUCT_PROOF_CONTRADICTION | OWNER_REVOKED | SUPERSEDED`; never retry an unknown non-idempotent external effect. Authenticated unknown-effect lookup uses a separate capacity-counted read-only protected reconciliation stage or explicit owner evidence. START’s confirmed effect atomically initializes operational-control epoch 0; later `PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL` use monotonic separate P05-C02 operational-control attempts under fresh owner evidence, even after the card is COMPLETE, without reopening acceptance. The pipeline produces evidence only, not product acceptance by itself.

A required card may accept an explicitly optional subcapability in its typed disabled or manual fallback only when its mandatory local core is complete and no availability claim is made. Never use this to omit required App Lock, encrypted-envelope, service-request, reliability, My Day, Parts & Stock, structured-voice, brand, accessibility, or release work. In Parts & Stock, Return must bind an exact prior Use, the same part/unit/work-material lineage, a KNOWN destination, and a positive quantity no greater than the CAS-protected outstanding unreturned amount; a standalone positive correction is Adjustment, never Return. Preserve one-writer atomicity, released readers and formats, compatibility corpus, immutable history, semantic reversals, interruption recovery, local-first/no-tracking truth, operational-contact/marketing separation, and exact security/delivery/identity disclaimers.

Keep every shipping target/configuration/archive at `TARGETED_DEVICE_FAMILY=1` and `UIDeviceFamily=[1]`. Add no native-iPad layout, metadata, screenshots, interaction work, or support claim. The bounded iPhone-app compatibility safety smoke on iPad is release safety only, not iPad support.

Begin with a concise preflight that states: package/hash result; fetched repository, active Phase 10, shared-base, and S10.6 truth; A00 disposition; exact first READY nonoverlapping card or typed blocker; separate V23 branch/worktree and path fence; cheap/targeted/official checks; five-standard-plus-three-GetMac allocation; and any exact owner-only condition. Then continue autonomously through all eligible V23 work without routine approval until the combined release candidate is genuinely complete or only explicit owner/external gates remain.

---
