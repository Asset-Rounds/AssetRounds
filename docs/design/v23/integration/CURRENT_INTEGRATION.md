# V23 single-app integration

Selected task: `V23-INTEGRATION-20260910`.

## Owner outcome and scope

The owner asks: "Can we try now. Can we merge into the main so it's one coherent app.?" This resumes the previously paused integration. Deliver one existing AssetRounds iPhone app containing the accepted S10 branding and the preserved V23 expansion behavior, with no duplicate app or parallel architecture. The prior instruction to skip Card 135 remains active. The frozen V23 Foundation/Blueprint/Handoff execution amendment sections 0.1, 0.3, 0.4 and 0.5 define the source-reconciliation and one-app invariants. Preserve all 146 card identities, their scope and truthful historical dispositions. This task does not convert provisional/static work into accepted implementation.

The owner further confirms: "I approve any changes and tasks instructions to get this to be merged into the branch so it's one coherent app . Also I would like phase 10 to be the \"looks\" of this expansion. So it retains the same look as app. I think I have those instructions in there." This authorizes necessary integration-task instructions and causal implementation/adoption changes without routine reapproval. Phase 10 is the visual authority for every retained expansion screen, not just the incumbent shell: use its semantic colors, typography, spacing, components, icons, accessible controls and native navigation treatment. Do not introduce a competing theme or claim visual parity without affected-state proof. This does not authorize unrelated features, destructive loss, release operations or weakened verification.

This is a new owner-directed integration task, not a replay of S10.6, V23 activation, old card generators or the pre-merge review. Do not mutate canonical coordination or manufacture acceptance receipts. Reconcile and reverify affected provisional work in canonical order as required by the frozen V5 override. Every additional causal correction or runtime-adoption batch must name its exact paths and requirement before editing. The source-combination checkpoint below is not the final outcome.

## Frozen inputs and preflight

- Repository: `Asset-Rounds/AssetRounds`; remote `https://github.com/Asset-Rounds/AssetRounds.git`.
- Isolated integration branch: `codex/v23-s10-integration-20260910`.
- Isolated worktree: `C:\AssetRounds-v23-s10-integration`.
- Accepted S10.6 phase/main base: `b1d04ae5e684aa9c6807af655089efa1df8a7ed6`.
- Historical S10 native product: `0adebd72ae0226a80e14eaf515ca133072fb1c76`.
- S10.6 qualified E6: `e28d6827bcf78c26dd6381926aa6aec73d7e9999`, run `34540218053`.
- S10.6 evidence K: `bcad8a2f630d590b697f6bae167438edb09d57b8`, direct child of E6; accepted receipt C/base above directly follows K.
- Exact C phase run `34542183023`, then exact-main run `34543737630`, both completed/success with original evidence. The S10 task released boundary ownership and confirmed this integration base.
- Independent final S10 audit: `C:\AssetRounds\Temp\S10_6_ReleasePreparation\integration-review\S10_PHASE_COMPLETE_FINAL_REVIEW.json`, SHA256 `2701EEEF461CE7B22C0EA373B026E1E6B9D42DC7F1DA1C1BAF4B89DA0F68C333`.
- Frozen expansion: `acbfb68355f903fe98638b6ef22e4814e7b48328`, tree `47e17fae6b73dccd5029ccf4ac7cca659196f225`.
- Reviewed correction input: `c45fa0ffa70de1e0cb9fb042e795511160019850`; clean and pushed before integration.
- Merge base: `01233f789b1cef5a6f56c7ff4caa9271409cd3bc`.
- Frozen V23 package digest: `99a2719885ad1abfb8cf5d49c6b2099754bb0ba4b5d27fcbae06476aad507570`; register `edd6109aab118cc35c91495b789f70eb0b7c4d5f3d0780ad7a1918e5379e4cbd`; direct graph `4e9feb8b0cb65deddd3a5802efb380911a3439e44f1a0dc56656eadb29aac2ae` (146 cards/230 edges).
- Frozen coordination input remains `51ef2b3d970a25b4c83df8c8238609316e37034e`; no coordination mutation or renewed acceptance is implied.

Live refs were inspected and fetched before creating the new worktree; the target branch/path were absent. Canonical S10 tracked files/index were clean but existing untracked work was present and is untouched. The review worktree was clean. Existing expansion, review and V30 worktrees/refs are preserved. Unexpected remote movement or unowned overlap requires read-only reconciliation, never force/reset/overwrite.

Accepted S10 static pins remain unchanged: plan `CA5C013968CC8E581801D16364AF56D19A17836CA5098C97B42ED02DE4D2B610`; runbook `8729B882F464595858633BEC5E7BBD80C0B057166A8712980ECD885C0D1E030E`; execution contract `64B1773F1AB318A8F29D781A610D583C940719424CF120C9E57135C116F32E3D`; activation `D7B7D64D8D5F3B61BFA8BF205F8F99ED7E5430EB8E72AE8BD4EB8561E79C5A5A`. Historical S10 evidence is validated at its original heads, never repinned to this new product candidate.

## Current batch: source reconciliation and first compiler diagnostic

Mechanically incorporate accepted S10 C into the new branch descended from reviewed V23 c45fa0f. Preserve both parents and all original refs. This source-branch merge is authorized by V23 section 0.1; it never creates a merge commit on `main`. Main can eventually advance only by non-force fast-forward to an unchanged, fully verified descendant after the final gates below.

The complete source inputs define a finite mechanical merge envelope; `SOURCE_RECONCILIATION.json` enumerates the exact changed paths from each input. Do not invent a new feature or alter nonconflicting source in this batch. Resolve the only three observed conflicts:

- `AGENTS.md`: select this owner-directed integration while preserving accepted S10 safety, deferrals and historical context.
- `FieldEvidenceApp/Features/Issues/IssueDetailView.swift`: keep S10 `DesignTokens.Spacing.space16`, evidence cards, semantic colors, state labels and primary actions; retain V23 contained accessibility identifiers and its optional resolved-work response callback/menu.
- `Scripts/ci-selection.json`: select the initial integration N8 diagnostic and retain all 15 regression methods from c45fa0f unchanged. UI is false for this compiler/regression diagnostic only, not final acceptance.

Only these additional nonshipping integration bookkeeping paths are writable in this batch:

- `docs/execution/CURRENT_TASK.md`: operative task pointer above unchanged historical S10 text.
- `docs/design/v23/integration/CURRENT_INTEGRATION.md`: exact active scope, progress and verification status.
- `docs/design/v23/integration/SOURCE_RECONCILIATION.json`: source-derived input, changed-path and conflict record, with no native/acceptance claim.

All automatic nonconflicting merge changes must be attributable to the two pinned inputs. Verify the accepted S10 app entry, launch, shell, Signs root, design system, assets, project/scheme and historical S10 evidence remain byte-identical in this first batch. No app/project exclusion, assertion relaxation, new dependency, new store, duplicate writer, runner switch, or test skip is a conflict resolution.

## First native question and exact route

Question: does the combined source compile on the accepted hosted toolchain, and do the 15 existing backup/measurement/shop-profile/round retry regressions execute and pass? The last review diagnostic `33980870020` failed before tests at `4175e75da9c4fb1a51220a2fe463142696507773`; c45fa0f contains independently reviewed parser corrections not yet tested natively. Preserve that failed original and all earlier evidence.

Use the accepted main `.github/workflows/ios-ci.yml` and `.github/workflows/ios-ci-worker.yml` without changes, ordinary `github-xcode-26.6-acceptance` lane, this integration branch ref, shard/segment/minimum-smoke `none`, empty shared-payload/map, `run_ui_smoke=false`. Workflow naming does not make this diagnostic formal V23 acceptance. Source and artifacts must bind the exact new integration head. Use Windows only for source/Python checks; no local Xcode claim.

Runner `macos-26`; Xcode26.6/build17F113; `/Applications/Xcode_26.6.app/Contents/Developer`; Debug/FieldEvidenceApp project and shared scheme; fresh iPhone17/iOS26.2; deployment18.0; unsigned. N8 is setup/build/unit/UI/total `300/600/900/0/2400`, readiness900, job90 minutes, cancellation disabled. Keep original build/test commands, failures, warnings, xcresults, logs and checksum enforcement. Inspect terminal originals before any evidence-driven retry; no duplicate active run, no rerun-by-ID and no known unchanged deterministic failure dispatch.

The initial selector's only change from c45fa0f is taskID `V23-INTEGRATION-20260910`. The 15 methods cover three backup decoder regressions, five measurement writer regressions, four shop profile regressions and three round-session regressions. A pass does not establish whole-app adoption or any skipped UI test. Before dispatch, require independent conflict review, clean committed exact head, matching remote ref/main baseline and current useful capacity. Parallel capacity is for distinct ready evidence, not duplicate builds of the same unresolved source.

## Remaining one-app completion requirements

1. Classify all provisional V23 candidates as compatible, correction-required or superseded against this accepted baseline, preserving original evidence and canonical order. No blanket PASS from a source merge.
2. Integrate the existing production composition and feature routes into the accepted app lifecycle. Preserve one session/writer/generation, startup recovery, the accepted design system and all incumbent flows; do not instantiate parallel roots or simply flip adoption booleans.
3. Prove populated accepted-schema migration, opening/reopening, generation replacement/stale-writer rejection, backup/restore/Erase, immutable report/media history, data rights and retry receipts. Do not test only fresh empty stores.
4. Verify all retained expansion feature routes and required native tests on the actual combined candidate. UI skips are not passes. Run affected-state branding/accessibility proof and the exact-candidate application closure; do not rerun unchanged S10's full historical freeze automatically.
5. Obtain independent integration review and complete exact-head evidence before main advancement. Re-fetch and require main to equal the accepted base (or a separately reconciled authorized descendant); use only a non-force fast-forward to the unchanged verified candidate and then required exact-main verification. Main is not updated by this first checkpoint.
6. Only after the verified main integration and recoverable history checks may the owner's earlier expansion-branch cleanup request be executed. Delete no branch or worktree while any retained source/evidence still depends on it. Preserve V30 and its ongoing work.

No fixed new-path allowance for the remaining runtime work is inferred from this list. Hydrate each demonstrated causal batch before editing. Do not add future-market behavior, cloud/accounts, payment changes, public claims, new permissions or external services beyond already explicit V23 scope.

## Preserved deferrals and truth

S10 has exactly five real accepted stages: Inventory, ComponentSystem, Migration, AutomatedLab and Release. S10.5 PhysicalExperience remains indefinitely DEFERRED until actual App Store release plus a new explicit owner instruction; no PhysicalExperience receipt or sixth accepted stage. S10.4 minimum-runtime coverage and smoke remain indefinitely DEFERRED with no retries or automatic activity. Both deferrals are nonblocking under their existing narrow owner policies, never PASS.

The three S10 name/trademark/URL-clearance obligations are OWNER_WAIVED under the sealed S10 instruction, not positive legal clearance. Actual releaseReady remains false. Card 135, owner S9.2/S9.3, signing/archive/export, live configuration, App Privacy, larger-display store assets, TestFlight and submission remain owner-only/unstarted. This integration does not manufacture external evidence or arm the deferred monitor/study cards.

Source checkpoint verification: all three conflicts are resolved. Independent integration review approved the actual source/authority changes for a nonaccepting checkpoint and one N8 diagnostic. The source inventory contains 102 accepted-S10 changed paths, 2,158 reviewed-expansion changed paths, and exactly the three listed intersecting paths. `SOURCE_RECONCILIATION.json` SHA256 is `478988BA9B14B0030E91D80A46E41550597470EBA67F30141496AF1926EAAED9`.

`python -B Scripts/v23/verify_card_contracts.py` passes 50 cases; `python -B Scripts/v23/verify_controller.py` passes 31 cases. `git diff --check` passes. The integration selector differs from c45fa0f only in taskID. Accepted main app/launch/design/assets/project/scheme/shell/SignsRoot/S10 evidence and the ordinary workflow/worker/smoke scripts compare byte-identical. One existing `@main` app and original bundle ID `com.palatis3.fieldrecord` remain; no new app target is added.

Current result: source combination ready for commit and the first exact-head hosted diagnostic. No combined native pass, runtime-adoption acceptance, main integration or branch deletion is claimed.
