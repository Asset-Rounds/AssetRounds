# Active integration brief

[CURRENT_INTEGRATION](CURRENT_INTEGRATION.md) records batches; [MERGE_READINESS](MERGE_READINESS.md) controls phases; [VERIFICATION_DUE](VERIFICATION_DUE.md) retains all open obligations. AGENTS.md is the working agreement.

## Current state — 2026-09-26, Codex takeover

- Work locally at `/Users/rentamac/Developer/AssetRounds` on `codex/v23-s10-integration-20260910`. V is pushed at `079159b1`; W read-performance checkpoint at `a6faa8f9`. RUI1 is pushed at `6dbfdbba`. X2 is pushed at `b82ac4de`; it compiles and passes34/34focused checks after diagnostic privacy and clone-history assertion corrections; both long clone methods failed with preview/terminal-fixture and hostile-stage/history obligations; X1 photo stopped after a shared backup-access fixture failure. Migration follows. Accepted main remains `b1d04ae5`.
- One existing app/project/shared scheme, sole writer and S10 design system. Preserve owner drafts, frozen design, V30, coordination ledger, `docs/design/s10/**` and `Release/**`. Card135 owner-only/skipped; physical/minimum-runtime verification deferred; releaseReady=false.
- Owner approved risk-based review, reversible implementation authority and family/prerequisite batching with proportionate development checks (AGENTS.md); gates and owner-reserved decisions remain.
- Owner switched all Claude roles to GPT-6 Astra, including the separate read-only independent reviewer. Root alone integrates, commits, pushes and dispatches; no PRs or merge commits.
- Verified local Xcode 26.6 (17F113), iOS 26.5 (23F77) Simulator, Python 3.14 and authenticated GitHub access. Local execution is development only. The iOS 26.2 (23C54) gate runtime remains hosted on GitHub.
- All five batch T branches and their six commits exist locally. Integrated on the handoff head without conflict; combined build passed, focused tests passed 56/61 with five retained failures, and independent GPT-6 Astra review approved the integration batch (compile risk LOW). Original branches remain preserved.
- Latest sole-collected development sweep `36232533297` at `1d3c80fa`: 3,009 passed, 435 failed, 13 interrupted, 6 skipped; exact 3,463 methods across38 partitions, 3h29m elapsed. Collector integrity/shared checks pass; root verified all53,072manifest files with zero mismatches. Evidence: `~/AssetRounds-v23-review-evidence/36232533297`. This older head predates T/U/V and is not current acceptance.

## Next work

1. X1 compiled and independently source-reviewed: six missing clone journal projections plus authentic pointer preservation, C49 and receipt fixtures. X2 native34/34PASS: package12, My Day2, receipt2, shared C50 one, C49 17/17 including the corrected diagnostic privacy boundary. X1 clone was stopped as a known-broken oracle run (partial logs only); X2 positive clone failed950s at stale photo-count expectation then terminal-fixture invalidCommand; hostile sibling failed199s at staged-import preservation then receiptHistoryCorrupt. Neither is waived.
2. RUI1 tooling passes335/335 and generator38/38; combined31checks and ordered3,473methods/28partitions pass. Parser cache remains held out for no useful gain; simpler cache-free depth spans pass335/42 checks, with identical outputs and a bounded12.9%CPU reduction, integrated with current selection parity. Hosted boot/fingerprint overlap and cold RUI1 qualification remain due; no coverage or watchdog reduction.
3. X1 photo failed after936s at the30s backup-access publication expectation before restore, then cleanup unlinked a still-open store. Known-failed run stopped, partial logs only. Diagnose startup phase and drain tasks before cleanup; preserve timeout and all ownership/hostile assertions. Raw-photo repeated proof cost is a separate performance candidate.
4. Legacy tuple bridge and canonical producer fixes have local passes, including aggregate terminal normalization. V9_03 shared-scratch semantic-copy correction is source-approved but uncompiled; first-open sourceUnavailable still needs named runtime attribution. Preserve full source-integrity checks. Every unit method must pass; no waiver.
5. Qualify RUI1 UI route, collect critical states and obtain the owner's genuine screenshot review; freeze one candidate for all phase gates, then fast-forward main and verify exact main.

## Merge and remaining scope

The S10 upgrade digest blow-up and backup export deadlock have reviewed corrections. Restore, migration, checkpoint, writer/fence, backup and Erase still require complete same-head verification. Existing local or earlier-head passes never transfer.

Phase 1 is platform and four-tab shell. Unfinished features remain unreachable behind `V23PhaseGateV1`, with the complete later-phase ledger in MERGE_READINESS. Full V23 scope is required before release. Round creation, capture/lifecycle completion, My Day, signoff and other production journeys retain their recorded obligations.

Owner items remain: pre-gate D2/D5 confirmation, maintenance reason granularity, replication version-pin authority, C55 reversal/restore decision, V23 privacy sign-off and release inputs. Ask only where frozen V23 material does not settle the product decision.

No settings/secrets, signing, release or account changes. Local evidence is retained in `.codex-temp/codex-20260926`; hosted originals and their SHA-256 manifests remain external.
