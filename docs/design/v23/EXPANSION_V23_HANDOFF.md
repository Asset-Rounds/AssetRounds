# AssetRounds Expansion V23 — Codex Handoff

<!-- V23_PACKAGE_MANIFEST_START -->
## V23 package manifest

Canonical logical hashing: decode UTF-8 with no BOM; normalize CRLF and lone CR to LF; remove the package-manifest start-marker line, every enclosed byte through the package-manifest end-marker line, and the immediately following LF when present; strip trailing SP and TAB from every remaining line; normalize to exactly one final LF; SHA-256 the resulting UTF-8 bytes; encode lowercase hexadecimal. Canonical package hashing uses `V23PackageDigestV2`: require exactly one marker-delimited manifest in each file and require all four blocks to be byte-identical; replace only the 64 lowercase hexadecimal characters in the manifest's Package digest value with 64 ASCII zeroes; normalize that zeroed block to LF, no trailing SP/TAB, and exactly one final LF; then SHA-256 `ASCII("V23PackageDigestV2") + 0x00 + UTF8(zeroedCanonicalManifestBlock) + 0x00` followed by, in ordinal-filename order, `UTF8(filename) + 0x00 + ASCII(lowercaseLogicalDigest) + 0x00`. The package digest therefore binds every visible manifest byte except its self-referential value. A verifier must compare the exact canonical block, not search for digest substrings. A hash proves integrity against a separately trusted expected digest; it is not a signature or independent authenticity claim.

Canonical structural hashing uses JSON arrays, JSON string escaping, no insignificant whitespace, UTF-8 without BOM, SHA-256, and lowercase hexadecimal. Every array-valued absence is JSON `[]`; Markdown `NONE` is display-only and never enters structural JSON. Register order is the published topological order with key order `id,title,classification,status,directPrerequisites,lineage`; graph uses that order with `id,directPrerequisites`; lineage is ascending numeric phase/card ID with `predecessor` (JSON null for added cards), `successor,disposition`. Impact uses topological order and `id,aggregateAcceptanceMemberships,invalidationConsumers,optionalCapabilityProviders,conformanceSubjects`. Relations first iterate dossier cards in published topological register order and emit base rows in fixed kind order aggregate, invalidation, conformance, contract-input, optional, predecessor provenance; then append dependency-disposition `PROVENANCE_REFERENCE` rows in the exact §6D ledger order. All relation rows use key order `source,kind,target,evidence,activation,fallback,invalidates,authority`. Contract facets retain contract-input relation order and key order `source,target,selector,sourceAuthorityRefs,activation,fallback`; source-authority refs are lexically sorted. Selectors use their published seven-row order with `id,members,consumers,predicate`. Dependency dispositions use published topological successor-card order then inherited predecessor-list order with `predecessorConsumer,predecessorProvider,currentConsumer,currentProvider,disposition,authority`. Member arrays retain their published order.

| File | Authority role | Logical SHA-256 |
|---|---|---|
| EXPANSION_V23_FOUNDATION_PLAN.md | Scope/register/graph/lineage | `4caf56670c6e211291a6a141e4c8d35762d1155c169c91668f044644f60811c6` |
| EXPANSION_V23_ARCHITECTURE_BLUEPRINT.md | Architecture/contracts/dossiers | `779d91091164d33fc81a77deb689d631c7cca1bc55a80e3806ce7f874f5b3d48` |
| EXPANSION_V23_HANDOFF.md | Execution/CI/acceptance | `ca3e563f072c1ce34207c41272e2755a8d8d474c40640a0612ab33a0deebeab9` |
| NEXT_CODEX_SESSION_PROMPT.md | Invocation only | `355d511ef73319561b7455d64f7f98cda0a655768282fb8b88f662ef9e13d2f7` |

- Package ID: `ASSETROUNDS-EXPANSION-V23-20260825`
- Manifest schema: `V23PackageManifestV1`
- Package digest algorithm: `V23PackageDigestV2`
- Package digest: `99a2719885ad1abfb8cf5d49c6b2099754bb0ba4b5d27fcbae06476aad507570`
- Card register digest: `edd6109aab118cc35c91495b789f70eb0b7c4d5f3d0780ad7a1918e5379e4cbd`
- Direct graph digest: `4e9feb8b0cb65deddd3a5802efb380911a3439e44f1a0dc56656eadb29aac2ae`
- Lineage digest: `8c5a573df6b9242096e53983da9c78cc4cb2246e6ce4b504f1e4d45d42173283`
- Impact projection digest: `a460620a0f0242fe0e71d8604284826204f000bb45fa09249c2db994dd0fa70b`
- Card relation digest: `9b5c7f664af7d79d219e3ca55a28352bc0da7d9ddf998033b9a82187b428fac4`
- Contract facet registry digest: `b255fe1249ef40cf835fb6717876f20eee864407c206e4ae3baf4a109ab8949f`
- Selector digest: `6ef4089521319677f3d69ed691d638dcc12521789c575c7939966e47670ce7f2`
- V21 dependency-disposition digest: `f30d779c19e94d57d9b3114c09ac07538588676606c78d2e369683ee91169b8c`
- Card count: `146`
- Direct edge count: `230`
<!-- V23_PACKAGE_MANIFEST_END -->

## 0. V23 execution amendment — scope-neutral operating rules

This amendment is authoritative over inherited V22 execution wording. V23 adds no product feature, foundation, persistent schema, public claim, route, root, or release platform. All 146 V22 cards and their product semantics remain intact; only execution governance changes.

### 0.1 Parallel operation with active Phase 10

V23 may begin while Phase 10 is still active. Both efforts use the same AssetRounds GitHub repository and remote, but each must use a distinct branch and a distinct Git worktree. They must never write through the same checkout. Before mutation, record the fetched remote identity, both heads, their merge base, dirty/untracked state, active worktrees, and a path-ownership ledger.

Phase 10 owns its current brand, shared-component, vocabulary, accessibility, snapshot, and accepted-state evidence paths until it closes. V23 may immediately implement dependency-ready work whose hydrated write fence is disjoint from those paths. A V23 card that reaches a Phase 10-owned path pauses only that lane; unrelated V23 work continues. Do not duplicate, replace, pre-empt, or independently freeze unfinished Phase 10 authority.

Use `phase/v23-expansion` in an adjacent `AssetRounds-v23-expansion` worktree. Phase 10 keeps its existing branch/worktree. When Phase 10 is accepted, integrate it to the repository's designated integration base first. Then fetch and non-force merge that accepted descendant into `phase/v23-expansion`. Resolve brand/shared-UI conflicts in favor of accepted Phase 10 authority while preserving V23 product behavior. Record the merge base, both parent heads, conflict paths, resolutions, and targeted reproof. Never force-push, reset, clean, overwrite, or discard either effort.

V23 may not claim final application, brand, visual, accessibility, archive, or release acceptance until accepted Phase 10 is incorporated. After incorporation, run affected-state proof for V23's changed states and one exact-candidate application closure. Do not repeat the original Phase 10 67-state freeze unless an accepted Phase 10 authority digest itself changed or the affected-state inventory proves those states were modified.

### 0.2 Dual-pool CI capacity: five standard GitHub plus three GetMac

The repository may use up to eight concurrent CI jobs when capacity is available:

- `STANDARD_GITHUB`: at most five concurrently executing standard GitHub-hosted jobs.
- `GETMAC`: at most three concurrently executing GetMac jobs using the repository's verified runner labels and configuration.
- Combined ceiling: at most eight concurrently executing jobs, with every job charged to exactly one pool.

Queue rather than exceed either pool. Matrix `max-parallel`, workflow concurrency, and the simplest available native runner controls are preferred. Do not build a custom scheduler, lease protocol, service, or additional workflow merely to reproduce a limit already enforced by the selected platform. Add a small repository controller only if current workflow inventory proves native controls cannot enforce the two ceilings across the applicable workflows.

At activation, inspect current workflow files, runner labels, repository/organization limits, active runs, and GetMac availability. Never infer that GetMac is configured from this plan alone. If GetMac is unavailable, continue with zero to five standard GitHub jobs; GetMac absence is reduced capacity, not a product blocker. If standard GitHub capacity is unavailable, use up to three verified GetMac jobs where compatible. Unknown or unclassified occupancy blocks only new dispatch into that pool until reconciled.

Use the pools for independent test shards, never concurrent source integration. Prefer one build feeding compatible test shards when immutable head/tree, toolchain, configuration, fixtures, and artifacts match. Keep official acceptance fail-fast and separate from the non-accepting diagnostic sweep.

### 0.3 Goal discipline and anti-overengineering guard

Implement the smallest production-quality change that satisfies the active V23 card and its declared acceptance evidence. Every changed path must map to a current card requirement, demonstrated causal correction, or necessary integration resolution. Do not add speculative features, generic frameworks, future-market behavior, duplicate abstractions, new roots, parallel stores/writers/renderers/importers/design systems, or unrelated cleanup.

Reuse the accepted repository's existing architecture and Phase 10 brand system. Extract or generalize only when the current requirement needs it, at least two current consumers demonstrably share the behavior, or an existing V23 contract explicitly requires the seam. Prefer local changes, existing platform APIs, deterministic fixtures, and targeted tests. Stop expanding a solution when the stated behavior is implemented, its edge cases are covered, and its declared acceptance evidence is green.

Do not turn planning contracts into shipping types or runtime machinery unless a card's actual implementation requires them. Planning receipts may remain evidence documents or CI artifacts. No requirement is satisfied by prose alone, but not every planning noun requires a Swift type.

### 0.4 Standing implementation authorization

The owner grants standing authorization for all in-scope, dependency-ready V23 implementation, bounded refactoring required by that implementation, tests, causal corrections, local commits, dedicated worktrees/branches, ordinary pushes, pull-request updates, and CI dispatch within the two-pool limits. Codex must continue autonomously through eligible work without requesting routine phase-by-phase approval.

This standing authorization does not permit destructive loss of user work, force-pushes, history rewrites, secret creation or disclosure, purchases or billing changes, runner-account provisioning, protected-branch/security-policy changes, public communication, App Store submission/promotion, legal declarations, or fabricated physical-device/external evidence. An unavailable external condition blocks only its affected lane; record it precisely and continue all unrelated READY work.

### 0.5 Completion and integration rule

Phase 10 and V23 converge into one AssetRounds application in the same repository. There is no second app and no long-lived fork. Phase 10 establishes the accepted brand/shared-UI baseline; V23 adds the preserved V22 product scope on top. Final V23 acceptance requires the combined exact candidate, no unresolved ownership conflicts, no duplicate systems, accepted affected-state brand/accessibility proof, and the existing V23 release gates.

**Package:** AssetRounds Expansion V23

**Frozen:** 2026-08-25

**Authority role:** Activation, hydration, deterministic card selection, parallel Phase 10/V23 branches, five-standard-plus-three-GetMac CI, integration, acceptance and diagnostics, corrections, owner gates, release, and continuation.

<a id="v23-handoff-mission"></a>
## 1. Mission and authorization

Implement V23 as the scope-identical execution-governance successor to V22 in the existing AssetRounds iPhone app. Preserve every V22 product, foundation, correction, exclusion, and release requirement. Add no feature or card. Do not ask for routine coding approvals. The owner authorizes all 118 `IMPLEMENT_NOW`, seven `IMPLEMENT_TOOLING_NOW`, and eight bounded `PREPARE_NOW` cards—133 autonomous cards total—plus in-scope causal corrections. Continue through dependency-ready cards until only an exact owner or external condition blocks the affected lane.

Nine `VALIDATE_NEXT`, two `DEFER`, one `OWNER_ACTION`, and one `MONITOR` card are outside autonomous implementation membership. They require the exact authority transition in their dossier and never receive silent release credit. No scorecard or workbook is required. These four Markdown documents are the complete planning authority.

<a id="v23-a00"></a>
## 2. V23-A00 activation and repository protection

Active Phase 10 does not globally block V23. It reserves its current brand/shared-component/vocabulary/accessibility/snapshot paths and final application-closure authority until accepted S10.6 exists. V23 may immediately work on dependency-ready cards whose hydrated path fences are disjoint. Never treat the active Phase 10 worktree, in-flight exception count, or unaccepted screenshots as accepted brand evidence.

### 2.1 Exact activation contract

1. Fetch `origin` without mutating a worktree.
2. Inspect whether freshly fetched `origin/main` contains accepted S10.6. If absent, record the active Phase 10 head and merge base; allow only disjoint V23 mutation and reserve brand/shared-UI/final-closure lanes until Phase 10 acceptance.
3. Require `origin/main` cleanly identifiable and no local user changes in the proposed worktree target.
4. Create the program branch `phase/v23-expansion` from the agreed shared base in a dedicated worktree named `AssetRounds-v23-expansion` adjacent to, not inside, the existing repository. It uses the same repository and remote as Phase 10 but never its branch or checkout. If either exact name exists with divergent identity, stop activation and preserve it; never delete or overwrite it.
5. Copy the four verified V23 documents byte-for-byte into `docs/design/v23/` with their frozen names. No V21 file is edited or deleted.
6. Commit only those four authority files as immutable installation head `I_A00` with message `V23-A00: install verified V23 authority`.
7. Outside the repository, emit `ProgramActivationReceiptV2` binding the accepted base and exact `I_A00` head/tree. It cannot name a commit that contains itself.
8. Copy that unchanged receipt to `docs/design/v23/receipts/V23-A00-program-activation.json` and commit only that file as immutable anchor head `H_A00` with message `V23-A00: anchor activation receipt`. The receipt continues to bind `I_A00`, never `H_A00`. Validate that `H_A00` has `I_A00` as its sole parent and differs only by the one receipt file.
9. Validate source and installed hashes, base identity, `I_A00`, the external receipt, and `H_A00`. A00 earns no card credit. `I_A00`/`H_A00` remain branch-only until C04 and are never promoted to `main` alone; their ancestry may become reachable from `main` only as part of the exact descendant C04 checkpoint. All card branches begin from the serialized phase integration head, initially `H_A00`. P00-C04's trusted-default-branch controller checkpoint is the sole pre-release main-integration exception. Its initial `ExpectedMainIntegrationBaseV2` is the accepted A00/S10 fetched main head. If main advances before C04, the prepared `MainDriftReconciliationIntentV1` → immutable receipt → expected-base CAS/recovery law may advance that expectation only when new main descends the old base, all changed-path/contract/privacy/brand/test/invalidation impact is resolved, and new main is non-force merged into the still-unaccepted phase branch followed by targeted reproof; rewritten/unrelated ancestry or unresolved impact blocks. After C01/C02 are accepted and the dormant C04 candidate passes local/incumbent CI plus complete workflow-enrollment/static proof, refetch `origin/main`, require it to equal the current expected base and be an ancestor of the exact C04 candidate, persist `IntegrationIntentReceiptV1` with `subjectAuthorizationKind=C04_PREACCEPTANCE_CHECKPOINT_PROOF` and the complete pre-push proof digest, then fast-forward only—never create a merge commit on main—to that unchanged candidate.

After refetch, the active state writer writes or recovers `C04CheckpointEffectReceiptV1(APPLIED_TRANSPORT_ACTIVATION_PENDING)` and atomically advances the actual-main pointer/CAS generation in `ExpectedMainIntegrationBaseV2` to the intended child with status `C04_TRANSPORT_ACTIVATION_PENDING`; this is neither C04 acceptance nor release credit. `IntegrationEffectRecoveryReceiptV1` for this kind may recover only that pending effect and never manufacture a card receipt. The current acceptance controller then idempotently completes the predeclared enrollment, transport genesis, capacity-policy guard, and policy-authority CAS. `C04TransportActivationEffectReceiptV1` and `C04TransportActivationEffectRecoveryReceiptV1` bind the durable activation decision and independently reconciled result, then apply one state-writer-owned multi-record CAS: ACTIVATE changes the base to `C04_SMOKE_PENDING` without card credit; INFRA_PENDING changes the card to `INFRASTRUCTURE_PENDING` and the base to `C04_TRANSPORT_ACTIVATION_INFRA_PENDING` with the exact failed plane and same-byte repair/resume token; PRODUCT_PROOF_CONTRADICTION changes the card to `CORRECTION_REQUIRED`, the base to `C04_CORRECTION_REQUIRED`, and atomically persists the exact product-correction or `ToolingInfrastructureRepairAuthorizationV1` branch authority; SUPERSEDED changes both records to their matching successor disposition. Recovery classifies before-effect, applied-matching, or divergent/ambiguous state and never mixes branches. Transient repair resumes only activation and requires a fresh hosted smoke; deterministic contradiction requires its forward branch and new checkpoint/activation/smoke. Only after every plane is active and matching does the exact trusted-default smoke run and end through a durable controller-owned `OfficialAcceptanceDecisionReceiptV1`.

Because the execution ledger and expected-base record share the protected external coordination transaction boundary, the active state writer consumes that already-durable decision digest and applies `C04SmokeDecisionEffectReceiptV1` by one multi-record CAS across only state-writer-owned records: `ACCEPT` persists the card receipt, appends `ACCEPTED`, and changes base status to `ACCEPTED_BASE`; `PRODUCT_FAIL` appends `ACCEPTANCE_FAILED`, persists immutable `C04PostIntegrationFailureReceiptV1`, and changes base to `C04_CORRECTION_REQUIRED`; `INFRA_PENDING` appends `INFRASTRUCTURE_PENDING`, creates no correction receipt, and changes base to `C04_SMOKE_INFRA_PENDING` with the exact fresh-smoke condition; `SUPERSEDED_NONPRODUCT` appends `SUPERSEDED`, creates no card/correction receipt, and retains `C04_SMOKE_PENDING` for a fresh successor attempt on the same governed main identity. `C04SmokeDecisionEffectRecoveryReceiptV1` classifies pre-effect at the expected generations, applied matching decision/effects, or divergent/ambiguous state; it retries only the exact missing state-writer effect, returns matching bytes, and quarantines every mixed/mismatched record. Product/C05 work is blocked until ledger state, receipt disposition, and base status agree. A product failure permits only a forward `fix/v23-p00-c04-<signature8>` descendant from current main; the failed main is never reset. A corrected descendant receives a new intent/effect/activation/smoke and only an ACCEPT child becomes the accepted P05 base. Infrastructure repair uses a fresh smoke attempt without product correction. No other card uses this pre-release exception.

`ProgramActivationReceiptV2` binds repository and remote URL identity, fetched `origin/main` head and tree, accepted S10.6 receipt and evidence digests, worktree resolved path, program branch, installed authority paths, source and installed logical hashes, package and structural digests, `I_A00` head and tree, dirty and untracked inventory before and after `I_A00`, executor, toolchain observation, intended receipt anchor path, and `V21_METADATA_EXCLUDED=true`. It also freezes deterministic protected repository-external locators/protection profiles for the execution ledger, expected-main integration-base cell, controller/policy/enrollment cells, queue, signer, and high-water witness before any of those planes is created; a later card context references these A00-bound locators and cannot invent or relocate them. It pins the controller-transport trust root: an A00-generated Ed25519 public verification key/key ID and algorithm, domain separator, intended `coordination/v23-state` branch/path, allowed controller workflow path, protected workflow ref policy, and the protected repository-external private-signer locator/protection disposition without disclosing the private key. Any workflow commit/blob seen at A00 is recorded only as `OBSERVED_AT_A00` non-authorizing provenance; it may differ from the later C04 implementation. Only `ControllerWorkflowEnrollmentV1` at the trusted C04 checkpoint authorizes exact workflow ref/commit/path/blob bytes. The private signing key is generated with platform secure randomness, stored only in the protected external coordination scope, excluded from repository, logs, artifacts, backups, diagnostics, and application targets, and destroyed under its recorded retention/recovery rule. No hosted dispatch is permitted until P00-C04 validates this pin and creates the signed genesis. `ActivationReceiptAnchorValidationV1` binds the observed `H_A00` head/tree, its sole `I_A00` parent, receipt digest/path, exact one-file diff, and validation result; it is external evidence and is never recursively committed.

### 2.2 V21 execution metadata is never inherited

Every V21 activation ID, branch, worktree, path, card count, dependency, runtime state machine, correction branch, phase closure, selector, and release-order instruction in the embedded appendix is discarded by `V21ToV23RequirementRebindingV1`. In particular, `V21-A00`, `phase/v21-p00-foundations`, `docs/design/v21/`, V21 card or exception counts, and V21 correction branch names are never executable V23 instructions.

### 2.3 Bootstrap before P00-C05

P00-C05's generator does not exist for the first cards. `BootstrapCardContextV1` is the only provisional hydrator for P00-C01, C02, C04, and C05. It is a hand-checked, machine-readable projection using the same canonical semantic-authority schema as `HydratedCardExecutionSpecV2`: exact register row and current dossier; policy/contract/journey references; predecessor block and translation; every applicable supersession clause; for each direct prerequisite, its immutable receipt, unique canonical `CardAcceptanceInclusionProofV1`, matching `CardAcceptanceInclusionProofRecoveryReceiptV1`/currentness, and current-candidate `CandidateAcceptanceCompatibilityReceiptV1` or exact supersession disposition; every §6B selector row where the card is member or consumer; all applicable §6C relation rows and §6D inherited-dependency dispositions; every applicable C07 allocation row; lifecycle/data/claims/brand impacts; named checks; and current authority digests. The prerequisite set carries exact expected/observed counts and rejects missing, duplicate, orphaned, divergent-proof, incompatible, or superseded evidence. It also records read-only repository owners, unresolved generated mechanics, and the bootstrap coordination-ledger locator/retention/writer lock plus current hash-chain/attempt state. Every immutable context binds the current verified `StateWriterAuthorityV2` owner/generation and ledger CAS tip; a later recovery generation emits a new context that explicitly supersedes the old one, and C05 may adopt only the latest unique context for each attempt. Two independent parse/check passes must agree on its digest. It cannot accept a card by itself.

For each bootstrap attempt those two agreeing passes also derive `BootstrapPathFenceV1`, the sole pre-C05 mutation/test authority. It binds package/context/card/attempt, exact base and candidate head/tree, current writer owner/generation/CAS tip, allowed repository roots/paths/symbols/generated outputs/tests/evidence, prohibited paths, expiry, and one-shot expansion rule. C01, C02, C04, and C05 may not mutate or execute candidate code outside it; discovering another required path first requires a fresh context and replacement fence before the write. At C05, canonical hydration enumerates every historical diff/artifact and proves it was within the fence active at that write, then requires canonical `PathFenceV1` to equal or safely refine the latest bootstrap fence without legitimizing an earlier out-of-fence change. Any gap, stale generation, or widening after the fact invalidates the attempt.

Immediately after A00 anchor validation and before any card context, the serialized activation controller persists `BootstrapStatePlaneGenesisIntentV1` for the exact A00-bound execution-ledger and expected-main-base locators. Expected-absent CAS creates `BootstrapExecutionLedgerEnvelopeV1` at writer generation 0 and `ExpectedMainIntegrationBaseV2` at the accepted A00/S10 fetched-main head with its initial CAS generation/status. `BootstrapStatePlaneGenesisReceiptV1` binds both intended initial values and per-plane creation results. `BootstrapStatePlaneGenesisRecoveryReceiptV1` classifies each plane independently as `ABSENT | APPLIED_MATCHING_GENESIS | DIVERGENT_OR_AMBIGUOUS`; exact absent/matching partial creation may be completed or returned byte-identically, divergence blocks, and neither a second ledger genesis nor an implicit expected-base first write is allowed. No context, transition, C04 checkpoint, or expected-base adoption proceeds until both matching geneses are durable.

The protected repository-external ledger uses the same append-only transition schema later owned by P00-C05. `ExecutionLedgerInitializationReceiptV1` binds the matching state-plane genesis, package/register/logical digests, and seeds exactly one first attempt per card in Foundation register order: both `DEFER`/planning-`DEFERRED` rows start live `DEFERRED`; every other row starts live `NOT_STARTED`. Planning status remains immutable provenance; later blocker, owner, and monitor triggers create explicit transitions rather than changing the seed. State-writer authority is the pair `StateWriterAuthorityV2(ownerID, writerGeneration)`. `A00_BOOTSTRAP_CONTROLLER` is the sole compare-and-swap owner before C05 adoption; it starts at generation 0 and may advance only to the immediately next generation through an exact `ExecutionLedgerRecoveryReceiptV1`. It writes C01/C02/C04 and C05-through-pre-adoption transitions, recovery receipts, acceptance card receipts/effect-recovery records, active-work projections, and hosted-result reconciliations; no C04 or C05 actor writes the same chain concurrently. C05 atomically adopts the frozen tip into `P00-C05` at `adoptedGeneration = lastVerifiedBootstrapGeneration + 1`; every A00 generation is then permanently revoked. There is no V1 state or `CURRENT_TASK` fallback.

Before any controller action, A00 creates `ProtectedControllerCellGenesisIntentV1` for each exact protected repository-external cell and performs expected-absent CAS genesis: `AcceptanceControllerAuthorityCellV1=A00_BOOTSTRAP_ACTIVE@CONTROLLER_EPOCH_0`, `RepositoryHostedCapacityPolicyAuthorityCellV1=NO_ACTIVE_POLICY@POLICY_EPOCH_0`, and `ControllerWorkflowEnrollmentAuthorityCellV1=NO_ACTIVE_ENROLLMENT@ENROLLMENT_EPOCH_0`. The embedded `ProtectedControllerCellGenesisReceiptV1` binds trust root, cell kind/locator, initial value/token, package/A00 head, and creation result. `ProtectedControllerCellGenesisRecoveryReceiptV1` classifies each retry as `ABSENT | APPLIED_MATCHING_GENESIS | DIVERGENT_OR_AMBIGUOUS`; ABSENT retries the identical expected-absent CAS, matching returns the same receipt, and divergence blocks. Partial multi-cell creation is recovered cell by cell from the frozen intent and grants authority only for cells whose exact genesis is proven; a second genesis or implicit epoch zero is forbidden.

`BootstrapAcceptanceControllerContextV1` then makes the proven `A00_BOOTSTRAP_ACTIVE(currentBootstrapControllerEpoch)` cell value—the initial value is epoch 0—the sole official-acceptance/diagnostic controller for C01, C02, C04, and C05 through C05's durable decision. It applies the exact V2 cell-order, fail-fast, diagnostic, correction, receipt-nonrecursion, and conservative hosted-capacity laws below even though the C04 implementation is not yet accepted. It emits only immutable `OfficialAcceptanceDecisionReceiptV1` decisions; `BootstrapCardContextV1` itself never accepts a card. For C01/C02, and for C05 `PRODUCT_FAIL | INFRA_PENDING | SUPERSEDED_NONPRODUCT`, `A00_BOOTSTRAP_CONTROLLER` independently verifies each decision and applies `OfficialAcceptanceEffectRecoveryReceiptV1`, persisting a card receipt only for C01/C02 ACCEPT and the matching state effect for every decision. C04 is categorically rejected by the generic effect and uses only the specialized decision-digest-bound transport/smoke effects above. Only C05 ACCEPT is rejected by the generic effect; it closes through the two-phase controller cutover and four-way state adoption below, so no C05 decision can strand `ACCEPTANCE_RUNNING`.

After C05 reaches targeted green, but before each C05 official attempt, its implemented generator produces canonical specs/fences for all four bootstrap cards and a `BootstrapSemanticEquivalenceReceiptV1(PASS)` on that exact candidate. The A00 acceptance controller runs the C05 official attempt and first persists the ordinary `OfficialAcceptanceDecisionReceiptV1(ACCEPT)`. `BootstrapC05AcceptanceDecisionReceiptV1(officialDecision=ACCEPT, adoptionEligibility=PASS)` is a typed adoption wrapper around—not a second decision: it binds the exact official-decision receipt digest and requires byte-identical program/card/attempt, candidate head/tree, order/start-fence/prerequisite/effective-authorization/result/artifact digests, terminal classification, and ACCEPT outcome, plus the matching semantic-equivalence receipt and adoption-only completeness fields. Any mismatch fails closed. Any C05 correction that changes generator/authority bytes invalidates equivalence and requires a new equivalence receipt plus fresh official attempt.

Only after both receipts are durable does the old controller create `AcceptanceControllerAdoptionIntentV1` binding the protected predecessor token, complete prior attempt/cell/stop/diagnostic/correction and queue/projection tips, C04 receipt/checkpoint, semantic-equivalence receipt, C05 decision, four frozen adoption-input tips, exact new owners, and one predeclared cutover effect ID. `AcceptanceControllerAuthorityCellV1` is a closed monotonic state machine: `A00_BOOTSTRAP_ACTIVE(epoch,resumeReceiptDigest?) | PENDING_C05_CUTOVER(epoch,intentDigest,effectID) | PENDING_C05_ABORT_EFFECT(epoch,abortIntentDigest) | PENDING_P00_C04_ACTIVATION(epoch,intentDigest,effectID) | P00_C04_ACTIVE(epoch,adoptionReceiptDigest,c05InclusionProofDigest,c05ProofRecoveryDigest)`. One protected CAS advances A00 ACTIVE to PENDING_C05_CUTOVER and emits `BootstrapAdoptionQuiescenceReceiptV1`; while any pending variant is active, neither controller may dispatch, decide, change queue/fence/projection/checkpoint/witness/stop state, or mutate a frozen tip. P00-C04 has no dispatch/decision authority yet.

Exactly one same-token branch may win from `PENDING_C05_CUTOVER`. The narrowly preauthorized state-writer cutover transaction atomically adopts the four frozen state planes and CASes the controller cell to `PENDING_P00_C04_ACTIVATION` with the predeclared effect ID. Alternatively, before that transaction, A00 may persist `BootstrapC05PendingAbortIntentV1` and CAS the same token to `PENDING_C05_ABORT_EFFECT`. Because both compare-and-swap the same protected cell token, adoption and abort cannot both commit. In abort-pending, the still-authoritative A00 state writer independently verifies the intent and emits/applies `BootstrapC05PendingAbortEffectReceiptV1` by one recoverable ledger CAS: the old C05 `ACCEPTANCE_RUNNING` attempt and its decision/equivalence eligibility become `SUPERSEDED`, every derived adoption eligibility is invalidated, and the next monotonic C05 attempt is created at `NOT_STARTED`. Only after that effect is durable may A00 perform a finalize-only CAS to fresh `A00_BOOTSTRAP_ACTIVE(nextEpoch,abortEffectReceiptDigest)` and create a new context/equivalence/official attempt. A crash is classified as before effect, applied matching, or divergent; it never reopens or strands the old attempt.

After the adoption branch, the state writer emits/reconstructs `BootstrapFourWayStateAdoptionReceiptV1` from the matching committed effect. The newly active P00-C05 state writer must then emit or recover the canonical C05 `CardAcceptanceInclusionProofV1` and durable matching `CardAcceptanceInclusionProofRecoveryReceiptV1`; this proof-only operation is the sole state-writer activity permitted while the controller cell is `PENDING_P00_C04_ACTIVATION`, and it cannot dispatch, decide, alter the accepted transition, or repeat the cutover effect. A00 then holds one finalization-only token—no dispatch, decision, queue, or projection authority—to CAS `PENDING_P00_C04_ACTIVATION` to `P00_C04_ACTIVE(nextEpoch,adoptionReceiptDigest,c05InclusionProofDigest,c05ProofRecoveryDigest)` and emit `BootstrapAcceptanceControllerAdoptionReceiptV1` binding the exact proof/recovery bytes; that final CAS permanently revokes every A00 controller capability. `AcceptanceControllerAdoptionRecoveryReceiptV1` classifies `A00_ACTIVE_BEFORE_PENDING | PENDING_CUTOVER_BEFORE_BRANCH | PENDING_ABORT_BEFORE_EFFECT | PENDING_ABORT_EFFECT_APPLIED_FINALIZE_A00 | PENDING_P00_ACTIVATION_EFFECT_APPLIED_PROOF_PENDING | PENDING_P00_ACTIVATION_PROOF_READY_FINALIZE | APPLIED_MATCHING_P00_ACTIVE | DIVERGENT_OR_AMBIGUOUS`. It recovers the canonical proof before finalization, retries only the identical next effect, returns matching evidence, or blocks. Both actors reread the cell before every action, and every mutation is CAS-fenced by its current token. The cutover transaction is the sole exception to disjoint controller/state-writer cell ownership and is bounded byte-for-byte by the old controller's signed intent; A00 remains bootstrap state/result owner until that exact transaction, and P00-C04 activation never precedes both the cutover and C05 proof recovery.

C01 and C02 cannot depend on the not-yet-integrated C04 hosted transport. `BootstrapLocalExecutionModeV1` is closed to `BOOTSTRAP_LOCAL_OFFICIAL | BOOTSTRAP_LOCAL_DIAGNOSTIC_NON_ACCEPTING`. Before either mode, the current `BootstrapCardContextV1` binds `TrustedBootstrapLocalExecutionProducerV1`: expected producer ID, signer domain/algorithm/public-key digest, clean local/incumbent macOS runner identity/fingerprint, allowed C01/C02 modes, toolchain constraint, validity window, and revocation token. The pin comes from A00/context authority—not from the envelope it authenticates—and any absent, envelope-supplied-only, wrong-key, wrong-producer, wrong-runner, expired, or revoked identity fails closed.

Official work uses `BootstrapLocalOfficialExecutionV1` plus the exact `AcceptanceStartFenceV1`, frozen order/cell identity, current `BootstrapPathFenceV1`, and a cryptographically random one-shot local nonce under the serialized A00 controller. After any diagnostic-eligible official failure, the controller separately creates `BootstrapLocalDiagnosticExecutionV1` binding `DIAGNOSTIC_NON_ACCEPTING`, `DiagnosticExecutionPlanV1`, exact `DiagnosticStartFenceV1`, issue/cell order, a distinct one-shot `BootstrapLocalDiagnosticLeaseV1`, local diagnostic nonce, reachable continue-through scope, and zero acceptance/release credit. It never reuses the official start fence, nonce, lease, or result. The diagnostic lease serializes this local runner but is not a fictional GitHub capacity lease. A qualifying failure cannot be declared diagnostically complete until this bounded local path terminates or records a typed local infrastructure blocker.

The pinned producer process—never the deciding controller or ledger writer—emits signed raw `BootstrapLocalExecutionResultEnvelopeV1` as the closed union `OFFICIAL_CELL_RESULT | DIAGNOSTIC_CELL_RESULT`. Both variants bind program/card/monotonic attempt, exact head/tree, package/authority/fence, local run/runner identity, controller epoch, pinned signer/producer identity, toolchain/runtime, start/terminal monotonic events, environment health, commands, exit/status, and immutable artifacts. The official variant additionally requires its acceptance fence/order/cell/wave/selector/timeout/artifact identity and prohibits diagnostic fields. The diagnostic variant requires its diagnostic fence/plan/lease/issue order/continue-through outcomes and prohibits official-decision or card-credit fields. Neither has a GitHub projection, hosted lane, claim, workflow enrollment, or inherent release credit. The A00 state writer alone independently retrieves/verifies the raw envelope and artifacts and emits tagged `BootstrapLocalExecutionResultReconciliationReceiptV1 = APPLY_ONCE | DUPLICATE_SAME_RESULT | LOST | LATE_AFTER_SUPERSESSION | CONFLICT_QUARANTINED`; the controller never produces or reconciles raw evidence. APPLY_ONCE feeds the matching official classification/decision/effect or diagnostic issue-set/terminal/effect path. DUPLICATE_SAME_RESULT resolves only to the original APPLY_ONCE bytes; LATE_AFTER_SUPERSESSION has no current effect. After the frozen local deadline plus an authoritative producer-liveness/artifact-absence check, LOST, or any identity/artifact conflict producing CONFLICT_QUARANTINED, is a typed infrastructure terminal rather than an unrepresentable result. For official mode it feeds only `AcceptanceCellTerminalClassificationReceiptV1(BOOTSTRAP_LOCAL_CELL, INFRASTRUCTURE_NONPRODUCT)` and the ordinary stop→INFRA_PENDING effect/fresh-attempt law; repair creates a new card attempt/order/fence/run and nonce. For diagnostic mode it feeds only `DiagnosticCellOutcomeV2(UNREACHABLE_INFRASTRUCTURE)` plus `DiagnosticTerminalDecisionReceiptV1(INFRA_PENDING)`/effect recovery, releases the exact local diagnostic lease after reconciliation, and requires a fresh diagnostic plan/lease/nonce to resume. No LOST or conflict yields product cause, acceptance, issue completion, or nonce reuse. Each nonce is consumed exactly once. Cross-mode, cross-card, cross-attempt, cross-fence, cross-runner, wrong-key, and same-head replay are hostile failures. C04 pre-integration checks remain nonaccepting local/incumbent preflight; only after C04 reaches the trusted-default checkpoint and completes transport activation does its hosted smoke produce the C04 official decision. Bootstrap local envelopes/decisions/receipts are adopted byte-identically at C05. Any attempt to use either bootstrap-local mode for C04 after integration or any later card fails closed.

`BootstrapSemanticEquivalenceReceiptV1` compares canonical `HydratedCardExecutionSpecV2`/`PathFenceV1` for C01, C02, C04, and C05 with the latest unique bootstrap context/fence through the shared semantic projection. Repository-derived paths, generated indexes, and other explicitly unresolved mechanics are filled rather than treated as semantic differences, but every historical changed path and artifact must be a subset of the exact fence active when produced, and canonical fences must equal or safely refine them. A mismatch emits `BootstrapSemanticMismatchInvalidationReceiptV1`; the A00 state writer applies a generation-fenced old/matching/divergent CAS that preserves old evidence, marks the affected bootstrap attempt and every dependent provisional result SUPERSEDED, and creates successor attempts. If mismatched C04 bytes already reached main, it authorizes only a forward C04 correction/tooling-repair checkpoint under the same trust root; it never rewrites main or revives a receipt. No controller adoption is permitted without exact PASS on the final C05 candidate.

`BootstrapLedgerAdoptionReceiptV1` verifies the exact initialization/transition hash chain through C05 `ACCEPTANCE_RUNNING`, attempts, response locators, recoveries, frozen tip, A00 owner ID, last writer generation, and latest-context/fence chain without editing, renumbering, or replaying history. `BootstrapAcceptanceEvidenceAdoptionReceiptV1` verifies the immutable external C01/C02/C04 decisions/card receipts/anchors plus each card's unique canonical `CardAcceptanceInclusionProofV1` and matching recovery/currentness result, the final `BootstrapSemanticEquivalenceReceiptV1`, pending-cutover quiescence intent/receipt, plus `BootstrapC05AcceptanceDecisionReceiptV1(officialDecision=ACCEPT, adoptionEligibility=PASS)` byte-for-byte; it cannot require C05's later accepted-transition proof or the final controller-adoption receipt. `BootstrapHostedTransportAdoptionReceiptV1` verifies signed cell geneses, transport-genesis/enrollment/policy checkpoints and witnesses, claims, result envelopes, reconciliations, nonces, stop intent/effect history, and C05 pre-adoption hosted outcomes byte-for-byte; and `ExpectedMainIntegrationBaseAdoptionReceiptV1` verifies C04's accepted receipt plus the exact expected-base value/history/hash/CAS generation. One idempotent four-state-plus-cutover-token transaction binds all four frozen receipt digests/tips, the exact `PENDING_C05_CUTOVER` token, predeclared effect ID, and C05 decision; atomically adopts the four state planes without rewrite, persists C05's immutable external card receipt, changes the state-writer owner to `P00-C05` at `lastVerifiedBootstrapGeneration + 1`, permanently revokes every A00 state-writer generation, appends C05's `ACCEPTED` transition binding the decision/card-receipt digests, and CASes the controller cell to matching `PENDING_P00_C04_ACTIVATION`. It emits/reconstructs `BootstrapFourWayStateAdoptionReceiptV1`; `BootstrapFourWayStateAdoptionRecoveryReceiptV1` classifies `BEFORE_EFFECT_AT_FROZEN_A00_TIPS_AND_PENDING_TOKEN | APPLIED_MATCHING_P00_C05_GENERATION_AND_PENDING_ACTIVATION | PARTIAL_OR_DIVERGENT`, retries only the identical transaction, returns matching evidence, or quarantines. A crash before the transaction leaves exact A00 state authority under the pending-cutover freeze and permits the mutually exclusive abort branch; a crash after it can only reconstruct the matching receipt, recover C05's canonical inclusion proof, and then finalize P00-C04 activation. The locator may change only through an atomic copy whose byte/hash equality and cutover are bound by the adoption receipts.

C04 declares the `ExpectedMainIntegrationBaseV2` schema and, after the matching A00 `BootstrapStatePlaneGenesisReceiptV1`, bootstrap-writes only later transitions from the already-durable accepted A00/S10 fetched-main initial value and any pre-C04 `MainDriftReconciliationReceiptV2`; it needs no future C05 accepted receipt. Its closed status is `ACCEPTED_BASE | C04_TRANSPORT_ACTIVATION_PENDING | C04_TRANSPORT_ACTIVATION_INFRA_PENDING | C04_SMOKE_PENDING | C04_SMOKE_INFRA_PENDING | C04_CORRECTION_REQUIRED`, and every status change is paired with the matching state-writer effect/recovery. `ExpectedMainIntegrationBaseAdoptionReceiptV1` is the fourth member of the frozen state-adoption transaction above: it binds C04's accepted receipt and exact genesis/value/history/hash/CAS epoch without rewriting the base or reconciliation chain. Any difference or stale epoch prevents the four-state cutover and blocks C05 ACCEPT adoption. After P00-C05 owner adoption, every A00/bootstrap-mode hydration, acceptance-evidence write, hosted-result reconciliation, expected-base write, and ledger write is prohibited; P00-C05's canonical state-writer duties continue under the adopted generation.

Read-only reconnaissance and off-repository planning may continue while S10 is active, but cannot mutate the app, install A00 against an unaccepted base, or claim card acceptance.

<a id="v23-context-hydration"></a>
## 3. Activation reading and bounded context

At A00 activation, verify hashes and read Foundation §§1–15, Blueprint §§1–8, this entire Handoff, the prompt, and current repository evidence. Do not reload the embedded V21 appendices wholesale before every card.

For each card, P00-C05 generates `AuthoritySliceManifestV1` and `HydratedCardExecutionSpecV2` containing:

- exact Foundation row and current Blueprint dossier;
- explicit policy, contract, and journey refs;
- exact semantic predecessor block, `V21PredecessorBlockIndexV1` translation, and every applicable `V21SemanticSupersessionManifestV1` clause disposition when inherited;
- current four-document hashes and source anchors;
- one exact `DirectPrerequisiteEvidenceSetV1` row per direct edge: accepted receipt, unique canonical `CardAcceptanceInclusionProofV1`, matching `CardAcceptanceInclusionProofRecoveryReceiptV1`/currentness, and current-candidate `CandidateAcceptanceCompatibilityReceiptV1` or exact supersession/reproof disposition; the set binds expected/observed counts and requires zero missing, duplicate, orphaned, divergent-proof, incompatible, or stale entries; for the ten exact P00-C03→P06 edges only, the specialized nonrelease-prerequisite law below replaces—not supplements—an acceptance receipt;
- the exact direct-prerequisite row; every §6B selector row in which the card is a member or consumer; every applicable §6C row across the six non-direct kinds—aggregate acceptance, invalidation, provenance, optional provider, conformance subject, and contract-input provider; the matching digest-bound `ContractFacetRegistryV1` selector and materialized exact source-owned member list for every contract-input row; plus this card's applicable V21 dependency-disposition rows and C07 allocation row;
- resolved declaration, canonical writer, artifact or codec writer, semantic lifecycle, and enrollment owners;
- exact repository paths, symbols, generated artifacts, exclusions, and allowed `PathFenceV1`;
- lifecycle, data handling, claims, brand, HIG, accessibility, localization, and privacy impacts;
- deterministic Golden, Alternate, Hostile, Interruption, and Recovery IDs expanded into named test methods and commands;
- prior attempts, normalized failure signatures, owner conditions, and acceptance artifacts.

A stale, incomplete, unresolved, or hash-mismatched slice cannot become READY. Reload the full activation core after an authority digest changes, a conflict is detected, or context compaction invalidates the slice.

<a id="v23-state-machine"></a>
## 4. One V23 execution state machine

`PlanningCardStatusV2` remains immutable package provenance. `CardExecutionStateV2` is the sole live V23 coordination state and explicitly supersedes every embedded V21 runtime state machine.

P00-C05 declares `BootstrapExecutionLedgerEnvelopeV1`, `ExecutionLedgerInitializationReceiptV1`, `BootstrapLedgerAdoptionReceiptV1`, `BootstrapAcceptanceEvidenceAdoptionReceiptV1`, `BootstrapHostedTransportAdoptionReceiptV1`, `ExpectedMainIntegrationBaseAdoptionReceiptV1`, `BootstrapFourWayStateAdoptionReceiptV1`, `BootstrapFourWayStateAdoptionRecoveryReceiptV1`, `StateWriterAuthorityV2`, `ExecutionLedgerRecoveryReceiptV1`, `CardExecutionAttemptV2`, `CardExecutionTransitionReceiptV2`, `CardExecutionResponseReceiptV2`, `ActiveWorkSetProjectionV1`, and `CurrentV23TaskProjectionV1`. One active writer uses append-only, crash-safe, compare-and-swap transitions keyed by card and monotonic attempt ID in the protected repository-external coordination ledger whose resolved locator and retention class were frozen first by `ProgramActivationReceiptV2`, created/recovered by `BootstrapStatePlaneGenesisReceiptV1`, then referenced without relocation by `BootstrapCardContextV1` and after adoption by `AuthoritySliceManifestV1`; it is never a shipping target, customer store, or build input. Writer authority is mutually exclusive by exact `(ownerID, writerGeneration)`: `A00_BOOTSTRAP_CONTROLLER` owns generation 0 and any receipt-authorized recovery successors until the frozen-tip C05 four-way adoption; the transaction changes owner to `P00-C05` at exactly the next generation. Later P00-C05 recoveries likewise increment only generation and never restore A00. A transition signed for the wrong owner or stale/nonconsecutive generation fails closed. Every transition binds prior/new state, reason, actor/authority, exact candidate head/tree when applicable, prerequisite and authorization digests, timestamps, and response/evidence locators. Initialization seeds DEFER rows as `DEFERRED` and all others as `NOT_STARTED` against the frozen package/register; adoption preserves the bootstrap chain byte-for-byte.

Startup verifies the hash chain and deterministically rebuilds `ActiveWorkSetProjectionV1`, an ordered set of every nonterminal active card/attempt with live state, candidate head/tree, branch/worktree, `PathFenceV1`, effective authorization, integration position, hosted lease/entry when any, and recovery disposition, plus the exact next eligible register ordinal. Disjoint cards may therefore remain concurrently `IMPLEMENTING` without a restart losing a lane or path lock. `CurrentV23TaskProjectionV1` is only a derived display of the first ordered active entry and grants no authority. A torn tail, malformed frame, duplicate sequence, unexpected owner/generation, or competing CAS branch fails closed to the last uniquely verified transition. `ExecutionLedgerRecoveryReceiptV1` binds bootstrap-or-canonical mode, package/ledger identity, failure class, last-good sequence/digest/owner/generation, expected and observed CAS values, every quarantined byte range/branch digest and protected locator, deterministic winning-chain rationale or `UNRESOLVED_CONFLICT`, same owner plus exactly next writer generation, the complete resumed active-work set, and proof that no accepted transition was edited, reordered, renumbered, or replayed. A torn suffix may be quarantined and the last-good chain resumed by the same owner at the next generation; recovery cannot change owner. Two independently valid conflicting successors remain `UNRESOLVED_CONFLICT` until authority selects one through a hashed correction, never by timestamp. A crash before recovery append is idempotently retried; a crash after append returns the same receipt. The A00 bootstrap writer may emit the same type before C05; all four adoption receipts bind the final A00 generation and C05 adopts it byte-identically into the next generation. Hostile tests cover truncation at every byte boundary, duplicate/conflicting CAS, stale owner/generation, recovery crash before/after append, repeated A00 recovery before C05, bootstrap adoption with multiple active disjoint lanes and quarantined tails, and projection rebuild. `CardExecutionStatusV1`, V21 `CURRENT_TASK`, and every embedded V21 runtime projection are prohibited as V23 fallbacks.

GitHub-hosted controllers receive no magical access to the external execution ledger. P00-C04 declares the capacity, acceptance/diagnostic fence, projection, active-dispatch, workflow-enrollment, high-water, claim, result, and external-occupancy contracts named in its dossier. A00 pins the Ed25519 public key, protected external signer identity, allowed GitHub repository, expected controller path and protected-ref policy, and the independently readable high-water-witness trust profile; it does not invent a future workflow commit or blob. Before the first V23 hosted product command, the current acceptance controller persists `ControllerTransportGenesisIntentV1` binding the exact C04 checkpoint subject, protected workflow bytes, `NO_ACTIVE_ENROLLMENT@ENROLLMENT_EPOCH_0` predecessor, an expected-absent external-occupancy plane, coordination branch/path, genesis projection/checkpoint/witness, and checkpoint/stop/run-claim namespaces/rulesets. The same frozen intent creates `ObservedExternalHostedOccupancyGenesisReceiptV1` and exactly `ObservedExternalHostedOccupancySetV1.EMPTY(generation=0,genesisDigest)`; the first complete scan may CAS only that gen-0 predecessor. It then creates or verifies `ControllerWorkflowEnrollmentV1(epoch=1)`, performs the one expected-token CAS that advances `ControllerWorkflowEnrollmentAuthorityCellV1` from `NO_ACTIVE_ENROLLMENT@ENROLLMENT_EPOCH_0` to `ACTIVE(epoch=1,enrollmentDigest,genesisReceiptDigest)`, and creates/verifies the dedicated linear `coordination/v23-state` branch. `ControllerTransportGenesisRecoveryReceiptV1` classifies the enrollment cell, external-occupancy gen-0 set, branch, projection, checkpoint, witness, and ruleset planes as `ABSENT | PARTIAL_MATCHING | COMPLETE_MATCHING | DIVERGENT_OR_AMBIGUOUS`: only exact absent or byte-identical partial planes from the frozen intent may be completed; complete matching returns the same genesis receipt, divergence blocks, and a second genesis is forbidden. The enrollment-cell transition uses `ControllerWorkflowEnrollmentEffectRecoveryReceiptV1`; no projection, admission witness, or dispatch is authoritative before both its ACTIVE token and the occupancy genesis are durable and a complete current scan has reconciled. The coordination branch permits only ordinary non-force appends of one frozen projection path, never triggers on push, and is never merged, fast-forwarded, copied, or cherry-picked into phase/main.

Writer ownership is disjoint. `A00_BOOTSTRAP_ACTIVE(currentEpoch)` through the final C05 decision, no dispatch/decision writer in any pending-cutover variant, and `P00_C04_ACTIVE(nextEpoch)` only after the matching four-state adoption receipt plus C05 canonical inclusion proof/recovery are durable is the sole capacity-queue, lease, acceptance/diagnostic-fence, projection, dispatch, external-occupancy-set, and official-decision authority. A00 retains only the exact abort or finalization-only token named above while pending. `A00_BOOTSTRAP_CONTROLLER` at its current verified state-writer generation through C05 state adoption and then `P00-C05` at the immediately next generation is the sole external card-ledger, acceptance-receipt/effect, and hosted-result retrieval/reconciliation writer. The state writer supplies signed ledger inclusion and effective-authorization facts; the acceptance controller independently verifies them, owns the queue/lease and occupancy-set decisions, and projects only bounded dispatch grants. Neither writes the other's chain except the one intent-bound controller-cell CAS inside the atomic cutover transaction. `BootstrapHostedTransportAdoptionReceiptV1` adopts the A00 state/result facts, claims, nonces, applied transitions, external-occupancy genesis, complete generation history, current set digest, and latest reconciliation/resolution receipt byte-for-byte; only after state adoption and C05 proof recovery may P00-C04 continue the same checkpoint/high-water/occupancy chains without a second genesis. C05 state authority and proof recovery are not prerequisites for bootstrap execution, but both are absolute prerequisites for P00-C04 activation.

After each dispatch-relevant transition, the acceptance controller emits secret-free `ControllerProjectionEnvelopeV2` signed under that trust root. It is a global snapshot, not one latest-only job authorization. Its canonical `ActiveDispatchSetV1` contains zero through five lexically lane-ordered `ControllerDispatchAuthorizationV1` reservation entries. `HostedWorkKindV1` is closed to `PRODUCT_PIPELINE | PROTECTED_RELEASE_PIPELINE | PROTECTED_RELEASE_RECONCILIATION | CONTROLLER_GUARD | POLICY_ACTIVATION_GUARD | CAPACITY_EVIDENCE_RENEWAL_GUARD | ROLLOVER_SELF_TEST`; `HostedWorkIdentityV1` supplies the kind-specific reservation identity. A product reservation embeds `HostedProductPipelineV1` with closed `HostedPipelineStageKindV1 = CLAIM_ONLY_GUARD | PRODUCT_CELL`, one pipeline nonce, distinct claim-stage identity/nonce and one logical product-stage identity with monotonic `HostedProductStageAttemptV1` identity/nonces, and state `CLAIM_STAGE_AUTHORIZED | CLAIM_STAGE_RUNNING | CLAIM_STAGE_TERMINAL | PRODUCT_STAGE_PRETRANSFER_NOOP_RECONCILED_WAITING_TRANSFER | PRODUCT_STAGE_AUTHORIZED(retryEpoch=0|1) | PRODUCT_STAGE_RUNNING | TERMINAL`. It binds execution mode (`TARGETED_NON_ACCEPTING | OFFICIAL_ACCEPTANCE | DIAGNOSTIC_NON_ACCEPTING | AUXILIARY_REFRESH`), card/attempt, candidate head/tree, cell ID, total ordinal, wave, official order digest when applicable, selector, fence, artifact, and release credit. A protected-release reservation embeds the P05-C02-only `ProtectedReleasePipelineV1` and sequential release stages defined below, current owner authorization, action/deployment identity, accepted archive and action-specific input identity, and explicitly carries no product/card-acceptance credit. A control/guard/renewal/rollover entry instead binds its policy/enrollment/activation/renewal/rollover attempt, expected authority-cell epoch/token, exact protected workflow bytes and result artifact. Every reservation also binds one capacity lease/lane, expiry, and exact enrollment. Entries are a bijection with typed queue reservations: duplicate reservation/stage/attempt identity, nonce, or lease; more than one reservation per lane; or a product cell absent from its frozen order fails closed. Later snapshots preserve every still-active reservation until terminal reconciliation or explicit revocation.

Unreserved historical, manual, or externally started jobs are never retroactively converted into `HostedWorkKindV1`, a dispatch reservation, or a V23 result. Each fresh complete all-ref/API scan produces `HostedExternalOccupancyObservationV1` rows for the separate `ObservedExternalHostedOccupancySetV1`, binding repository/workflow/ref/SHA/run/run-attempt/job/matrix identity, discovery observation, provider start/status/terminal truth, current hard-cap configuration generation and enforcement-scope disposition, with `retroactiveAuthority=false` and no lease, admission witness, claim, projection entry, card/release credit, or canonical hosted-result envelope. The active acceptance controller applies the first complete scan from the exact durable gen-0 EMPTY predecessor, and every later scan from its exact current predecessor, through `ExternalHostedOccupancyReconciliationReceiptV1`: a CAS over the prior occupancy-set generation that binds query/cursor/completeness, matched V23 jobs, contained external jobs, unknown/uncontained jobs, debits/releases, and any configuration-gate effect, and classifies `BEFORE_EFFECT_AT_PRIOR_GENERATION | APPLIED_MATCHING_SET | DIVERGENT_OR_AMBIGUOUS`. Exact replay is idempotent; a torn/divergent set blocks.

API uncertainty retains every prior debit and blocks starts by default. `ExternalHostedOccupancyUnavailableTerminalResolutionReceiptV1 = AUTHORITATIVE_TERMINAL_REFETCH | AUTHENTICATED_DELETION_AFTER_MAX_RUNTIME_AND_COMPLETE_LIVE_SCAN | OWNER_SECURITY_PROSPECTIVE_RESET | UNRESOLVED_RETAIN_DEBIT` is the only other way to resolve a retained debit. AUTHORITATIVE_TERMINAL_REFETCH binds the ordinary terminal provider record. AUTHENTICATED_DELETION requires the immutable observed start/job identity; the provider/pool maximum execution deadline and retention policy effective at start; elapsed deadline plus frozen safety grace; authenticated deletion/retention/audit evidence; complete consistent repository/organization/pool live-run and retry scans showing no matching or successor job; and unchanged enclosing hard-cap configuration. OWNER_SECURITY_PROSPECTIVE_RESET requires all V23 reservations drained, repeated complete current live scans, explicit owner/security authorization naming the unavailable record and residual risk, a fresh governed policy/configuration generation, and records a continuity gap so the prior interval receives no retroactive ceiling claim. UNRESOLVED retains the debit and configuration block. The controller applies any releasing resolution as one next-generation occupancy-set CAS with `BEFORE_RESOLUTION_AT_PRIOR_GENERATION | APPLIED_MATCHING_RESOLUTION | DIVERGENT_OR_AMBIGUOUS` recovery; deletion, age, timeout, 404, or owner assertion alone never clears a slot. The occupancy-set generation/digest and latest genesis/reconciliation/resolution receipt are bound into every projection and `HostedCapacityAdmissionWitnessV1`.

`OWNER_SECURITY_PROSPECTIVE_RESET` may release the unavailable debit only with one additional conjunctive safety proof: either the old job's provider/pool maximum-execution deadline plus frozen grace has elapsed, or one uninterrupted native hard mechanism of at most five is proven to have enclosed that exact possibly-live job identity continuously from its last observation through the reset and still encloses it after reset. The latter must use the same enclosing mode/scope or a truly repository/account-wide cap whose scope includes both the old job and every newly admitted job; a pool/scope migration, configuration gap, or fresh mechanism that excludes the old runner is ineligible. Without one of those proofs, owner approval and a fresh generation may record the blocker but must select `UNRESOLVED_RETAIN_DEBIT`. A reset never converts a continuity gap into ceiling evidence.

Every observed external `in_progress` job inside the proved hard-mechanism scope subtracts from availability: `availableForV23 = max(0, effectiveHostedCapacity - observedExternalInProgress - v23InProgress)`. Discovery immediately blocks new V23 dispatch; unstarted V23 reservations are revoked or returned to their frozen queue positions, while already-running work may drain only when authoritative proof still guarantees the total cannot exceed the hard cap. If a job is outside or cannot be proven inside the enforcement scope, or if `observedExternalInProgress + v23InProgress > effectiveHostedCapacity`, the configuration/audit truth is contradictory: enter `HOSTED_CAPACITY_CONFIGURATION_REQUIRED`, block/drain hosted work, and require owner/security review plus fresh policy activation. The native hard mechanism—not the five logical lanes—is the safety boundary; no document claims an unknown external start was prevented. Any hard-limit/configuration mutation, accidental, authorized, or malicious, ends the continuity claim at its mutation time until fresh governed activation; an affected interval cannot claim the five-job ceiling was proven. Controller, guard, activation, renewal, rollover, product/refresh stages, protected-release/reconciliation stages, and observed external jobs all subtract from the same effective hosted-job budget while actually `in_progress`.

The envelope also binds domain/key ID, repository, package/register/ledger-chain/current-candidate/effective-authorization digests, monotonic sequence, exact predecessor projection/checkpoint, policy/enrollment/high-water digests, generated/expiry times, and zero customer data/secrets. Its embedded `ControllerDispatchProofBundleV1` contains package/register/graph roots; the current ledger transition and signed inclusion proof; for every direct edge, the prerequisite receipt plus unique canonical accepted-transition inclusion proof, matching proof-recovery/currentness result, and current compatibility/supersession disposition, or the exact P00-C03→P06 nonrelease satisfaction proof; and the complete hydration/effective-authorization/trigger/fence chain for every active entry. Expected and observed edge/evidence counts must agree with zero orphan or divergent proof. Customer/work payload, credentials, secrets, arbitrary paths, and unbounded receipt bodies are prohibited.

The projection appends by ordinary non-force one-file commit. `ControllerProjectionMirrorReceiptV2` binds the same facts, prior/new mirror heads, signature verification, and one-file diff. The protected signer then creates the one annotated immutable `ControllerProjectionCheckpointV1` ref at `refs/tags/assetrounds-v23-state-checkpoints/<zeroPaddedSequence>-<projectionDigest>` and advances `ControllerProjectionHighWaterWitnessV1` by monotonic compare-and-swap in the already governed repository-external coordination store. The witness is secret-free, independently readable by the runner, signed, version-retained, and binds genesis, sequence, projection/checkpoint/commit digests, workflow-enrollment epoch, current ruleset/protected-ref digest, predecessor witness, writer epoch, and expiry. A projection is dispatchable only when branch head, unique highest gap-free checkpoint, current ruleset, and independently fetched witness all agree. Missing/unreachable/stale/rewound witness or ruleset mismatch fails closed. Deleting a checkpoint tail and rewinding the branch cannot expose an old prefix as current because it disagrees with the retained external high-water witness. If the witness store cannot provide authenticated read and monotonic CAS, hosted dispatch remains blocked; this tooling witness is not an application backend and carries no customer data.

An uncertain branch/tag/witness write is recovered in order: refetch all three planes, finish a missing exact checkpoint or witness only for the predeclared signed head, or quarantine any divergence. No second genesis resets a high-water mark. Loss of signer, witness, protected refs, or current ruleset is a typed infrastructure blocker. Malicious compromise of the external signer and witness trust root together is outside the claimed boundary and is never described as protected by GitHub alone.

A GitHub controller fetches the branch, checkpoints, current ruleset, and high-water witness. It verifies genesis/signature, linear ancestry, latest sequence/predecessor, exact one-file history, package/ledger-root agreement, each active entry's unique identity and inline inclusion/compatibility/authorization/fence proof, unexpired authorization, current workflow enrollment, and recomputed path-fence/slot eligibility before dispatch. It needs no customer ledger and receives only the secret-free witness read. `workflow_dispatch`, issue/PR text, environment variables, external locators, and user-supplied card/path/test values are inert hints and cannot broaden the signed bundle.

Controller bytes may change without stranding V23 only through a same-trust-root monotonic two-phase rollover. `ControllerActivationSubjectV1` is closed to `C04_CHECKPOINT(C04CheckpointEffectReceiptV1,mainTip)` or `P05_PHASE(PhaseIntegrationReceiptV2,mainTip)`; capacity-evidence renewal is a distinct no-rollover subject below. `ControllerWorkflowRolloverIntentV1` binds that subject; old/new enrollment epochs and exact protected ref/commit/path/blob; candidate workflow proof; committed capacity-policy authority predecessor/new epoch and activation-receipt digest; permissions/events; capacity/no-bypass checks; old-controller non-accepting conformance results; zero active product/diagnostic/refresh/protected-release pipeline leases plus exactly one bounded `ROLLOVER_SELF_TEST` lease; exact terminal official/diagnostic decision/result receipts; predecessor checkpoint/witness; and one legal cutover. `ControllerWorkflowEnrollmentAuthorityCellV1` is the only enrollment truth. The old enrolled controller and signer CAS it from ACTIVE(epoch) to `PENDING_SELF_VERIFICATION(epoch+1,intentDigest)` and append that pending value to the existing checkpoint/witness chain; the old epoch retains rollover/recovery-only authority, but neither epoch may dispatch product or protected-release work. The pending controller runs only the exact capacity-counted guard/self-test with no checkout or product command and verifies the same activation subject and policy digests. PASS performs one protected CAS that embeds `ControllerWorkflowRolloverReceiptV1`, marks epoch+1 ACTIVE, and revokes old authority. Failure/timeout CASes an aborted-pending disposition that restores the unchanged old ACTIVE authority for a new monotonic intent; it never rewinds an epoch token or creates a new genesis. `ControllerWorkflowEnrollmentEffectRecoveryReceiptV1` classifies every pending/active/aborted retry as `BEFORE_EFFECT_AT_EXPECTED_TOKEN | APPLIED_MATCHING_INTENDED_TOKEN | DIVERGENT_OR_AMBIGUOUS`, retrying the identical CAS, returning matching evidence, or blocking. `ControllerWorkflowRolloverDispositionV1 = UNCHANGED_CONTROLLER | ACTIVE(receiptDigest,epoch,authorityCellToken) | PENDING_BLOCKED | ABORTED_OLD_REMAINS_ACTIVE` is required by release hydration. A C04 correction or typed C04 tooling-infrastructure repair may use the C04 subject under the current A00 controller, same genesis/trust root, and monotonic policy/enrollment epochs before a fresh smoke; P05 uses the phase subject. Every projection, claim, result, product dispatch, and protected-release dispatch binds the committed policy evidence generation and active enrollment-cell token.

Each product dispatch uses the one compound `PRODUCT_PIPELINE` reservation and two sequential security boundaries under its same lane. First, a pinned `CLAIM_ONLY_GUARD` stage independently fetches the current authority cell, projection/checkpoint/witness, matching pipeline entry, pipeline/stage nonces, fresh capacity witness, and stop namespace. At claim-stage start it requires the claim ref to be ABSENT, or byte-identically owned by the same claim job on retry; it cannot require a claim that it has not yet created. It has the minimum `contents: write` permission needed for the protected claim namespace, performs no repository checkout, cache restore, third-party or untrusted action, script from candidate bytes, or product command, and attempts to create `HostedRunClaimV1`. The canonical annotated tag binds projection/checkpoint/witness/authority-cell and pipeline-entry digests; claim nonce; repository; workflow enrollment/ref/commit/path/blob; claim workflow run/run-attempt/job/matrix IDs; the predeclared logical product-stage identity and bounded retry policy—but not a not-yet-known concrete retry run ID—candidate; execution mode; typed cell/order identity; lease/lane; claim time and expiry. Atomic creation of `refs/tags/assetrounds-v23-run-claims/<nonceDigest>` permits one winner; an identical same-claim-job retry resumes, another exits `DUPLICATE_NON_EXECUTING`. Permission, API, signature, or service failure before tag creation remains a terminal claim-stage fact with `claimDisposition=NOT_CREATED`; an observed mismatching tag is `OBSERVED_CONFLICT`. Neither outcome can authorize product work.

After the active state writer reconciles a claim-stage terminal, the acceptance controller may apply the claim-to-product transfer only for `claimDisposition=CREATED_MATCHING`, preserving the same reservation/lane and binding the immutable claim/result plus the logical product-stage identity. `ProductStagePretransferArbitrationV1` makes the transfer/no-op race commutative. Its two independently monotonic inputs are the matching transfer and the apply-once initial-attempt no-op reconciliation; each controller CAS binds the same pipeline, claim terminal, logical stage, epoch-0 attempt/nonce, queue token, and projection generation. `ProductStagePretransferArbitrationRecoveryReceiptV1 = BEFORE_INPUT | APPLIED_MATCHING_INPUT | DIVERGENT_OR_AMBIGUOUS` covers either input and the derived retry authorization. If transfer wins first, `CLAIM_STAGE_TERMINAL → PRODUCT_STAGE_AUTHORIZED(retryEpoch=0)`; a later matching no-op CAS advances directly to `PRODUCT_STAGE_AUTHORIZED(retryEpoch=1,freshStageNonce)` only after authoritative API proof that the epoch-0 job is terminal. If no-op reconciliation wins first, `CLAIM_STAGE_TERMINAL → PRODUCT_STAGE_PRETRANSFER_NOOP_RECONCILED_WAITING_TRANSFER`; the later matching transfer advances directly to epoch 1. The CAS loser refetches and applies only its still-missing input, so both orders converge to one epoch-1 authorization and can never create two retries. Exact duplicate inputs are idempotent. If epoch 0 actually enters `PRODUCT_STAGE_RUNNING`, a no-op for that same attempt is contradictory and quarantined. `NOT_CREATED`, `OBSERVED_CONFLICT`, or any other claim failure is classified as infrastructure, releases the reservation only after apply-once reconciliation proves no stage is in progress, and can never authorize product.

The initial dependent product job may be ordered by `needs`, carries `contents: read` or stricter permissions and no write credential, and before checkout performs one bounded, irreversible precommand cutoff. If the exact matching transfer is present at that cutoff, it may continue under epoch 0; if absent, that run irrevocably selects `PRODUCT_STAGE_PRETRANSFER_NOOP`, executes no checkout/cache/third-party/candidate/product command, and may not switch to product execution even if transfer appears later. Its terminal envelope records `transferAtCutoff=ABSENT`, zero work, and `transferAtTerminal=ABSENT | APPLIED_MATCHING_TRANSFER`; both terminal observations are valid arbitration inputs. That is a representable nonexecuting result, not PASS or failure evidence; the pipeline reservation/lane remains held. A duplicate byte-identical no-op resolves to the original reconciliation and has no second effect; a distinct second no-op, retry replay, or identity mismatch ends the pipeline as infrastructure and requires a fresh pipeline/claim nonce. Retry epochs are retained and never deleted or reused. At an authorized product-stage start and immediately before its first product command, the job independently requires the matching immutable claim, exact current stage-attempt epoch/nonce, PRODUCT_STAGE_AUTHORIZED entry, authority, capacity witness, prerequisite/effective-authorization fence, and stop state. `persist-credentials: false` is required but is never misrepresented as revoking a job-scoped token. Claim and product attempts each count while `in_progress`; terminal claim plus the stage CAS prevents overlap on one lane. Claim refs and their ruleset/protection evidence remain retained through package expiry. Administrative bypass/deletion of a protected claim ref is explicitly outside the claimed replay-resistance boundary; any observed missing previously reconciled claim is a terminal hosted-transport blocker requiring owner/security review and a fresh attempt/pipeline nonce, never proof that the old nonce is reusable. P00-C04 declares/verifies/lifecycles claims, while the active state writer only retrieves and reconciles them.

Hosted output never writes the external ledger or creates its own canonical envelope. The active state writer—A00 bootstrap before adoption, P00-C05 after—independently retrieves raw GitHub run/job status, logs/attestations, provider truth when applicable, and immutable artifacts from the pinned repository/API and creates tagged-union `HostedExecutionResultEnvelopeV1`. `PRODUCT_PIPELINE_STAGE_RESULT` binds exactly one stage/subvariant: a claim-stage result carries `claimDisposition=CREATED_MATCHING | NOT_CREATED | OBSERVED_CONFLICT` and prohibits product fields; `PRODUCT_STAGE_PRETRANSFER_NOOP_RESULT` requires the matching logical stage/attempt, proves transfer absent at the irreversible precommand cutoff, records terminal transfer as `ABSENT | APPLIED_MATCHING_TRANSFER`, proves zero checkout/cache/product execution, and prohibits product artifacts/credit; `PRODUCT_STAGE_EXECUTION_RESULT` requires the matching created claim, stage-transfer receipt, current concrete retry epoch/nonce, and terminal authority fence. `PROTECTED_RELEASE_STAGE_RESULT` binds exactly one release subvariant: `RELEASE_CLAIM_GUARD_RESULT` carries `claimDisposition=CREATED_MATCHING | NOT_CREATED | OBSERVED_CONFLICT` and prohibits provider credential/effect/lease/decision fields; `RELEASE_EXTERNAL_MUTATION_PRELEASE_NOOP_RESULT` requires the matching created claim and stage transfer, a complete job-captured pre-effect snapshot, and `ProtectedReleasePreEffectNoopDispositionV1 = REQUIRED_MARGIN_UNAVAILABLE | PROVIDER_PRECONDITION_STALE | AUTHORIZATION_OR_TOKEN_STALE | STOP_OR_INCIDENT_ACTIVE | AUTHORITY_INPUT_OR_POLICY_CHANGED | MUTATION_GATE_ALREADY_INVALIDATED | SAFETY_SOURCE_HIGH_WATER_ADVANCED | INFRASTRUCTURE_BEFORE_EFFECT_LEASE`. `SAFETY_SOURCE_HIGH_WATER_ADVANCED` is legal only for SAFETY_CONTAINMENT_PAUSE when the all-or-none effect-start transaction observes the still-OPEN source-sequence cell at a committed high-water greater than the authorization/frontier merged-through value, has written no lease or provider command, and binds the complete new source/PENDING allocation snapshot; it is never used for an ordinary action or a post-fence source. The infrastructure case additionally binds `ProtectedReleasePreEffectInfrastructureReasonV1 = RUNNER_CANCELLED_OR_CRASHED | PROTECTED_CREDENTIAL_UNAVAILABLE | PROVIDER_PREFLIGHT_UNAVAILABLE | EFFECT_START_COORDINATION_UNAVAILABLE | TOOL_INITIALIZATION_FAILED`. A failure before a complete snapshot exists may not fabricate that no-op variant and must emit `RELEASE_EXTERNAL_MUTATION_PRELEASE_UNCERTAIN_RESULT`. Every hosted no-op requires `effectLease=ABSENT`, proves zero provider mutation command/response/effect, and prohibits external-effect-decision fields. `RELEASE_EXTERNAL_MUTATION_PRELEASE_UNCERTAIN_RESULT` instead binds `EFFECT_START_STATE_UNAVAILABLE | ZERO_PROVIDER_COMMAND_NOT_PROVED | RAW_TERMINAL_EVIDENCE_INCOMPLETE`, makes no absent-lease/no-effect claim, and may only enter protected reconciliation; `RELEASE_EXTERNAL_MUTATION_RESULT` requires the matching created claim, stage transfer, owner-authorization cell/effect lease, action-specific input and external-effect disposition; `RELEASE_PROVIDER_RECONCILIATION_RESULT` requires its separate read-only reconciliation authorization and query disposition. `CONTROL_JOB_RESULT` binds exactly one controller/policy-activation/capacity-renewal/rollover identity with no fictional claim or product job. External occupancy observations are categorically excluded from this union and follow `HostedExternalOccupancyObservationV1`; they have no V23 result/reconciliation effect. Every hosted-result variant carries its applicable projection/checkpoint/witness/active-entry/nonces; capacity lease/lane and fresh admission witness; policy-evidence/configuration/workflow epochs/tokens; run/run-attempt/job/matrix identities; execution mode and typed work identity; exact candidate/order/selector/fence when applicable; start/terminal event sequence and times; independent status/exit; stop/cancellation facts; and artifact/checksum digests. Reconciliation rejects any missing required or inapplicable populated variant field and classifies `APPLY_ONCE | DUPLICATE_SAME_RESULT | LOST_OR_EXPIRED | LATE_AFTER_SUPERSESSION | CONFLICT_QUARANTINED`; only `APPLY_ONCE` permits one idempotent CAS ledger append or queue/guard/rollover/release decision input. Product or protected-release preclaim failure, product pretransfer no-op, and a fully proved protected-release post-transfer/prelease no-op therefore all have valid terminal envelopes. `NOT_CREATED` is typed infrastructure; `OBSERVED_CONFLICT` quarantines the protected claim namespace and requires owner/security review. None can authorize an impermissible second stage or populate a fictional effect decision, and a lane releases only after its applicable apply-once state/reconciliation result plus API proof that no matching stage/job is `in_progress`. The product no-op follows the one bounded arbitration/retry path without inventing product execution. A lost result before any external-effect lease may use a fresh signed nonce only under its typed retry or read-only reconciliation law; a protected mutation whose effect lease exists can never use that rule and must follow the lost-post-effect UNKNOWN bridge below. A late result cannot mutate a superseded attempt; conflicting identity/status/artifacts quarantine both. Hostile tests include forged projections, wrong authority/workflow/policy/ref/head/fence/cell order, nonce replay, claim/product/control/release-job substitution, absent/conflicting claim, product and protected-release preclaim failure, protected-release prelease infrastructure/uncertainty/race/recovery, pretransfer early wake/transfer race/retry replay, checkpoint-tail deletion plus rewind, witness outage/rollback, uncertain three-plane push, stale sequence, duplicate callback, missing artifact, external-effect uncertainty, cancellation race, and crash before/after CAS. This is tooling transport, not an application backend.

P05-C02 hosted release mutation uses only `PROTECTED_RELEASE_PIPELINE`; it may never masquerade as `PRODUCT_PIPELINE` or an external-occupancy observation. `ProtectedReleaseStageKindV1` is closed to `RELEASE_CLAIM_GUARD | RELEASE_EXTERNAL_MUTATION | RELEASE_PROVIDER_RECONCILIATION`; the first two are sequential nonoverlapping stages of one mutation pipeline, while reconciliation is a separately reserved read-only pipeline. `ProtectedReleasePipelineV1` is closed to `CLAIM_STAGE_AUTHORIZED | CLAIM_STAGE_RUNNING | CLAIM_STAGE_TERMINAL | EXTERNAL_MUTATION_AUTHORIZED | EXTERNAL_MUTATION_RUNNING | ZERO_DISPATCH_CLOSURE_PENDING(window,closureToken,predecessorDigest) | RECONCILIATION_REQUIRED | TERMINAL`. Only an apply-once `RELEASE_CLAIM_GUARD_RESULT(CREATED_MATCHING)` may transfer `CLAIM_STAGE_TERMINAL → EXTERNAL_MUTATION_AUTHORIZED`; `NOT_CREATED | OBSERVED_CONFLICT` terminalize as described above and never create an effect lease. Transfer recovery is old/matching/divergent and preserves the same lane without overlap. `ProtectedReleaseActionKindV1` is closed to `SIGN_AND_UPLOAD | PROMOTE_TESTED_BUILD | SUBMIT_FOR_REVIEW | START_PHASED_RELEASE | PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL`. `ProtectedReleaseActionInputV1` is the disjoint union `LOCAL_ARTIFACT_INPUT(acceptedArchiveDigest,releaseSelectionGeneration,requiredMargin) | APP_STORE_BUILD_INPUT(providerAccountID,appID,bundleID,version,targetVersionID,immutableBuildID,buildProvenanceDigest,currentLinkedBuildID?,expectedVersionState,providerGeneration) | APP_STORE_RELEASE_INPUT(providerAccountID,appID,version,immutableReleaseID,phasedReleaseID,expectedProviderState,providerGeneration)`. SIGN_AND_UPLOAD alone uses LOCAL_ARTIFACT_INPUT and requires `ReleaseTransportUsabilityDispositionV1=CURRENT_WITH_MARGIN`; PROMOTE_TESTED_BUILD and SUBMIT_FOR_REVIEW use the current accepted APP_STORE_BUILD_INPUT; phased-release actions use APP_STORE_RELEASE_INPUT. Later actions must not rebuild or refresh merely because the already-uploaded local artifact expired. Exactly one input variant is populated and any action/input mismatch fails closed.

`ProtectedReleaseActionTransitionMatrixV1` freezes one canonical release ID, schema version, ordered row encoding, raw-provider-to-normalized-state map, action-specific preconditions, and successors against the pinned provider-adapter/schema version; any adapter/schema/status-map change invalidates the matrix and all unstarted row authorizations. SIGN_AND_UPLOAD requires the current accepted local artifact and no conflicting matching upload, and succeeds only with the provider-confirmed immutable build identity. PROMOTE_TESTED_BUILD means linking that exact processing-complete, eligible immutable build—backed by accepted TestFlight evidence—to the exact target App Store version; the target must accept linkage and have no conflicting linked build, and the successor is that same build linked. SUBMIT_FOR_REVIEW requires that same build already linked, complete submission metadata/compliance, and normalized `VERSION_READY_FOR_REVIEW`; the successor is normalized `REVIEW_SUBMISSION_REQUEST_ACCEPTED` for the same version/build. Those normalized states are adapter contracts, never claims that Apple exposes identically named enum values, and each binds the raw status/payload/schema evidence used to derive it. START_PHASED_RELEASE requires the exact existing phased-release resource/ID for the eligible approved release and `PhasedReleaseState.INACTIVE`, with successor ACTIVE; an absent resource is owner/provider setup or a separately authorized future action, never inferred creation. PAUSE_PHASED_RELEASE requires ACTIVE and succeeds only at PAUSED. RESUME_PHASED_RELEASE requires PAUSED and succeeds only at ACTIVE. COMPLETE_PHASED_RELEASE_TO_ALL requires ACTIVE or PAUSED and succeeds only at COMPLETE; there is no fictional permanent HALT action. A PAUSED→COMPLETE row is enabled only when the pinned adapter/provider proof supports one conditional mutation; otherwise the owner must authorize and reconcile RESUME, then separately authorize and reconcile COMPLETE, never hide two calls inside one action/lease. These phased-state values are the provider's closed `INACTIVE | ACTIVE | PAUSED | COMPLETE` set ([Apple `PhasedReleaseState`](https://developer.apple.com/documentation/appstoreconnectapi/phasedreleasestate)); Apple documents pause/resume and release-to-all behavior in [App Store Connect Help](https://developer.apple.com/help/app-store-connect/update-your-app/release-a-version-update-in-phases). Every authorization/effect/result binds the exact canonical matrix-row digest, predecessor state/generation, intended successor, provider schema version, raw/normalized state evidence, and current provider refetch. An unmatched transition fails before mutation. A provider-accepted asynchronous response is not success until reconciliation confirms the intended successor; uncertainty remains UNKNOWN and is never retried blindly.

`ProtectedReleaseAuthorizationV1` binds owner evidence, exact action/input variant, accepted candidate/chain, policy/evidence/configuration/enrollment tokens, protected environment, intended provider/account/application/deployment identity, idempotency key when supported, allowed external effect, secrets disposition, authorization generation, revocation token, and expiry. The pipeline/effect-start input is the closed `ProtectedReleaseMutationAuthorizationV1 = ORDINARY(ProtectedReleaseAuthorizationV1) | SAFETY_CONTAINMENT_PAUSE(P05C02SafetyPauseAuthorizationV1)`. Both variants carry the same action/input/provider/deployment/secret/idempotency/expiry/revocation fields and authorize one exact effect; ORDINARY additionally requires the current accepted P05/evidence chain and ordinary incident/stop absence, while SAFETY_CONTAINMENT_PAUSE replaces only those fields with the exact trigger, predecessor-closure receipt, historical launch/adoption chain, expected trigger-derived stop/incident/supersession inputs, and PAUSE-only admission mode defined below. No implicit conversion, partially populated hybrid, or safety variant for another action is legal. `ProtectedReleaseAuthorizationAuthorityCellV1 = ACTIVE(authorizationGeneration,token,expiry) | EFFECT_IN_PROGRESS(effectNonce,effectLeaseExpiry) | REVOKED(reason,token) | TERMINAL(decisionDigest,token)`. `ProtectedReleaseMutationGateV1 = OPEN(actionEpoch,actionKind,authoritySnapshotDigest) | EFFECT_IN_PROGRESS(actionEpoch,effectNonce,effectLeaseExpiry,authoritySnapshotDigest) | INVALIDATED(actionEpoch,reason) | TERMINAL(actionEpoch,decisionDigest)`. A hydrator/controller may create only immutable signed `ProtectedReleaseAdmissionIntentV1`; it never writes either canonical cell. The active state writer consumes that intent and performs one serializable admission transaction. For a non-control action it creates the matching `ACTIVE + OPEN` pair; for `PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL`, including safety PAUSE, the same transaction also CASes the exact `ReleaseOperationalControlAuthorityCellV1` from `IDLE(epoch=n)` to `AUTHORIZED(epoch=n)` or from a fully reconciled `TERMINAL | OWNER_PENDING(epoch=n)` to `AUTHORIZED(epoch=n+1)`. `ProtectedReleaseAdmissionEffectRecoveryReceiptV1 = BEFORE_ADMISSION | APPLIED_MATCHING_ADMISSION | DIVERGENT_OR_AMBIGUOUS` binds all two or three records, the intent, epochs, tokens, and owner evidence; the canonical store exposes all or none, a matching retry returns identical bytes, and any partial observation is corruption rather than a state to complete. Queue reservation or claim dispatch is prohibited until APPLIED_MATCHING_ADMISSION. The first generic action uses expected-absent generation/action epoch 0; after a fully reconciled terminal or invalidation, fresh authority, zero active/unknown effect, and current provider truth create only the next generation/epoch. Each epoch is retained and never reopened, deleted, or reused. The snapshot digest covers the selected closed authorization variant and every common/variant field; ordinary owner authorization/revocation/expiry, current P05 state and release-selection generation, evidence-chain/incident state, stop namespace, policy/configuration/enrollment/capacity generations, action input, provider identity, and the applicable build or release generation remain mandatory when applicable. Every repository-controlled invalidator must submit its exact change through the active state writer. Under `admissionMode=ORDINARY`, every matching revocation, stop, incident, P05/selection supersession, policy/enrollment/configuration change, or action-input replacement competes on and invalidates that action epoch. Under `admissionMode=SAFETY_CONTAINMENT_PAUSE`, the exact bound trigger source and stop token derived from it are expected inputs, while every different/new invalidator still competes and invalidates. While EFFECT_IN_PROGRESS the state writer records only the corresponding committed `PENDING_AFTER_COMMIT_POINT` source receipt and never claims it prevented the call. External provider transitions are protected with a provider-supported conditional generation/precondition and idempotency key; if the provider cannot enforce the precondition, the action is owner-gated or any uncertainty enters reconciliation rather than a blind retry. The documented GitHub administrator/configuration trust boundary remains explicit and every start still requires the fresh admission witness.

No zero-dispatch state terminalization begins from a still-dispatchable pipeline state. `ProtectedReleaseZeroDispatchClosureClaimV1` is the closed window union `PRECLAIM_BEFORE_DISPATCH | PRECLAIM_RESULT_TERMINAL | POSTCLAIM_PRETRANSFER | POSTTRANSFER_PREDISPATCH`. `ProtectedReleaseZeroDispatchClosureClaimRecoveryReceiptV1 = BEFORE_CLOSURE_CLAIM | APPLIED_MATCHING_CLOSURE_CLAIM | COMPETING_STAGE_WON | DIVERGENT_OR_AMBIGUOUS` binds the exact pipeline predecessor, action/control epochs, reason/invalidation token, authoritative queued/running-job scan, and fresh closure token. The controller first CASes only the exact legal predecessor—respectively `CLAIM_STAGE_AUTHORIZED`, nontransferable `CLAIM_STAGE_TERMINAL`, transferable `CLAIM_STAGE_TERMINAL`, or `EXTERNAL_MUTATION_AUTHORIZED`—to `ZERO_DISPATCH_CLOSURE_PENDING(window,closureToken,predecessorDigest)` while retaining the reservation/lane. Claim dispatch, transfer, mutation dispatch, and closure all CAS the same predecessor: if a stage wins, closure returns COMPETING_STAGE_WON and must route to the next legal window or hosted no-op/uncertainty path without terminalizing state under the losing window; if closure wins, that stage is forever forbidden. A crash after the closure claim exposes a durable nondispatchable state and resumes the matching state recovery; it never releases the lane.

`RELEASE_CLAIM_GUARD` has no candidate checkout or provider secret and attempts the protected, atomic `ProtectedReleaseRunClaimV1` creation. API/permission failure before creation emits `NOT_CREATED`; a mismatching existing claim emits `OBSERVED_CONFLICT`; both are valid claim-stage terminals with no effect lease, provider decision, or external mutation. Before that claim result or an expiry/cancellation/invalidation before dispatch may terminalize the pipeline, the matching PRECLAIM closure claim above must be APPLIED_MATCHING_CLOSURE_CLAIM. `ProtectedReleasePreclaimTerminalRecoveryReceiptV1 = BEFORE_STATE_EFFECT | APPLIED_MATCHING_NO_EFFECT_TERMINAL | MATCHING_INVALIDATION_TERMINAL | DIVERGENT_OR_AMBIGUOUS` then binds the exact closure claim, admission receipt, claim-stage result or authoritative zero-dispatch proof, authorization/gate/control epochs, and `CLAIM_NOT_CREATED | CLAIM_CONFLICT | AUTHORIZATION_EXPIRED_BEFORE_CLAIM | INVALIDATED_BEFORE_CLAIM | OWNER_CANCELLED_BEFORE_CLAIM`. From matching `ACTIVE + OPEN`, one state-writer transaction terminalizes both generic cells and, for a control action, its matching operational `AUTHORIZED` cell without effect credit. From compatible `ACTIVE | REVOKED` plus an already-won `INVALIDATED` gate, the transaction preserves the invalidation, terminalizes the applicable operational cell, and records the matching no-effect terminal; it never rewrites the winning invalidator. Any effect lease or competing stage rejects this path. `ProtectedReleasePreclaimQueueRecoveryReceiptV1 = BEFORE_QUEUE_TERMINAL | APPLIED_MATCHING_QUEUE_TERMINAL | DIVERGENT_OR_AMBIGUOUS` then lets only the controller CAS the exact matching ZERO_DISPATCH_CLOSURE_PENDING state to TERMINAL and release its lane after the state receipt and a fresh authoritative proof that no matching job is queued or in progress. An owner/security claim conflict remains quarantined after closure and cannot silently retry; another attempt requires explicit resolution, a fresh retained epoch/intent/authorization/claim nonce, and no active or unknown effect. These receipts therefore close expiry, cancellation, or invalidation after admission but before dispatch without a stale claim job escaping between state and queue effects.

An apply-once `CREATED_MATCHING` claim does not force an invalidated action to transfer or dispatch. If expiry, owner cancellation, or invalidation wins after claim reconciliation but before durable stage transfer, the controller must first win the POSTCLAIM_PRETRANSFER closure claim against the same `CLAIM_STAGE_TERMINAL` predecessor used by transfer. `ProtectedReleasePostclaimPretransferTerminalRecoveryReceiptV1 = BEFORE_STATE_EFFECT | APPLIED_MATCHING_NO_EFFECT_TERMINAL | MATCHING_INVALIDATION_TERMINAL | DIVERGENT_OR_AMBIGUOUS` then binds that closure claim, immutable claim/result, exact admission and action/control epochs, authoritative `stageTransfer=ABSENT`, zero mutation-stage dispatch/job, absent lease, and `AUTHORIZATION_EXPIRED_AFTER_CLAIM | INVALIDATED_AFTER_CLAIM | OWNER_CANCELLED_AFTER_CLAIM`. Its one state-writer transaction closes `ACTIVE + OPEN` or preserves compatible `ACTIVE | REVOKED + INVALIDATED`, terminalizes the applicable operational AUTHORIZED cell, and records no-effect truth without deleting or reusing the claim. `ProtectedReleasePostclaimPretransferQueueRecoveryReceiptV1 = BEFORE_QUEUE_TERMINAL | APPLIED_MATCHING_QUEUE_TERMINAL | DIVERGENT_OR_AMBIGUOUS` then lets only the controller CAS the exact matching ZERO_DISPATCH_CLOSURE_PENDING state to TERMINAL and release the lane after the state receipt plus a fresh authoritative proof that no matching external-mutation job is queued or in progress. Transfer and closure claim are decided by one expected-state CAS: transfer wins and closure may not terminalize under this window; closure wins and transfer is forever forbidden. Any later attempt uses a fresh retained action epoch, pipeline, and claim nonce.

If durable stage transfer wins but expiry, owner cancellation, or invalidation arrives before the mutation-stage job is dispatched, the controller must first win the POSTTRANSFER_PREDISPATCH closure claim against the same `EXTERNAL_MUTATION_AUTHORIZED` predecessor used by dispatch. `ProtectedReleasePosttransferPredispatchTerminalRecoveryReceiptV1 = BEFORE_STATE_EFFECT | APPLIED_MATCHING_NO_EFFECT_TERMINAL | MATCHING_INVALIDATION_TERMINAL | DIVERGENT_OR_AMBIGUOUS` then binds that closure claim, immutable claim/transfer, exact admission and action/control epochs, authoritative zero mutation-stage dispatch/run/job, absent lease, and the matching after-transfer/before-dispatch reason. Its state-writer transaction closes or preserves the generic invalidation tuple and terminalizes the applicable operational AUTHORIZED cell with zero effect. `ProtectedReleasePosttransferPredispatchQueueRecoveryReceiptV1 = BEFORE_QUEUE_TERMINAL | APPLIED_MATCHING_QUEUE_TERMINAL | DIVERGENT_OR_AMBIGUOUS` lets only the controller CAS the exact matching ZERO_DISPATCH_CLOSURE_PENDING state to TERMINAL and release the lane after that state receipt and a fresh zero-queued/zero-running-job scan. Dispatch and closure claim race on the same pipeline CAS: dispatch wins into EXTERNAL_MUTATION_RUNNING and closure may not terminalize under this window, while closure wins and dispatch is forever forbidden. No branch invents a runner result.

`RELEASE_EXTERNAL_MUTATION` starts only after apply-once `CREATED_MATCHING` reconciliation and controller stage transfer, retains the same lane without overlap, and receives the minimum protected credential through GitHub's secret/environment boundary. Immediately before the first provider command, it refetches the current authorization cell, mutation gate, owner evidence/revocation, expiry, projection/checkpoint/witness, capacity/configuration/policy/enrollment tokens, stop and evidence-incident state, action-specific input, P05/chain authority, selection/margin only for LOCAL_ARTIFACT_INPUT, immutable build identity/status only for APP_STORE_BUILD_INPUT, and exact release/phased-release identity plus expected provider state/generation only for APP_STORE_RELEASE_INPUT. One serializable effect-start transaction proves that complete refetched snapshot, expected generations, and provider precondition still match, then CASes both the unexpired authorization cell and OPEN mutation gate—and the exact operational `AUTHORIZED` cell when applicable—to the same `EFFECT_IN_PROGRESS` nonce and emits `ProtectedReleaseExternalEffectLeaseV1`; the admission-mode rules above decide which invalidators compete and which exact safety-trigger inputs remain expected. The lease has a short predeclared deadline/margin covering one exact provider call. An invalidation that wins first yields no mutation; a request observed after the effect lease wins is recorded as its durable `PENDING_AFTER_COMMIT_POINT` source and cannot be falsely claimed to have prevented the already-committed call. A matching standalone stop or other explicitly containment-worthy release-safety incident additionally creates `ProtectedReleasePostCommitSafetyInvalidatorReceiptV1(kind=STANDALONE_STOP | RELEASE_SAFETY_INCIDENT)`, binding the authoritative request/incident receipt, token, action/control epoch, effect lease, release identity, and chronology. Missing margin where applicable, stale provider state/generation, stale token, or an invalidator not expected by the selected admission mode exits without provider mutation through `RELEASE_EXTERNAL_MUTATION_PRELEASE_NOOP_RESULT`, never through the lease-backed result. Credentials never enter repository refs, projections, logs, artifacts, or the app.

For `admissionMode=SAFETY_CONTAINMENT_PAUSE`, that same serializable effect-start transaction additionally CASes the exact current frontier generation from ACTIVE_CONFIRMED to PAUSE_EFFECT_IN_PROGRESS with the same effect-lease digest and CASes the per-release `ReleaseSafetyPauseSourceSequenceCellV1` from OPEN to PAUSE_FENCED. It may win only when `committedSourceHighWater == currentFrontier.mergedThroughSourceSequence`; it records that common value as `leaseSourceHighWater`. The authorization/gate/control/lease/frontier/source-sequence tuple is all-or-none. A source commit that wins first raises the shared high-water and forces the exact zero-effect `SAFETY_SOURCE_HIGH_WATER_ADVANCED` prelease no-op; a recovered source-frontier advance that wins first changes the expected current pointer and likewise forces a typed stale-authorization no-op. Neither branch writes a lease or provider command. Once PAUSE_EFFECT_IN_PROGRESS and PAUSE_FENCED win, later sources still atomically commit their source/PENDING truth with sequences above `leaseSourceHighWater`, but cannot advance the current frontier or rebind the in-flight authorization until the matching PAUSE effect is conclusively recovered.

The active state writer independently verifies and apply-once reconciles conclusive prelease no-effect proof before any queue release. `ProtectedReleaseNoEffectProofSourceV1 = HOSTED_CAPTURED_SNAPSHOT(noopEnvelopeDigest,snapshotDigest) | RECONCILIATION_DERIVED(uncertainReconciliationDigest,derivedSnapshotDigest,derivedNoopReason)` is disjoint: the first requires the runner's complete last pre-effect snapshot; the second exists only after the uncertainty receipt independently proves absent lease, zero provider command/effect, and a canonical snapshot derived from the exact persisted admission/gate/control records plus current provider precondition. `ProtectedReleasePreEffectNoopStateEffectRecoveryReceiptV1 = BEFORE_STATE_EFFECT | APPLIED_MATCHING_STATE_TERMINAL | MATCHING_INVALIDATION_TERMINAL | DIVERGENT_OR_AMBIGUOUS` binds that source, action epoch, claim and stage-transfer digests, closed no-op reason, snapshot, absent effect lease, zero provider mutation evidence, prior authorization-cell/mutation-gate states, ledger append, and resulting states. It also binds `operationalControlCellDisposition=NOT_APPLICABLE | AUTHORIZED_TO_TERMINAL_MATCHING | ALREADY_TERMINAL_MATCHING`; PAUSE, RESUME, COMPLETE, and safety-PAUSE attempts must use one of the latter two against their exact `ReleaseOperationalControlAuthorityCellV1` epoch, while non-control release actions must use NOT_APPLICABLE. From matching `ACTIVE + OPEN`, one serializable canonical-store transaction CASes the generic authorization and mutation-gate records to their matching no-effect `TERMINAL` values, CASes a matching operational cell from `AUTHORIZED` to `TERMINAL(noopDecisionDigest)` when applicable, and appends the release-ledger terminal. From a matching already-won `INVALIDATED` gate and compatible `ACTIVE | REVOKED` authority state, the same all-or-none transaction preserves the invalidation, terminalizes the exact applicable AUTHORIZED operational cell, and records the matching no-effect terminal fact. A crash exposes either the entire predecessor tuple or the entire matching successor tuple; a partial tuple is corruption and quarantines rather than a legal recovery state. Any effect lease, operational cell left AUTHORIZED/EFFECT_IN_PROGRESS after a claimed applied transaction, provider mutation command/response, unknown effect, mismatched reason/source/snapshot/control epoch, or divergent state quarantines the action. The acceptance controller then consumes only the durable matching state-effect recovery—including `ALREADY_TERMINAL_MATCHING` for every applicable operational cell—as a terminal queue input. `ProtectedReleasePreEffectNoopQueueRecoveryReceiptV1 = BEFORE_QUEUE_TERMINAL | APPLIED_MATCHING_QUEUE_TERMINAL | DIVERGENT_OR_AMBIGUOUS` CASes the exact `EXTERNAL_MUTATION_AUTHORIZED | EXTERNAL_MUTATION_RUNNING` pipeline to `TERMINAL` and releases its reservation/lane only after authoritative API proof that its matching job is terminal and none is `in_progress`; a crash before either owner's effect resumes only that owner's identical CAS. Neither receipt grants retry, release, product-acceptance, or provider-effect credit. Crash/race tests cover every invalidator immediately before refetch, between refetch and effect-start, a no-op racing a winning invalidation, normal and safety operational-control no-op terminalization, crash before/after the atomic state transaction, crash between complete state and queue terminalization, and the existing after-lease/provider-response cases.

A `RELEASE_EXTERNAL_MUTATION_PRELEASE_UNCERTAIN_RESULT` is never accepted as a no-op or mutation result. `ProtectedReleasePreEffectUncertainReconciliationReceiptV1 = LEASE_ABSENT_ZERO_COMMAND_CONFIRMED | LEASE_PRESENT_ROUTE_TO_POST_EFFECT_RECOVERY | PROVIDER_UNCHANGED_NO_EFFECT_CONFIRMED | REMAINS_UNKNOWN_OWNER_EVIDENCE_REQUIRED | DIVERGENT_OR_AMBIGUOUS` binds the exact action/control epoch, job terminal, raw-evidence gap, independently refetched authorization/gate/lease truth, action-specific provider precondition/successor query, and claim/stage-transfer identity. LEASE_ABSENT or PROVIDER_UNCHANGED may feed the ordinary prelease-noop state recovery only by emitting the `ProtectedReleaseNoEffectProofSourceV1.RECONCILIATION_DERIVED` member with its canonical derived snapshot and exact no-op reason; it never rewrites the hosted result or pretends the runner captured a snapshot. Those conclusive no-effect branches must keep the original pipeline in `EXTERNAL_MUTATION_AUTHORIZED | EXTERNAL_MUTATION_RUNNING` until `ProtectedReleasePreEffectNoopStateEffectRecoveryReceiptV1` and then `ProtectedReleasePreEffectNoopQueueRecoveryReceiptV1` terminalize it; they may never first move it to RECONCILIATION_REQUIRED or release its lane. Only LEASE_PRESENT_ROUTE_TO_POST_EFFECT_RECOVERY or REMAINS_UNKNOWN_OWNER_EVIDENCE_REQUIRED may move the original pipeline to `RECONCILIATION_REQUIRED` and release its old lane after this receipt plus authoritative terminal API proof; the separately authorized read-only reconciliation consumes a new lane. No uncertainty branch grants mutation retry.

Missing mutation output after a durable effect-start lease uses a distinct bridge, never the generic fresh-nonce retry. `ProtectedReleaseLostPostEffectStartRecoveryReceiptV1 = BEFORE_UNKNOWN_BRIDGE | APPLIED_MATCHING_UNKNOWN_BRIDGE | DIVERGENT_OR_AMBIGUOUS` requires the matching immutable effect lease and action/control epoch, authoritative terminal run/job identity, exhaustive expected artifact lookup with absent/invalid result evidence, zero previously accepted mutation result, idempotency/provider identifiers, and current authorization/gate states. The active state writer apply-once emits `ProtectedReleaseExternalEffectDecisionV1(UNKNOWN_RECONCILIATION_REQUIRED)` and the read-only reconciliation authority input without claiming success/failure; the controller then moves the exact original pipeline to `RECONCILIATION_REQUIRED` and releases its lane only after terminal API proof. The generic “fresh signed nonce” rule applies only to a new read-only reconciliation reservation or a pre-effect attempt after conclusive NOT_APPLIED authority; it can never redispatch the mutation whose lease exists. A torn bridge recovers old/matching/divergent, an absent lease cannot use this path, and an unknown provider outcome remains owner-pending rather than retried.

`ProtectedReleaseExternalEffectDecisionV1 = APPLIED_CONFIRMED | NOT_APPLIED_SAFE_TO_RETRY | UNKNOWN_RECONCILIATION_REQUIRED | INFRA_PENDING | PRODUCT_PROOF_CONTRADICTION | OWNER_REVOKED | SUPERSEDED`; an uncertain provider effect forbids every mutation retry. If provider truth needs authenticated access, the controller creates a separate capacity-counted `PROTECTED_RELEASE_RECONCILIATION` reservation using `ProtectedReleaseStageKindV1(RELEASE_PROVIDER_RECONCILIATION)`, `ProtectedReleaseReconciliationAuthorizationV1`, a read-only protected credential, fresh capacity witness, and no mutation permission. `ProtectedReleaseReconciliationReceiptV1 = APPLIED_CONFIRMED | NOT_APPLIED_SAFE_TO_RETRY | REMAINS_UNKNOWN_OWNER_EVIDENCE_REQUIRED | INFRA_PENDING | SUPERSEDED` binds the original effect/lease/idempotency key, exact provider query and immutable response evidence. If automation cannot access or conclusively interpret provider truth, `OwnerProviderReconciliationEvidenceV1` is the explicit owner/manual-only path and the tranche remains `OWNER_PENDING`; absence is a representable blocker, never permission to retry.

Recovery dispatch is closed by admission mode. For ORDINARY, the active state writer independently retrieves/reconciles the proof and alone emits/applies `ProtectedReleaseEffectRecoveryReceiptV1.ORDINARY` as one effect over the matching authorization cell, mutation-gate epoch, action/operational disposition, and ledger result. For SAFETY_CONTAINMENT_PAUSE, `ReleaseSafetyPauseFrontierEffectRecoveryReceiptV1` below is the sole state-writing recovery effect; it atomically writes the generic authorization/gate/operational/ledger terminalization, frontier/sequence-cell outcome, and the canonical `ProtectedReleaseEffectRecoveryReceiptV1.SAFETY_FRONTIER(frontierRecoveryDigest)` wrapper. The wrapper is not a second effect, and the ordinary recovery path must reject the safety admission mode. Both branches classify their whole owned tuple as old, partial-matching, complete-matching, or divergent, complete only an exact partial matching transaction, and never repeat an unknown non-idempotent action. An ordinary original mutation lane may release after terminal API reconciliation and a matching no-op, uncertainty, lost-post-effect, or mutation-result reconciliation input. A safety-PAUSE lane may terminalize and release after authoritative terminal API proof only when the matching frontier recovery and its SAFETY_FRONTIER wrapper are durable; REMAINS_UNKNOWN_EFFECT may move only to the separate governed read-only reconciliation lane while leaving the frontier PAUSE_EFFECT_IN_PROGRESS and sequence cell PAUSE_FENCED. These receipts can support P05-C02 evidence only after state-writer inclusion; they never create product acceptance by themselves.

`P05C02OperationalControlAttemptV1` makes post-start controls reachable without reopening or erasing the accepted release card. The state-writer-confirmed `START_PHASED_RELEASE` effect atomically creates `ReleaseOperationalControlAuthorityCellV1.IDLE(releaseIdentity,controlEpoch=0)` with expected-absent/matching/divergent recovery. If P05-C02 instead proves a pre-existing or owner-manually started matching phased release, `ExistingPhasedReleaseOperationalControlAdoptionReceiptV1` binds the accepted P05-C02 disposition, exact immutable release/phased-release identity, independently refetched provider `ACTIVE | PAUSED` state and generation, owner evidence, historical accepted candidate/evidence chain, and expected-absent control-cell key; one active-state-writer CAS creates the same epoch-0 IDLE cell, returns the byte-identical existing cell on a matching retry, or quarantines divergent identity/state. Adoption grants no START-effect, product-acceptance, or provider-mutation credit. P05-C03 activation requires one of these two exact cell-genesis proofs, so no live monitored phased release lacks governed controls.

The cell is closed to `IDLE(releaseIdentity,controlEpoch) | AUTHORIZED(controlEpoch,action,ownerEvidenceDigest,inputDigest,matrixRowDigest,expiry) | EFFECT_IN_PROGRESS(controlEpoch,effectNonce) | TERMINAL(controlEpoch,decisionDigest) | OWNER_PENDING(controlEpoch,reason)` and may admit one monotonic `PAUSE_PHASED_RELEASE | RESUME_PHASED_RELEASE | COMPLETE_PHASED_RELEASE_TO_ALL` attempt from an exact owner request or a P05-C03 observation escalated to owner authorization. Only the active state writer may perform `IDLE(epoch=0) → AUTHORIZED(epoch=0)` or fully reconciled `TERMINAL | OWNER_PENDING(epoch=n) → AUTHORIZED(epoch=n+1)`, and it does so only inside the `ProtectedReleaseAdmissionIntentV1` two-or-three-record transaction and recovery defined above, with zero active/unknown effect, fresh owner evidence, a matching transition-matrix row, and current release/provider truth. Old epochs are immutable; queue admission sees only APPLIED_MATCHING_ADMISSION. The exact pinned protected runner may then write only the matching AUTHORIZED→EFFECT_IN_PROGRESS transition inside the same serializable authorization/mutation-gate effect-start transaction. Under ordinary authority this auxiliary control plane is independent of the original P05-C02 workflow state and remains reachable throughout any non-superseded live or accepted state—including IMPLEMENTING, TARGETED_GREEN, WAITING_ACCEPTANCE_SET, ACCEPTANCE_RUNNING, ACCEPTED, HANDOFF_PENDING, and COMPLETE—without mutating that card state. It therefore never performs an illegal `COMPLETE → READY`, changes the historical card receipt, manufactures release acceptance, or rebuilds an uploaded artifact. Ordinary PAUSE, RESUME, or COMPLETE requires current `APP_STORE_RELEASE_INPUT`, exact provider generation/state, current accepted P05/evidence chain, fresh owner evidence, and the same protected claim, mutation-gate, unknown-effect reconciliation, capacity, secret, and state-writer laws.

Safety containment is the sole exception to the ordinary current-chain and incident/stop-absence predicates, never to effect identity, serialization, or unknown-effect safety. Every potential source has a predeclared immutable source-effect intent digest and effect ID. `ReleaseActiveSafetyStopReceiptV1` is the disjoint established-release source: the active state writer binds an authoritative standalone stop request/token, exact START genesis or existing-release adoption, current control epoch, independent provider ACTIVE proof/generation, and zero active/unknown release-control effect; if an effect lease or possible effect exists, this source is forbidden and the post-commit source applies. `ReleaseSafetyPauseTriggerSourceV1` is the closed union `P05_OR_PRODUCT_SUPERSESSION(sourceIntentDigest,effectID,ReleaseTransportSupersessionEffectRecoveryReceiptV1) | AUTHORITY_ONLY_EVIDENCE_INCIDENT(sourceIntentDigest,effectID,AuthorityOnlyEvidenceIncidentEffectRecoveryReceiptV1) | ESTABLISHED_ACTIVE_STOP(sourceIntentDigest,effectID,ReleaseActiveSafetyStopReceiptV1) | POST_COMMIT_SAFETY_INVALIDATOR(sourceIntentDigest,effectID,ProtectedReleasePostCommitSafetyInvalidatorReceiptV1)`. Each member becomes valid only after the active state writer proves its canonical source tuple and matching recovery committed. The original effect and PENDING evaluation name only the predeclared intent/effect ID—never a not-yet-produced receipt or its digest—and the later recovery receipt binds/verifies the complete tuple. A pending request, an uncommitted or unrecovered invalidator, or provider state alone is never a trigger source.

`ReleaseSafetyPauseSourceSequenceCellV1 = OPEN(committedSourceHighWater) | PAUSE_FENCED(frontierGeneration,effectLeaseDigest,leaseSourceHighWater,currentCommittedSourceHighWater)`, with genesis `OPEN(0)`, is the sole per-release sequence allocator and the common serialization cell for source commits and PAUSE effect-start. Each source state effect uses one all-or-none canonical-store transaction to CAS that cell, assign exactly the prior high-water plus one as `sourceSequence`, persist the immutable source outbox, and persist exact `ReleaseSafetyPauseTriggerEvaluationV1.PENDING(sourceEffectID,releaseIdentity,sourceSequence)`. Under PAUSE_FENCED it increments only `currentCommittedSourceHighWater`, preserves `leaseSourceHighWater` and the fence identity, and thereby proves the source is in the post-lease interval. The immutable source outbox binds its sequence-cell predecessor and successor plus the source/PENDING record digests. Later genesis recovery accepts only that exact atomic transition and a current cell that is its same successor or a contiguous monotonic descendant; it never expects current bytes to equal an old cell version and never rewinds the allocator. Source and PENDING bytes name only the predeclared intent/effect ID and stable source sequence; they explicitly exclude the later source-recovery, frontier, terminal-evaluation, and trigger digests. `ReleaseSafetyPauseSourceEvaluationGenesisRecoveryReceiptV1 = BEFORE_SOURCE_EFFECT | APPLIED_MATCHING_SOURCE_AND_PENDING | DIVERGENT_OR_AMBIGUOUS` classifies the whole source-plus-PENDING-plus-sequence-cell transition; a partial tuple, sequence gap, duplicate sequence, or non-descendant current cell is corruption and quarantines rather than a legal recovery state. Source truth may therefore commit even if provider refetch or the control plane is unavailable, but cannot merge until its matching source-effect recovery and compound genesis recovery are durable.

`ReleaseSafetyPauseTriggerEvaluationV1` is closed to `PENDING(sourceEffectID,releaseIdentity,sourceSequence) | MERGED_INTO_FRONTIER(frontierGeneration,orderedOpenSourceSetDigest) | DEFERRED_BEHIND_PAUSE_EFFECT(frontierGeneration,effectLeaseDigest) | UNKNOWN_RECONCILIATION_REQUIRED(frontierGeneration,reconciliationAuthorizationDigest) | SUBSUMED_BY_NEWER_FRONTIER(frontierGeneration) | CONFIRMED_NONACTIVE(frontierGeneration,providerState,providerGeneration,zeroPossibleActivationProofDigest)`. The frontier plane is an append-only set of generation records plus one generation-CAS current pointer. Each record uses `ReleaseSafetyPauseTriggerFrontierV1 = EMPTY(frontierGeneration) | EVALUATING(frontierGeneration,orderedOpenSourceSetDigest,mergedThroughSourceSequence,pendingCommittedSourceInterval) | ACTIVE_CONFIRMED(frontierGeneration,orderedOpenSourceSetDigest,providerProofDigest,providerGeneration) | PAUSE_EFFECT_IN_PROGRESS(frontierGeneration,orderedOpenSourceSetDigest,effectLeaseDigest,providerGeneration) | CONFIRMED_NONACTIVE(frontierGeneration,orderedSourceSetDigest,providerState,providerGeneration,zeroPossibleActivationProofDigest) | CONTAINED(frontierGeneration,orderedSourceSetDigest,containmentDisposition,containmentProofDigest,providerState,providerGeneration) | SUPERSEDED_TO_SUCCESSOR(frontierGeneration,successorGeneration,recoveryDigest)`, where `pendingCommittedSourceInterval = EMPTY | RANGE(lowerExclusive,upperInclusive)`. Every frontier record and the current pointer bind `mergedThroughSourceSequence`; that value is a gap-free prefix and can advance from N only after every committed source through N has matching source-effect and compound genesis recovery and a terminal or cumulative-open-set disposition. A pending interval records custody of atomically committed source/PENDING records that have not yet joined that validated prefix; it grants no trigger-source validity, open-set membership, or PAUSE authority. Outside PAUSE_EFFECT_IN_PROGRESS, the active state writer processes every next sequence in order: after its matching recoveries are durable it CASes a successor frontier with the resulting cumulative ordered open-source set, advances merged-through, and shrinks the pending prefix; a missing recovery leaves EVALUATING and a divergent recovery quarantines without dropping the interval. A later ordinary source above the recorded interval remains discoverable from OPEN(committedSourceHighWater) and extends the same ordered work. The writer never skips, replaces, or forgets an unresolved source. While the current generation is PAUSE_EFFECT_IN_PROGRESS, every source with `sourceSequence > leaseSourceHighWater` remains durable PENDING/DEFERRED behind that exact lease and cannot advance the pointer. CONTAINED is terminal for that exact generation and cannot authorize another PAUSE.

`ReleaseSafetyPauseTriggerEvaluationRecoveryReceiptV1 = BEFORE_EVALUATION_EFFECT | MERGED_MATCHING_FRONTIER | APPLIED_MATCHING_PENDING_OR_UNKNOWN | ACTIVE_CONFIRMED_TRIGGER_EMITTED | APPLIED_MATCHING_NONACTIVE_TERMINAL | SUBSUMED_BY_NEWER_FRONTIER | DIVERGENT_OR_AMBIGUOUS` verifies matching source-effect recovery and compound source/PENDING genesis, then drives read-only provider reconciliation only for the current frontier. Recovery for an older generation returns SUBSUMED_BY_NEWER_FRONTIER and cannot touch owner/control state. ACTIVE first persists ACTIVE_CONFIRMED using provider-proof digest only; the state writer then emits `ReleaseSafetyPauseTriggerV1` bound to the immutable frontier identity and completed ACTIVE_CONFIRMED frontier digest, so neither object contains the other's digest and no successor digest appears in predecessor bytes. COMPLETE may close immediately as CONFIRMED_NONACTIVE. PAUSED or INACTIVE may close only with proof that no matching START/RESUME lease, provider request, queued dispatch, or unknown effect can later reach ACTIVE; otherwise they remain EVALUATING/UNKNOWN. Unavailable/ambiguous truth or any possible activation stays unresolved and is retried; a later reconciled START/RESUME successor feeds the current frontier rather than creating a competing trigger. Every trigger, safety owner-pending effect, and `P05C02SafetyPauseAuthorizationV1` binds the current frontier generation and ordered source-set digest. Before a PAUSE effect-start fence, a newly committed source immediately raises the shared source high-water and defeats the stale authorization; only after both source recoveries are durable may it advance the frontier. After PAUSE_EFFECT_IN_PROGRESS, it is deferred behind that lease and only the frontier-effect recovery can open the next generation. `ReleaseSafetyPauseAuthorityEffectRecoveryReceiptV1 = BEFORE_TRIGGER_EFFECT | APPLIED_MATCHING_OWNER_PENDING | AUTHORIZED_PREDECESSOR_CLOSURE_REQUIRED | OWNER_PENDING_PREDECESSOR_SUPERSEDED | EFFECT_ALREADY_IN_PROGRESS_RECONCILIATION_REQUIRED | PROVIDER_ALREADY_PAUSED_OR_COMPLETE | DIVERGENT_OR_AMBIGUOUS` consumes only the current ACTIVE_CONFIRMED frontier plus its canonical trigger and never guesses or overwrites a live predecessor.

`ReleaseSafetyPauseFrontierEffectRecoveryReceiptV1 = BEFORE_FRONTIER_EFFECT_START | EFFECT_START_APPLIED_MATCHING | APPLIED_MATCHING_CONTAINED | NOT_APPLIED_SUCCESSOR_OPENED | REMAINS_UNKNOWN_EFFECT | DIVERGENT_OR_AMBIGUOUS` closes the PAUSE-effect race and is the only apply-once state effect for a safety-PAUSE result. It binds the exact safety authorization, trigger/frontier generation and source-set digest, effect lease/idempotency identity, generic and operational control epochs, provider reconciliation, `leaseSourceHighWater`, `currentCommittedSourceHighWater`, every exact atomically committed source/PENDING record in `(leaseSourceHighWater,currentCommittedSourceHighWater]`, and current-pointer predecessor. It does not require those deferred sources' semantic recoveries to be complete. UNKNOWN leaves the same generation PAUSE_EFFECT_IN_PROGRESS, the sequence cell PAUSE_FENCED, and all newer sources deferred.

Conclusive PAUSED or COMPLETE truth atomically terminalizes the generic authorization, mutation gate, operational control, and ledger effect; writes the matching `ProtectedReleaseEffectRecoveryReceiptV1.SAFETY_FRONTIER` wrapper; and transitions the authorized generation to CONTAINED with `PAUSE_APPLIED | ALREADY_PAUSED | RELEASE_COMPLETE`. If the exact deferred interval is nonempty, the same transaction always creates one successor `EVALUATING` record with an empty validated open set, `mergedThroughSourceSequence=leaseSourceHighWater`, and `pendingCommittedSourceInterval=RANGE(leaseSourceHighWater,currentCommittedSourceHighWater)`; it never waits for or fabricates source recovery. If the interval is empty, no containment successor is needed. Conclusive NOT_APPLIED while provider is ACTIVE performs the same single owned-tuple terminalization and wrapper write, marks the old generation SUPERSEDED_TO_SUCCESSOR, and always creates one successor EVALUATING record whose validated open set is exactly the original unresolved set, whose merged-through remains `leaseSourceHighWater`, and whose pending interval is that exact deferred range or EMPTY. Either conclusive branch advances the current pointer to the required successor when one exists and returns the sequence cell to OPEN at the same `currentCommittedSourceHighWater`; the ordinary ordered merger later validates and consumes the pending interval one source at a time. NOT_APPLIED requires fresh provider reconciliation, trigger, predecessor closure, and owner authorization before another PAUSE.

The recovery transaction CASes the still-matching PAUSE_FENCED cell, so a racing source either enters its exact deferred interval or commits after reopening and joins the ordinary pending suffix. Any nonempty gap between OPEN(currentCommittedSourceHighWater) and the successor's merged-through blocks a later PAUSE effect-start until every intervening source is recovered and merged or quarantined; quarantine remains release-blocking. The all-or-none state-writer transaction and matching recovery never permit the ordinary effect-recovery writer to run, never bind the old PAUSE decision to the successor set, never treat a pending deferred record as a validated trigger source, never drop a committed source, never treat a pre-lease but unmerged source as post-lease, and never permit RESUME/COMPLETE while a source or external effect is unresolved.

`ReleaseSafetyPausePredecessorClosureReceiptV1 = NO_PRIOR_ACTIVE_ATTEMPT | AUTHORIZED_NOT_DISPATCHED_TERMINALIZED | PRELEASE_NOOP_FULLY_RECONCILED | POSTLEASE_EFFECT_FULLY_RECONCILED | OWNER_PENDING_PREDECESSOR_SUPERSEDED | DIVERGENT_OR_AMBIGUOUS` binds the prior control epoch, generic authorization/gate, operational cell, queue/reservation, run/job, lease/command/effect truth, and every terminal recovery. If the prior cell is `AUTHORIZED` with no dispatched/in-progress stage, the controller must first obtain `ProtectedReleaseZeroDispatchClosureClaimRecoveryReceiptV1.APPLIED_MATCHING_CLOSURE_CLAIM` for the exact preclaim, postclaim/pretransfer, or posttransfer/predispatch predecessor and retain its lane. Only then may the active state writer apply-once terminalize the exact generic action/gate and operational cell with zero-effect evidence through the matching named state recovery; only after that durable state recovery and a fresh authoritative zero-queued/zero-running scan may the controller terminalize the closure-pending pipeline and release the lane through the matching named queue recovery. If a stage began without a lease, the full prelease-noop state and queue recoveries—including operational-cell terminalization—must finish in their defined state-before-queue order. If a lease or effect may exist, unknown-effect reconciliation must finish before safety authorization. Epoch movement is closed: `IDLE(n) → OWNER_PENDING(n,SAFETY_PAUSE_REQUIRED,triggerDigest)` uses the same unused epoch; `TERMINAL(n) → OWNER_PENDING(n+1,...)` retains immutable terminal n; and unrelated `OWNER_PENDING(n,otherReason)` is atomically retained as superseded while creating `OWNER_PENDING(n+1,...)`, producing OWNER_PENDING_PREDECESSOR_SUPERSEDED. A byte-identical retry against the exact safety-pending trigger returns the existing bytes and never increments again. AUTHORIZED/EFFECT_IN_PROGRESS first follow their named closure to immutable TERMINAL(n), then use n+1. Only NO_PRIOR_ACTIVE_ATTEMPT or a fully closed predecessor lets trigger recovery create that exact target. Crashes before the closure claim leave the predecessor dispatchable and unchanged; crashes after it leave a nondispatchable retained-lane predecessor; crashes after state recovery resume only final queue recovery; and divergent epochs/tokens quarantine without an orphan lane.

Fresh explicit owner evidence may then create `P05C02SafetyPauseAuthorizationV1` for `PAUSE_PHASED_RELEASE` only, binding the current ACTIVE_CONFIRMED evaluation/frontier, canonical trigger, cumulative ordered source-set digest, predecessor-closure receipt, exact release/provider generation and ACTIVE state, historical launch/adoption genesis, and a current zero-active/zero-unknown-effect proof. Its immutable `admissionMode=SAFETY_CONTAINMENT_PAUSE` makes the exact frontier/source set and any stop token causally derived from it required expected inputs—not `STOP_OR_INCIDENT_ACTIVE` or `AUTHORITY_INPUT_OR_POLICY_CHANGED` no-op causes. The authorization snapshot, canonical PAUSE matrix-row input, mutation-gate digest, precommand refetch, and effect-start lease all bind that admission mode, trigger, and frontier generation. Any different or newer stop, incident, supersession, release/provider generation, policy/configuration/enrollment token, input, owner revocation, or authority change still loses the effect-start race and emits the typed prelease no-op; a qualifying source that commits after effect-start remains durably deferred behind that lease and is opened only by the frontier-effect recovery above. It does not require the now-superseded product chain to become current and cannot authorize RESUME, COMPLETE, a new release identity, or any product/release credit. The same protected claim, canonical PAUSE matrix row, serialized mutation gate, prelease-noop terminal, unknown-effect reconciliation, capacity, secret, and state-writer laws otherwise apply. Throughout this package, an incident that “blocks release/evidence work” blocks ordinary release/evidence work but explicitly permits only this trigger-bound, fresh-owner safety-containment PAUSE pipeline after predecessor closure; it never permits RESUME, COMPLETE, or unrelated mutation. Until the incident is closed and a fresh accepted P05 chain exists, RESUME and COMPLETE remain blocked; a confirmed safety PAUSE merely records truthful containment. P05-C03 remains observation-only: it may publish typed escalation evidence but cannot authorize or execute control. Every terminal control attempt updates only the operational-control ledger and truthful release disposition; a later ordinary control requires the next epoch and fresh authority.

Every `ProtectedReleaseEffectRecoveryReceiptV1` for `START_PHASED_RELEASE` or `RESUME_PHASED_RELEASE` must run the mandatory post-effect safety-trigger check before its action/control epoch is considered fully reconciled. `ReleaseSafetyPausePostEffectTriggerRecoveryReceiptV1 = SUCCESSOR_NOT_ACTIVE | ACTIVE_WITH_NO_QUALIFYING_PENDING_SOURCE | ACTIVE_TRIGGER_APPLIED_MATCHING | DIVERGENT_OR_AMBIGUOUS` binds the recovered effect/lease, exact release identity, provider successor/generation, operational-control result, and every `PENDING_AFTER_COMMIT_POINT` standalone-stop, release-safety-incident, authority-only-evidence-incident, or P05/product-supersession record. The state writer must first apply or recover each invalidator's canonical committed source, merge it into the current `ReleaseSafetyPauseTriggerFrontierV1`, and reconcile that frontier; a pending request alone can never synthesize authority. If the reconciled successor is ACTIVE, the current frontier becomes ACTIVE_CONFIRMED, emits or returns its noncircular canonical trigger, and applies predecessor-closure/owner-pending recovery. A standalone post-commit stop uses `ProtectedReleasePostCommitSafetyInvalidatorReceiptV1`; evidence incidents and P05/product supersessions use their exact committed effect-recovery receipts. SUCCESSOR_NOT_ACTIVE proves terminal nonactive provider truth plus zero possible later activation; ACTIVE_WITH_NO_QUALIFYING_PENDING_SOURCE proves the complete negative source set. A crash after source commit, frontier merge, ACTIVE confirmation, or trigger publication resumes only that current generation; older recovery returns SUBSUMED_BY_NEWER_FRONTIER. No START/RESUME success may be marked fully reconciled, release a qualifying pending source, or enable another ordinary control until this receipt is durable. This closes the race where effect-start wins while the provider is still INACTIVE/PAUSED and the invalidator arrives before the provider later becomes ACTIVE.

Closed states:

- `NOT_STARTED`
- `HYDRATING`
- `READY`
- `IMPLEMENTING`
- `TARGETED_GREEN`
- `WAITING_ACCEPTANCE_SET`
- `ACCEPTANCE_RUNNING`
- `ACCEPTANCE_FAILED`
- `DIAGNOSTIC_COMPLETE_NON_ACCEPTING`
- `VALIDATION_RESULT_NONRELEASE`
- `MONITORING_COMPLETE_NONRELEASE`
- `CORRECTION_REQUIRED`
- `INFRASTRUCTURE_PENDING`
- `OWNER_PENDING`
- `ACCEPTED`
- `HANDOFF_PENDING`
- `COMPLETE`
- `DEFERRED`
- `SUPERSEDED`

Allowed transitions:

| From | Allowed next states |
|---|---|
| `NOT_STARTED` | `HYDRATING`, `DEFERRED`, `OWNER_PENDING`, `SUPERSEDED` |
| `HYDRATING` | `READY`, `CORRECTION_REQUIRED`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `READY` | `IMPLEMENTING`, `OWNER_PENDING`, `SUPERSEDED` |
| `IMPLEMENTING` | `TARGETED_GREEN`, `VALIDATION_RESULT_NONRELEASE`, `MONITORING_COMPLETE_NONRELEASE`, `DEFERRED`, `CORRECTION_REQUIRED`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `TARGETED_GREEN` | `WAITING_ACCEPTANCE_SET`, `ACCEPTANCE_RUNNING`, `INFRASTRUCTURE_PENDING` only for C04's typed pre-acceptance transport activation with exact resume, `CORRECTION_REQUIRED`, `SUPERSEDED` |
| `WAITING_ACCEPTANCE_SET` | `ACCEPTANCE_RUNNING`, `CORRECTION_REQUIRED`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `ACCEPTANCE_RUNNING` | `ACCEPTED`, `ACCEPTANCE_FAILED`, `INFRASTRUCTURE_PENDING`, `SUPERSEDED` |
| `ACCEPTANCE_FAILED` | `DIAGNOSTIC_COMPLETE_NON_ACCEPTING`, `CORRECTION_REQUIRED`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `DIAGNOSTIC_COMPLETE_NON_ACCEPTING` | `CORRECTION_REQUIRED`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `VALIDATION_RESULT_NONRELEASE` | `SUPERSEDED` only as one half of the atomic fresh-authority successor-attempt creation; otherwise terminal for that bounded validation attempt |
| `MONITORING_COMPLETE_NONRELEASE` | `SUPERSEDED` only as one half of the atomic fresh-authorization successor-monitor-attempt creation; otherwise terminal for that bounded monitor attempt |
| `CORRECTION_REQUIRED` | `IMPLEMENTING`, `OWNER_PENDING`, `SUPERSEDED` |
| `INFRASTRUCTURE_PENDING` | the receipt-recorded resumable predecessor or successor, `OWNER_PENDING`, `SUPERSEDED` |
| `OWNER_PENDING` | `HYDRATING` after exact owner evidence authorizes bounded executor work, the receipt-recorded nonexecution resume state, `DEFERRED`, `SUPERSEDED` |
| `ACCEPTED` | `HANDOFF_PENDING`, `COMPLETE`, `SUPERSEDED` only through declared invalidation |
| `HANDOFF_PENDING` | `HYDRATING` after exact next-tranche owner evidence, `COMPLETE`, `INFRASTRUCTURE_PENDING`, `OWNER_PENDING`, `SUPERSEDED` |
| `DEFERRED` | `SUPERSEDED` only as one half of the atomic exact-owner-re-entry successor-attempt creation; otherwise terminal for that bounded/deferred attempt |
| `COMPLETE` | `SUPERSEDED` only through declared invalidation |
| `SUPERSEDED` | terminal; a successor attempt begins at `NOT_STARTED` |

Every unlisted transition fails closed. State is keyed by `(cardID, monotonicAttemptID)`: no terminal attempt is reopened. Fresh one-shot re-entry authority performs one state-writer CAS that supersedes the exact prior terminal, invalidates its current satisfaction evidence, and creates the next attempt at `NOT_STARTED`; partial old/new creation is recovered as old, matching, or divergent. `VALIDATE_NEXT` and `DEFER` policy profiles categorically reject `TARGETED_GREEN`, `WAITING_ACCEPTANCE_SET`, `ACCEPTANCE_RUNNING`, `ACCEPTED`, `HANDOFF_PENDING`, `COMPLETE`, `V23CardAcceptanceReceiptV2`, selector credit, and release credit for every attempt even where the general table permits those states for shipping classifications. Diagnostic, nonrelease-validation, and monitoring-complete states therefore have no legal path to `TARGETED_GREEN` or `ACCEPTED`. The C04 `TARGETED_GREEN → INFRASTRUCTURE_PENDING` exception requires `C04TransportActivationEffectReceiptV1(INFRA_PENDING)` and records exact resume to the same candidate's TARGETED_GREEN/transport-activation-pending point; it grants no generic targeted-green escape. Infrastructure and owner receipts record the exact resume state; they never reset work ambiguously.

### 4.1 Deterministic eligibility and selector

Every `VALIDATE_NEXT` and `DEFER` card is categorically ineligible before P00-C05 has accepted, acquired canonical state-writer ownership at the adopted generation, and can generate the exact nonautonomous trigger/hydration/fence chain. In particular P00-C03 is not a bootstrap card: its lower register ordinal creates no pre-C04/C05 demand-validation obligation or deadlock. Only C01, C02, C04, and C05 use `BootstrapCardContextV1`.

After C05, pre-eligibility for hydration requires package-derived autonomous authority or an exact class-appropriate read-only hydration authorization, plus a complete `DirectPrerequisiteEvidenceSetV1`. Ordinarily each direct edge requires an accepted receipt, unique canonical `CardAcceptanceInclusionProofV1`, matching proof-recovery/currentness result, and current-candidate compatible/successor disposition. The only exception is the ten frozen P00-C03→P06 edges: `NonreleasePrerequisiteSatisfactionReceiptV1` binds the latest P00-C03 `BoundedNonreleaseValidationReceiptV2`, its unique `NonreleaseResultInclusionProofV1`, exact ledger terminal, observed-at/valid-through interval, evidence/protocol/source digests, explicit revalidation triggers, current compatibility, and normalized result. At satisfaction time P00-C05 independently proves no newer/superseding P00-C03 attempt exists, current time is within `validThrough`, every revalidation-trigger digest is unchanged, and the inclusion/current-result proof still verifies. Only `VALIDATED_SUPPORTED` yields `SATISFIED_GO_EVIDENCE`; expired, trigger-changed, `VALIDATED_NOT_SUPPORTED | INCONCLUSIVE | DEFERRED_AGAIN` evidence yields a typed unsatisfied disposition and no P06 hydration. Owner-authorization expiry and result freshness are separate fields; one never substitutes for the other. It grants no acceptance, implementation, selector, or release credit. Every other P06 prerequisite remains an ordinary accepted-compatible edge. A later fresh P00-C03 attempt may supersede the old nonrelease result under its one-shot owner authority. `ExecutionHydrationAuthorizationV2` binds card/package, immutable classification/status, authorizing owner, satisfied trigger evidence, bounded semantic scope, allowed path envelope and prohibited paths, expiry/one-shot rule, and `mutationAllowed=false`; it intentionally does not guess an exact `PathFenceV1`. Corrections first; otherwise the lowest Foundation register ordinal among pre-eligible candidates enters read-only `HYDRATING` only from that attempt's `NOT_STARTED`. For a prior `DEFERRED` or `VALIDATION_RESULT_NONRELEASE` terminal, the state writer must first atomically supersede it and create the next monotonic `NOT_STARTED` attempt under fresh one-shot authority. Repeated candidates follow that same ordinal order.

`EffectiveExecutionAuthorizationV2` is the sole mutation/test eligibility value. Autonomous classifications derive it from the frozen package plus their accepted hydration. A `VALIDATE_NEXT` or `DEFER` card uses two stages: its exact dossier-defined trigger first produces `ExecutionHydrationAuthorizationV2`; hydration then derives the exact `HydratedCardExecutionSpecV2` and `PathFenceV1`; finally `ExecutionAuthorityTransitionV2` binds that spec/fence, allowed tests/effects, expiry/one-shot rule, `releaseCredit=false`, and `ExecutionPathFenceAuthorizationReceiptV1`. The fence receipt proves the exact fence is a subset of the owner-authorized path envelope and violates no prohibited path; the subset proof is deterministic and needs no second approval. A non-subset fence fails closed and requires new owner authority before mutation. These receipts override runtime eligibility only: they never mutate `PlanningCardStatusV2`, change classification, or add a member to a frozen selector. Both classes use only a distinct attempt's `NOT_STARTED → HYDRATING → READY → IMPLEMENTING` path and may close only in `VALIDATION_RESULT_NONRELEASE | DEFERRED` (or a typed correction/infrastructure/owner/supersession side state). The immutable classification/policy-profile guard rejects every accepting transition or receipt before the ledger CAS, and the projection independently rechecks it.

`BoundedNonreleaseValidationReceiptV2` preserves `sourceTerminal` from the card's exact `NonautonomousTriggerManifestV1` row and records one normalized terminal: supported/go evidence → `VALIDATED_SUPPORTED`; unsupported/no-go evidence → `VALIDATED_NOT_SUPPORTED`; inconclusive → `INCONCLUSIVE`; defer/re-enter-later → `DEFERRED_AGAIN`. It binds the exact `DirectPrerequisiteEvidenceSetV1` digest/counts/edge evidence, `EffectiveExecutionAuthorizationV2`, path fence, protocol/source/evidence digests, `observedAt`, independently justified `validThrough`, and closed revalidation-trigger digest set at start and terminal. P00-C03 maps `OWNER_RECORDED_GO_EVIDENCE_ONLY → VALIDATED_SUPPORTED`, `NO_GO → VALIDATED_NOT_SUPPORTED`, `INCONCLUSIVE → INCONCLUSIVE`, and `DEFER → DEFERRED_AGAIN`. The manifest freezes the same exhaustive mapping for every other card-specific enum; an unmapped value fails closed. Before inclusion, the active state writer independently terminal-refetches the prerequisite set, effective authorization, evidence/source digests, owner revocation, and attempt currentness. If any changed or expired, it supersedes the attempt and emits no usable inclusion or satisfaction proof. Otherwise the first three normalized outcomes close the attempt as `IMPLEMENTING → VALIDATION_RESULT_NONRELEASE`; `DEFERRED_AGAIN` closes it as `IMPLEMENTING → DEFERRED`, and the state writer emits `NonreleaseResultInclusionProofV1` for the unique ledger transition. The attempt receives no acceptance, selector, implementation, or release credit and never passes through `TARGETED_GREEN` or an official accepting plan. A later attempt needs fresh one-shot owner authority; one CAS supersedes the exact prior terminal, invalidates its satisfaction/inclusion proof, and creates the distinct next monotonic attempt at `NOT_STARTED`. It never transitions the old attempt back to hydration. Changing release membership requires a successor package.

READY requires: valid current hydration; one zero-orphan `DirectPrerequisiteEvidenceSetV1` whose ordinary edges are accepted/inclusion-proven/current-compatible and whose exact P00-C03→P06 exception, when present, is `SATISFIED_GO_EVIDENCE`; no unresolved owner condition; no invalidated required authority; and a valid `EffectiveExecutionAuthorizationV2` that permits the card's bounded work.

P05-C01's ACCEPT effect atomically persists its card receipt/state, canonical `ReleaseTransportSelectionV1` as `ORIGINAL_VALID(originalReceiptDigest, releaseMutationInputDigest, observedExpiry, requiredMargin, generation)`, and the corresponding `ReleaseTransportRefreshAttemptAuthorityCellV1.EMPTY(selectionGeneration,refreshAttemptEpoch=0)`. The first accepted candidate uses expected-absent generation 0 for both release records; a fresh accepted successor must CAS an exact `SUPERSEDED(reason,authorityDigest,generation)` predecessor to the next generation and can never reuse or overwrite an active selection or refresh epoch. Every declared P05 invalidation or supersession—product-proof contradiction, governed-input/dependency invalidation, postrelease defect, or authorized abandonment—uses `ReleaseTransportSupersessionEffectReceiptV1` to change P05 state, selection, and matching refresh cell together. Its exact predecessor-state union is `ACCEPTED | HANDOFF_PENDING | INFRASTRUCTURE_PENDING | COMPLETE`; refresh and late-refresh contradiction require COMPLETE, while pre-integration proof contradictions bind their actual earlier state/generation. `ReleaseTransportSupersessionEffectRecoveryReceiptV1` classifies all three records as before-effect, partial-matching, applied-matching, or divergent/ambiguous; no one-sided supersession may authorize a successor. `ReleaseTransportSelectionInitializationReceiptV1` and `ReleaseTransportSelectionInitializationRecoveryReceiptV1` likewise classify card+selection+epoch-0 cell as `ABSENT_FIRST_SELECTION | SUPERSEDED_AT_EXPECTED_GENERATION | PARTIAL_MATCHING_INITIALIZATION | APPLIED_MATCHING_INITIAL_SELECTION | DIVERGENT_OR_AMBIGUOUS`, complete only exact partial effects, and reject P05 ACCEPT with a missing or competing release record. It then moves `ACCEPTED → HANDOFF_PENDING` while phase integration, capacity-policy activation, and controller rollover remain. An activation/rollover infrastructure failure uses `HANDOFF_PENDING → INFRASTRUCTURE_PENDING` with an exact repair resume to `HANDOFF_PENDING`; a deterministic proof contradiction supersedes P05, selection, and refresh cell. Only matching `PhaseIntegrationReceiptV2`, committed `RepositoryHostedCapacityPolicyActivationReceiptV1`, `ControllerWorkflowRolloverReceiptV1` when disposition is ACTIVE, `ControllerWorkflowRolloverDispositionV1(UNCHANGED_CONTROLLER | ACTIVE)`, and the matching active `ControllerWorkflowEnrollmentAuthorityCellV1` token permit P05-C01 `COMPLETE`.

P05-C02 cannot become pre-eligible earlier. It follows `NOT_STARTED → OWNER_PENDING` and binds those receipts/dispositions, current `ExpectedMainIntegrationBaseV2`, and `AuthorityOnlyEvidenceChainDispositionV1`. Its first hydration uses `NO_AUTHORITY_ONLY_CHILD_AT_PHASE_TIP` only when current main equals the product phase tip; if an already accepted P05 mirror/evidence child exists, it instead binds the exact verified `TIP(receiptDigest,head)` chain. A current tranche can never satisfy its own input, so no circular receipt is allowed. Every hydration binds canonical `ReleaseTransportSelectionV1 = ORIGINAL_VALID(...) | REFRESHED_CURRENT(refreshReceiptDigest,artifactDigest,observedExpiry,requiredMargin,generation) | SUPERSEDED(reason,authorityDigest,generation)` and derives, without mutating that value merely because time passed, `ReleaseTransportUsabilityDispositionV1 = CURRENT_WITH_MARGIN(observedAt,margin) | EXPIRED_REFRESH_REQUIRED(observedAt,requiredMargin,predecessorSelectionGeneration)`. Only `ProtectedReleaseActionKindV1(SIGN_AND_UPLOAD)` consumes LOCAL_ARTIFACT_INPUT and requires CURRENT_WITH_MARGIN on ORIGINAL_VALID or REFRESHED_CURRENT. Submission and promotion consume immutable accepted APP_STORE_BUILD_INPUT; phased-release controls consume exact APP_STORE_RELEASE_INPUT with current provider state/generation. Neither later input depends on expired local-artifact margin or triggers a redundant rebuild. A physical/usability tranche that consumes no provider input uses explicit `NON_TRANSPORT_CONSUMER` and can never authorize provider mutation. Each exact owner evidence tranche emits `OwnerActionEvidenceReceiptV1`, a read-only hydration authorization for only its bounded validation, secret-redaction, and `ProtectedReleaseLedgerArchiveV1` work. Hydration derives the exact spec/fence; `ExecutionAuthorityTransitionV2` plus `ExecutionPathFenceAuthorizationReceiptV1` proves the fence is within the owner-evidence envelope, then derives tranche-scoped `EffectiveExecutionAuthorizationV2` binding owner evidence, hydrated spec/fence, action/input variant, allowed tests/effects, expiry/one-shot rule, and `releaseCredit=false`. Missing later owner evidence returns to `OWNER_PENDING`/`HANDOFF_PENDING` with an exact resume condition. Once every required tranche is validated and archived without secrets, P05-C02 proceeds through `TARGETED_GREEN → ACCEPTANCE_RUNNING → ACCEPTED → COMPLETE`; its accepted release-disposition receipt plus inclusion/compatibility proof is the ordinary direct-prerequisite evidence for P05-C03.

P05-C03 `MONITOR` becomes pre-eligible only after the ordinary P05-C02 direct-edge evidence set proves an accepted disposition of `RELEASED` or `PHASED_RELEASE_ACTIVE`. The P00-C05 hydrator/state authority—not the observer—creates `PostReleaseMonitorActivationV1` after independently verifying that disposition, then derives the exact observation-only fence, binds current expected main/evidence chain, direct-prerequisite-set/release-identity/effective-authorization digests, and creates `MonitorExecutionFenceReceiptV1` only after exact bounded-path proof. It follows `NOT_STARTED → HYDRATING → READY → IMPLEMENTING → MONITORING_COMPLETE_NONRELEASE`. The P05-C03 observation actor emits only `PostReleaseMonitoringReceiptV1`, binding those same immutable predecessor digests, the release disposition/identity, bounded watch window and sources, locally/owner-supplied observations, privacy/no-telemetry posture, issue/escalation outcomes, and zero silent shipping changes; it cannot create its activation/fence, integrate its branch, write ledger state, or self-terminalize.

Publication uses one narrow own-output exception rather than requiring an impossible unchanged chain. `MonitorAuthorityChildFinalizationReceiptV1` binds exactly the current monitor attempt/activation/fence, predecessor expected-base and chain digests, the monitoring-receipt digest, one prepared authority-only integration intent/preflight, its exact intended child, the resulting `AuthorityOnlyEvidenceIntegrationReceiptV2`, and successor expected-base/TIP digests. The intended child must contain only the allowlisted secret-free monitoring evidence for that attempt, preserve governed inputs/archive/build identity, and be the direct authority-only child of the bound predecessor; it cannot include another tranche or depend circularly on its own integration receipt. After remote integration and the recoverable base/TIP effect match, the active state writer terminal-refetches P05-C02 edge/inclusion/compatibility, release identity/disposition, effective authorization, attempt currentness, and this exact predecessor→own-child mapping, then emits/applies `MonitorExecutionEffectRecoveryReceiptV1` against the successor base/TIP. The contract-input invalidation rule therefore treats only that byte-identical own child as compatible finalization; any unrelated base/chain change, extra child content, different head, incident, supersession, or later descendant still supersedes the attempt and invalidates its current result. Recovery is old/matching/divergent and never republishes the branch. This closure never runs the official accepting plan, emits `V23CardAcceptanceReceiptV2`, enters `TARGETED_GREEN`/`ACCEPTED`, or grants selector/release credit. A later authorized monitoring window is one atomic state-writer effect: if the prior attempt is `MONITORING_COMPLETE_NONRELEASE`, CAS it to `SUPERSEDED` while creating exactly one monotonic successor at `NOT_STARTED` under fresh `PostReleaseMonitorActivationV1`; expected-old/matching/divergent recovery prevents an orphan supersession or duplicate successor. The old terminal is never reopened. Neither P05-C02 nor P05-C03 enters the 132-member autonomous selector, and neither may be simulated by repository evidence.

For P05-C03 `PHASED_RELEASE_ACTIVE` pre-eligibility, the P05-C02 direct-edge evidence must additionally bind the matching current `ReleaseOperationalControlAuthorityCellV1` plus exactly one state-writer-confirmed START genesis or `ExistingPhasedReleaseOperationalControlAdoptionReceiptV1`; absence or divergent identity blocks monitor activation. A fully `RELEASED` disposition does not invent a phased-control cell.

Corrections for an accepted official failure have priority. Otherwise select the lowest Foundation register ordinal among READY cards for `IMPLEMENTING`. For concurrent implementation lanes, repeatedly select the next-lowest READY ordinal whose hydrated path fence is disjoint from every entry in `ActiveWorkSetProjectionV1`, then atomically add its complete lane entry before mutation. Terminal/released lanes are removed only by a ledger transition. This pre-hydration and post-hydration ordering is complete—there is no undefined severity field. There are 133 autonomous-authorized cards, but `AutonomousRequiredAcceptedSetV1` contains exactly 132 pre-P05 receipts because P05-C01 consumes the set and is excluded from its own input. VALIDATE_NEXT, DEFER, OWNER_ACTION, and MONITOR never enter that frozen set; any authorized separate work retains its own nonrelease receipt.

Aggregate selectors gate official acceptance, not implementation readiness. If a card such as P04-C27 has implemented and reached targeted green before its exact later-member selector is complete, it enters `WAITING_ACCEPTANCE_SET`; this never blocks those later members from becoming READY.

<a id="v23-github-capacity"></a>
## 5. GitHub Actions and integration capacity

`RepositoryHostedCapacityPolicyV1` applies to the `STANDARD_GITHUB` pool and enforces—not merely observes—at most five standard GitHub-hosted jobs simultaneously `in_progress`. `HostedCapacityEnforcementModeV1` is closed to: `MACOS_PLATFORM_SUBLIMIT_LE_5`, permitted only when current authoritative GitHub plan evidence proves a hosted-macOS maximum no greater than five and every repository workflow/ref/event/matrix/control path is eligible macOS; `DEDICATED_LARGER_RUNNER_POOL_MAX_CONCURRENCY_LE_5`, permitted only when every standard-hosted path is forced into one governed larger-runner pool whose configured maximum concurrency is no greater than five; `AUTHORITATIVE_ACCOUNT_OR_ORG_LIMIT_LE_5`, permitted only with current GitHub settings/API/support evidence that the enforceable scope includes every standard-hosted path; or `HOSTED_CAPACITY_CONFIGURATION_REQUIRED`, which blocks only that pool and never grants acceptance. Separately, the `GETMAC` pool permits at most three concurrently executing jobs using verified repository runner labels/configuration. Every job is charged to exactly one pool; the combined ceiling is eight. Prefer native workflow concurrency and matrix `max-parallel`; do not create a custom scheduler when native controls enforce the ceilings. Missing GetMac capacity reduces throughput but does not block product work that can run locally or on standard GitHub capacity.

Before each reservation batch and again at each job/stage start, the controller or pinned guard obtains `HostedCapacityAdmissionWitnessV1` from the authoritative GitHub settings/API. It is valid for at most five minutes, binds current policy/evidence/enrollment tokens, configuration revision/snapshot/audit cursor, all-ref workflow-inventory high-water digest, `ObservedExternalHostedOccupancySetV1` generation/digest, observed live jobs, proved hard limit, and `effectiveHostedCapacity = min(provedHardLimit, 5)`. One witness may serve only the same wave whose jobs start inside that five-minute interval. A revision/snapshot/inventory/occupancy mismatch, missing fresh query, unclassified live job, expired evidence generation, or out-of-band configuration change blocks new starts and triggers occupancy reconciliation, renewal, or fresh policy activation; running work is stopped/superseded where safe and reconciled, never ignored. The controller uses as many currently available slots as the fresh value permits and never invents five when the proven limit is one through four. GitHub's standard total concurrency must never be called five merely because a macOS sublimit is five. The guarantee's explicit administrative trust boundary excludes malicious bypass by an account/organization administrator who can alter the native limit/protected configuration and conceal the audit trail; any observed authorized administrative change is still a revocation event, not silently accepted, and no affected interval retains the prior ceiling claim. If no accepting mode can be proved, `HostedCapacityConfigurationGateV1(HOSTED_CAPACITY_CONFIGURATION_REQUIRED)` blocks hosted dispatch while local/offline work continues. Five YAML group names alone cannot police historical or unenrolled workflows.

P00-C04 inventories every workflow-bearing remote branch/tag/default ref, every `.github/workflows` job, called reusable/nested workflow, matrix/fan-out child, event, schedule, release, legacy, unrelated, controller, claim, activation, and rollover job. Each row freezes repository, event, workflow ID/path, source ref and SHA/blob, expansion bound, `runs-on`, enforcement scope, lane selector, activation epoch, and enrollment disposition. Unknown or unenrolled runners, dynamic reusable targets, unbounded matrices, or a runner path outside the selected hard mechanism fail the static guard. Protected ref/ruleset closure prevents new legacy branch updates where supported; any historical/manual run that can escape the proved mechanism blocks activation rather than being optimistically counted.

There are exactly five fixed protected native GitHub concurrency groups: `assetrounds-hosted-1` through `assetrounds-hosted-5`, each with `cancel-in-progress: false` and at most one governed `in_progress` job. They provide lane serialization and fairness beneath—not instead of—the selected hard mechanism; lanes above `effectiveHostedCapacity` remain unavailable. Every job or expanded matrix child, including controller/claim-stage/activation/renewal/rollover/protected-release/reconciliation work, resolves one available lane before dispatch; a running control job subtracts one from the effective budget. `HostedCapacityLeaseV1` binds repository, event, workflow/ref/SHA/job/matrix or pipeline identities, policy/evidence/configuration/enrollment epoch/token, fresh `HostedCapacityAdmissionWitnessV1`, lane, candidate, `HostedWorkKindV1`/`HostedWorkIdentityV1`, active-entry/pipeline-and-stage nonces, reservation/start/expiry, stage-transfer CAS when applicable, and terminal release. Job start refetches the witness inputs; stale or changed configuration exits through a representable nonexecuting terminal. Pending jobs do not count as `in_progress`, but at most one outstanding reservation may exist per lane and a product or protected-release pipeline retains that reservation across its sequential nonoverlapping stages.

`HostedCapacityQueueReceiptV1` is the durable external FIFO/CAS queue solely written by the active acceptance controller (A00 acceptance through C05 decision, then P00-C04, with no writes during pending C05 cutover). Its key includes repository/event/workflow/ref/SHA, typed reservation identity, and matrix or pipeline identity; product pipelines additionally include card/attempt/cell/ordinal/wave, claim/logical-product identities, and concrete stage-attempt epoch/nonces, while protected-release pipelines include authorization/action/input/deployment generations plus every stage. It records enqueue, lane reservation, claim-stage dispatch/terminal, protected stage transfer, pretransfer-noop terminal/reconciliation, protected-release prelease-noop state-terminal input and queue recovery, one bounded product-stage retry authorization, second-stage dispatch/terminal, protected reconciliation dispatch/terminal when any, pending replacement/cancellation, retry nonces, terminal release, expiry, and supersession. The card-ledger writer never mutates this queue. If GitHub replaces/cancels a pending entry, the controller reconciles and resubmits the still-authorized stage with a fresh stage nonce in frozen order. A lane releases only after API reconciliation proves no matching stage/job is `in_progress`; an unclassified live job blocks new dispatch rather than being ignored.

Before the C04 trusted-default-branch checkpoint, drain all legacy pending/in-progress hosted work, run C01/C02 and C04 preflight through local/incumbent CI, and permit no parallel V23 hosted dispatch. The current A00 acceptance controller activates policy/enrollment epoch 1 only after current quota evidence and the all-ref workflow scan prove one accepting enforcement mode. Every later candidate that changes workflow/policy bytes must produce `CandidateWorkflowEnrollmentProofV1` before P05 acceptance: exact candidate blobs, all events/refs/matrices/control jobs, bounded expansion, five-group guard, permissions, hard-mechanism containment, static no-bypass result, old-controller compatibility, and next policy/enrollment digest. P00-C04 is sole schema/lifecycle owner and validator; the sole instance writer is the `P05-C01` workflow-proof evidence actor under exact current-controller authorization. The P05 candidate is only the bound subject, never an actor. Before integration, product acceptance cells run under the currently active enrolled controller against the candidate head; candidate workflow bytes remain inert and receive only static/old-controller proof plus guard-only checks. Only the newly integrated candidate workflow is barred from checkout/product commands until its policy activation and enrollment rollover commit.

Policy activation is itself durable. `RepositoryHostedCapacityPolicyAuthorityCellV1(policyEpoch, policyDigest, enforcementMode, enforcementScopeDigest, enforcementEvidenceDigest, evidenceGeneration, activationReceiptDigest)` is protected outside mutable refs and begins only from the proven `NO_ACTIVE_POLICY@POLICY_EPOCH_0` genesis. Before each C04 checkpoint or P05 phase integration, the current acceptance controller—A00 initially, P00-C04 after final adoption—persists `RepositoryHostedCapacityPolicyActivationIntentV1` binding predecessor cell/CAS token, `ControllerActivationSubjectV1`, candidate proof, intended main child, exact policy/enforcement evidence, predeclared guard work identity/spec/artifact locator and expected result predicate, and sole legal next epoch. It cannot bind a result that does not yet exist. After main refetch and integration/base CAS, the capacity-counted guard verifies the exact integrated bytes without product checkout; the active state writer independently creates/reconciles its `CONTROL_JOB_RESULT`. `RepositoryHostedCapacityPolicyActivationDecisionV1 = ACTIVATE | INFRA_PENDING | PRODUCT_PROOF_CONTRADICTION | SUPERSEDED` binds that result. ACTIVATE performs one protected policy-cell CAS that embeds the immutable `RepositoryHostedCapacityPolicyActivationReceiptV1` and actual reconciled result digest. `RepositoryHostedCapacityPolicyActivationRecoveryReceiptV1` classifies `ABSENT_AT_PREDECESSOR | APPLIED_MATCHING_INTENDED_EPOCH | DIVERGENT_OR_AMBIGUOUS`, retrying, returning identical evidence, or blocking.

Expiry of otherwise matching `GitHubHostedQuotaEvidenceV1` never forces an invented C04/P05 integration and never permits stale dispatch. `HostedCapacityEvidenceRenewalSubjectV1` and `RepositoryHostedCapacityEvidenceRenewalIntentV1` bind the current policy-cell token, unchanged policy digest/enforcement mode/scope/hard limit, predecessor evidence generation/digest/expiry, new source query, one predeclared `CAPACITY_EVIDENCE_RENEWAL_GUARD` identity/result predicate, and sole legal next evidence generation. If the predecessor evidence is already expired, the controller first drains every other hosted reservation and may dispatch exactly one renewal guard under the still-configured hard mechanism; if that cannot be proved, renewal is read-only API/local evidence or hosted dispatch remains blocked. The guard performs no candidate checkout, product/release command, or card acceptance. `RepositoryHostedCapacityEvidenceRenewalDecisionV1 = RENEWED_UNCHANGED_SCOPE | INFRA_PENDING | SCOPE_OR_LIMIT_CHANGED | SUPERSEDED`. RENEWED CASes only evidence digest/expiry/generation while policy epoch/digest/mode/scope remain byte-identical and emits `RepositoryHostedCapacityEvidenceRenewalReceiptV1`; every later projection binds the new cell token. Infra leaves hosted dispatch blocked but local work continues. Scope/limit change cannot use renewal; it enters configuration-required or a fresh governed policy activation. `RepositoryHostedCapacityEvidenceRenewalEffectRecoveryReceiptV1` classifies `BEFORE_EFFECT_AT_EXPECTED_EVIDENCE_GENERATION | APPLIED_MATCHING_RENEWAL | DIVERGENT_OR_AMBIGUOUS`. Renewal grants no card, acceptance, selector, or release credit.

For C04, enrollment/genesis/policy activation is one pre-smoke transport-activation disposition governed by the C04 transport effect: transient service/runner/identity failure enters `C04_TRANSPORT_ACTIVATION_INFRA_PENDING` and exact same-byte repair; a deterministic C04 tooling/policy conformance defect enters `C04_CORRECTION_REQUIRED` and permits only a forward product correction or typed `infra-fix/v23-p00-c04-<signature8>` tooling repair under `ToolingInfrastructureRepairAuthorizationV1`, new checkpoint intent/effect, same genesis/trust root, monotonic policy/enrollment epochs, and fresh smoke. For P05, transient failure leaves P05-C01 in resumable infrastructure-pending handoff; deterministic integrated candidate workflow/policy contradiction atomically supersedes P05-C01, its release-transport selection, and matching refresh authority cell and requires an exact `release-fix/v23-<card-suffix>-<signature8>` candidate plus fresh full P05 acceptance. Rollover self-test applies the same transient-versus-deterministic classification; it never loops monotonic intents over known-bad bytes. Product dispatch must bind the committed policy epoch and matching active enrollment-cell token.

Recommended logical allocation, dynamically reusing freed slots:

1. Compile, static, architecture, claims, and localization checks.
2. Unit, contract, lifecycle, and hostile-parser tests.
3. Integration, interruption, recovery, and compatibility tests.
4. UI journey, accessibility, visual, and affected HIG state tests.
5. Coverage extraction, uninstrumented performance, archive, and release evidence.

Cancel only nonauthoritative, superseded targeted or diagnostic work when its artifacts are explicitly abandoned. Never cancel official acceptance, protected release, or evidence-producing work merely to free capacity. Share immutable build and test artifacts only when head, tree, source digests, toolchain, configuration, test plan, fixture, and instrumentation agree. Hostile capacity tests race all trigger/ref classes (including old branches/tags and manual dispatch), matrices, reusable workflows, scheduled overlap, controller-plus-workers, pending replacement, cancelled/lost jobs, stale leases, candidate-policy activation, capacity-evidence renewal, controller rollover, and protected-release stages. The platform-observed maximum must never exceed `min(provedHardLimit, 5)` and must have a complete queue/reconciliation trail; when five slots are actually available, a separate nonaccepting utilization test may prove all five without making five a requirement for valid lower-cap plans.

Parallel agents may research or implement disjoint path-fenced card branches. In every template, `<card-suffix>` is exactly lowercase `pNN-cNN` obtained by removing the leading `V23-` from the canonical card ID; it never includes another `v23-` (for example, V23-P03-C55 → `p03-c55`). Autonomous release-scope branch: `card/v23-<card-suffix>-<bounded-slug>`. Correction branch: `fix/v23-<card-suffix>-<signature8>`. P05-C02 authority-only evidence branch: `evidence/v23-p05-c02-<tranche-id8>-<bounded-slug>`. P05-C03 observation branch: `monitor/v23-p05-c03-<release-id8>-<window-id8>`. One integrator serially rebases or merges product card/correction branches non-force into `phase/v23-expansion` in dependency-valid, Foundation-register-ordinal order; evidence/monitor branches follow only their separate authority-only integration law. Never force push, rewrite accepted evidence, or run five concurrent source integrations.

Only after P00-C05 acceptance, authorized `VALIDATE_NEXT` or `DEFER` work uses `validation/v23-<card-suffix>-<bounded-slug>` from the exact authority-bound base. Its fence permits only the named validation/prototype/evidence and cleanup; `releaseCredit=false`. `BoundedNonreleaseValidationReceiptV2` externally binds trigger/owner authority, base/head/tree, exact changed/evidence/scratch paths, commands/results, cleanup disposition, no public effect, prerequisite/effective-authority terminal fence, observed-at/valid-through/revalidation triggers, the exact dossier-specific `sourceTerminal`, its manifest-frozen normalized terminal `VALIDATED_SUPPORTED | VALIDATED_NOT_SUPPORTED | INCONCLUSIVE | DEFERRED_AGAIN`, and resulting `VALIDATION_RESULT_NONRELEASE | DEFERRED` state. No validation branch, commit, generated artifact, prototype, or receipt may be merged, fast-forwarded, cherry-picked, or copied into `phase/v23-expansion` or `main`. Preserve it as isolated evidence under its retention rule. A future shipping implementation requires successor package authority and newly authored/reviewed code and proof; validation results grant no implementation or release credit.

Integrate a card branch before starting official acceptance. Once an exact head/tree is accepted, never rebase, amend, cherry-pick into, or otherwise rewrite that accepted commit. Later integration creates a new candidate head, and every relation whose activation predicate matches must be re-proved before its consumer can accept. Old receipts remain immutable historical evidence and are marked superseded rather than edited.

A required card may accept with an unavailable optional subcapability only when its mandatory local core is complete and the Blueprint-authorized typed disabled or manual fallback is proven with zero public availability claim. Examples are `DISABLED_NO_SELECTED_PROFILE`, a disabled rating link while Support remains complete, dormant communications and measurement, an unavailable on-device speech path with complete manual entry, and an unavailable optional encrypted choice while the standard surface remains complete. This is not permission to silently disable a required core.

<a id="v23-test-economy"></a>
## 6. Test economy and evidence quality

For each card:

1. Run formatting, compiler, static, schema, graph, claims, and localization checks.
2. Run the smallest named unit and contract tests for the owned cause.
3. Run affected integration, lifecycle, hostile, interruption, and recovery tests.
4. Run affected UI, accessibility, visual, and journey tests.
5. Start official acceptance once after targeted green.
6. Reuse accepting `xcresult` for `xccov` and other receipts; do not rerun merely for coverage.
7. Run broad exact-candidate gates only at milestone or final closure or when their exact invalidation class changes.

Never accept performance from coverage instrumentation. Do not repeatedly run the entire suite while a targeted failure is open. Optimization means better deterministic fixtures, sharding, reuse, selectors, and causal correction—not less proof or skipped states.

<a id="v23-acceptance-diagnostic"></a>
## 7. Official acceptance and diagnostic separation

Official acceptance is exact-head and fail-fast. Before dispatch, `AcceptanceCellOrderV1` freezes every selected cell's stable ID, total ordinal, wave, exact selector, timeout, artifact identity, and the current `DirectPrerequisiteEvidenceSetV1` plus `EffectiveExecutionAuthorizationV2` digests. The prerequisite set binds the direct-graph digest, expected/observed counts, each edge identity/ordinal, source receipt, canonical inclusion proof, matching proof-recovery/currentness result, and DIRECT_EDGE compatibility/supersession digest; accepting-card order requires `nonreleaseSpecialEdgeCount=0`. For hosted official cells, those identities are carried bijectively through queue, active-dispatch entry, claim-only and product jobs, hosted result, reconciliation, classification, observations, decision, and effect receipt. The C01/C02 local exception carries the same order/cell/fence authority through its local plan, raw envelope, local reconciliation, tagged local classification, observations, decision, and effect, with no fictional hosted identities. `AcceptanceStartFenceV1` binds controller authority epoch, card/attempt/candidate/order digest, prerequisite-set/effective-authorization/invalidation-generation digests, monotonic fence sequence, initial `RUN_ALLOWED`, and terminal `COMPLETED_ACCEPTED | STOPPED_PRODUCT | STOPPED_INFRASTRUCTURE | STOPPED_SUPERSEDED`. The controller rechecks all of them before every dispatch and at terminal decision. Any prerequisite, inclusion/proof-recovery, compatibility, invalidation, or effective-authority change closes the attempt `STOPPED_SUPERSEDED`; stale PASS cells cannot accept. A hosted wave may contain only lanes currently available under the five-job cap and is published as one bounded `ActiveDispatchSetV1`; the controller dispatches only its exact entries in frozen order. A local C01/C02 plan consumes no hosted lane or projection.

At claim-only stage start, every hosted official cell independently refetches the current acceptance-controller authority cell, unique agreeing checkpoint/ruleset/high-water witness, its pipeline entry/stage, nonces, and protected stop-intent namespace, and requires the claim ref to be absent or an exact same-claim-job retry. At product-stage start and immediately before the first product command it refetches those facts again and additionally requires the matching immutable claim plus completed claim-to-product stage-transfer CAS. Only the exact current controller epoch, official `RUN_ALLOWED` pipeline stage, and absence of a same-attempt stop intent permits the applicable stage. A stopped, superseded, missing, stale, unavailable, non-bijective, or mismatched state emits `NOT_STARTED_STOP_FENCED`, reconciles/releases its reservation when safe, and exits before candidate checkout/executable cache/product work. Fetch or verification failure is infrastructure; an old projection never overrides a newer stop intent or authority-cell epoch. Every controller-side queue/fence/projection/checkpoint/witness/stop/stage-transfer mutation is fenced by the current authority-cell CAS token, and runners require that same token. A controller that read epoch 0 before adoption but writes afterward cannot advance the protected cell or witness and its projection is nondispatchable. The C01/C02 `BOOTSTRAP_LOCAL_OFFICIAL` exception follows its explicit local result/decision/reconciliation law above and never pretends to have hosted transport evidence.

Every non-success terminal is deterministically classified as `PRODUCT_FAILURE_ELIGIBLE | INFRASTRUCTURE_NONPRODUCT | CONTROLLER_INDUCED_NONPRODUCT | STOP_FENCED_NONPRODUCT`. Product eligibility requires proof that the authorized product command actually ran on a healthy, identity-matching runner and the failure or semantic timeout came from the tested product contract. GitHub/service/runner/capacity/projection/witness/claim/transport/signature/cache corruption/lost-artifact failures and ambiguous timeouts are infrastructure. The immutable `AcceptanceCellTerminalClassificationReceiptV1` has a closed transport-context union. `HOSTED_CELL` binds projection/entry/nonces, claim-only/product-run identities, and `HostedExecutionResultReconciliationReceiptV1(APPLY_ONCE)` and prohibits local fields. `BOOTSTRAP_LOCAL_CELL` is legal only for C01/C02 `BOOTSTRAP_LOCAL_OFFICIAL`; it binds the authority-pinned producer and local plan/mode/run/runner identity and prohibits every hosted projection/lane/claim/workflow field. With local reconciliation APPLY_ONCE it also binds the raw `BootstrapLocalExecutionResultEnvelopeV1(OFFICIAL_CELL_RESULT)` and terminal event; with LOST or CONFLICT_QUARANTINED it instead binds the plan deadline, producer-liveness/artifact-absence or conflict evidence, requires class INFRASTRUCTURE_NONPRODUCT, and prohibits a raw-result success claim. Both variants bind program/card/attempt, controller and applicable policy/workflow epochs, candidate head/tree, fence/order digest, work/cell ID/ordinal/wave, selector/artifact expectation, terminal-or-typed-absence facts and stop facts, class/rationale, and receipt generation. Diagnostic local results use only `DiagnosticCellOutcomeV2` and cannot enter this official classification union. Any missing/inapplicable populated field, mismatch, or replay quarantines. Classification alone never transitions the attempt; infrastructure emits `AcceptanceInfrastructureReceiptV1` and never emits a product failure signature or correction.

For the first authoritative independent non-success, the controller computes the class in memory, then makes `AcceptanceStopIntentV1` the first durable controller effect: it binds the raw reconciled result digest, computed class/rationale, trigger identity, current authority/policy/workflow epochs, and `STOPPED_PRODUCT` or `STOPPED_INFRASTRUCTURE`. Exact attempt supersession uses the same protocol with `STOPPED_SUPERSEDED` and its immutable supersession authority. Only after intent publication may the classification receipt be persisted. The controller dispatches nothing new and requests cancellation of queued/pending and safely cancellable siblings. The active state writer independently reconciles intent and classification into the ledger; the controller then emits the stopped projection and `AcceptanceStopEffectReceiptV1`. Before any dispatch or projection after restart, it scans all authoritative terminal results; an unmatched non-success deterministically recreates the exact stop intent before work continues. `AcceptanceStopRecoveryReceiptV1` closes intent/effect/projection crash points: “intent absent” means no declared stop only after that terminal scan proves no unmatched non-success; matching intent with missing effects resumes reconciliation; matching complete effects return idempotently; divergence blocks. No cross-store atomicity is claimed.

The controller records every cell's dispatch, claim/product starts, pre-command fences, authoritative independent-terminal time/event sequence, controller-observed time, cancellation request/terminal, stop-fenced, timeout, and classification. A sibling whose product start occurred before stop-intent publication may finish or reach a typed cancellation timeout; a not-yet-started product job must cancel or emit `NOT_STARTED_STOP_FENCED`. The protocol does not pretend the intent was visible before publication.

After every dispatched official cell is terminal, controller-cancelled, stop-fenced, or at a typed cancellation timeout and every capacity lane is reconciled/released, product-cause candidates are exactly the `PRODUCT_FAILURE_ELIGIBLE` terminals whose authoritative terminal sequence proves the independent outcome occurred no later than stop publication. Controller-induced and stop-fenced results are expected consequences only when a matching product/infra/supersession stop exists; an orphan controller-induced or stop-fenced-only terminal becomes `ORPHAN_CONTROLLER_TERMINAL_WITHOUT_STOP_TRIGGER` infrastructure so every attempt closes. Clock source/resolution and equal-time tie handling are frozen in `AcceptanceCellOrderV1`; incomparable or contradictory ordering is infrastructure. If the product set is nonempty, the canonical first cause is its lowest total ordinal, not callback order; `PRODUCT_FAIL` binds that cause plus lexically ordered infrastructure blockers. If it is empty and infrastructure exists, `INFRA_PENDING` binds the blockers. If exact supersession is the sole terminal authority, `SUPERSEDED_NONPRODUCT` closes the attempt. Mixed product/infrastructure evidence always selects PRODUCT_FAIL, preserving blockers without making them causes. Same-attempt work may resume only to reconcile already-produced facts/effects; any product rerun after infrastructure repair uses a fresh attempt, order, fence, nonces, claims, and leases because the old stop intent is immutable.

Only after full official reconciliation and lane release may the controller create a separate same-head `DiagnosticExecutionPlanV1`. It binds source attempt/head/tree, every ordered planned cell, safety/reachability predicate, unique diagnostic work identities/nonces/leases, `releaseCredit=false`, and no acceptance transition. `DiagnosticCellOutcomeV2 = PASS_NO_ISSUE | ISSUE | UNREACHABLE_INFRASTRUCTURE | UNREACHABLE_SAFETY | UNREACHABLE_SUPERSEDED` is closed and every planned cell receives exactly one terminal outcome. An ISSUE—including contrast, accessibility, or UI-state failure—accumulates in `DiagnosticIssueSetV2` and must not stop, cancel, or skip any later safe reachable cell. Only typed infrastructure, an explicit safety fence, or exact supersession may stop further execution; every remaining cell is still recorded with its exact UNREACHABLE cause. Thus the nonaccepting pass continues through every issue it can safely reach and reports the complete reachable issue set.

`DiagnosticStartFenceV1(DIAGNOSTIC_RUN_ALLOWED | DIAGNOSTIC_STOPPED_INFRASTRUCTURE | DIAGNOSTIC_STOPPED_SAFETY | DIAGNOSTIC_SUPERSEDED | DIAGNOSTIC_COMPLETED)` is independent of the terminal official fence; active entries use dispatch mode `DIAGNOSTIC_NON_ACCEPTING`, so a stopped official fence cannot authorize them and does not make them unreachable. The controller closes the fence and persists `DiagnosticTerminalDecisionReceiptV1` through one protected CAS with outcome `COMPLETE_NON_ACCEPTING | INFRA_PENDING | SUPERSEDED_NONPRODUCT`, binding the full plan/outcome accounting, issue-set digest, head/tree, controller epoch, and all released leases. `DiagnosticEffectRecoveryReceiptV1` lets the active state writer apply exactly one matching ledger effect: COMPLETE enters `DIAGNOSTIC_COMPLETE_NON_ACCEPTING`, INFRA enters `INFRASTRUCTURE_PENDING` with exact fresh-diagnostic resume, and SUPERSEDED enters `SUPERSEDED`. Recovery classifies decision-only, applied matching, or divergent/ambiguous state and never promotes issues to acceptance. No diagnostic result can become card/release acceptance.

Every independently proven `PRODUCT_FAILURE_ELIGIBLE` terminal occurring after stop publication becomes `LateAcceptanceProductObservationV1`, permanently nonaccepting and never part of canonical cause selection. It is ordered after pre-stop sibling observations and before/with diagnostic findings, and must later be reproduced or explicitly superseded. The diagnostic sweep is mandatory before correction when the canonical product cause, a qualifying product-eligible pre-stop `AcceptanceSiblingObservationV1`, or a late product observation is `CONTRAST`, `ACCESSIBILITY`, or `UI_STATE`. For other causes it is optional. Infrastructure may mark unsafe diagnostic cells `UNREACHABLE`, but requires infrastructure repair/fresh attempt—not product correction—unless separate product evidence exists.

P00-C04 closes every official attempt with immutable `OfficialAcceptanceDecisionV1 = ACCEPT | PRODUCT_FAIL | INFRA_PENDING | SUPERSEDED_NONPRODUCT` and external `OfficialAcceptanceDecisionReceiptV1`. `ACCEPT` requires exactly one independently reconciled PASS for every frozen gating cell on the exact candidate/order/fence; zero missing, skipped, cancelled, stop-fenced, unreachable, incompatible, quarantined, product-failed, or infrastructure-pending cells; valid artifacts; released lanes; and a terminal refetch proving the exact frozen `DirectPrerequisiteEvidenceSetV1`, per-edge inclusion/compatibility/supersession dispositions, effective authorization, and invalidation generation remain current and unchanged. The decision receipt binds those exact digests/counts/edge identities and `nonreleaseSpecialEdgeCount=0`. The same protected controller CAS that persists the ACCEPT decision closes the official fence from RUN_ALLOWED to `COMPLETED_ACCEPTED`, so neither is a circular prerequisite or separately torn effect. `PRODUCT_FAIL` requires the canonical nonempty product-cause set and binds sorted infrastructure blockers without selecting them as causes. `INFRA_PENDING` requires an empty product-cause set and at least one infrastructure blocker, including an orphan controller terminal. `SUPERSEDED_NONPRODUCT` requires exact supersession authority or a changed prerequisite/authorization/invalidation fence and no product cause. No prose or aggregate green substitutes for this predicate.

For every decision, the active state writer independently verifies the controller receipt and terminal-refetches the same prerequisite-set, per-edge compatibility/supersession, effective-authorization, and invalidation digests before applying exactly one state effect under `OfficialAcceptanceEffectRecoveryReceiptV1`, binding expected state generation and canonical winner CAS. If an ACCEPT decision becomes stale between controller CAS and state effect, the state writer emits `AcceptanceEffectAuthoritySupersessionReceiptV1` binding the unchanged historical decision, exact changed authority/evidence, and current supersession generation; it creates no card receipt and appends only `SUPERSEDED`. `ACCEPT` otherwise persists immutable external `V23CardAcceptanceReceiptV2` and CAS-appends `ACCEPTED`; `PRODUCT_FAIL` creates no card receipt and appends `ACCEPTANCE_FAILED` with cause/blockers; `INFRA_PENDING` creates no card receipt and appends `INFRASTRUCTURE_PENDING` with exact blocker/fresh-attempt condition; `SUPERSEDED_NONPRODUCT` creates no card receipt and appends `SUPERSEDED`. P05-C01's ACCEPT variant extends this same one effect with the release-selection initialization CAS; it is not a second card effect. Recovery classifies decision-only, stale-ACCEPT superseded, ACCEPT decision+receipt without state, matching completed state, or divergent/mixed state; it retries exact missing effects, returns matching bytes, or quarantines. A card receipt binds the preexisting decision, order, start fence, prerequisite-set, effective-authorization, and invalidation digests but never the later transition digest, avoiding recursion. Every READY/direct-prerequisite, compatibility, selector, aggregate, or release consumer must also verify the unique canonical signed hash-chain inclusion proof and matching proof-recovery/currentness result for the `ACCEPTED` transition that binds that decision and receipt; an orphan receipt has no acceptance authority. No decision strands `ACCEPTANCE_RUNNING`. For C04 smoke the specialized multi-record effect controls and performs the same terminal fence. For C05, the generic effect is required for `PRODUCT_FAIL | INFRA_PENDING | SUPERSEDED_NONPRODUCT`; only C05 ACCEPT is rejected by it, and its cutover transaction performs the same terminal fence. `BootstrapC05AcceptanceDecisionReceiptV1` binds `officialDecision=ACCEPT`, `adoptionEligibility=PASS`, the ordinary official-decision digest, final semantic-equivalence receipt, bootstrap controller epoch, and complete pre-adoption hosted facts, and only that decision enters pending cutover/adoption. No accepted state grants READY, selector, aggregate, compatibility, or release credit without a durable ACCEPT decision, card receipt, canonical inclusion proof, and matching proof recovery/currentness result; the brief post-effect proof-recovery state grants no downstream credit and never permits an effect replay. No non-ACCEPT state exists without its matching decision/effect.

A correction group is legal only when every failure has the same renderer or component, demonstrated root cause, normalized signature family, and safe write fence. Similar appearance, neighboring screens, or one test file is insufficient. Unrelated corrections have distinct branches, commits, receipts, and reproof. Fix the causal owner, then rerun the exact invalidation consumers in `V23ImpactGraphManifestV1`; do not blindly reimplement or retest the entire transitive graph.

The official fail-fast cause is first in the correction queue. Every other pre-stop sibling enters `AcceptanceSiblingObservationV1` only when classified `PRODUCT_FAILURE_ELIGIBLE`; infrastructure remains solely in `AcceptanceInfrastructureReceiptV1` and is never sent to product correction. Product sibling observations are permanently nonaccepting and ordered by frozen ordinal then signature; late product observations follow, then diagnostic findings by owning-card ordinal, diagnostic ordinal, and signature. Reproduce each product observation/finding on the current targeted head before changing code; a nonreproducing result emits a supersession receipt. Reproducing observations obey demonstrated-root-cause grouping and separate-write-fence laws. Start the next official attempt only after the official cause, every still-reproducing required product observation/diagnostic group, and every blocking infrastructure condition are targeted green, repaired, or explicitly superseded.

<a id="v23-correction-protocol"></a>
## 8. Correction protocol

1. Preserve exact head, tree, command, plan or shard, environment, artifacts, semantic state, and normalized signature.
2. Reproduce with the cheapest deterministic check.
3. Identify the owning contract, renderer, or state—not merely the failing test.
4. Hydrate the causal dossier and exact invalidation row.
5. Make the smallest authority-consistent change inside its path fence.
6. Add or repair the regression test before broad acceptance.
7. Run cheap and targeted proof, then start a fresh official attempt on one clean candidate.
8. Emit one correction receipt linking cause, changes, tests, consumers, and superseded failure.

Never broaden scope, change a public contract, regenerate global brand authority, renew an accessibility exception, relax a threshold, or alter privacy and security claims incidentally. Use the owning card or a hashed corrigendum.

<a id="v23-milestones"></a>
## 9. Milestone integration order

1. A00, accepted S10.6 baseline, and V23 lineage lock.
2. CI, relation, test composition, and coverage corrections.
3. Production architecture, Swift concurrency, and Swift 6.
4. Data survival, writer, lifecycle, and released-data compatibility.
5. Shared kernel, routes, drafts, search, shell, App Lock, and encryption.
6. Labels and scan; Parties, contacts, Site roles, handoffs, and import.
7. Installation and standalone punch as independently accepted milestones.
8. Portable review, out-of-band response, and service-request intake.
9. Resources and cost; Parts & Stock.
10. Scheduling and My Day.
11. Service history and qualified reliability.
12. Templates, evidence details, signoff, and structured voice.
13. Adapter-profile truthful disposition, rating and support, and findability.
14. Application-wide inherited-brand, HIG, accessibility, and localization closure.
15. Exact iPhone release candidate and owner physical and App Store gates.

The selector may execute independent slices earlier when direct prerequisites and path fences permit; these are integration closure milestones, not a forced mega-branch.

<a id="v23-owner-gates"></a>
## 10. Owner-only gates

Routine implementation, tests, local commits, causal corrections, and in-scope GitHub Actions need no new approval. Owner action is required only when Codex cannot perform or truthfully infer it:

- final Phase 10/S10.6 acceptance or a Phase 10-owned path needed by the active V23 lane;
- signing, account, App Store Connect, TestFlight, legal, or export-compliance action;
- external provider, format, or licensing selection;
- real customer communication or public publication;
- physical Face ID, Touch ID, printing, or field-pilot evidence unavailable to the executor;
- release promotion, phased-release control, or materially new scope.

A missing owner action blocks only its affected card. Continue unrelated READY work. Never substitute simulator evidence for physical truth or invent credentials, demand validation, consent, delivery, identity, legal advice, or App Store outcomes.

<a id="v23-acceptance-receipt"></a>
## 11. Acceptance receipt

`V23CardAcceptanceReceiptV2` binds program/card/attempt, dossier, policy, contract, journey, relation, and authority digests; exact head and tree; controller/policy/workflow epochs; `OfficialAcceptanceDecisionReceiptV1`, frozen order, `COMPLETED_ACCEPTED` fence, zero-missing/nonpassing set, expected state-writer owner/generation and intended effect identity; branch and worktree; changed paths and symbols; the exact `DirectPrerequisiteEvidenceSetV1` digest, graph digest, expected/observed counts, every direct-edge identity/ordinal, source receipt, canonical accepted-transition inclusion proof, matching proof-recovery/currentness result, DIRECT_EDGE compatibility/supersession digest, terminal currentness result, and `nonreleaseSpecialEdgeCount=0`; exact `EffectiveExecutionAuthorizationV2` and invalidation-generation digests; schema and contract deltas; migration and lifecycle inventory; commands, results, and artifact digests; coverage, performance, and journey effects; brand, HIG, accessibility, localization, claims, and privacy impact; correction lineage; remaining owner conditions; and aggregate and invalidation registrations. The later ledger transition binds the same decision/receipt/effect/terminal-authority identity; `CardAcceptanceInclusionProofV1` nonrecursively proves that transition after the effect and its recovery receipt closes the publication crash point. C04 instead binds its smoke/base effect and C05 its four-state cutover effect under the same link law.

`CardAcceptanceInclusionProofV1` is a timestamp-free canonical byte function of the immutable accepted ledger transition. Its uniqueness key is `(programID,packageDigest,logicalLedgerID,ledgerGenesisDigest,cardID,attemptID,acceptedTransitionSequence,acceptedTransitionDigest,proofSchemaVersion)`, where `packageDigest` is exactly the lowercase SHA-256 named “Package digest” in the four-file V23 package manifest; its body binds the immutable genesis-to-transition hash path/accumulator, candidate head/tree, decision/card-receipt/specialized-or-generic-effect digests, and the transition's historical writer owner/generation/signature. Its canonical body contains no current physical locator/tip, re-emitter or recovery-actor identity, wall clock, nonce, or fresh signature. After any ACCEPT effect—including C04 smoke or C05 adoption—the current active state writer emits it; if a crash occurs first, the same writer or a later receipt-authorized successor generation, including P00-C05 after A00 adoption, runs `CardAcceptanceInclusionProofRecoveryReceiptV1 = ABSENT_AFTER_MATCHING_ACCEPTED_TRANSITION | PRESENT_MATCHING_CANONICAL_PROOF | DIVERGENT_OR_MULTIPLE`. ABSENT performs expected-absent publication of the deterministically re-derived byte-identical proof and rereads on a race; PRESENT returns the unique exact bytes; DIVERGENT_OR_MULTIPLE quarantines every candidate and blocks downstream credit. The separate recovery receipt binds the proof key/digest and observed proof set, recovery actor/current writer generation and authority/adoption chain, current physical locator and verified chain tip, classification/action/result, and authentication, so locator or ownership transfer never changes proof bytes. A crash after transition, after proof publication, or before recovery-receipt publication resumes here and never re-appends ACCEPTED. Every READY, compatibility, selector, aggregate, and release consumer requires both the unique canonical proof and a matching current recovery/currentness result; a durable accepted transition without recoverable proof remains non-crediting, never a reason to repeat the accepted effect.

A receipt is invalid after incompatible source, toolchain, authority, fixture, or state changes. Prose, an orphan receipt without `CardAcceptanceInclusionProofV1`, a green diagnostic, partial selection, skipped test, screenshot alone, dirty candidate, stale receipt, or unreviewed generation cannot accept a card.

Card receipts remain immutable and bind the historical exact head/tree on which they accepted. At a current candidate, the active state-writer authority emits `CandidateAcceptanceCompatibilityReceiptV1` only after independent verification. Its context is a closed tagged union: `DIRECT_EDGE(sourceCardID,targetCardID,edgeOrdinal,directGraphDigest) | SELECTOR_MEMBER(selectorID,memberCardID,memberOrdinal,selectorDigest)`. Both variants bind original receipt/head/tree, canonical `CardAcceptanceInclusionProofV1`, matching `CardAcceptanceInclusionProofRecoveryReceiptV1`/currentness, current candidate head/tree, accepted-head ancestry, governed path/contract/fixture/toolchain digests, every activated invalidation input, compatibility result, and any required narrow reproof or superseding receipt; mismatched context fields or ordinals fail closed. The direct-edge variant feeds only `DirectPrerequisiteEvidenceSetV1`, including edges such as P05-C01→C02 and P05-C02→C03 that have no selector context. The selector-member variant feeds only the named selector. Compatible historical receipts may be carried forward without rerunning the card only when the accepted-transition proof and matching recovery/currentness also verify; an incompatible, orphaned, proof-divergent, or changed member requires a targeted current-candidate successor reproof and the old receipt remains immutable. `AggregateAcceptanceReceiptV1` then binds the current candidate, selector ID/digest, ordered member IDs, original receipt, canonical inclusion-proof and proof-recovery/currentness digests, selector-context compatibility-receipt digests, successor reproofs, and a zero-missing/zero-incompatible result. “One head/tree” means this current aggregate evaluation, not pretending all original attempts shared one commit and not rerunning all 132 without an invalidation cause.

All card, compatibility, aggregate, correction, owner-evidence, release, and phase-integration receipts use `ReceiptAnchorEnvelopeV1`: first write immutable external content-addressed evidence only after the candidate commit exists, binding its digest, durable locator, retention class, producer/toolchain, and verification result. A receipt never enters the commit it names. If a repository mirror is required, copy the exact unchanged bytes as the sole authority-only change in a child anchor `H`; the receipt still binds parent candidate `I`, the mirror is excluded from every build/product input and semantic invalidation, and an external validation binds `H`, its sole `I` parent, and the one-file diff. Subsequent phase work may start from governed `H`, but acceptance and compatibility always distinguish evidence anchor from proved candidate. C04's optional mirror is created only on the phase branch after C04 accepts and never advances main; main remains exact C04 candidate `I` and the phase child becomes later ancestry. P05-C01's receipt remains external until candidate `I` is fast-forwarded and identically verified on main; `PhaseIntegrationReceiptV2` and P05-C02 bind `I`, not a mirror. Any later P05-C01/phase receipt mirror uses the authority-only evidence-child protocol and never substitutes for product/archive identity. This is the generic nonrecursive law; A00 is its specialized activation instance.

<a id="v23-release-closure"></a>
## 12. Exact release closure

Before the expensive P05-C01 archive/acceptance run, fetch `origin/main` and require it to equal the current `ExpectedMainIntegrationBaseV2` derived from accepted P00-C04 integration plus accepted drift/evidence history, and require that base to be an ancestor of the phase candidate. When fetched main instead advances through a legitimate compatible linear descendant, first persist `MainDriftReconciliationIntentV1` binding the exact branch before/after identities and sole legal non-force merge; then perform that merge; then write the immutable receipt and expected-base CAS/recovery. `currentUnacceptedIntegrationBranch` is closed to: `phase/v23-expansion` from activation until its first successful P05 phase integration when no C04 correction/repair is active; `fix/v23-p00-c04-<signature8>` while base status is `C04_CORRECTION_REQUIRED`; `infra-fix/v23-p00-c04-<signature8>` under exact tooling-infrastructure repair authority; or `release-fix/v23-<card-suffix>-<signature8>` after first P05 integration. A phase-branch reconciliation after C04 acceptance additionally binds the accepted C04 receipt/checkpoint, base compatibility, changed workflow/policy impact, and every required targeted C04 reproof; unresolved incompatibility moves to the appropriate C04 forward branch instead. The intent also binds old expected-base value/generation, observed main/ancestry, exact diff/governed-input impact, and proposed receipt digest/locator. Receipt bytes are inert until one expected-base CAS embeds them. `MainDriftReconciliationEffectRecoveryReceiptV1` classifies `BEFORE_EFFECT_AT_OLD_BASE | APPLIED_MATCHING_RECEIPT | DIVERGENT_OR_AMBIGUOUS`, retrying, returning identical evidence, or blocking. Crashes, replay, arbitrary descendants, rewritten ancestry, and torn receipt/base pairs fail closed. P05-C01 then requires one clean candidate, exact 132-receipt pre-P05 autonomous accepted set, all seven selectors, package/graph/relation/rebinding integrity, released-data corpus, hostile imports, recovery/Erase, App Lock, encryption, service request, reliability, My Day, stock, voice, J01–J14, FJ01–FJ17, coverage, uninstrumented performance, localization, inherited brand/HIG/accessibility, privacy/claims, Release-hook absence, and iPhone archive matrix. It earns its own 133rd autonomous acceptance receipt.

Immediately before main integration, fetch `origin/main` again. `PhaseIntegrationReceiptV2` requires main to equal current `ExpectedMainIntegrationBaseV2` and permits only a fast-forward of the unchanged accepted P05-C01 candidate. Acceptance already includes the C04-declared/validated `CandidateWorkflowEnrollmentProofV1` instance written by the authorized P05-C01 proof actor for every workflow/policy byte, with exact next digest or `UNCHANGED_FROM_ACTIVE_ENROLLMENT`. Persist kind-specific `IntegrationIntentReceiptV1`, push non-force, refetch, and verify remote/local main, archive/build, package, acceptance, governed inputs, workflow bytes, and the exact prior authority-only chain generation/head from which a release-fix descended. Every successful initial or successor P05 integration then uses one `PhaseIntegrationStateEffectReceiptV2` to atomically advance `ExpectedMainIntegrationBaseV2` and set `AuthorityOnlyEvidenceChainDispositionV1=NO_AUTHORITY_ONLY_CHILD_AT_PHASE_TIP(newProductTip,newGeneration)`. The initial effect requires an expected-absent chain cell; a successor consumes the exact prior NO_CHILD/TIP generation and preserves the complete old evidence ancestry/history without treating it as the new product identity. `PhaseIntegrationStateEffectRecoveryReceiptV2` classifies both records as old, partial-matching, complete-matching, or divergent; it completes only exact partial effects and never leaves base and chain split. P05-C01 then remains `HANDOFF_PENDING` until that effect, prepared policy-activation authority-cell CAS, and matching enrollment-cell/rollover receipts close. Only capacity-counted guard work may run first under the already configured hard mechanism; newly integrated candidate workflow bytes may not checkout or run product/release commands before activation. Activation or rollover transient infrastructure failure enters resumable P05-C01 `INFRASTRUCTURE_PENDING`; only exact same-byte repair returns to HANDOFF_PENDING. A deterministic proof/conformance contradiction supersedes P05, selection, and the matching refresh authority cell and requires an exact release-fix plus fresh full acceptance. Only phase receipt + matching base/NO_CHILD effect + committed activation + `UNCHANGED_CONTROLLER | ACTIVE` rollover disposition, matching rollover receipt when ACTIVE, and active enrollment-cell token yields COMPLETE and unlocks P05-C02.

Every retry first searches append-only expected-base, evidence-chain, incident-resolution, and integration-finalization history for the exact attempt, intent, authorization, intended child, receipt, and state effect. If a finalized match exists and current main equals that child or a fully accepted linear authority-only descendant chain with unchanged governed inputs/archive, return the same success as `FINALIZED_MATCHING_IN_HISTORY_WITH_ACCEPTED_DESCENDANT`; do not misclassify a delayed callback as divergence. Otherwise classify current remote state `ABSENT_AT_OLD_BASE | APPLIED_MATCHING_INTENDED_CHILD | DIVERGENT_OR_AMBIGUOUS`: retry the same push, finish the missing receipt/multi-record effect after revalidation, or quarantine. This applies to C04 checkpoint, P05 phase, authority-only evidence, and postrelease fix integrations. It never blesses an arbitrary descendant. Drift, non-fast-forward topology, or changed build inputs fail closed; never rebase, cherry-pick, amend, or merge into an artifact-bound accepted candidate. P05-C02/C03 operate only at the identical product tip, a verified accepted authority-only chain, or an exact incident-resolution rejoin and revalidate the full chain before every hydration/external action.

After that point, P05-C02 release-ledger evidence and P05-C03 monitoring receipts use separate authority-only evidence branches. The chain cell already exists because every P05 phase integration atomically initialized/reset it to `NO_AUTHORITY_ONLY_CHILD_AT_PHASE_TIP(productTip,generation)`. Before push, `AuthorityOnlyEvidencePreflightReceiptV1` binds the exact one-child diff; allowlisted secret-free evidence paths; rejection of every source/project/workflow/test/fixture/package/asset/localization/entitlement/configuration/generated-input path; unchanged governed-input/archive digests; and completed secret plus customer/work-data scan. The integration intent binds that preflight. After push/refetch, exact diff, governed-input/archive comparison, and scans repeat before `AuthorityOnlyEvidenceIntegrationReceiptV2`. `AuthorityOnlyEvidenceIntegrationEffectReceiptV1` atomically CASes `ExpectedMainIntegrationBaseV2` and `AuthorityOnlyEvidenceChainDispositionV1` from the same predecessor to the new receipt/head. Its recovery classifies old/partial-matching/complete-matching/divergent state; exact partial effects may be completed, complete matching returns identical evidence, and divergence blocks. Hydration is invalid unless integration receipt, main head, expected base, and chain disposition all agree. The normal chain value is `NO_AUTHORITY_ONLY_CHILD_AT_PHASE_TIP(productTip,generation) | TIP(receiptDigest,head,generation,incidentResolutionDigest?)`; NO_CHILD applies only when main is exactly the current product tip. No tranche may depend on the integration receipt it is currently producing. The sole compatibility exception is P05-C03's exact `MonitorAuthorityChildFinalizationReceiptV1`: its observation was authorized against the predecessor and its terminal state is applied only after the byte-identical one-child publication advances base/TIP; it grants no readiness or acceptance and cannot excuse any other chain change.

Any contradiction discovered by the mandatory post-push refetch means main has already advanced while expected base/chain remain at the parent. Create redacted `AuthorityOnlyEvidencePostPushViolationReceiptV1`, a closed tagged union of `SECRET_OR_CUSTOMER_DATA_EXPOSURE | PROHIBITED_PATH_DELTA | GOVERNED_INPUT_DELTA | ARCHIVE_IDENTITY_DELTA | SCAN_OR_POLICY_CONTRADICTION`, binding path/digest, violated preflight predicate, intended child, chronology, and no prohibited bytes. `AuthorityOnlyEvidenceExposureReceiptV1` is the specialized exposure subtype and contains only path/digest/data-class and chronology—never the exposed bytes. In the same generation-fenced state-writer effect, create or advance `AuthorityOnlyEvidenceIncidentStateV1 = OPEN_RELEASE_BLOCKED | OWNER_REMEDIATION_PENDING | PRIVACY_SECURITY_REVIEW_PENDING | LEGAL_DECISION_PENDING | RESOLUTION_EFFECT_PENDING | CLOSED_FALSE_POSITIVE_REJOINED | CLOSED_REMEDIATED_REJOINED | CLOSED_RELEASE_ABANDONED`, record stop/cancel/isolation effects, and keep release/evidence work blocked. `AuthorityOnlyEvidenceIncidentEffectRecoveryReceiptV1` uses expected-old/applied-matching/divergent recovery so violation receipt, optional exposure subtype, incident state, and effects cannot tear. Exposure requires credential rotation/revocation when applicable and exact privacy/security/legal review; other tags require the matching owner/security/release review but never inherit exposure claims. `AuthorityOnlyEvidenceIncidentDispositionReceiptV1` binds the violation tag, owner authority, applicable privacy/security/legal findings, containment, retention/delete/export consequences, exact refs/artifacts, whether retained history is permissible, whether protected-ref history remediation is required, and remaining obligations. The disposition is necessary but cannot itself rejoin main/base/chain.

When an authority-only evidence incident opens for a governed phased-release identity, its prepared incident-effect intent already contains the source effect ID. The same generation-fenced canonical-store transaction always creates the source outbox and exact PENDING `ReleaseSafetyPauseTriggerEvaluationV1` keyed only to that intent/effect ID, incident generation, release identity, and monotonic source sequence; it does not condition the obligation on a contemporaneous provider query or name its not-yet-produced recovery receipt. `ReleaseSafetyPauseSourceEvaluationGenesisRecoveryReceiptV1` proves the all-or-none source/PENDING tuple, while `AuthorityOnlyEvidenceIncidentEffectRecoveryReceiptV1` later binds/verifies the complete incident tuple before the source can merge into the current frontier. Independent refetch/reconciliation advances that frontier to ACTIVE_CONFIRMED, CONFIRMED_NONACTIVE, or unresolved EVALUATING/UNKNOWN under the closed rules above. Its separate control-cell recovery may continue even while evidence integration and ordinary release effects remain blocked. A resulting trigger contains no permission to pause until fresh explicit owner authorization, and incident closure never silently resumes a paused release or discards an unresolved source/frontier.

`AuthorityOnlyEvidenceIncidentResolutionV1` is closed to `FALSE_POSITIVE_FINALIZE_ORIGINAL_CHILD | REMEDIATED_FORWARD_REJOIN | AUTHORIZED_HISTORY_REMEDIATION_REJOIN | ABANDON_RELEASE_BLOCKED` for every violation tag. False-positive finalization requires renewed independent proof of every failed predicate, creates the delayed `AuthorityOnlyEvidenceIntegrationReceiptV2` for the unchanged original intended child, and atomically advances base/TIP. Forward rejoin requires the applicable owner/privacy/security/legal approvals, a sanitized or otherwise compliant descendant, exact diff and scans, rotated/revoked credentials as applicable, re-established governed-input/archive identity, proof that retained ancestry is permissible, and `AuthorityOnlyEvidenceRemediationReceiptV1`; customer/work data or another prohibited object that must be removed from history can never use this outcome. Authorized history remediation is an exceptional owner-gated operation with exact old/new ref/object mapping, protected-ref coordination, applicable legal/security/privacy approval, retained audit evidence, and clean refetch; it is never inferred from ordinary correction and never runs by default. Abandon leaves base/TIP at their safe predecessor, keeps release blocked, and grants no fresh attempt.

`AuthorityOnlyEvidenceIncidentResolutionEffectReceiptV1` atomically applies the chosen closed incident state plus matching `ExpectedMainIntegrationBaseV2` and chain disposition: false-positive or approved remediation ends at a verified `TIP(...,incidentResolutionDigest)`; abandonment changes incident state only and preserves the safe predecessor. `AuthorityOnlyEvidenceIncidentResolutionEffectRecoveryReceiptV1` classifies old/partial-matching/complete-matching/divergent state and completes only exact intended effects. Hydration or a fresh evidence attempt remains prohibited until a rejoin outcome is fully applied and current main/base/TIP agree. Ordinary forward correction cannot declare an exposure erased, any violation contained, or a child compliant, lawful, or safe.

Each accepted authority-only child or fully applied incident rejoin advances `ExpectedMainIntegrationBaseV2` and `AuthorityOnlyEvidenceChainDispositionV1.TIP` in one recoverable effect but not product build identity, and may parent the next tranche. The accepted product candidate remains build/release identity even when an evidence-only child is main. Any post-push violation blocks release until its typed disposition and resolution effect close with a safe current main/base/TIP; a governed-input or archive-identity delta also requires the named product/release correction and fresh applicable proof before rejoin. Evidence branches never return to `phase/v23-expansion` or manufacture product acceptance.

A product defect found after the first release integration never returns code to the old phase branch and never rewrites the shipped candidate. Mark the exact affected receipts/candidate superseded through declared invalidation; start `release-fix/v23-<card-suffix>-<signature8>` from the current validated main/evidence-chain tip; hydrate the causal owner and exact consumers; implement the smallest correction; and run targeted reproof plus a complete fresh P05-C01 exact-candidate gate. That branch preserves all authority-only evidence ancestry. Its starting main tip is the current `ExpectedMainIntegrationBaseV2`; any subsequent main advance uses `MainDriftReconciliationReceiptV2` whose `currentUnacceptedIntegrationBranch` is that exact release-fix branch, never `phase/v23-expansion`. Only the unchanged newly accepted correction candidate may fast-forward to main under a new `PhaseIntegrationReceiptV2` and the intent/effect-recovery protocol. This is forward-fix lineage, not reuse of the old archive or release credit.

If owner delay makes the selected release artifact or `ReleaseMutationInputV1` lack the required two-hour margin, derived `ReleaseTransportUsabilityDispositionV1(EXPIRED_REFRESH_REQUIRED)` blocks only transport-consuming P05-C02 effects while canonical `ReleaseTransportSelectionV1` remains the exact predecessor value/generation. Refresh is legal only after P05-C01 is COMPLETE at that exact accepted candidate; no earlier state can authorize it. `ReleaseTransportRefreshAttemptAuthorityCellV1 = EMPTY(selectionGeneration,refreshAttemptEpoch) | ACTIVE(selectionGeneration,refreshAttemptEpoch,attemptID,token) | TERMINAL(selectionGeneration,refreshAttemptEpoch,decisionDigest,terminalKind)` is the one-active-refresh truth. Selection initialization creates EMPTY at epoch 0. An expected-token CAS creates one ACTIVE attempt; a second is rejected. A terminal may advance to the next ACTIVE epoch only through fresh one-shot authorization and may never be deleted, reused, or reopened. Create `ReleaseTransportRefreshAttemptV1` against unchanged accepted P05-C01 and the exact `(selectionGeneration,refreshAttemptEpoch)`. Expiry/margin evidence plus byte-identical accepted-main source, workflow, test, toolchain, configuration, fixtures, package, and governed inputs derives bounded authorization/fence for the complete artifact proof, immutable handling, and refresh decision only. The attempt uses C04's effective-capacity dispatch/classification/claim/result laws without reopening historical product acceptance merely for elapsed time.

`ReleaseTransportRefreshDecisionV1` has closed outcomes `REFRESHED | INFRA_PENDING_NO_ACCEPTANCE_CHANGE | PRODUCT_PROOF_CONTRADICTION | SUPERSEDED`. It can be persisted only after every authorized refresh cell/result/reconciliation and lane terminal is accounted for. Its canonical-winner order makes any authoritative `PRODUCT_PROOF_CONTRADICTION` dominate success; REFRESHED is legal only with zero contradiction and no live/unknown result. It binds exact attempt epoch, authority-cell token, candidate, predecessor selection/P05 state generations, reconciled cells/artifacts, and winner proof. Under `ReleaseTransportRefreshEffectRecoveryReceiptV1`, REFRESHED atomically terminalizes that epoch, CASes the old selection to `REFRESHED_CURRENT` with next generation, persists `ReleaseTransportRefreshReceiptV1`, and creates the new generation's EMPTY refresh cell; INFRA terminalizes only the auxiliary refresh epoch, changes neither P05-C01's historical/COMPLETE state nor selected transport, leaves transport-consuming P05-C02 work blocked, and requires fresh authorization for the next epoch; PRODUCT_PROOF_CONTRADICTION persists `ReleaseTransportRefreshContradictionReceiptV1` and uses the general three-record P05/selection/refresh-cell supersession effect described below to terminalize the exact epoch; SUPERSEDED records no competing effect only when exact matching supersession already exists.

An independently verified late contradiction for the same accepted candidate is never discarded as stale merely because REFRESHED already won. `LateReleaseTransportContradictionReceiptV1` binds its original attempt/result inclusion, why it was absent from the earlier closure, current selection/P05/refresh-cell generations, and product-proof contradiction. The active state writer rebases only the safety effect—not the old success—onto the current `ORIGINAL_VALID` or `REFRESHED_CURRENT` generation. `ReleaseTransportSupersessionEffectReceiptV1` is one serializable transaction across P05 state, selection, and the matching refresh authority cell: it binds the exact allowed predecessor state/generation, appends its `→ SUPERSEDED` transition, advances selection to `SUPERSEDED(reason,authorityDigest,nextGeneration)`, and terminalizes/revokes any ACTIVE refresh epoch. Refresh/late contradiction requires predecessor COMPLETE; other declared P05 invalidations may consume exact ACCEPTED, HANDOFF_PENDING, INFRASTRUCTURE_PENDING, or COMPLETE. Every P05 invalidation and every refresh-admission writer competes on that same tuple, so neither a new ACTIVE refresh nor a success effect can appear after supersession wins. `ReleaseTransportSupersessionEffectRecoveryReceiptV1` classifies all three records as old, partial-matching, complete-matching, or divergent and completes only exact partial effects. Retry classification is `BEFORE_EFFECT_AT_EXPECTED_GENERATION | APPLIED_MATCHING_CANONICAL_WINNER | APPLIED_MATCHING_LATE_SAFETY_SUPERSESSION | DIVERGENT_OR_AMBIGUOUS`; crash-before/after attempt-cell/receipt/CAS, active-refresh cleanup, duplicate attempt admission, both callback orders for success versus contradiction, and replay tests are mandatory. A successful refresh grants no card credit; any governed-input change requires a fresh P05 candidate. Every P05-C02 transport-consuming authorization binds and immediately rechecks the canonical selection generation, refresh-cell epoch/state, absence of an active/unknown refresh contradiction, and margin before effect.

When a product-proof or other P05 supersession effect commits for a governed phased-release identity, its prepared supersession-effect intent already contains the source effect ID. The same state-writer canonical-store transaction always creates the source outbox and exact PENDING `ReleaseSafetyPauseTriggerEvaluationV1` keyed only to that intent/effect ID, predecessor tuple, intended successor generation, release identity, and monotonic source sequence; it never circularly names its not-yet-produced recovery receipt. `ReleaseSafetyPauseSourceEvaluationGenesisRecoveryReceiptV1` proves the all-or-none source/PENDING tuple, while `ReleaseTransportSupersessionEffectRecoveryReceiptV1` later binds/verifies the complete supersession tuple before the source can merge into the current frontier. Provider-query failure, non-ACTIVE truth with a possible START/RESUME effect, or a control-cell outage cannot erase that obligation and does not block truthful product supersession; the frontier remains EVALUATING/UNKNOWN until read-only reconciliation reaches ACTIVE_CONFIRMED or a valid CONFIRMED_NONACTIVE terminal. This does not add the control cell to the P05/selection/refresh atomic tuple. `ReleaseSafetyPauseAuthorityEffectRecoveryReceiptV1` then independently and idempotently moves only the matching control epoch toward owner-pending containment after the current ACTIVE_CONFIRMED frontier emits its trigger, while the superseded product tuple remains final. A matching in-progress provider effect is reconciled before any safety-pause authorization, never cancelled or retried by inference.

P05-C02 requires owner physical-iPhone and uncoached usability, truthful App Store privacy, accessibility, encryption, and metadata decisions, signing, archive, TestFlight, submission, and explicit promotion. The non-native compatibility smoke is safety-only, not iPad support. P05-C03 monitors the accepted release and 30-day outcomes without silently activating telemetry, marketing, or remote services.

## 13. Completion response

When implementation is genuinely complete, report accepted head and tree, package and receipt digests, exact automated and physical evidence, owner actions completed, release disposition, and deferred and validation-only items. Never say complete while a required shipping card is unaccepted, silently disabled, or blocked without an explicit scope change.
