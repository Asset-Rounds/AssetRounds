# Repository Instructions

Implement only the one current card selected in `docs/execution/CURRENT_TASK.md`. When that file explicitly enables phase and program autopilot, the narrow same-phase transition and phase-boundary integration described below are also authorized; adjacent improvements are not.

## Build route

Author on Windows. Compile, test, and run the iOS Simulator only through the task-named GitHub Actions macOS workflow. “Local” describes device-local app data, not the build host. Never claim an iOS build/test result from Windows. XcodeBuildMCP and an owner-operated Mac are not required.

## Authority

1. `docs/execution/CURRENT_TASK.md` controls the current card's outcome, paths, environment, allowed GitHub operations, verification budget, immutable program map and phase-main base `P`, phase-autopilot state/span, and boundary-integration authority.
2. The exact SHA-256-pinned `docs/product/BUILD_PLAN_V4.md` controls product invariants and phase order.
3. The exact SHA-256-pinned `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` controls only the selected card's sequence, files, acceptance, tests, and next card where consistent with the plan.
4. Existing tests and code describe the baseline but cannot expand scope.

The runbook is a catalog, not blanket authority. Execute only the card ID currently selected in CURRENT_TASK. Never combine, skip, reorder, or pre-implement cards. Phase autopilot may select only the runbook's immediate next card when it is inside the exact authorized same-phase span and the current card already has accepted green exact-head CI. Program autopilot may cross a phase boundary only after the closed phase branch, exact fast-forward `main` update, and exact-main CI all pass; it then selects only the next phase's first runbook card on the frozen next branch.

`docs/execution/DECISIONS.md` is history, not active authority. A decision becomes active only after it is incorporated into the task and, when needed, the plan hash. A task/plan-requested delta from current code or tests is not a conflict. An invariant outside the named delta is preserved while Codex diagnoses and corrects the current card; precedence never authorizes silently overriding a product invariant.

## Fail-closed preflight

Before editing or any mutation, resolve each required preflight fact from the repository, Git history, GitHub history, the pinned plan/runbook, and the current card. A blank/`UNSET` field, unresolved placeholder, pin mismatch, selected-card mismatch, incomplete predecessor, overlapping unowned dirty work, or temporarily unavailable route is a read-only hold: re-read, re-fetch, wait, gather diagnostics, or apply the authorized mechanical authority/correction update; it is not an approval-message gate and does not authorize guessing product truth. A complete committed HANDOFF for a still-selected boundary card remains immutable, but program autopilot resumes its idempotent boundary state machine and may append later corrective evidence after green verification. Hosted-CI and product verification failures use the persistent recovery loop below without another owner message.

For an ordinary card G0, verify the phase branch and integrated/card-base SHA `M`, observe task-start authority HEAD `A`, and prove `M..A` contains only the declared authority documents. Freeze phase-main base SHA `P`: S0 uses bootstrap `B`; each later phase uses the preceding phase's accepted exact-main phase-close/verification SHA. `P` stays byte-for-byte unchanged throughout the phase and is the boundary comparison value for `origin/main`; `M` is the preceding card's accepted implementation or verification head, or `P` for the phase's first card. The historical S0.1 recovery set `M` to failed `I2` and required its authority head to be the direct child changing exactly the four recorded authority documents; it did not change `P`. Initial S0.1 retains the explicit N/A predecessor-iOS-run statement, while recovery evidence records both failed exact-head runs. At phase start, owner- or program-autopilot-prepared `A` normally changes only hydrated `CURRENT_TASK.md`. A same-phase `A` may change only prior HANDOFF plus immediate-next CURRENT_TASK. The historical S0 boundary authority head `R` directly parents completed `C`; `C..R` is exactly its four recorded authority documents, while historical `P`, `M`, `A`, `I3`, accepted card CI, HANDOFF, `C`, failed integration runs, and `R` remain immutable. An infrastructure G0 preserves historical M/A and instead proves immutable E/F/K gate identity, `K` directly parents `F`, `F..K` contains only the declared recovery paths, all pins/counters match, and E..K has no protected product change; an explicit execution-policy amendment may also replace selector watchdog values with the exact repinned schema. `K` is observed only after commit and never self-recorded. `A` is observed at ordinary card G0 and recorded later; never self-record it.

At G0, validate the new task's expected selector object and tier against the frozen runbook and workflow schema. `Scripts/ci-selection.json` may be absent for initial S0.1 or may still equal the accepted predecessor card during a later transition; that state is not next-card evidence and is not a G0 mismatch. An explicit execution-policy/watchdog migration may replace it atomically with the exact repinned runbook/current-task object. Otherwise, if G0 passes, creating/replacing it with the new exact object is the first implementation-support mutation. It must match the current task before implementation commit or dispatch.

The Codex GUI/session posture is Full access: `sandbox_mode=danger-full-access`, `approval_policy=never`, and network enabled. Broader filesystem, network, connector, plugin, credential, or external-write capability is not a blocker and does not expand product scope: use only the current card, frozen program order, named repository/refs/workflow, and the recovery paths below. When a required service or runner route is temporarily unavailable, wait and retry the exact route; never substitute an unpinned route. Windows is expected and the named macOS workflow remains the only iOS build/test route.

## Autonomous verification and correction recovery

No additional owner approval message is required for implementation or verification recovery within the current card, frozen phase/program order, named repository, refs, workflow, and allowed product or CI-harness paths.

Candidate history is append-only and has no retry or cardinality ceiling per exact workflow/ref/head. Workflow concurrency uses `cancel-in-progress: false`. If any candidate is green with complete required evidence, accept it and do not dispatch another. If one candidate is queued or running, wait for it. Otherwise inspect all terminal candidates, their complete logs, and available artifacts, then dispatch exactly one new `workflow_dispatch` candidate at the same branch ref, exact head, inputs, pinned runner/toolchain/Simulator selector, and tier. Each fresh-runner redispatch creates a new run ID; never use Actions rerun on a failed run ID. Repeat until green or until evidence identifies a repository correction. Terminal count, recurrence, overlap, ambiguity, missing artifacts, and a previously used failure or repair label are provenance, not authority consumption or terminal conditions.

For a diagnosed product, compiler, test, or card-acceptance failure, create one direct-child task-scoped correction commit changing only the current card's allowed product paths and required CURRENT_TASK evidence/pins, then repeat exact-head verification. Successive implementation heads are `I`, `I2`, `I3`, and so on; there is no numeric correction or rerun ceiling. The accepted product implementation `E` is the first such head with green exact-head required evidence. Never weaken the card outcome, GOLDEN, ALT-1, selectors, tests, watchdog values, evidence, or acceptance.

For a diagnosed hosted-runner, pinned-toolchain/runtime, exact-Simulator, Actions service/transport, or lane-harness failure, either redispatch the same exact head on a fresh runner or create one direct-child `K(n+1)` containing one minimal diagnosed correction. Successive K commits may refine the same operation, mechanism, or repair. Failure/repair records, hashes, trailers, prior candidates, and their labels remain append-only audit provenance rather than consumption gates. There is no K, correction, or redispatch ceiling. When existing evidence is insufficient, first gather another exact-head fresh-runner candidate; if the harness still cannot expose the cause, one diagnostic K may add non-accepting evidence collection before the next run.

Each correction commit contains one diagnosed operational delta so its result remains attributable; successive commits may address successive evidence. A K may change only required CURRENT_TASK evidence/pins and the smallest causally required CI-harness subset of `.github/workflows/ios-ci.yml`, `Scripts/build-smoke.sh`, `Scripts/test-smoke.sh`, `Scripts/ui-smoke.sh`, and `Scripts/run-with-timeout.sh`. Preserve product/project/test/fixture/asset bytes, selector semantics, pinned environment, exact product commands and destinations, timeout exit behavior, evidence/checksum/upload enforcement, and raw aggregate accounting. The workflow's cleanup watchdogs are exact and non-accepting: Simulator readiness `900` seconds; N8 `300/600/900/0/2400`, P12 `300/600/900/900/3300`, and F25 `300/900/1200/1800/4500` for setup/build/unit/UI/total; and a 90-minute job watchdog. Exceeding one keeps the run non-green and feeds the same evidence-driven retry/correction loop; it is not an authority limit.

Infrastructure commits retain the seven legacy trailers—`Infrastructure-Gate`, `Infrastructure-Support-Index`, `Infrastructure-Failed-Run`, `Infrastructure-Failed-Head`, `Infrastructure-Failed-Ref`, `Infrastructure-Failure-Signature`, and `Infrastructure-Repair-Key`—when the current task uses them. G0 derives positive monotonic indices and verifies direct parent, run/ref/head provenance, declared paths, and protected product equality. Matching or overlapping signatures and repair keys may recur when later evidence shows the prior correction was insufficient; prose may not falsify or delete history.

CURRENT_TASK preserves ordered ref roles and exact accepted predecessors. At a boundary, verify the phase first and then main. Re-fetch immediately before every mutation. Advance phase and main only by a verified non-force fast-forward to the exact green verification head; if a ref has already reached that head, resume without another push. Unexpected movement triggers read-only fetch, ancestry/provenance inspection, and waiting or recreation of unaccepted work as a direct child of the valid authorized lineage; never overwrite or silently reconcile unrelated history. Accept only green exact-head CI with complete checksummed artifacts. Record `E` as product implementation evidence and any distinct `K` as verification/integration/next base. If phase or exact-main verification exposes a product failure before the next phase starts, the boundary card stays active for successive task-scoped correction heads; preserve prior HANDOFF entries and append corrective evidence after green phase and exact-main verification.

Historical S0 evidence remains factual and immutable: K1 `0da62482db7e3f90742876ef05574de9e9569e32`, K2 `400f8827f5451b9a44f4d846340b51377554069d`, and their failed runs are never accepted or rerun by run ID. K3 `e569951909c2ee8aff4253bae0d442550c229a16` passed exact-head phase run `31590956549`; exact-main K3 run `31591804256` failed only the historical 300-second Simulator-readiness watchdog. That failure now resumes through this persistent loop. I3 remains S0.1 product implementation evidence until a diagnosed product correction is actually required.

A required path outside the current card or CI-harness recovery subset remains out of scope; gather evidence or continue with an in-scope correction rather than weakening acceptance. The loop never authorizes force-push, merge commit, PR, signing, TestFlight/App Store upload, deployment, submission, repository settings, secrets, or S9.2/S9.3.

## Scope

- Read the current task, exact pinned plan, and exact selected pinned runbook card before editing. Do not enter `/plan`; the active card is already planned.
- Touch only its allowed paths and stay inside its file envelope.
- Paths in `CURRENT_TASK.md` are fully expanded individual repository-relative file paths. Apply the runbook's mechanical app-root rule before G0: app-module shorthand such as `Domain/...` becomes `FieldEvidenceApp/Domain/...`, and every brace/set expression becomes individual files. Globs, directory roots, brace/set expressions, and `/**` are invalid in a hydrated task. Never guess a location. Forbidden paths win. Count each created, changed, deleted, or renamed repo path once. A file-cap override is valid only when the exact selected frozen runbook card explicitly states it; self-authored CURRENT_TASK wording cannot raise a cap.
- Preserve user changes; do not reformat or refactor unrelated code.
- Reuse current navigation, models, design tokens, dependencies, fixtures, checked-in project, and shared scheme.
- Device-local and hosted product behavior require separate task IDs; hosted CI is verification, not hosted product scope.
- When explicitly authorized, stage only task-owned paths, commit, push the exact phase branch, dispatch/observe the named workflow, and download its artifacts. At an accepted phase boundary with program autopilot enabled and the boundary integration flag `yes`, Codex may update `main` only by a verified non-force fast-forward from the exact phase-close or verification commit, dispatch/accept exact-main CI, create only the frozen next phase branch from that green `main`, and hydrate only its first card. A non-fast-forward, unexpected ref movement, merge conflict, or mismatched CI head triggers re-fetch, ancestry/provenance inspection, waiting, or recreation of unaccepted work as a direct child of the valid lineage; never force, merge, or silently reconcile unrelated history. PR creation, signing, TestFlight upload, App Store actions, deployment, and other external mutation remain forbidden unless a separate release task names them.
- Never use broad staging or include pre-existing/unowned changes.
- Do not add packages, targets, capabilities, entitlements, permissions, migrations, APIs/backends, accounts/auth, analytics, payment behavior, public metadata, deployment, or App Store submission unless the task explicitly names it.
- Later plan sections constrain compatibility but do not authorize artifacts now. Do not add unused entities, protocols, services, routes, generalized seams, stubs, or fixtures for a later slice.
- Owner-provisioned `.codex/config.toml` and `DECISIONS.md` are read-only unless explicitly named. Static authority documents remain stable during ordinary card implementation, but an explicit owner-directed execution-policy amendment may update them atomically with their pins. A diagnosed K may update CURRENT_TASK evidence/pins and the smallest causal subset of the five allowlisted CI-harness paths in the recovery policy; no prior K permanently freezes those paths or consumes later correction authority. CURRENT_TASK otherwise remains read-only from G0 through accepted card CI except for an authorized transition, next-phase authority commit, or exact recovery record/repin delta. Full machine/network/tool access never expands product scope.
- Append-only `docs/execution/HANDOFF.md` is always authorized bookkeeping and excluded from the file cap, but it is never part of the implementation commit whose SHA CI verifies.
- `Scripts/ci-selection.json` is the only standing implementation support exception. Each card may replace it with the exact runbook/CURRENT_TASK selector object; it is excluded from the cap and grants no product scope. No other script is a standing exception.
- After accepted product implementation and green exact verification-head CI, append the handoff. Same-phase transition still requires enabled autopilot, flag `yes`, immediate next card inside the span, exact HANDOFF-plus-CURRENT_TASK, and fresh G0. At a boundary create/push HANDOFF-only `C`, then verify phase and exact-main in order. Reuse any green matching candidate, wait for one active candidate, and otherwise inspect terminal evidence and dispatch exactly one fresh-runner candidate; repeat or create a diagnosed direct-child K until both refs have green exact-head evidence. Reprove remote phase and main at the accepted verification head. For non-S9 next authority, re-fetch and resume an absent, accepted-head, or exact-A state; investigate other collisions and recreate only unaccepted work as a direct child of the valid lineage. Hydrate CURRENT_TASK-only A from the accepted head, non-force create or fast-forward the exact next branch, then run fresh G0. Never force, merge, repair divergence by overwrite, or duplicate active work. Continue through S9.1 only; S9.2/S9.3 remain owner-only.
- S0's failed `main@C` run `31567299022`, failed `main@R` run `31571125674`, aggregate-overrun K1 phase run `31579936670`, and setup-overrun K2 phase run `31583957094` are immutable historical evidence and must never be rerun or called successful. K3 `e569951909c2ee8aff4253bae0d442550c229a16` passed exact-head phase run `31590956549`; exact-main K3 run `31591804256` failed only its historical 300-second Simulator-readiness watchdog. The owner-directed direct-child K4 policy migration may update authority evidence/pins, workflow watchdog/concurrency values, and the exact selector watchdog object while preserving product/project/test/fixture/asset bytes. K4 or later diagnosed recovery then uses the smallest causal CI-harness subset. The first head with green exact-head phase and exact-main evidence becomes the accepted S0 verification/phase-close head and S1 base while I3 remains S0.1 product implementation evidence.

## Verification

- Use only the task's exact phase branch, workflow/ref, runner label, expected Xcode version/build, project/shared scheme/configuration, Simulator model/OS selector, smoke scripts, and validated selector object. Manual dispatch uses the named branch ref, never a raw SHA: first verify it points to the expected verification head (`E` normally or eligible `K`), allow no intervening push, and accept only a matching run `head_sha`.
- Do not run `xcodebuild` locally on Windows. Run the smallest relevant test bundle and golden XCUITest/Simulator path on the GitHub macOS runner.
- A card pass comes from a successful run whose `head_sha` equals an implementation sequence head `I`, `I2`, `I3`, and so on; historical S0.1 uses accepted product head `I3`. When infrastructure recovery is required, exact green `K` is verification evidence only: prove `E..K` contains solely the declared nonproduct recovery paths, retain `E` as implementation evidence, and record/use `K` as the accepted verification/integration head and next base. A phase integration pass additionally requires successful UI-enabled CI at the exact non-force-fast-forwarded `main` head.
- Use the exact task tier and `ci-selection.json` values from runbook Section 6: N8 = 300/600/900/0/2400 seconds with UI false/empty; P12 = 300/600/900/900/3300 with UI true/one selector; F25 = 300/900/1200/1800/4500 with UI true/one selector. Simulator readiness is 900 seconds, the job watchdog is 90 minutes, and workflow concurrency uses `cancel-in-progress: false`. The workflow input must match. Step and total watchdogs remain non-accepting evidence boundaries; exceeding one feeds the same retry/correction loop rather than ending authority. Missing required logs, `.xcresult`, or enabled-UI screenshot fails the candidate and triggers evidence gathering, retry, or correction.
- Inspect the complete failing step and artifacts. For a product/card failure, create one diagnosed task-scoped direct-child correction and rerun exact-head CI; repeat through `I2`, `I3`, and later heads until acceptance passes. For a hosted-infrastructure or harness failure, use the persistent K/fresh-runner loop. A budget assertion remains non-green and must not disguise a compiler, test, selector, product, or unrelated acceptance failure as infrastructure; classification determines the next correction path, not whether authority continues.
- No exhaustive matrices, fuzzing, endurance tests, broad scans, or full UI suite unless separately authorized.

## Handoff

Before completion, read `docs/execution/KNOWN_BUGS.md`. Golden-path, regressing, or prohibited-class defects are blockers. An eligible defect can enter `KNOWN_BUGS.md` only after owner approval and explicit task authorization of that path; otherwise record it only in `HANDOFF.md`.

Record the phase/card; changed paths; immutable phase-main base `P`; integrated/card-base SHA `M` and predecessor evidence; observed task-start authority SHA `A` and authority-only diff result; product implementation SHA `E`; any distinct infrastructure verification head `K`; workflow branch ref/expected head, run ID/URL/head SHA, and selector tier/input; exact commands/results; runner image/Xcode; resolved Simulator/OS/UDID; artifacts; acceptance evidence; every defect/blocker; boundary state; and the next unstarted card in `docs/execution/HANDOFF.md`. Never self-record the future transition or phase-close commit that will contain the entry. A recovery after an already-complete boundary HANDOFF must not amend that HANDOFF; accepted integration evidence belongs in the next card's CURRENT_TASK and later handoff. Phase autopilot may start only the immediate next card inside its authorized same-phase span after committing the transition and passing fresh G0. Program autopilot records accepted exact-main integration evidence in the next phase's CURRENT_TASK and subsequent handoff. Only S9.2 signing/TestFlight and S9.3 App Store submission require the owner.
