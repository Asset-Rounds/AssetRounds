# Repository Instructions

Implement only the one current card selected in `docs/execution/CURRENT_TASK.md`. When that file explicitly enables phase and program autopilot, the narrow same-phase transition and phase-boundary integration described below are also authorized; adjacent improvements are not.

## Build route

Author on Windows. Compile, test, and run the iOS Simulator only through the task-named GitHub Actions macOS workflow. “Local” describes device-local app data, not the build host. Never claim an iOS build/test result from Windows. XcodeBuildMCP and an owner-operated Mac are not required.

## Authority

1. `docs/execution/CURRENT_TASK.md` controls the current card's outcome, paths, environment, allowed GitHub operations, verification budget, immutable program map and phase-main base `P`, phase-autopilot state/span, and boundary-integration authority.
2. The exact SHA-256-pinned `docs/product/BUILD_PLAN_V4.md` controls product invariants and phase order.
3. The exact SHA-256-pinned `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` controls only the selected card's sequence, files, acceptance, tests, and next card where consistent with the plan.
4. Existing tests and code describe the baseline but cannot expand scope.

The runbook is a catalog, not blanket authority. Execute only the card ID currently selected in CURRENT_TASK. Never combine, skip, reorder, or pre-implement cards. Phase autopilot may select only the runbook's immediate next card when it is inside the exact authorized same-phase span and the current card already has accepted green exact-head CI. Program autopilot may cross a phase boundary only after the closed phase branch, exact fast-forward `main` update, and exact-main CI all pass; it then selects only the next phase's first runbook card on the frozen next branch.

`docs/execution/DECISIONS.md` is history, not active authority. A decision becomes active only after the owner revises the task and, when needed, the plan hash. A task/plan-requested delta from current code or tests is not a conflict. Stop only when an invariant outside the named delta is incompatible; precedence never authorizes silently overriding a product invariant.

## Fail-closed preflight

Before editing or any mutation, stop if a required preflight task field is blank/`UNSET`; an input needed now retains an unresolved owner placeholder; the plan/runbook/workflow path or hash differs from exact bytes; the selected runbook card does not match; predecessor evidence is incomplete; edits overlap unowned dirty work; or the recorded Windows/GitHub/runner/Xcode/runtime/tool route is unavailable. A complete committed HANDOFF for the still-selected boundary card forbids rerunning or editing that card, but program autopilot may resume the idempotent boundary state machine in `CURRENT_TASK.md`: validate the recorded implementation/run and HANDOFF-only phase-close commit, then continue from the exact already-observed phase/main/run/next-branch state without duplicating an accepted dispatch. A mechanically proven hosted-CI infrastructure failure may enter the standing recovery lane below without another owner approval message; that lane never reopens the card, changes its product implementation, or treats an infrastructure head as card implementation evidence.

For an ordinary card G0, verify the phase branch and integrated/card-base SHA `M`, observe task-start authority HEAD `A`, and prove `M..A` contains only the declared authority documents. Freeze phase-main base SHA `P`: S0 uses bootstrap `B`; each later phase uses the preceding phase's accepted exact-main phase-close/verification SHA. `P` stays byte-for-byte unchanged throughout the phase and is the boundary comparison value for `origin/main`; `M` is the preceding card's accepted implementation or verification head, or `P` for the phase's first card. The historical S0.1 recovery set `M` to failed `I2` and required its authority head to be the direct child changing exactly the four recorded authority documents; it did not change `P`. Initial S0.1 retains the explicit N/A predecessor-iOS-run statement, while recovery evidence records both failed exact-head runs. At phase start, owner- or program-autopilot-prepared `A` normally changes only hydrated `CURRENT_TASK.md`. A same-phase `A` may change only prior HANDOFF plus immediate-next CURRENT_TASK. The historical S0 boundary authority head `R` directly parents completed `C`; `C..R` is exactly its four recorded authority documents, while historical `P`, `M`, `A`, `I3`, accepted card CI, HANDOFF, `C`, failed integration runs, and `R` remain immutable. An infrastructure G0 preserves historical M/A and instead proves immutable E/F/K gate identity, `K` directly parents `F`, `F..K` contains only the frozen lane paths, all pins/counters match, and E..K has no protected product or selector change. `K` is observed only after commit and never self-recorded. `A` is observed at ordinary card G0 and recorded later; never self-record it.

At G0, validate the new task's expected selector object and tier against the frozen runbook and workflow schema. `Scripts/ci-selection.json` may be absent for initial S0.1 or may still equal the accepted predecessor card during a later transition; that state is not next-card evidence and is not a G0 mismatch. The S0.1 recovery requires the selector to already equal the exact I2/current-task bytes and forbids mutating it. Otherwise, if G0 passes, creating/replacing it with the new exact object is the first implementation-support mutation. It must match the current task before implementation commit or dispatch.

The owner-selected Codex GUI/session posture is expected to be Full access: `sandbox_mode=danger-full-access`, `approval_policy=never`, and network enabled. Do not stop because filesystem, network, connector, plugin, credential, or external-write capability is broader than the active card. Full access is capability, not product or task authority: use only the paths, repository/ref/workflow arguments, and external actions named in `CURRENT_TASK.md`. Stop only when a required operation is unavailable or an attempted action would exceed that active authority. Windows is expected; stop only when the named macOS workflow cannot provide the required verification.

## Autonomous hosted-CI infrastructure recovery

No additional owner approval message is required. An exact authorized terminal non-green run is mechanically eligible only when complete Actions logs and checksum-verified artifacts prove that its first causative failure is confined to hosted-runner provisioning, pinned toolchain/runtime readiness, exact-Simulator boot or migration, Actions service/transport, or lane-owned harness scheduling/accounting, with no compiler diagnostic, product test result/assertion, selector mismatch, or product-code failure. A pre-repository failure may instead use authoritative run/job/step metadata plus complete logs proving no repository command ran. An upload/transport failure requires complete logs proving required-evidence validation and checksum creation succeeded first. The sole legacy no-manifest bootstrap is exact K2 phase run `31583957094` at `400f8827f5451b9a44f4d846340b51377554069d`, whose GitHub metadata, complete logs, and uploaded partial artifact prove setup was the first failure, no product command ran, and every later skip/missing-evidence/checksum failure was deterministic; it authorizes K3 only and is consumed. K3 moves the setup elapsed export before its assertion and checksum creation before the setup-plus-artifact assertion, so every later post-checkout classification requires checksum-verified evidence. Budget eligibility is closed: setup `<=120` may authorize a correction only when it is the first failure before any product command; aggregate total may authorize a correction only when every constituent, product command, selector, required-evidence check, checksum, and upload passed. Build/unit/UI ceiling failures and every other acceptance failure are ineligible. All failed assertions remain non-green. Ambiguity is ineligible.

For one immutable gate identity—phase, card, workflow, ordered required ref roles, product implementation head `E`, and first failed run/head—support heads form one direct-child sequence `K1,K2,...`. There is no numeric K ceiling and no owner-approval gate. Gate bytes are UTF-8/no-BOM/no-terminal-newline and use exactly `gate-v1|repository-id=<decimal>|workflow-id=<decimal>|workflow-path=<path>|job=<id>|phase=<id>|card=<id>|phase-branch=<ref>|required-ref-roles=<comma-ordered>|E=<40-lower-hex>|P=<40-lower-hex>|selector-sha256=<64-lower-hex>|tier=<N8|P12|F25>|first-failed-run=<decimal>|first-failed-head=<40-lower-hex>`. The lane is evidence-bounded: before `K(n+1)`, fresh read-only G0 derives `n` from reachable gate-lineage commits and every exact-ref/head GitHub candidate; freezes one canonical first-failure record and SHA-256, one separate canonical repair record and SHA-256, one causal diagnosis, and one minimal operational delta; proves the new K directly parents failed verification head `F`; and proves neither failure nor repair was consumed by an earlier K. Canonical UTF-8 records have no BOM or terminal newline and use exactly `failure-v1|gate=sha256:<64-lower-hex>|operation=<operation>|guard=<guard>|fault=<fault>|mechanism=<mechanism>|product=<product>|artifact=<artifact>` and `repair-v1|gate=sha256:<64-lower-hex>|key=<repair>`. Closed values are: operation `runner-provisioning|selection|toolchain-simulator-selection|setup-budget|simulator-readiness|build|unit|ui|required-evidence|checksum|total-budget|artifact-upload`; guard `provider-start|selection-exit|toolchain-selection-exit|setup-elapsed-lte-setup-budget|readiness-request-to-ready-lte-300|build-wrapper-exit|unit-wrapper-exit|ui-wrapper-exit|required-evidence-exit|checksum-exit|elapsed-seconds-lte-total-budget|artifact-upload-exit`; fault `provider-unavailable|timeout-124|assertion-nonzero|command-nonzero|transport-nonzero`; mechanism `runner-provisioning|pinned-toolchain-runtime-readiness|exact-simulator-readiness|actions-service-transport|lane-harness-scheduling-accounting`; product `not-run|pass`; artifact `none|partial-upload-no-checksum|checksum-verified|full-upload`; repair `provider-fresh-runner|exact-simulator-readiness-barrier|dedicated-prebuild-exact-udid-readiness|schedule-readiness-outside-build-envelope|background-exact-simulator-readiness-concurrent-with-build-joined-before-tests|artifact-transport-only`. Run IDs, SHAs, refs, display names, timestamps, UDIDs, observed durations, log hashes, and cascade sets are provenance only; they cannot distinguish failures. The same operation+mechanism or repair key is consumed gate-wide across phase and main regardless of guard/fault/product/artifact wording. CURRENT_TASK cannot choose/reset the index, relabel an overlap, or rewrite history.

One K may contain mechanical CURRENT_TASK evidence/pins plus exactly one operational correction for its failure/repair across the smallest causally required lane subset. It may not bundle alternatives, adjacent hardening, cleanup, or a second diagnosis. The current owner-directed K3 conversion may amend `AGENTS.md`, the plan, runbook, execution contract, CURRENT_TASK, workflow, and `Scripts/build-smoke.sh` exactly once. After K3, those four static authority documents and all four smoke/timeout scripts are immutable under the lane; every later K may change only a causally required subset of `docs/execution/CURRENT_TASK.md` and marker-delimited infrastructure-recovery bodies in `.github/workflows/ios-ci.yml`. CURRENT_TASK may record exact evidence/identities/counters and repin workflow bytes but cannot broaden eligibility, paths/actions, frozen values, or gate identity. Each failure and repair may be targeted once. Recurrence or overlap after its correcting K stops immediately and can never authorize a redispatch or repository delta. The ordinary one-fix `I2` budget remains separate.

Every K directly parents F. Preserve HANDOFF, selector, project/app/tests/fixtures/assets, product behavior, test selectors, runner/Xcode/Simulator selection, N8/P12/F25 values, workflow trigger/permissions/action pins, exact build/test/UI commands/flags/destinations/result paths, selector/tier/result validation, `set -euo pipefail`, process-group timeout/TERM/KILL/exit-124 behavior, command/pipeline exit propagation, existing evidence/checksum/upload enforcement, and the exact setup/evidence and raw aggregate wall-clock endpoints, values, accounting, and assertions. Exact Simulator readiness remains bounded at 300 seconds and fully counted inside the raw aggregate; it may be scheduled outside setup but never subtracted, prorated, or excluded from any interval whose frozen endpoints contain it. G0 proves both the one-commit `F..K` delta and cumulative `E..K` protected-tree/workflow-spine equality to the immutable gate baseline, with every earlier lane hunk attributable to one earlier signature and no cumulative frozen-value/assertion change.

K3 and later commits require exactly one each of these trailers: `Infrastructure-Gate`, `Infrastructure-Support-Index`, `Infrastructure-Failed-Run`, `Infrastructure-Failed-Head`, `Infrastructure-Failed-Ref`, `Infrastructure-Failure-Signature`, and `Infrastructure-Repair-Key`. G0 recomputes the canonical gate/failure/repair hashes; requires positive index `n`, failed head equal to the commit parent, exact run/ref/head provenance, and no earlier matching/overlapping failure or repair. Index 1 seeds a new gate from the literal schema; index greater than 1 must reuse the immediately preceding same-gate support trailer and index `n-1`. Current S0 K3 is the explicit legacy seed whose K1/K2 are fixed indices 1/2. Prose/trailer/history disagreement stops.

Candidate history is closed independently per exact workflow/ref/head, but retry consumption is gate-wide. Zero candidates dispatches the initial run once; one active waits; one green accepts that ref role. One terminal failure takes exactly one route: dispatch one same-head fresh-runner candidate only for pre-repository provider failure or post-checksum upload/transport failure when complete evidence shows no repository delta, or create the next K when evidence proves a distinct eligible failure/repair with one unique lane correction. Before that no-code retry, canonicalize its failure and repair under the frozen grammar; both must be unconsumed across every gate ref. Classification of the first terminal candidate plus its exact GitHub receipt consumes both gate-wide, and the subsequent retry candidate is their only allowed disposition. GitHub history is authoritative immediately; CURRENT_TASK mirrors the receipt at the next otherwise-authorized commit and cannot reset it. Setup/aggregate failures never redispatch same-head. Creating the next K retires every unused dispatch/retry at F. Exactly two candidates are valid only when the first is that consumed eligible provider/transport transient and the second is active, green, or terminal. A green second accepts; a terminal second may create the next K only for a distinct eligible failure/repair with one unique lane correction. Any recurrence/overlap of a consumed failure or repair at this or another ref stops immediately; a second retry, ambiguity/ineligibility, any other ordering, or more than two candidates also stops. Failed run IDs are never rerun.

CURRENT_TASK freezes ordered ref roles, each exact predecessor, and a cursor. Advance one role once by non-force fast-forward only when it equals its named predecessor and every earlier role has green exact-K evidence; if it already equals K, do not push and resume that exact candidate history; later roles must remain at their predecessor. Any other ref/provenance/candidate state stops without reconciliation. At a boundary, phase verifies first, then main. Accept only green exact-K CI with complete artifacts/checksums. Record E as product implementation evidence and K separately as the verification/integration/next base. If an open card's K exposes a product failure, ordinary unused I2 may proceed from the latest support head; a closed boundary card never reopens.

Exact S0 conversion bridge: K1 `0da62482db7e3f90742876ef05574de9e9569e32` targeted pre-build exact-Simulator readiness. K2 `400f8827f5451b9a44f4d846340b51377554069d` targeted aggregate readiness scheduling. Both remain failed historical support heads. Exact phase run `31583957094` at K2 passed selection and pinned toolchain/scheme/Simulator inspection but first failed unchanged setup `232 > 120` after the K2 boot request put exact-Simulator readiness on setup's critical path; build/unit/UI did not run, later missing-evidence/checksum failures were deterministic consequences, total `233 <= 720`, and upload succeeded. It is never accepted or redispatched. Direct-child K3 may change exactly the seven paths named above and target only this distinct setup/readiness-scheduling failure. Its one operational correction restores setup inspection before readiness starts, runs the bounded exact-UDID readiness step in GitHub's tracked background mode concurrently with the unchanged exact-destination build, removes only build-smoke's redundant readiness command, and explicitly joins the readiness step before any test. The 300-second readiness and 180-second build ceilings remain independent; both intervals remain inside raw total. All P12 values, aggregate endpoints, selector, product, tests, and exact xcodebuild invocation remain unchanged. The first current K at index 3 or later with green exact-head phase and green exact-main CI becomes S0 verification/phase-close and S1 base while I3 remains S0.1 implementation evidence.

Technical stops—not approval stops—are unresolved/ambiguous first cause, failure/repair identity, or unique delta; a repeated or overlapping consumed failure/repair; a required path outside the lane; more than one operational delta; frozen selector/tier/budget endpoint/value/assertion, product/test/evidence weakening; compiler/test/product failure; insufficient classification evidence outside the explicit fallback; ref divergence/non-fast-forward/intervening movement; invalid candidate cardinality/order; or unavailable required GitHub route. Report the technical blocker and do not request authority text. Numeric K index is never a stop. The lane never authorizes force-push, merge commit, PR, signing, upload, deployment, submission, settings, secrets, or another release/external mutation.

## Scope

- Read the current task, exact pinned plan, and exact selected pinned runbook card before editing. Do not enter `/plan`; the active card is already planned.
- Touch only its allowed paths and stay inside its file envelope.
- Paths in `CURRENT_TASK.md` are fully expanded individual repository-relative file paths. Apply the runbook's mechanical app-root rule before G0: app-module shorthand such as `Domain/...` becomes `FieldEvidenceApp/Domain/...`, and every brace/set expression becomes individual files. Globs, directory roots, brace/set expressions, and `/**` are invalid in a hydrated task. Never guess a location. Forbidden paths win. Count each created, changed, deleted, or renamed repo path once. A file-cap override is valid only when the exact selected frozen runbook card explicitly states it; self-authored CURRENT_TASK wording cannot raise a cap.
- Preserve user changes; do not reformat or refactor unrelated code.
- Reuse current navigation, models, design tokens, dependencies, fixtures, checked-in project, and shared scheme.
- Device-local and hosted product behavior require separate task IDs; hosted CI is verification, not hosted product scope.
- When explicitly authorized, stage only task-owned paths, commit, push the exact phase branch, dispatch/observe the named workflow, and download its artifacts. At an accepted phase boundary with program autopilot enabled and the boundary integration flag `yes`, Codex may update `main` only by a verified non-force fast-forward from the exact phase-close commit, dispatch/accept exact-main CI, create only the frozen next phase branch from that green `main`, and hydrate only its first card. Any non-fast-forward, unexpected ref movement, merge conflict, or mismatched CI head stops without reconciliation. PR creation, signing, TestFlight upload, App Store actions, deployment, and other external mutation remain forbidden unless a separate release task names them.
- Never use broad staging or include pre-existing/unowned changes.
- Do not add packages, targets, capabilities, entitlements, permissions, migrations, APIs/backends, accounts/auth, analytics, payment behavior, public metadata, deployment, or App Store submission unless the task explicitly names it.
- Later plan sections constrain compatibility but do not authorize artifacts now. Do not add unused entities, protocols, services, routes, generalized seams, stubs, or fixtures for a later slice.
- Owner-provisioned `.codex/config.toml` and `DECISIONS.md` are read-only unless explicitly named. The current owner-directed S0 K3 conversion may amend the seven exact paths named above once. After K3, AGENTS, plan, runbook, execution contract, and all four smoke/timeout scripts are immutable under the lane; a later K may use only frozen CURRENT_TASK plus marker-delimited infrastructure-recovery workflow bodies. CURRENT_TASK remains read-only from G0 through accepted card CI except for an authorized transition/next-phase authority commit or exact lane record/repin delta. Full machine/network/tool access never expands product scope.
- Append-only `docs/execution/HANDOFF.md` is always authorized bookkeeping and excluded from the file cap, but it is never part of the implementation commit whose SHA CI verifies.
- `Scripts/ci-selection.json` is the only standing implementation support exception. Each card may replace it with the exact runbook/CURRENT_TASK selector object; it is excluded from the cap and grants no product scope. No other script is a standing exception.
- After accepted product implementation and green exact verification-head CI, append the handoff. Same-phase transition still requires enabled autopilot, flag `yes`, immediate next card inside the span, exact HANDOFF-plus-CURRENT_TASK, and fresh G0. At a boundary create/push HANDOFF-only `C`. Remote `main=P` permits one non-force fast-forward; `main=C` resumes; anything else stops. Any named workflow-dispatch candidate at `ref=main/head=C` is reused: accept success, wait active, and on terminal non-success apply the standing infrastructure classification before stopping; dispatch once only if absent. Reprove remote phase+main at the accepted verification head. For non-S9 next authority, valid local state is absent/accepted-head/exact A and remote state absent/exact A; every other collision stops. Hydrate CURRENT_TASK-only A from that head; if local is exact A and remote absent, non-force create remote at A; otherwise resume exact remote A; then run fresh G0. Never force, repair divergence, or duplicate work. Continue through S9.1 only; S9.2/S9.3 remain owner-only.
- S0's failed `main@C` run `31567299022`, failed `main@R` run `31571125674`, aggregate-overrun K1 phase run `31579936670`, and setup-overrun K2 phase run `31583957094` are immutable historical evidence and must never be rerun or called successful. CURRENT_TASK authorizes direct-child K3 for the one distinct setup/readiness-scheduling correction. The first green exact-head phase and exact-main current K at index 3 or later becomes the accepted S0 verification/phase-close head and S1 base while I3 remains the sole S0.1 product implementation evidence. HANDOFF, selector, project, app, and tests stay unchanged.

## Verification

- Use only the task's exact phase branch, workflow/ref, runner label, expected Xcode version/build, project/shared scheme/configuration, Simulator model/OS selector, smoke scripts, and validated selector object. Manual dispatch uses the named branch ref, never a raw SHA: first verify it points to the expected verification head (`E` normally or eligible `K`), allow no intervening push, and accept only a matching run `head_sha`.
- Do not run `xcodebuild` locally on Windows. Run the smallest relevant test bundle and golden XCUITest/Simulator path on the GitHub macOS runner.
- A card pass normally comes from a successful run whose `head_sha` equals implementation commit `I` or its single product fix `I2`; historical S0.1 uses accepted `I3`. When the standing infrastructure lane is eligible, exact green `K` is verification evidence only: prove `E..K` contains solely the declared nonproduct lane paths, retain `E` as implementation evidence, and record/use `K` as the accepted verification/integration head and next base. A phase integration pass additionally requires successful UI-enabled CI at the exact fast-forwarded `main` head.
- Use the exact task tier and `ci-selection.json` values from runbook Section 6: N8 = 90/150/240/0/480 seconds with UI false/empty; P12 = 120/180/180/240/720 with UI true/one selector; F25 = 180/240/300/780/1500 with UI true/one selector. The workflow input must match. Step ceilings and setup-through-checksum total are enforced; artifact upload remains under the 30-minute hard stop. Missing required logs, `.xcresult`, or enabled-UI screenshot fails the run.
- Inspect the complete failing step and artifacts. For a product/card failure, apply at most one concrete task-scoped `I2` and rerun once; a non-green product rerun stops. For a mechanically eligible hosted-infrastructure failure, use only the monotonic evidence-bounded K lane. A budget assertion is eligible only under the exact first-cause rule above; never disguise a compiler, test, selector, product, or unrelated acceptance failure as infrastructure. Neither lane resets or expands the other.
- No exhaustive matrices, fuzzing, endurance tests, broad scans, or full UI suite unless separately authorized.

## Handoff

Before completion, read `docs/execution/KNOWN_BUGS.md`. Golden-path, regressing, or prohibited-class defects are blockers. An eligible defect can enter `KNOWN_BUGS.md` only after owner approval and explicit task authorization of that path; otherwise record it only in `HANDOFF.md`.

Record the phase/card; changed paths; immutable phase-main base `P`; integrated/card-base SHA `M` and predecessor evidence; observed task-start authority SHA `A` and authority-only diff result; product implementation SHA `E`; any distinct infrastructure verification head `K`; workflow branch ref/expected head, run ID/URL/head SHA, and selector tier/input; exact commands/results; runner image/Xcode; resolved Simulator/OS/UDID; artifacts; acceptance evidence; every defect/blocker; boundary state; and the next unstarted card in `docs/execution/HANDOFF.md`. Never self-record the future transition or phase-close commit that will contain the entry. A recovery after an already-complete boundary HANDOFF must not amend that HANDOFF; accepted integration evidence belongs in the next card's CURRENT_TASK and later handoff. Phase autopilot may start only the immediate next card inside its authorized same-phase span after committing the transition and passing fresh G0. Program autopilot records accepted exact-main integration evidence in the next phase's CURRENT_TASK and subsequent handoff. Only S9.2 signing/TestFlight and S9.3 App Store submission require the owner.
