# Repository Instructions

Implement only the one current card selected in `docs/execution/CURRENT_TASK.md`. When that file explicitly enables phase and program autopilot, the narrow same-phase transition and phase-boundary integration described below are also authorized; adjacent improvements are not.

## Build route

Author on Windows. Compile, test, and run the iOS Simulator only through the task-named GitHub Actions macOS workflow. “Local” describes device-local app data, not the build host. Never claim an iOS build/test result from Windows. XcodeBuildMCP and an owner-operated Mac are not required.

## Authority

1. `docs/execution/CURRENT_TASK.md` controls the current card's outcome, paths, environment, allowed GitHub operations, verification budget, immutable program map and phase-main base `P`, phase-autopilot state/span, and boundary-integration authority.
2. The exact SHA-256-pinned `docs/product/BUILD_PLAN_V4.md` controls product invariants and phase order.
3. The exact SHA-256-pinned `docs/execution/V4_IMPLEMENTATION_RUNBOOK.md` controls only the selected card's sequence, files, acceptance, tests, and next card where consistent with the plan.
4. Existing tests and code describe the baseline but cannot expand scope.

The runbook is a catalog, not blanket authority. Execute only the card ID currently selected in CURRENT_TASK. Never combine, skip, reorder, or pre-implement cards. Phase autopilot may select only the runbook's immediate next card when it is inside the exact authorized same-phase span and the current card already has accepted green exact-head CI. Program autopilot may cross a phase boundary only after the closed phase branch, exact fast-forward `main` update, and exact-main CI all pass; it then selects only the next phase's first runbook card on the frozen next branch.

`docs/execution/DECISIONS.md` is history, not active authority. A decision becomes active only after the owner revises the task and, when needed, the plan hash. A task/plan-requested delta from current code or tests is not a conflict. Stop only when an invariant outside the named delta is incompatible; precedence never authorizes silently overriding a product invariant.

## Fail-closed preflight

Before editing or any mutation, stop if a required preflight task field is blank/`UNSET`; an input needed now retains an unresolved owner placeholder; the plan/runbook/workflow path or hash differs from exact bytes; the selected runbook card does not match; predecessor evidence is incomplete; edits overlap unowned dirty work; or the recorded Windows/GitHub/runner/Xcode/runtime/tool route is unavailable. A complete committed HANDOFF for the still-selected boundary card forbids rerunning or editing that card, but program autopilot may resume only the idempotent boundary state machine in `CURRENT_TASK.md`: validate the recorded implementation/run and HANDOFF-only phase-close commit, then continue from the exact already-observed phase/main/run/next-branch state without duplicating an accepted dispatch. The sole owner-authorized S0 phase-boundary recovery declared by the pinned plan/runbook/CURRENT_TASK may instead create one authority-only `R` directly after the exact completed HANDOFF commit `C`; it never reopens or edits S0.1, never changes HANDOFF or implementation bytes, and receives only the one new exact-main decision explicitly declared there.

Verify the phase branch and integrated/card-base SHA `M`, observe task-start authority HEAD `A`, and prove `M..A` contains only the declared authority documents. Freeze phase-main base SHA `P`: S0 uses bootstrap `B`; each later phase uses the preceding phase's accepted exact-main phase-close SHA. `P` stays byte-for-byte unchanged throughout the phase and is the boundary comparison value for `origin/main`; `M` is the preceding card's accepted implementation SHA or `P` for the phase's first card. The sole S0.1 recovery exception sets `M` to failed `I2` and requires `A` to be its direct child changing exactly the four owner-named authority documents; it never changes `P`. Initial S0.1 retains the explicit N/A predecessor-iOS-run statement, while recovery S0.1 records both failed exact-head runs as predecessor evidence. At phase start, owner- or program-autopilot-prepared `A` normally changes only hydrated `CURRENT_TASK.md`. Before implementation or CI, an explicit owner decision may install a finite authority/config/workflow amendment when CURRENT_TASK declares the decision, exact cumulative `M..A` paths, and repinned source hashes. A same-phase `A` may change only prior HANDOFF plus immediate-next CURRENT_TASK, except for the exact four-document S0.1 recovery amendment. The sole S0 phase-boundary recovery uses a separate proof: local `R` directly parents completed `C`, `C..R` is exactly the four owner-named authority documents, remote phase and `main` remain `C`, and the next phase branch is absent. `R` is observed only at its post-commit G0 and never self-recorded; historical `P`, `M`, `A`, `I3`, accepted card CI, HANDOFF, and `C` remain immutable. Its next G0 proves all frozen repository, program, toolchain, selector, timeout, workflow, and prohibition fields unchanged. Only frozen card fields and exact current/ref-specific command arguments may rotate. `A` is observed at ordinary card G0 and recorded later; never self-record it.

At G0, validate the new task's expected selector object and tier against the frozen runbook and workflow schema. `Scripts/ci-selection.json` may be absent for initial S0.1 or may still equal the accepted predecessor card during a later transition; that state is not next-card evidence and is not a G0 mismatch. The S0.1 recovery requires the selector to already equal the exact I2/current-task bytes and forbids mutating it. Otherwise, if G0 passes, creating/replacing it with the new exact object is the first implementation-support mutation. It must match the current task before implementation commit or dispatch.

The owner-selected Codex GUI/session posture is expected to be Full access: `sandbox_mode=danger-full-access`, `approval_policy=never`, and network enabled. Do not stop because filesystem, network, connector, plugin, credential, or external-write capability is broader than the active card. Full access is capability, not product or task authority: use only the paths, repository/ref/workflow arguments, and external actions named in `CURRENT_TASK.md`. Stop only when a required operation is unavailable or an attempted action would exceed that active authority. Windows is expected; stop only when the named macOS workflow cannot provide the required verification.

## Scope

- Read the current task, exact pinned plan, and exact selected pinned runbook card before editing. Do not enter `/plan`; the active card is already planned.
- Touch only its allowed paths and stay inside its file envelope.
- Paths in `CURRENT_TASK.md` are fully expanded individual repository-relative file paths. Apply the runbook's mechanical app-root rule before G0: app-module shorthand such as `Domain/...` becomes `FieldEvidenceApp/Domain/...`, and every brace/set expression becomes individual files. Globs, directory roots, brace/set expressions, and `/**` are invalid in a hydrated task. Never guess a location. Forbidden paths win. Count each created, changed, deleted, or renamed repo path once. A file-cap override is valid only when the exact selected frozen runbook card explicitly states it; self-authored CURRENT_TASK wording cannot raise a cap.
- Preserve user changes; do not reformat or refactor unrelated code.
- Reuse current navigation, models, design tokens, dependencies, fixtures, checked-in project, and shared scheme.
- Device-local and hosted product behavior require separate task IDs; hosted CI is verification, not hosted product scope.
- When explicitly authorized, stage only task-owned paths, commit, push the exact phase branch, dispatch/observe the named workflow, and download its artifacts. At an accepted phase boundary with program autopilot enabled and the boundary integration flag `yes`, Codex may update `main` only by a verified non-force fast-forward from the exact phase-close commit, dispatch/accept exact-main CI, create only the frozen next phase branch from that green `main`, and hydrate only its first card. Any non-fast-forward, unexpected ref movement, merge conflict, or mismatched CI head stops without reconciliation. PR creation, signing, TestFlight upload, App Store actions, deployment, and other external mutation remain forbidden unless a separate release task names them.
- Never use broad staging or include pre-existing/unowned changes.
- Do not add packages, targets, capabilities, entitlements, permissions, migrations, APIs/backends, accounts/auth, analytics, payment behavior, public metadata, deployment, or App Store submission unless the task explicitly names it.
- Later plan sections constrain compatibility but do not authorize artifacts now. Do not add unused entities, protocols, services, routes, generalized seams, stubs, or fixtures for a later slice.
- Owner-provisioned `.codex/config.toml`, `AGENTS.md`, the frozen plan, and `DECISIONS.md` are read-only unless explicitly named. `CURRENT_TASK.md` is read-only from G0 through accepted card CI; it may change only during the authorized same-phase transition, next-phase first-card authority step, the one explicitly owner-named four-document S0.1 implementation recovery amendment, or the one explicitly owner-named four-document S0 phase-boundary recovery `R`. Those four files become read-only again immediately after `R`; no other authority, implementation, workflow, selector, or HANDOFF path joins that exception. Full machine/network/tool access never expands product scope.
- Append-only `docs/execution/HANDOFF.md` is always authorized bookkeeping and excluded from the file cap, but it is never part of the implementation commit whose SHA CI verifies.
- `Scripts/ci-selection.json` is the only standing implementation support exception. Each card may replace it with the exact runbook/CURRENT_TASK selector object; it is excluded from the cap and grants no product scope. No other script is a standing exception.
- After accepted exact implementation-head CI, append the handoff. Same-phase transition still requires enabled autopilot, flag `yes`, immediate next card inside the span, exact HANDOFF-plus-CURRENT_TASK, and fresh G0. At a boundary create/push HANDOFF-only `C`. Remote `main=P` permits one non-force fast-forward; `main=C` resumes; anything else stops. Any named workflow-dispatch candidate at `ref=main/head=C` is reused: accept success, wait active, stop on terminal non-success, dispatch once only if absent. Reprove remote phase+main=C. For non-S9 next authority, valid local state is absent/C/exact A and remote state absent/exact A; every other collision stops. Hydrate CURRENT_TASK-only A from C; if local is exact A and remote absent, non-force create remote at A; otherwise resume exact remote A; then run fresh G0. Never force, repair divergence, or duplicate work. Continue through S9.1 only; S9.2/S9.3 remain owner-only.
- The sole pinned S0 phase-boundary recovery overrides only the already-failed `main@C` integration decision: never rerun failed run `31567299022`; create authority-only `R` directly after `C`; prove `C..R` is exactly `AGENTS.md`, the pinned plan, the pinned runbook, and CURRENT_TASK; non-force advance phase `C→R`, then `main C→R`; reuse a matching `main/head=R` candidate or dispatch exactly once only if absent. Every terminal non-success at `R` stops permanently. A matching success makes `R` the accepted S0 phase-close and S1 phase-main base; only then may program autopilot create/hydrate S1.1 from `R`. This exception authorizes no implementation, HANDOFF, selector, workflow, timeout, product behavior, or product implementation change outside the four authority documents and no additional S0 recovery.

## Verification

- Use only the task's exact phase branch, workflow/ref, runner label, expected Xcode version/build, project/shared scheme/configuration, Simulator model/OS selector, smoke scripts, and validated selector object. Manual dispatch uses the named branch ref, never a raw SHA: first verify it points to the expected implementation SHA, allow no intervening push, and accept only a matching run `head_sha`.
- Do not run `xcodebuild` locally on Windows. Run the smallest relevant test bundle and golden XCUITest/Simulator path on the GitHub macOS runner.
- A card pass must be from a successful run whose `head_sha` equals the task's implementation commit SHA `I` (or the single allowed fix SHA `I2`). The one owner-authorized S0.1 recovery may instead accept only its exact resulting recovery implementation commit `I3` after a fresh G0 proves `M=I2` and the four-document authority-only amendment. A phase integration pass additionally requires successful UI-enabled CI whose `head_sha` equals the exact fast-forwarded `main` phase-close commit. The sole S0 boundary recovery may treat authority-only `R` as that accepted phase-close only after exact-main UI-enabled CI succeeds at `head_sha=R`; `R` is never card implementation evidence and does not replace the accepted `I3` card result. Record both identities in the current/next authority evidence. Other authority or phase-close bookkeeping commits are never implementation evidence.
- Use the exact task tier and `ci-selection.json` values from runbook Section 6: N8 = 90/150/240/0/480 seconds with UI false/empty; P12 = 120/180/180/240/720 with UI true/one selector; F25 = 180/240/300/780/1500 with UI true/one selector. The workflow input must match. Step ceilings and setup-through-checksum total are enforced; artifact upload remains under the 30-minute hard stop. Missing required logs, `.xcresult`, or enabled-UI screenshot fails the run.
- Inspect the complete failing step and artifacts, apply at most one concrete task-scoped fix, then rerun once. If that post-fix rerun is not green, stop regardless of whether its failure is identical. The sole owner-authorized S0.1 recovery exception is valid only when the pinned plan, runbook, and CURRENT_TASK name `I2` as `M`, exactly one two-script `I3` delta, and exactly one final P12 dispatch; a non-green recovery run stops with no further edit or dispatch. The separate sole S0 boundary recovery is an authority-head phase-integration decision, not a card fix or rerun: it preserves all implementation/workflow/selector/tier/timeout bytes, receives exactly one `main@R` candidate/dispatch, and stops on non-green. Neither exception resets or expands any other card's allowance.
- No exhaustive matrices, fuzzing, endurance tests, broad scans, or full UI suite unless separately authorized.

## Handoff

Before completion, read `docs/execution/KNOWN_BUGS.md`. Golden-path, regressing, or prohibited-class defects are blockers. An eligible defect can enter `KNOWN_BUGS.md` only after owner approval and explicit task authorization of that path; otherwise record it only in `HANDOFF.md`.

Record the phase/card; changed paths; immutable phase-main base `P`; integrated/card-base SHA `M` and predecessor evidence; observed task-start authority SHA `A` and authority-only diff result; implementation SHA; workflow branch ref/expected head, run ID/URL/head SHA, and selector tier/input; exact commands/results; runner image/Xcode; resolved Simulator/OS/UDID; artifacts; acceptance evidence; every defect/blocker; boundary state; and the next unstarted card in `docs/execution/HANDOFF.md`. Never self-record the future transition or phase-close commit that will contain the entry; the next G0 observes transition `A`, while git history records phase-close bookkeeping. The S0 boundary recovery must not amend the already-complete HANDOFF; its accepted `R` integration evidence belongs in S1.1 CURRENT_TASK and the later S1.1 handoff. Phase autopilot may start only the immediate next card inside its authorized same-phase span after committing the transition and passing fresh G0. Program autopilot records accepted exact-main integration evidence in the next phase's CURRENT_TASK and subsequent handoff. Only S9.2 signing/TestFlight and S9.3 App Store submission require the owner.
