# AssetRounds integration instructions

## Active task and authority

Work only in `C:\AssetRounds-v23-s10-integration` on `codex/v23-s10-integration-20260910`. The owner selected `V23-INTEGRATION-20260910`; this is source reconciliation into one existing app, not acceptance of provisional cards.

Start with `docs/design/v23/integration/ACTIVE_BRIEF.md`. It is a navigation/checkpoint aid, not new product authority. `docs/execution/CURRENT_TASK.md` selects `docs/design/v23/integration/CURRENT_INTEGRATION.md`; read its active policy and only the precise workstream sections needed. Reuse verified frozen plan/runbook/package pins while unchanged. Resolve conflicts using the controlling task and frozen product authority, never inferred product truth.

Accepted main is `b1d04ae5e684aa9c6807af655089efa1df8a7ed6`. Preserve S10 branding, the existing app/project/shared scheme/design system and five accepted receipts. No second app, store, writer or renderer. Card135 remains owner-only/skipped; minimum-runtime and physical verification remain DEFERRED; releaseReady remains false. No signing, distribution, submission or fabricated evidence.

## Scope and verification

- Before a workstream's first edit, record its source-proven requirement, exact file envelope, owner, shared interfaces and paired tests in CURRENT_INTEGRATION. Short causal additions suffice; do not append repetitive audit narratives.
- Preserve all existing owned drafts, untracked work, frozen branches, V30 and the coordination ledger. Never broad-stage, reset, overwrite or reformat unrelated work.
- Author on Windows. Native compilation and Simulator tests run only through the task's named hosted workflow, exact branch/head, pinned environment and source-defined selector. No local Xcode build or substitute route.
- One collector audits each original once. Preserve complete logs, warnings, artifact ownership/digests/manifests and failures externally. Reuse validated evidence; distinguish compilation, units, UI, human review and formal acceptance.
- Diagnose failures and correct within scope without routine approval. Batch compatible fixes; do not weaken predicates, tests, coverage, watchdogs or evidence. No duplicate run, cancellation, Actions rerun, speculative dispatch or dispatch of a known deterministic failure. Wait for an active run; inspect terminal evidence before its successor.
- Refresh relevant refs and owned dirty state before mutations. Root alone commits and pushes explicitly owned files. Use verified non-force linear updates only; investigate unexpected ref movement. No force-push, merge commit, PR, settings/secrets mutation or release operation.
- Main stays unchanged until complete retained same-head compilation and functional/compatibility evidence, qualified coverage of every required runtime/execution kind, one independent integration semantic review, critical affected-state S10 journey checks and genuine human review of those critical states pass. Qualified GitHub coverage can satisfy the provider gate alone; Bitrise is optional when it adds no required coverage. Track noncritical visual polish explicitly and complete it before release. Preserve non-force integration and exact-main verification; source readiness is not acceptance.

## Model routing and efficient execution — cost profile, owner activated 2026-09-23 UTC

- Restore the saved pre-performance one-helper approach with the amendments below. Prefer conserving Astra usage over faster parallel delivery. This profile remains active until the owner changes it and supersedes conflicting inherited/historical routing instructions. Saved profiles and switching instructions are in `docs/execution/agent-workflows/README.md`; preserve later unrelated safety and product changes when switching.

- Keep the primary hands-on: diagnose the critical path, implement understood fixes, integrate and verify. Honor the owner's current model, reasoning and speed choices; repository policy must not change them. A slower pace is acceptable, but required tests, independent review and acceptance quality remain unchanged.

- Default to zero helpers and at most one active helper. Delegate only for a required independent review or a bounded task whose avoided work clearly justifies its handoff and integration cost. Root handles small understood fixes and routine comparisons. No standing team, duplicate investigation, speculative parallel work, recursive delegation or helper merely to occupy available capacity. Root alone allocates workers, stages, commits, pushes and dispatches.

- When a helper is justified, explicitly use `gpt-6-astra` with `reasoning_effort = "high"` for coding, causal diagnosis and consequential complete-contract review. Do not use Extra High (`xhigh`), max or ultra helpers or automatically escalate effort; retain High until the owner changes this preference. Use scripts or root for routine extraction, inventory, mechanical checks and CI collection instead of launching a helper.

- The current desktop collaboration catalog supports `gpt-6-astra` at `high`. Verify availability at the actual spawn boundary; never guess identifiers, use unsupported `light`, or silently substitute models. Retain owner `agents.max_threads = 3` as capacity, with this policy allowing only one active helper. Preserve primary/session settings and all other TOML values. Do not add unsupported default-subagent or caching settings or change global configuration.

- Reuse a relevant High helper through closely related corrections only when its context and ownership still fit. Do not resume an Extra High helper under the new profile. If independent review is needed later, give one High replacement a short handoff of established findings, exact changed paths/pins, retained evidence and the remaining question; do not repeat already approved unchanged work or send full conversation history.

- Give each helper one question, exact owned paths, source/head pins, relevant invariants, paired tests, expected result and stopping condition. Agree shared interfaces before parallel edits; serialize shared-file changes or use isolated patches. Return a concise result with changed paths, checks, findings and unresolved work. Root integrates each owned delta once and runs the combined checks. Retain one independent reviewer for the complete combined affected boundary; an author never approves their own delta. Do not split a shared contract into partial approvals.
- Reuse the same worker for closely related work when its model, effort, retained context and ownership still fit; keep the same independent reviewer for consequential corrections to that contract. Prefer a delta-only follow-up with changed pins/files, new evidence and the remaining question. Reuse verified artifacts instead of full histories. Reset context for a substantially different workstream or demonstrated stale-context cost. Authors never approve their own changes; root alone stages, commits, pushes and dispatches. Identical staged/committed bytes use qualified deterministic binding, not another model endorsement.
- Group compiler/test failures by shared cause, API/initializer, actor boundary, receipt contract or fixture family. Read the first causal diagnostic and necessary caller context once; census affected callers/sibling fixtures when their shared contract changes, fix all demonstrated compatible instances, then review and verify the batch. Do not guess that matching text proves the same cause or expand into unrelated cleanup.
- Keep one concise current checkpoint per failure family/workstream in the existing records: owner/helper identity and routing, original run/head and dependency pins, diagnostic evidence paths, established findings, exhausted hypotheses, changed/owned files, review disposition, affected tests and next discriminating check. Update that checkpoint rather than duplicating history. Reuse findings while their relevant inputs remain exact; recheck mutable refs, drafts, index and CI state at action boundaries. Context memory is navigation, not fresh evidence. Never repeat full-log or authority-history reads, reopen settled investigations or generate per-head copies of unchanged helpers.
- Use parameterized scripts and bounded/event-driven waits for routine CI collection. One collector retains each original's complete logs, warnings, artifacts, ownership and digests; consume its summary thereafter. Deduplicate by head, environment and source-defined question before dispatch. Wait for the original's terminal audited result before its successor; no duplicate run, cancellation, Actions rerun or unchanged known-failure retry. Retain the narrow reviewed diagnostic exception below.
- Freeze source, checkpoint text and the typed review packet. Before semantic review, run focused deterministic checks for workflow inputs/environment, Git executable modes, exact ordered test selection and the real changed entry point; mocks must not bypass the changed contract. Remaining long regression suites may run alongside read-only semantic review, but all required checks and review must pass before adoption/commit/dispatch. Preserve hosted Swift compilation. Facts checks never substitute for semantic approval.
- Owner-approved gate policy, 2026-09-23: give each consequential batch one independent reviewer for its complete affected boundary. CI review traces public inputs, worker environment, command entry and evidence collection; Swift review covers callers, access/actor boundaries, fixtures and recovery effects. After approval, qualified scripts bind the exact reviewed source/helper/policy pins, raw/normalized bytes, Git modes, candidate tree, direct parent and fresh refs to the actual index and committed route. No repeated model index/commit endorsement for matching inputs. Any relevant mismatch fails closed and returns only the changed contract to the same reviewer. Record semantic approval and automated binding as different evidence; never label automation as an independent reviewer. This prospective rule supersedes older per-stage endorsement instructions without altering historical evidence.
- Review conclusions must name what was checked, findings and what remains unverified. Static/source/protocol review must never imply compilation or runtime success. Close a finding by reviewing its correction only, widening inspection when dependencies changed. Record each required gate separately from avoidable rework.
- Continue the existing five-batch measurement in docs/design/v23/integration/REVIEW_EFFICIENCY.md; retain completed results and identify the active profile for each batch. Record effort, helper count and purpose, review findings, post-approval defects and native originals consumed by those defects, plus available elapsed time and usage evidence. Separate required gates from avoidable rework; mark unknown values honestly. Reduce concurrency when integration/review churn costs more than it saves. Fold updates into causal checkpoints; no monitoring agent or claimed token savings without evidence.
- Continue within existing permissions and usage/run limits. Stop an attempt that produces no new evidence: state the unresolved condition and next evidence needed, then work on an independent dependency-ready task if available. Do not spend repeated model turns polling, rephrasing a blocker or retrying it. Report a genuine impasse honestly.
- Use docs/design/v23/integration/MERGE_READINESS.md as the current merge-critical-path checklist: blocking question, next dependency-ready batch, unexecuted coverage and final human/provider/main gates. Historical S10 is the accepted base; complete the expansion inside that app and advance main after the controlling gates. Complete source-proven failure families in one batch; after shared blockers clear, schedule distinct supported groups on existing qualified capacity. Parallel work must have disjoint questions and owners.
- Separate candidate source checks, reusable protocol tests and native execution. Reuse protocol execution only after a qualified mechanism proves its complete source/runtime/verifier dependency fingerprint unchanged; until then retain required checks. Always refresh mutable drafts, index and remote refs at mutation/dispatch boundaries. Never present Windows source/protocol success as Swift compilation or test execution.
- Require each native run to name its expected new evidence and decision afterward. Review diagnostic code with simple explicit types and default-off behavior before spending a native cycle. A compiler/setup failure is distinct from a measured runtime result; correct its evidenced cause before a successor, preserving diagnostic scope and all run limits. Do not infer a runtime cause from repeated log volume.
- Prefer measured bottleneck removal over more dispatches: retain queue/setup/build/test/collection times and distinguish elapsed time from runner cost. Keep an already-qualified dependent candidate and its evidence immutable while its prerequisite remains unsettled; defer another rebind/full qualification until adoption is dependency-ready unless a changed contract needs an earlier discriminating check. Batch compatible corrections first, then qualify the actual combined candidate; never transfer an old pass to changed inputs or skip required checks. Qualify cache or build-once/test-many changes with exact toolchain/source/artifact bindings and cold/miss reliability before adoption. Keep the current route while its replacement is unqualified; do not broaden native environments or acceptance claims. Fold checkpoint-only updates into the next causal batch when practical so they do not create an untested intermediate parent.
- Keep ACTIVE_BRIEF short and current and VERIFICATION_DUE explicit. Finish complete production journeys and preserve owned drafts, required tests/coverage, watchdogs, qualified native environments, critical human review and exact-main acceptance. Noncritical visual polish remains explicitly due before release. Measure efficiency from retained timings, diagnostics and available usage evidence; do not claim savings before measuring them.

Use the existing five GitHub and three Bitrise capacity only for distinct, source-defined, dependency-ready questions under CURRENT_INTEGRATION. Never dispatch duplicate, speculative, known-failing or unqualified acceptance runs. One collector audits each original once. Preserve all native-environment and final-main gates.

## Claude Code continuation decisions — owner approved 2026-09-24

These supersede conflicting routing and route-binding text above; every product, safety, evidence and main/release gate is unchanged.

- Independent review: when work continues in Claude Code, one separate Claude reviewer subagent (read-only, never the author) is the independent semantic reviewer in place of `gpt-6-astra`. Keep zero helpers otherwise and at most one active; record the reviewer model and verdict.
- Helper concurrency, owner changed 2026-09-25: use as many Claude subagents as help deliver faster. This supersedes the zero/one-helper limit above for Claude Code work. Each helper gets one bounded question with disjoint files or read-only scope. Implementation helpers work in isolated worktrees or return patches. The root alone integrates, stages, commits, pushes and dispatches. The independent reviewer never authors the code it approves. Native capacity, no-duplicate/no-rerun/no-cancel rules and every product, evidence and main/release gate are unchanged.
- Owner decisions, 2026-09-25 (in-session answers):
  1. **My Day fork history:** the bounded normalized-history amendment in `.codex-temp/my-day-fork-lineage/FORK_COMPATIBILITY_AMENDMENT.md` is approved. Fork restoration keeps the incumbent C47/C49/C55 journal normalization, and one fixed `retainedTargetFieldDraftSnapshotSHA256` is added to the existing `MyDayForkLineageV1` record (1,024-byte maximum unchanged). This unblocks C57 fork lineage and the Finding schema slot.
  2. **UI route budget RUI1 approved:** setup/artifact 300 s, build 1,800 s, tests 900 s, UI 900 s, total 3,900 s, 90-minute job cap. It applies to the reminder UI route and to critical-journey UI/screenshot evidence. It is qualified per route before use and grants no acceptance.
  3. **Final same-head coverage (amended by the owner the same day after the coverage census):** one shared build per head (build-for-testing, 2,400 s build limit) feeds test-only partitions run with `test-without-building`, each with a 3,000 s test budget, five at a time, covering every unit method (3,397 at 31126e9), not only the selector pool. One early development-only full sweep runs first to surface failures; the final same-head pass on the frozen phase candidate is still required. The route is development-only until a cold qualifying original proves the shared payload binding, safe extraction, no-rebuild and per-partition results; caches or shared products are never acceptance evidence by themselves.
  4. **Merge scope, chosen by root at the owner's request:** a phased merge replaces "no partial V23 merge" for advancing main; it does not change release. Main advances in phases, and each phase contains only journeys that are fully wired and pass every existing gate for that phase:
     - same-head full unit coverage;
     - qualified UI evidence;
     - one independent integration review;
     - genuine human review of critical states;
     - a non-force fast-forward and exact-main verification.
     No schema, persistence, backup or restore change may merge unless it is complete and verified. Unfinished features stay unreachable, with no production entry point, and each is listed in an explicit phase ledger in MERGE_READINESS with its remaining gates. Nothing is silently dropped. Release still requires the full V23 scope; releaseReady stays false.
  5. **Completed-work signoff:** the stored purpose `WORK_DETAIL_COMPLETED_RESPONSE_V1` permanently means "approval response" (acknowledgment and change request get their own purposes later); the recorded actor responsibility is `ACKNOWLEDGED_BY`, never `APPROVED_BY`, and the UI never says "Approved".
  6. **S10 card-time tests:** S10-era tests that assert card-time CI state (their own `ci-selection.json`, `NOT_RUN` statuses, worker lines) and already failed at accepted main are rewritten to prove the committed S10 history (`docs/design/s10/**` records and recorded commits) instead of live CI state. Their semantic brand checks stay, and whole-file source pins are re-pinned at the frozen phase candidate with recorded provenance. `docs/design/s10/**` and `Release/**` are never edited, so the five accepted receipts stay valid for b1d04ae.
  7. **Privacy:** the S10 privacy supply-chain review stays untouched as history. A V23 successor review (`docs/design/v23/integration/privacy-supply-chain-review-v23.json`) records the current files and privacy facts, and S10_6 checks S10 rows that are unchanged plus the successor for changed rows. The owner signs off the successor before release.
  8. **Development environment (owner, 2026-09-25):** development moves to a persistent cloud Mac that the owner rents and sets up, running Claude Code, Xcode 26.6 and the iOS 26.2 Simulator. Local builds and targeted test runs there are allowed for development feedback. The official merge and release evidence still comes only from the hosted GitHub route. This supersedes the "no local Xcode build" rule for development only.
  9. **Development-run rules relaxed (owner, 2026-09-25):** for development-only routes (the development batch, the shared-coverage development sweep and any development lane), root may rerun an original after an infrastructure failure (runner, setup, network or artifact transport; never to retry failing tests), cancel a run already known to be broken, and run distinct development batches in parallel. Each such action is recorded in the ledger. Merge and release gates keep the strict one-shot, sole-collector, no-rerun rules.
  10. **V23 becomes the base (owner, 2026-09-25):** after Phase 1 merges, development is trunk-based on main. Each finished V23 feature merges on its own behind `V23PhaseGateV1` once verified, and there is no further long-lived integration branch. Over time, S10 brand protection moves from exact-byte source pins to design-system rules (closed vocabulary), behaviour tests and screenshot review, keeping the five accepted receipts as history.
  11. **Bitrise (owner, 2026-09-25):** the connected Bitrise Build Hub (runner group `Asset Roundddd`) may be used, with larger M4 machines and Xcode build cache once the owner selects them, for development runs and parallel sweep capacity on Bitrise's available iOS 26 runtime. Official evidence stays on the pinned GitHub route unless Bitrise later provides the exact runtime and is qualified.
- Reasoning effort, owner changed 2026-09-25: the owner first selected **ultra**, then changed the Claude Code primary session to **Extra (xhigh)** and asked that Claude subagents also run at Extra. The owner sets the session level in the app; the repository records the choice and never sets it. Claude subagents inherit the session level, and the documented agent definitions have no per-agent effort key, so a helper's effort is recorded as the session level at spawn (Extra from this change onward) unless the platform reports otherwise. Batches are measured under the active effort in REVIEW_EFFICIENCY. The `gpt-6-astra` High line above applies only to Codex helpers.
- Streamlined native route: `.codex-temp/native-tools/v23-original.py` dispatches exactly one original per head+selection at the pushed, index-empty head (an exclusive permanent attempt record and `C:/AssetRounds-v23-review-evidence/v23-original-ledger.jsonl` refuse any repeat; any unledgered run at that head or an active run of the same selection refuses dispatch; capacity 5; distinct closed selections at one head may run concurrently) and is its sole collector (exclusive claim, retained run/job metadata, logs zip and original artifact zip with SHA-256 manifests, derived `summary.json`). It replaces per-run pinned binder/dispatcher transitions. The committed workflow and `Scripts/v23-native-ci.py` still enforce the closed selection, exact parent/trees, budgets and evidence. No duplicate, rerun, cancellation or unchanged known-failure dispatch.
- Development tier D50 `(300, 1800, 3000, 0, 5100)` with a 120-minute job cap applies only to the closed development questions `c36-live-host-no-index-build30m` (exact14, after audited original36039088906 proved the 900-second test watchdog cannot hold it), `c36-round-item-mount-no-index-build30m` (exact7) and `c36-startup-retirement-no-index-build30m` (exact3), under the owner's standing authorization to change rules needed to reach merge (root attestation of the owner's handoff goal, not reviewer-verifiable; owner confirmation is due at the next touchpoint). Timing basis for the two extensions: in audited original36054441411 comparable production-session methods took 144-732 s each, one startup mode failed at 80 s and its successor was interrupted only after about 265 s of the remaining D50 budget, so exact7 and exact3 were expected to exceed the 900-second D30 test watchdog. Measured afterward in audited 36075174540: mount exact7 ran about 92 s in total, so D50 is only an unused ceiling for that rebound question; startup exact3 used 367 s while failing early. New development questions for these harnesses default to D30. Ordinary, D30 and acceptance budgets are unchanged; D50 grants no acceptance.
- `ProtectedFileSimulatorTimingV1` (DEBUG Simulator only) emits aggregate `V23_PROTECTED_FILE_TIMING_V1` lines to measure the fallback verification cost before any performance change. It never affects a verification decision.

## Reusable development build allowance — owner approved 2026-09-20

The owner permits the 30-minute proposal to be permanent or temporary. Reuse the closed no-index development routes below after source/helper qualification. All use build1800/test900/setup-artifact300/total3000/UI0/job90min except the three D50 rows (live-host, round-item-mount and startup-retirement); ordinary and acceptance budgets remain unchanged.

| Closed diagnostic selector | Exact existing method boundary |
| --- | --- |
| `c36-restore-review-no-index-build30m` | Six restore-review journeys |
| `c36-field-autosave-no-index-build30m` | Exact existing autosave method, including its terminal-ownership recovery regression. Isolate the corrected failure family after audited35949714322; preserve historical14/full coverage due,1064/69 and every assertion. Qualify before use; development only. |
| `c36-live-host-no-index-build30m` | Exact ordered13 newly enrolled live-operation/host/C05 methods (field1, host9, media3), then the existing S8 golden accessibility spine; qualify live-host-v1 at1078/70 and exact14 before use. D50 no-index budgets build1800/test3000/setup-artifact300/total5100/UI0/job120 since the 2026-09-24 owner decision (audited36039088906 exhausted D30 test900); preserve every historical selector, full production/lifecycle coverage and all final gates. |
| `c36-round-item-mount-no-index-build30m` | Exact ordered7 methods of the whole-class group `c36-round-item-mount` (profile round-item-mount-v1, 1085/71), parent/trees pinned to the reviewed candidate. D50 budgets as above; development only, stage B journeys and all final gates remain due. |
| `c36-startup-retirement-no-index-build30m` | Exact ordered3 startup private-retirement methods from the live-host question with test-only route/phase diagnosis. D50 budgets as above; no assertion or product predicate change. |
| `c36-round-item-completion-no-index-build30m` | Exact ordered12: the six V23ProductionRoundItemCompletionTests methods (profile round-item-completion-v1, 1091/72) then the six enrolled Round readiness methods; shares the rebound round-item parent/trees. D30 budgets; development only, B3 and all final gates remain due. |
| `c36-saved-review-fields-no-index-build30m` | Exact saved-review/discard9 followed by durable field-edit5; qualify ordered14 and1064/69 before use. Existing D30/no-index budgets; development only, complete C36 host/lifecycle and all final gates remain due. |
| `reminder-production-no-index-build30m` | Ordered33-method union of the four reminder groups |
| `notification-schedule-erase-no-index-build30m` | Exact existing ordered28 notification-schedule-erase methods, including the eight populated reminder cases; qualify before use |
| `notification-interruption-no-index-build30m` | Existing retained-live-context method then EveryInterruption method in original notification28 order; exact2 development-only subset after original35869740275 measured893/900seconds; preserve full28 coverage due, all assertions and existing budgets; qualify before use |
| `finding-profile-fixtures-no-index-build30m` | Exact ordered Finding70 components, corrected V9_69 profile11, then all five corrected C38 recurrence fixtures; qualify complete86 and1054/68 before use; development only, full production-owner gates retained |
| `restore-history-no-index-build30m` | Six restore-review methods followed by the golden replacement-union method |
| `activity-contracts-no-index-build30m` | Exact existing C47 H01 method from ordinary `activity-contracts`; no product/test changes |
| `activity-codec-punch-no-index-build30m` | Ordered11 methods from the closed `activity-codec-evolution` group, then the four existing enrolled C34 G01/H01/I01/R01 methods in pool order, then existing C47 H01; qualify the exact16 union before use |
| `activity-completed-source-no-index-build30m` | Ordered62 new completed-contract/source-read methods (manifest13, file21, production12, C20seven, bundled-release9), then existing C47H01; qualify exact63 before use; development only, full publication/lifecycle gates retained |
| `replacement-union-no-index-build30m` | Exact complete ordered12 methods from existing `replacement-packet-union`; qualify before use |
| `replacement-remainder-no-index-build30m` | Exact first11 methods of the same ordered12; qualify the disjoint partition before use |
| `replacement-report-fork-no-index-build30m` | Exact final report-Fork method of the same ordered12; qualify the disjoint partition before use |
| `erase-recovery-no-index-build30m` | Exact ordered13 methods from ordinary `erase-recovery` |
| `erase-drain-timing-no-index-build30m` | First Erase13 method: deferred old-context drain, with bounded phase timings |
| `erase-remainder-no-index-build30m` | Remaining ordered12 Erase13 methods |

The Erase1/12 split addresses audited original35586305552: its slow lifecycle method consumed661 seconds and the combined900-second test budget interrupted the final method. Require exact ordered disjoint union with historical13, unchanged assertions and separate timing/completion evidence. This isolates test budgets; it does not establish a performance fix or authorize an unchanged retry. Retain runner-cost measurements and all historical routes.

Each candidate needs exact source/parent/environment binding, one independent semantic review followed by qualified deterministic actual-index/committed-route binding, original-attempt deduplication and sole collection. Reuse parameterized helpers. Every original must answer a new evidenced question. Preserve all methods, historical selectors and budgets; no unchanged retry, duplicate, rerun, cancellation, paid capacity or acceptance transfer. Measure compilation/method timings before more splits or shared builds. This reusable allowance does not reopen consumed historical originals.

Erase original35548772193 at0d1b340 is audited: build1200/exit124,0Swift errors,13NotStarted. Its next diagnostic preserves the app/tests/project and the13 methods while applying the qualified existing no-index30-minute recipe. Stop if that enlarged route yields no discriminating evidence; choose the next causal change from retained build evidence.

Original `35488379036` at `94df47d940ef3cdbc993336a157c5145d84ee812` is audited: build hit1200 seconds/exit124, zero Swift errors, six tests NotStarted. The next development question is whether the unchanged corrected app/tests finish compilation and the six restore journeys execute with1800 seconds. No automatic retry, duplicate, Actions rerun, cancellation, paid capacity, provider/merge/release acceptance or test weakening. Stop if the enlarged limit yields no discriminating evidence; use the retained build evidence to choose the next causal change.

## Historical one-run GitHub build diagnostics — consumed

The owner authorizes one diagnostic original on the existing GitHub `macos-26` route: build watchdog 1800 seconds and accounted setup/build/test/artifact budget 3000 seconds. Keep setup/artifact 300 seconds, unit tests 900 seconds, UI disabled and the 90-minute job cap. This narrow exception supersedes the unchanged-watchdog rule only for the closed `c36-parent-finalization-check-no-issue-build30m` selector on the reviewed direct descendant of `6289befddaf75036c7fb7a4d971ba7cc171ec003`; preserve the committed app, Swift tests and project bytes.

Use the existing pinned environment, actual-index/committed-route review, original-attempt check and sole collector. Keep normal and acceptance budgets unchanged. No paid upgrade, duplicate dispatch, Actions rerun or automatic retry is authorized. Audit the terminal original before the next causal fix or run; this experiment grants no provider, merge or release acceptance. Record the original ID when dispatched and its outcome when audited so the allowance cannot be silently reused.

Allowance consumed: GitHub original `35444123826`, commit `690a1202664066a5934e513576c7cc7818e35c3c`. Audited: one compiler type mismatch, build failed after 853 seconds; tests did not start. The 30-minute limit was not reached. Correct that source-proven failure and resume ordinary-budget development checks; do not dispatch another build30m original.

New separate allowance, owner approved 2026-09-19: one original for closed selector `c36-restore-review-no-index-build30m` on the reviewed direct descendant of `a7c9d82bdb961e9a4dc57cd5f01340c4d0ae8dbd`. Original `35479951867` is terminal/audited: build1200 timeout, no Swift errors, six tests NotStarted. This approval permits build1800/total3000 with setup/artifact300, tests900, UI0 and job90min unchanged. Preserve the exact a7 app, Swift tests, UI tests, project/scheme, six restore methods, pinned GitHub environment and reviewed no-index command. Root selects a temporary diagnostic; normal/acceptance budgets remain unchanged. Require independent actual-index and committed-route review, original-attempt deduplication and sole collection. Record the original ID when dispatched and audited outcome before a successor. No automatic retry, duplicate, rerun, cancellation, paid capacity or acceptance credit. This allowance does not reuse the consumed parent-finalization experiment above.

Allowance consumed by original `35482404167`, commit `5a0df2b7c1d50c6ad509e2d13cf50abca141a82d`. Terminal/sole-collected/root-audited ACFEDC42: build failed after516s with two diagnostics from one private test-encoder access; six tests NotStarted. The30-minute watchdog was not reached. Correct the evidenced fixture cause and resume ordinary-budget development checks. Do not dispatch another original under this allowance.

For this CI batch, review only the combined protocol/helper delta and bind unchanged source evidence deterministically. Use parameterized helpers and event waits, retain one compact failure-family checkpoint, and avoid new source layers while their native dependency remains unverified. Resume production integration from retained findings after usable native feedback; do not restart settled investigations.

## Working cadence — owner confirmed 2026-09-14

Completed-test evidence collection correction, 2026-09-19: retain all N8 native/job watchdogs. Simulator diagnostic transport v2 distinguishes completed collection (30 seconds total, 29.5 seconds work, one 10-second app-container lookup) from interrupted collection (unchanged 3/2.5/2 seconds). Completed collection consumes the existing 900-second test allowance; it adds no native time and authorizes no retry. Retain exact v1 historical interpretation, source/container ownership, original hashes, partial evidence and failure status. A missing or invalid transport remains a failure and grants no acceptance/provider credit. This corrects the audited original35465985235 collection timeout; compilation and its seven tests passed only on its original head.

Apply the existing efficiency policy directly:

- Keep ACTIVE_BRIEF current and short (normally no more than 60 lines/6 KB); update its sections instead of prepending history. Read prior detail only for a relevant unresolved question.
- Use one scoped checkpoint per consequential batch and the review sequence above; reuse unchanged evidence rather than creating another review process.
- Reuse reviewed dispatcher/collector code through its existing parameter interfaces and immutable pins. Avoid per-head script copies and repeated reviews of unchanged helpers or generated records. Test and review diagnosed logic changes.
- Use one collector per original and consume its verified summary/hashes thereafter. Retain complete originals externally. Use event-driven status and useful source work while CI runs; do not add a polling worker for unchanged state.
- Keep ownership, native-route, test and final-main requirements. Apply the active provider and visual gate classification above: qualified GitHub may suffice; critical human-reviewed journeys precede merge and noncritical polish precedes release. This cadence grants no alternate native environment, cancellation, speculative dispatch or release action.

## Delivery efficiency — owner approved 2026-09-15

- Prioritize complete production journeys through the existing app: real entry, sole writer/service, durable effects, restart/recovery and visible result, with populated-store and no-effect failure coverage. Build additional foundations only when that journey requires them. Keep all frozen V23 requirements and the selected single-app integration scope.
- Finish compatible causal fixes in one consequential batch. Census affected callers and sibling fixtures once when a public initializer, receipt or fixture contract changes. Reuse authentic production-generated evidence; keep independent expected values and hostile cases.
- Apply the review sequence above after focused deterministic checks: one semantic review of the full affected contract and paired regressions, then automated exact-index/commit binding. Reuse unchanged findings; close actual corrections with the same reviewer. Every required test still passes before its applicable gate.
- Keep an explicit ledger of required but unexecuted tests and journeys. The current diagnostic selector pool is not the final coverage ceiling. Reconcile frozen requirements, retained regressions and later causal cases before acceptance; never count unselected tests as passing or silently defer their enrollment.
- After bounded source diagnosis cannot locate an observed failure, permit one reviewed instrumented diagnostic for that recorded question. Record the audited original, exhausted hypotheses, exact instrumentation and source-defined selector. Preserve every case, assertion, production predicate, environment and watchdog. Await and audit its predecessor; retain each original once; use the new diagnosis before any successor. No unchanged retry or acceptance credit; fix source-proven blockers first. This is the narrow exception to the known-failing-dispatch ban.
- Use retained build-phase and test timings to choose optimizations and schedule dependency-ready groups. Distinguish runner-seconds from elapsed time and method runtime from setup overhead. Run only affected supported groups during development; freeze the final candidate after production adoption is complete.
- Implement selector generation at the next necessary coverage transition from one versioned, reviewed, checked-in closed manifest. Preserve exact method membership, unknown/duplicate/overlap rejection, coverage reconciliation, dispatch hashes, budgets and historical evidence replay. No arbitrary runtime selector overrides.
- Scope and qualify CI improvements as finite experiments: build-phase instrumentation first, then the smallest justified exact-toolchain cache, supported unit-only build improvement or same-provider build-once/test-many pilot. Measure end-to-end benefit and cold-build reliability. Cache misses must work; caches remain hints, not acceptance evidence. Shared products require immutable source/environment/artifact bindings, safe extraction, no-rebuild proof and per-consumer results. Keep current watchdogs and S10 historical admissions. Do not put a broad CI rewrite ahead of production adoption.
- Bitrise is optional for merge when qualified GitHub covers every required runtime, execution kind and test. Restore/use Bitrise only after exact-runtime availability and controlled qualification; existing ARM64 download selection is implemented. A missing required kind remains a blocker on any provider. Different pools, paid capacity, settings, secrets or environments still need the existing concrete authority and qualification.
- Resolve product decisions and prepare critical-journey human-review checklists early. Track noncritical visual polish separately for completion before release. Preserve full V23 scope, same-head functional/compatibility evidence, qualified execution kinds, exact-main verification and owner-only release gates. App Store metadata, privacy, rights, assets and owner actions remain required for release. No silent feature reduction. Main may advance only by complete, verified phases under the owner's phased-merge decision of 2026-09-25 (unfinished features unreachable and listed in MERGE_READINESS); release still requires the full V23 scope, and accepted S10 is not automatically release-ready.

## Preserved historical instructions

The exact pre-change AGENTS.md is retained in `docs/execution/AGENTS_HISTORY_20260911.md`. Its enduring constraints remain applicable where relevant and not superseded by the selected integration task or this owner-approved cost policy. Its retired S10 card-selection and serial-development instructions are provenance, not active integration work orders. Consult only a relevant section when resolving an actual authority question; do not load it by default. If a different task is selected, read its applicable historical authority before acting.
